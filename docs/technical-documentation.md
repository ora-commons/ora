# Ora — Technical Documentation

*The deepest layer of Ora's documentation: the installed-system source of truth for engineers, architects, and evaluators. Two accepted companion documents — an accessible overview and a how-to guide — derive from this one.*

> **Composite implementation pin.** Chapters 1–7, 9, and 12–17 retain their established audited pins except where a later currency note says otherwise. Chapter 8's isolated Aside help retrieval is current to `d5d4ceba`; Chapter 11's removable first-party video feature is current to `a68568f2`. Chapter 10 describes the current visual-primary implementation: terminal visual authority, native/compiler output, ownership-safe editing, headless noninteractive rendering, and durable outcomes. Scoped current-state notes in Chapters 1–2 describe the present Windows launcher, native slash-command parsing, production Knowledge Library with the P2 shared Markdown Read/CodeMirror Edit surface, and the source-level five-card Overview shell with its Daily Note Read in Ora, P1 external-open actions and P3 Matrix Tasks; Chapters 2, 4, 5, and 8 additionally describe the current Model invocation outcomes, current-request fallback, substitution warnings, Gear/resource decisions, and protected Model Profile deletion/retrieval-rebuild preflight. These Model notes do not re-pin the surrounding chapters. Chapter 12 describes the current separate Scheduled Trigger surface, its durable handoffs, local-only routine descriptors, and macOS active-run sleep protection; Chapter 14 describes current Matrix Tasks, shared authoritative resolution, coordinated MOM writes and the canonical active-project read seam used by Overview; Chapter 15 describes the activated Documentation-Code Parity workflow; Chapter 16's Trace Walk paragraphs are current to the terminal-probe admission and retention protections described there without re-pinning the surrounding diagnostics chapter; and Chapter 18 describes standalone Programming plus its documentation-review contract. Chapter 18 supersedes the former governed-Process runtime and its automatic entry, process-bound automation, trigger, inspector, and lifecycle surfaces; it does not remove the separate Trigger facility in Chapter 12. This document is the vault canonical; its body-only repository mirror is `docs/technical-documentation.md`.

---

## What Ora Is

Ora is a multi-model orchestrator: a system that routes a single analytical task across several large language models — some running locally on the operator's machine, some reached over cloud APIs — and drives them through a fixed eight-step adversarial pipeline. The pipeline's job is reliability. A large language model asked to analyze, then asked to check its own analysis, tends to ratify what it already produced; Ora replaces that self-review with a structured contest between models and against persistent external state.

The reframe this whole system rests on is assisted human intelligence. Ora is not an autonomous analyst and does not present itself as one. It is an apparatus that makes a human operator's analytical work more reliable — the models supply generation and adversarial pressure; the human supplies the problem, the judgment about what matters, and the authority to act. I state this once and do not soften it later in the document: every "AI" component named here is a mechanism for assisting human intelligence, not a substitute for it.

Two distinctions run through the entire architecture and are worth fixing at the outset.

The first is **explicit Programming versus ordinary Inquiry**. Inquiry remains the foreground analytical pipeline and is never intercepted. Programming begins from its dedicated control, inspects a real Git repository, waits for one plan approval, executes on a task branch, and uses a fresh reviewer before committing accepted slices.

The second is an **explicit framework suite**. PEF, PIF, PFF, CFF, OFF, and DCA remain important named frameworks selected for analytical work:

- **PEF** — Problem Evolution Framework
- **PIF** — Process Inference Framework
- **PFF** — Process Formalization Framework
- **CFF** — Corpus Formalization Framework
- **OFF** — Output Formalization Framework
- **DCA** — Decision Clarity Analysis

Each is defined in its own canonical. Frameworks provide instructions and do not create Programming Runs, capability registrations, automatic triggers, or background execution.

## Why This Document Leads With Reliability

Reliability is the problem this architecture exists to demonstrate, so it is the lens the document leads with. The failure it targets is specific and well-documented: a single language model, asked to review or verify its own output, is an unreliable critic of itself. It re-derives the same reasoning, re-commits the same errors, and reports confidence it has not earned. Self-review is a race condition where the checker and the thing being checked share the same state — the check passes because it was never independent.

Ora's response is to make the checking independent, and to make it survive across turns. Four subsystems do the reliability-restoration work, and I frame each one throughout this document by the failure mode it prevents rather than by any performance figure:

- **The eight-step adversarial pipeline** prevents single-model self-ratification. It stages generation, cross-model challenge, consolidation, and formalization as distinct steps run by distinct models, so the critic is not the author.
- **Persistent vault state** prevents the amnesia failure — a model reasoning only from its context window, with no durable memory of prior conclusions, contradictions, or provenance. The vault is external, versioned state the pipeline reads and writes across sessions.
- **The capability gate** prevents unearned action. Before the system reaches for a tool or an external effect, a gate decides whether the request is well-formed enough to proceed; a malformed request is refused rather than run.
- **Oversight and governance** prevent silent drift — the slow divergence between what the system claims to do and what it actually does, unnoticed because nothing was watching.

One honesty constraint governs every reliability claim in this document. The framing is **qualitative**. I name the failure mode a component prevents; I do not attach a success rate, a per-step reliability budget, or a percentage, because the installed system does not instrument those numbers. Where such a budget would genuinely help an evaluator, I say plainly that it is not measured. A reliability claim you cannot trace to an instrumented metric in the code is a claim I do not make.

## How To Read This Document

This is the deepest layer of Ora's documentation — the source of truth for engineers, architects, and evaluators. Every subsystem chapter follows the same five-part depth model, in this order, so you can navigate any chapter by the question you are asking:

1. **Problem** — what fails without this subsystem; the concrete user or system risk it addresses.
2. **Design Decision & Tradeoffs** — the architectural move, the main alternative I rejected and why, the tradeoff accepted, and the open problems that remain.
3. **Implementation** — runtime entrypoints, data flow, key modules, config surfaces, and behavior under both normal operation and failure. This section braids *why* into *how* rather than separating them.
4. **Evidence & Verification** — the tests, parity checks, and packets that confirm the subsystem works, and — explicitly — the surfaces that are *not* covered.
5. **Expert Reading Notes** — what to inspect first, and where a future change is most likely to introduce drift.

Each chapter then closes with a sixth, mandatory subsection: **Platform compatibility**, exposing the per-subsystem platform checks (see the Platform-Support Statement below for why this is non-negotiable).

The chapters are grouped into six parts:

- **Part I — Install & Run** (chapters 1–3): getting Ora onto a machine and starting it.
- **Part II — Analytical Core** (chapters 4–7): the pipeline, the framework suite, and the model-orchestration machinery.
- **Part III — Knowledge, Tools & Media** (chapters 8–11): the vault, capability dispatch, and the media/visual subsystems.
- **Part IV — Supervision & Governance** (chapters 12–15): oversight, execution review, and the governance apparatus.
- **Part V — Operate & Extend** (chapters 16–17): running the installed system and extending it without forking core.
- **Part VI — Standalone Programming** (chapter 18): explicit entry, inspect-first planning, approval, real repository execution, fresh independent review, accepted-slice commits, evidence tools, and Git-native recovery.

Five appendices (A–E) follow the chapters. They are consolidated into this one document rather than split out, so that the source of truth stays in a single place.

**User-facing nomenclature (current-state note).** The interface concepts this document describes carry user-facing names decided after the original pin: a working session is a **Dialogue** (formerly a conversation); the text-input pane is the **Inquiry** pane; the text-output pane is the **Findings** pane; the visual/canvas pane, when discussed as part of a Dialogue, is the **Exhibits** pane (in pure image-rendering contexts "image pane" or "visual pane" remains acceptable); the small side-model chat panes in the upper right are the **Aside**; the unified upper List/Visual inventory workspace opened from the sidebar is the **Knowledge Library**; and the default project — the container work lands in when no project is selected — is the **Commons** (formerly General). Outside the current-state chapters identified in the composite pin, this document remains code-level and pinned to commit `7a5e8f40`, so it deliberately keeps the internal identifiers and UI strings as they exist at that commit — `conversation.json`, the `conversations` ChromaDB collection, `conversation_memory.py`, the `sidebar` model slot, `sidebar.js`, `visual-panel.js`, endpoint names, and quoted UI labels are all unchanged and correct as written. Read "conversation" in code-adjacent prose as the mechanism behind a Dialogue, and the nexus id `general` as the internal identifier of the Commons at that pinned commit. **Currency note (2026-07-11, post-pin):** the code-level rename has since landed on `main` at commit `062b67a7` — the internal nexus id is now `commons`, with the legacy id `general` still recognized everywhere, permanently. Except for the named current-state notes and Chapters 10 and 18, the chapter bodies remain pinned to `7a5e8f40` and are not re-audited against `062b67a7`; this parenthetical exists only so a reader comparing this document to a current checkout isn't misled by a claim that was true at the pin and has since changed. The user-facing documents ([[Guide — Using Ora]], [[Reference — Ora Accessible Overview]]) use the new names throughout.

## Platform-Support Statement

The primary tested target is **macOS on Apple Silicon**. That is the platform the installed system at commit 7a5e8f40 was developed and run on, and it is the only platform for which I will say a surface "works" on the strength of evidence. Every other platform — Windows-native, WSL, generic Linux — is somewhere between untested and partial, and each chapter's Platform compatibility subsection labels the specific surfaces accordingly. I do not label a non-mac surface "works" without evidence; where a surface was not actually run off-Mac, it is labeled "untested."

There is a real portability apparatus, but it is partial, not complete. It consists of `orchestrator/runtime_paths.py` (path resolution that avoids hardcoded home directories), `orchestrator/platform_check.py` (runtime platform detection), `scripts/check-portability.py` (a static scan for portability hazards), and `orchestrator/tests/test_portability.py` (the portability test suite). This apparatus is the mechanism by which Ora is being made cross-platform; at 7a5e8f40 it does not yet certify any non-mac platform as fully supported.

Four concrete limitations still prevent a supported native-Windows claim, and I name them here because they recur in individual chapters:

1. **No clean-room Windows result.** The native installer, launcher, browser handoff, process ownership checks, and complete server lifecycle have not been exercised together on Windows. Code-present remains untested, not supported.
2. **POSIX-only operational helpers.** The managed Documentation-Code Parity hooks and several maintenance/self-heal scripts are `sh` or `bash` programs. Git for Windows or WSL may provide a shell, but native Windows without one cannot run those paths; startup self-heals that invoke `bash` fail open with logging and leave their existing artifacts in place.
3. **Apple-only local inference.** MLX local models remain Apple-Silicon-only. A native Windows install must use cloud endpoints or an independently configured non-MLX engine; the current Windows path has not proved that complete arrangement.
4. **Unverified native filesystem and process edges.** `%USERPROFILE%` expansion, `msvcrt` locking, Windows process-tree termination, keyring backends, and application-specific consumers of quoted or backslash paths remain unverified end to end. The shared slash-command parser now preserves native backslashes and removes balanced wrapper quotes, but that parser correction is not a general Windows certification.

## How To Read the Status Labels

Status honesty is load-bearing in this document. Every feature is labeled with one of these exact words, and the word means precisely what it says:

- **implemented** — built, landed in `~/ora` main at 7a5e8f40, and operating.
- **partial** — built but not fully live: incomplete, or gated behind a feature flag that defaults OFF. A flag that defaults OFF is *partial*, never *implemented*, no matter how complete the code behind it is.
- **planned** — designed and intended, not yet built.
- **deferred** — consciously postponed; a decision was made not to build it now.
- **unmerged-code-review** — implemented in an unmerged worktree and sitting at the code-review gate; not merged, not installed, not landed.
- **stale** — present in the tree or the docs but superseded, orphaned, or contradicted by current code. Where a docstring or CLAUDE.md contradicts the code, I trust the code and, only where it is load-bearing, note the drift.

The load-bearing application of these labels: **Execution-Review Phases 1–4 are landed** in `~/ora` main at 7a5e8f40 (the commit itself is the Phase 1–4 portability retrofit, PR #181). **Execution-Review Phase 5 is unmerged-code-review** — it is implemented, but it lives in a separate worktree (`~/ora-worktrees/phase5-impl`, branch `execution-review-phase5-impl`, at 82553162) sitting at the code-review gate. Phase 5 is not merged to main, not installed, and not landed at 7a5e8f40 (the worktree path/branch/SHA are out-of-pin metadata; Phase 5 has since merged post-pin as PR #185). The installed system as described at 7a5e8f40 has Execution-Review coverage ending at Phase 4. I never call Phase 5 "installed," "landed," or "design-only"; it is code that exists and is waiting on merge.

## Core vs. Project (the generic-prototype boundary)

Ora is a generic prototype, and its core stays generic on purpose. Project-specific code — anything that encodes the assumptions of one particular publication, client, or use case — lives in that project's own `ora-project/` plugin directory and is registered to core through a pointer file, never inlined into `~/ora/orchestrator/` or the core config. This boundary is what keeps the generic prototype reusable across projects instead of accreting one project's special cases into the shared substrate; the mechanics of the plugin dir, the pointer-file registry, and the extension seams are covered in the **Project Plugin Convention** chapter.


---

# Part I — Install & Run

## 1. Installation & Runtime Profiles

### Problem

A `git clone` of Ora is not a running system. The checkout gives you source, a set of seed configuration files under `config/`, and a pile of `.md` frameworks. It does not give you a populated model-routing configuration, an inference-engine choice, API credentials, a launcher, or any evidence that a single model call will complete. Six concrete failures sit between the checkout and a working install, and the install/runtime-profile subsystem exists to prevent each one.

First, **empty routing**. The pipeline reads `config/routing-config.json` for endpoints and slot assignments; a fresh clone's slots are not populated against the live provider catalog, so without a populate step every slot resolves to nothing and the first prompt dies with no model to call.

Second, **wrong engine**. Ora runs local models through MLX (Apple's Metal-backed inference runtime) on Apple Silicon and through Ollama (a Go-based local-model server) everywhere else. A clone ships `engine: "auto"` sentinels that are correct on no machine until something resolves them to the real engine for that host. Ship an unresolved config and the server tries to load MLX weights on a Linux box with no Metal.

Third, **silent cost misconfiguration**. The desktop auto-populate step and the server rewrite each make a deliberate cheapest-defensible choice per slot. Get this wrong — let a paid model sit silently at the head of a fallback chain — and a background publication loop bills real tokens on every call. This is not hypothetical; the server-side free-chain design is documented in-code as a response to a runaway-process incident.

Fourth, **no launcher**. `server.py` needs `ORA_HOME`, a resolvable Python interpreter, a set of runtime feature-flag environment variables, and a stale-process kill before it can bind a port. A user typing `python3 server/app.py` gets none of that.

Fifth, **non-idempotent, non-resumable installs**. A source install makes several network round-trips (catalog refresh, registry sync, smoke round-trip). Any of them can fail transiently. Without recorded state, a failure halfway through forces a full restart from a partially-mutated tree.

Sixth, **the porting trap**. Ora was built on and for one machine (Apple M4 Max, macOS). Every hardcoded `~/ora`, every `pkill`, every `/opt/homebrew/bin/python3` shebang is a latent failure on any other platform. Without a single place that resolves roots and a standing linter that flags platform-specific constructs, portability rots silently, and you only discover it when someone runs the clone on Windows.

The subsystem's job is to carry a checkout across these six gaps to a server that binds a port and answers a prompt — and to do so on more than one class of machine without pretending the non-Mac paths are as tested as the Mac one.

### Design Decision & Tradeoffs

**The decision: three purpose-built install surfaces, not one universal installer.** Ora ships (1) a nine-step Python desktop installer, `scripts/install.py`, for the macOS Solo profile; (2) a Bash headless installer, `scripts/install-server.sh`, for a Linux API-only server; and (3) macOS `.app` bundles plus shell launchers (`start.sh`, `Ora.app`) for runtime launch. These are three code paths with different languages, different assumptions, and no shared install-orchestration module.

The rejected alternative is the obvious one: a single cross-platform installer that detects the environment and branches. It loses here, and the repo carried the fossil of the alternative until that fossil was retired: `installer/`'s 21-file natural-language layered installer spec, deleted because it gave executable-looking instructions for a system that no longer exists. Git history keeps it; the only thing left under `installer/` is `macos/`, the launchd plist template and app-launcher shim `scripts/ora-launchd.sh` actually reads. The reason a universal installer fails is that the three targets do not share a spine. The desktop path is interactive, keychain-backed, and installs local MLX weights; the server path is non-interactive, env-file-backed, and forbids local models entirely; the `.app` path is not an installer at all but a double-click runtime launch. A universal installer would be a tree of platform conditionals whose branches are each exercised by exactly one environment — the worst possible test-surface-to-code ratio. Three focused tools each do one job and can each be read end-to-end.

**The tradeoff is drift.** Three independent paths with no shared module guarantee that a change in one does not propagate to the others, and the installed system demonstrates the cost. The desktop installer auto-populates the **`budget`** preset (`scripts/install.py:1560,1572`) and then bakes all four preset cards through the runtime's own baker; `help/install-guide.md` said "Optimum preset" for months, a name `runtime_paths.py:865` has never carried — it defines `PRESET_NAMES = ("free", "budget", "speed", "premium")`, with no "optimum" at all. That one is now corrected on both sides. The server installer's own docstring (`install_server_config.py:11-16`) claims it points slots at `anthropic-api-haiku` and `anthropic-api-sonnet-4-5`, while the code directly below it (`SERVER_SLOT_ASSIGNMENTS`, lines 102-114) writes **free OpenRouter chains**. `install-server.sh` and `docs/cloud-ora-install.md` name `qwen3.6-plus`/`qwen3.6-35b-a3b`. Three descriptions of one behavior, all wrong, none load-bearing at runtime because the code is authoritative — but each a trap for a reader who trusts the prose. Trust the code.

**Second decision: Solo is the only supported profile; Hybrid and Organization are reserved names that hard-refuse.** `DEPLOYMENT_PROFILES` (`install.py:222-243`) declares all three, but only Solo carries `supported_now: True`. Selecting either other profile does not degrade or approximate — it refuses with an explicit "reserved for future Ora installs, not supported today" message (`install.py:1017-1021`). This is the same explicit-not-implemented-over-silent-wrong-answer discipline the system applies elsewhere, here at the install boundary. The alternative — quietly treating Hybrid as Solo — was rejected because the profiles differ in `api_pool_size` and concurrency assumptions; a silent alias would produce a subtly mis-configured multi-machine deployment that fails later and further from the cause. The open problem here is load-bearing: Hybrid and Organization appear in user-facing framing ("permanently free local AI") but the concurrency architecture that would unblock them (gated on "G1.27 network discovery and concurrency validation") is unbuilt. The names are promises the installed system cannot yet keep, and it says so at the refusal point rather than pretending.

**Third decision: centralize path resolution in one module as the porting strategy.** `orchestrator/runtime_paths.py` is the single source for every Ora root — `ORA_HOME`, `VAULT`, `CONVERSATIONS`, the runtime overlay dirs — each built from `os.path.expanduser("~")` and each overridable by an environment variable (lines 14-38). It also owns `norm_key`/`within_base` (case- and separator-normalized path comparison with a directory boundary, lines 49-74) and a cross-platform advisory file lock (`fcntl` on POSIX, `msvcrt` on Windows, guarded imports, lines 87-134). The rejected alternative — hardcode `~/ora` at each call site — is exactly the state the codebase is climbing out of, and the climb is incomplete: `orchestrator/platform_check.py:19-20` still hardcodes `WORKSPACE = os.path.expanduser("~/ora/")` and reads `routing-config.json` from that literal path rather than through `runtime_paths.routing_config_path()`. That is a real, in-tree instance of the drift the module was built to eliminate, and it is a reading-notes hotspot below.

**Fourth decision: the server writes an all-free fallback chain with no paid safety net.** `install_server_config.py` rewrites every slot to a chain of `:free` OpenRouter models (`PREMIUM_BUCKET_CHAIN` for writing slots, `FAST_BUCKET_CHAIN` for utility, lines 50-113) and states plainly (lines 61-66): "No paid safety net on the writing-slot chain. If every free option exhausts simultaneously, the call fails … but no token charges. This is a deliberate cost-protection choice after a runaway-process incident." Note that the file's own header comment (lines 42-44) contradicts this, describing paid models sitting "at the bottom of the chain as a safety net" — stale narration the code below overrides. The failure mode the code actually prevents is a runaway background loop silently rolling over to paid models. The tradeoff is availability: a simultaneous free-tier exhaustion drops the task rather than paying to complete it. For an async, non-interactive publication cadence that is the correct trade; for an interactive desktop it would not be, which is one more reason the two install paths are separate.

### Implementation

**Desktop install — `scripts/install.py`.** The entrypoint is `main()` (lines 1839-1901). A `models` subcommand short-circuits to `scripts/local_models.py` for post-install model management (lines 1843-1844, 1790-1801); a `converters` subcommand re-runs the Pandoc/Typst provisioning alone; otherwise the run drives a fixed nine-step pipeline (lines 1877-1885): preflight → dependencies → converters → profile select → catalog refresh → registry sync → auto-populate → smoke test → external-API orientation. Each step is a function returning `bool`; the loop halts on the first `False` with `INSTALL_HALTED at step '<name>'` and exits non-zero (lines 1888-1894). On success it emits the deterministic `COMPLETION_MARKER` — `"INSTALL_COMPLETE: 0 warnings, 0 errors"` (line 125, emitted 1896) — which the test protocol greps for.

Idempotence and resumability run through `install-state.json`. `save_state` records `steps_completed` and timestamps (`load_state`/`save_state`, lines 479-495); `--resume` skips any step already in that set (lines 1889-1891); `--reset` unlinks the state and log but explicitly leaves vault, conversations, and downloaded models alone (`reset_install`, lines 512-519). `--dry-run` threads through every step as a preview flag.

Step behavior, braided with why: **Preflight** (step 1, lines 722-786) resolves every user-storage root before any install work begins, and its hard gates are Python ≥3.11, ≥5GB free disk, write permission at both the checkout and the resolved Documents directory, and a vault path that already exists or can be created. Two of its checks are deliberately *not* gates. Missing document-conversion dependencies only produce a note, because step 2 installs them from `requirements.txt` and halting here is what once made the published install path unusable on a clean machine. And an unreachable OpenRouter warns and proceeds (lines 756-771), printing the same catalog-outage policy step 5 implements — the prediction and the behavior come from one constant, so pre-flight can never promise a survivable outage that the install then halts on. **Catalog refresh** (step 5) and **registry sync** (step 6) subprocess out to `refresh-catalog.py` and `sync_model_registry.py`, and neither is fatal for the ordinary failure. A refresh that cannot complete — missing script, 120-second timeout, non-zero exit — falls back to the catalog packaged with the checkout and states plainly what is stale about it (`_catalog_refresh_fallback`, lines 1062-1088); the install halts there only when `_catalog_baseline` finds no usable catalog to fall back on, because every later step picks its models out of that file. Registry sync is non-fatal in every case (lines 1155-1209), because `boot.py` can fall back to routing-config capability flags if the registry is absent. Step 6 also gates the empirical vision-capability probe behind `ORA_INSTALL_PROBE=1` — default-appending `--no-probe` (lines 1187-1188) to keep installs non-flaky. This is why the probe is **partial (flag-gated-off)**, not implemented-on: the default install does not run it. **Auto-populate** (step 7, lines 1545-1598) writes `config/configurations/user-pipeline.json` from the `budget` preset and bakes the four preset cards the Models pane promises; it halts if a promised preset is missing or holds a model in no slot afterwards, and applies that same standard to `user-pipeline` itself, because that is the configuration Ora actually serves from. **Smoke test** (step 8, lines 1678-1736) populates a `smoke-test-free.json` config, extracts candidate free models (`_extract_smoke_models`), and — only if an OpenRouter key is present in env or keyring — walks up to five of those candidates until one completes a real chat round-trip (`_openrouter_smoke_call`, lines 1618-1675). An auth failure (401/403) stops the install; a mere rate-limit or unavailability does not, because free models flake and that is not the user's misconfiguration. **External-API orientation** (step 9) prints the recommended key set and optionally opens provider pages.

The install-time comment lag is worth flagging inline: `install.py:1107-1121` still narrates an "Artificial Analysis intelligence-index check" in the imperative, while the surrounding docstring correctly states that Chatbot Arena replaced AA as the intelligence source and AA is now optional. The code path is correct; the comment block is stale.

**Runtime launch — `start.sh`.** The Mac launcher (lines 1-104) exports `ORA_HOME` (line 12) — the note at lines 5-10 explains why: a path helper once fell back to a literal `"~"` when `expanduser` could not resolve `HOME`, spawning stray `~/ora/...` directories, so exporting `ORA_HOME` guarantees resolution even if the passwd lookup is transiently unavailable. It then exports seven runtime feature-flag variables (`ORA_RAG_SELECTION`, `ORA_RAG_FIT_GATE_SLOT`, `ORA_WEB_EXTRACTION`, `ORA_RUNTIME_ENGRAM_PROMOTION`, `ORA_RUNTIME_ENGRAM_AUTOCOMMIT`, `ORA_DELIVERABLE_SCRUB`, `ORA_OR_STATS`, lines 18-57), `pkill`s a stale `server/app.py` (line 60), resolves Python (Homebrew then PATH, lines 63-71), starts the server with `--oversight` unless `--no-oversight` is passed (lines 76-81), polls ports 5000-5010 for a `/health` response (lines 83-100), and opens a browser via an `open`/`xdg-open`/`start` cascade (lines 89-95). Current macOS behavior (scoped post-pin correction): the default 5000–5010 probe list is compatible with the system Bash 3.2, so a successful readiness response that passes the existing checkout-identity and supervision checks opens the default browser without starting or restarting the service; an explicit `PORT` is probed alone for unsupervised launches and remains refused under launchd.

**Current Windows launcher — `start.bat` (scoped post-pin correction).** The tracked Windows launcher now honors an explicit `ORA_HOME` or derives the checkout from its own location, prefers that checkout's virtual-environment Python, then falls back to `py -3` and `python`. It validates an explicit `PORT`, stops only a server process whose recorded identity still belongs to this checkout, sets the Execution Review loop, and starts `server/app.py` with oversight enabled by default. The diagnostic `--no-oversight` argument is consumed by the launcher; every other argument is forwarded unchanged. Launch occurs with delayed environment expansion disabled so argument text containing `!` is not corrupted. Its health probe accepts only a server reporting the same resolved Ora root before it opens the browser and records the owned PID for `stop.bat`.

Those ownership and oversight improvements do not make the Windows and POSIX runtime defaults equivalent. `start.bat` sets `ORA_EXECUTION_LOOP=1`, but it does not set the POSIX foreground launcher's defaults for RAG selection and fit-gate slot, web extraction, runtime engram promotion and auto-commit, deliverable scrubbing, or OR statistics. Unless the caller supplies those variables, the corresponding code defaults decide the Windows behavior and some paths remain off or configured differently. The Windows launch can therefore still be a reduced or differently configured pipeline even though oversight itself is on. It also has no clean-room Windows result, and its native process, keyring, browser, and filesystem behavior remains unverified. The Mac checkout may additionally carry machine-local launch artifacts such as a launchd wrapper or `.app` bundle; those are not authority for a fresh clone and can still diverge from the tracked launchers.

**Current native slash-command paths (scoped post-pin correction).** `orchestrator/slash_commands.py` selects Windows-mode tokenization when `os.name == "nt"`. That mode preserves backslashes in paths and then removes only balanced surrounding single or double quotes, so an input such as a quoted `%USERPROFILE%\Documents\...` path reaches the handler without POSIX escape interpretation or quote characters. This closes the parser-level corruption defect; resolution by each downstream command and the complete native-Windows flow remain untested.

**Engine resolution — `orchestrator/platform_check.py`.** `detect_platform()` (lines 23-34) returns MLX only when `os == "Darwin"` and `arch == "arm64"`; every other combination gets Ollama. `startup_check()` (lines 62-103) reads `routing-config.json`, rewrites each `engine: "auto"` local endpoint to the resolved engine (adding `http://localhost:11434` for Ollama), and writes the file back. This is the resolve-once-and-persist move: settle the engine at boot rather than re-deriving it on every model call. RAM detection (`get_system_ram_gb`, lines 37-59) branches `sysctl` / `/proc/meminfo` / `wmic`.

**Server install chain — `install-server.sh` → `install_server_config.py` → `install_server_smoke.py`.** The shell script (`set -euo pipefail`) hard-guards on `uname -s == Linux` and tells a Mac user to use `install.py` instead (lines 82-84). It apt-installs the dep set, creates a `.venv`, pip-installs the Linux Python subset (no `mlx-lm`; the `DEPS` array names `flask`/`flask-cors`, `chromadb`, the `anthropic`/`openai`/`google-generativeai` SDKs, and `keyring` — the last only to silence a boot.py import, lines 138-158), installs Ollama and pulls `nomic-embed-text`, runs the config rewrite, prompts for keys into `~/.config/ora-server.env` at `chmod 600`, and runs the smoke test. `install_server_config.py::main()` (lines 160-236) synthesizes any missing free-model endpoints, validates that every needed id is present (fails with exit 2 if not), backs up the original to `routing-config.json.pre-server-install` exactly once, then overwrites `slot_assignments`, `default_endpoint`, `gear4_overrides`, `buckets`, and `operational_context` to API-only. `install_server_smoke.py` runs five assertions (`main()`, lines 92-99): routing loads via `boot.load_routing_config`, every slot resolves to an enabled/active endpoint, no slot references `local-mlx-*`/`local-ollama-*`, `OPENROUTER_API_KEY` is in the environment, and `chromadb` imports — exiting non-zero on any failure.

One implementation smell inside `install_server_config.py`: `NEW_FREE_ENDPOINTS` is defined twice (lines 72-76, then 88-100). The second binding shadows the first, so only the 11-entry list is ever used; the 3-entry list is dead. It is harmless because the second is a superset, but it is a landmine for the next editor who changes the wrong one.

**Config surfaces** the subsystem owns: `install-state.json` / `install.log` (desktop progress); `routing-config.json` (+ `.pre-server-install` backup); `config/configurations/user-pipeline.json` and `smoke-test-free.json`; `config/local-models.json`; `~/.config/ora-server.env`; the `ORA_*` environment variables; and the system keychain (`keyring`, service `ora`) for desktop keys — bypassed entirely on the server, which uses the env file.

### Evidence & Verification

**What is instrumented and passes.** `install_server_smoke.py` is the only automated install verifier: five hard assertions, run as step 6/6 of the server install, non-zero exit on any failure. `check-portability.py` is a standing linter over the whole tree — a RULES table of regexes for `pkill`, hardcoded Homebrew paths, `osascript`, `launchctl`, `/tmp`, `~/Library`, and similar, with a platform-guard downgrade pass (a file that branches on `sys.platform`/`platform.system()` downgrades its own matches) and an allowlist of intentionally Mac-only files (`ALLOWED_MAC_ONLY_FILES`, lines 178-190, which includes `Ora.app/Contents/MacOS/ai`, `Ora Models.app/Contents/MacOS/ai`, `start.sh`, `stop.sh`). `check_missing_files` (lines 275-290) would flag an absent `start.bat`/`stop.bat` as CRITICAL — but at this commit both `start.bat` and `stop.bat` exist and are tracked, so that rule does not fire. A live run at commit 7a5e8f40 reports **CRITICAL: 11, HIGH: 32, MEDIUM: 69, LOW: 59, INFO: 8**, exit 1 on the CRITICALs. The 11 CRITICALs are nine `homebrew-python-hardcode` findings (`uninstall/uninstall.sh:12`, three in `output_runtime.py`, and others) plus two macOS test-string matches in `test_shell_profiles.py` — not a missing Windows launcher. This is the commit whose message is "macOS+Windows portability retrofit," and the Windows launchers landed here.

**What is protocol-only, not automated.** `docs/install-testing.md` is a manual clean-room protocol: a three-environment matrix (two Mac Minis plus a Parallels Windows 11 ARM VM), a Solo-only ship criterion, and a pass checklist keyed on the `COMPLETION_MARKER` grep plus an `invoke_chat` round-trip. **There is no automated end-to-end test for `install.py`, `platform_check.py`, `runtime_paths.py`, or `local_models.py`.** The cross-platform advisory lock in `runtime_paths.locked_file` has guarded POSIX/Windows branches but no visible unit test exercising the `msvcrt` path. No reliability budget or per-step success rate is instrumented anywhere in this subsystem — the smoke checks are pass/fail assertions, not measured rates. Any percentage would be invented; none is claimed.

**Explicitly untested surfaces** (do not read the code's cross-platform branches as evidence they run):
- The Parallels Windows-native install gate has not been passed. Windows-native and WSL desktop installs are documented-but-unverified, even though `start.bat`/`stop.bat` now provide checkout ownership, oversight-on-by-default launch, the `--no-oversight` opt-out, argument forwarding, and identity-bound health/process handling.
- `wmic` RAM detection (`platform_check.py:50-56`, `local_models.py:113-121`) is untested and at risk — `wmic` is deprecated and removed in recent Windows 11 builds.
- The `msvcrt` file-lock branch, `norm_key` Windows normalization, `%USERPROFILE%` root resolution, and the `keyring` Windows/Linux backends are code-present, run-absent.
- `install-server.sh` is exercised on Ubuntu 24.04 (Hetzner CX33) only; its `apt-get` assumption means Fedora/Arch need porting, and WSL-Ubuntu is plausible-but-untested.

**Drift the reader must not trust the docs over the code on:** the server slot picks (free OpenRouter chains in `install_server_config.py` vs the anthropic/qwen names in its own docstring and `install-server.sh`/`cloud-ora-install.md`); the server embedding default (`nomic-embed-text` on the server vs `bge-m3` (1024-dim) on the Mac desktop per CLAUDE.md 2026-05-29 — a genuine divergence that risks a ChromaDB dimension mismatch on any Mac↔server collection round-trip, and one that is *undocumented in the install path*); and `docs/README.md`'s reference to a nonexistent `Local AI.command` desktop launcher.

### Expert Reading Notes

**Read these first, in this order.** (1) `install.py:222-243` and `:996-1028` — the `DEPLOYMENT_PROFILES` table and the profile gate tell you the whole Solo-only policy and its refusal semantics in two screens. (2) `install.py:1877-1885` — the nine-step pipeline list is the spine; every step function hangs off it. (3) `runtime_paths.py:14-46` — the root-resolution block is the contract every other module depends on for pathing; if you are porting Ora, this is the file. (4) `install_server_config.py:50-113` — `PREMIUM_BUCKET_CHAIN`, `FAST_BUCKET_CHAIN`, and `SERVER_SLOT_ASSIGNMENTS` are the actual server routing; read the chains, ignore the docstring and the header comment. (5) `platform_check.py:23-34,62-103` — the engine fork and the `auto`-resolution rewrite.

**Where future changes most likely cause drift.** The three-install-path split is the standing drift generator; the already-diverged descriptions (preset name, server slots, embedding model) are what happens when someone edits one path and not the paired doc. Treat any change to a preset name, a slot-assignment id, a routing-config schema key, or an endpoint id as a multi-file change: grep all three install paths plus `docs/` before you consider it done. Specifically —

- **`platform_check.py:19-20`** hardcodes `~/ora/` instead of calling `runtime_paths.routing_config_path()`. It is the clearest in-tree violation of the centralized-paths decision and the first thing to fix if `ORA_HOME` ever needs to point somewhere non-default; today, engine resolution silently ignores `ORA_HOME`.
- **The Mac launch paths disagree on environment.** Only `start.sh` (7 flags, `--oversight`) is tracked at this commit; the author's machine also has an untracked `run-ora-server.sh` launchd wrapper and an `Ora.app` bundle that export different flag sets. They will drift further with every new feature flag unless the flag set is factored into a shared sourced file, and a feature enabled only in `start.sh` is invisible to anyone who launches via the untracked wrapper or app bundle. (The untracked launchers are not in the commit, so a fresh clone launches only via `start.sh`.)
- **`run-ora-server.sh` is not in the commit.** A clean checkout of 7a5e8f40 does not contain it; it exists only as an untracked file on the author's machine (a 2026-07-02 recreation), so it is machine-local and can differ per machine. The committed, reproducible launcher is `start.sh`.
- **`install_server_config.py`'s duplicated `NEW_FREE_ENDPOINTS`** — edit the second definition (lines 88-100), never the first; the first is dead.
- **`installer/`'s natural-language layer set is gone.** The 21 Markdown files (manifest, appendix, phase1/, phase2/) described an installer that was never the live path and were retired rather than reconciled; git history holds them. `installer/macos/` stays — it holds the launchd plist template and the app-launcher shim `scripts/ora-launchd.sh` installs.

**Blast radius.** A preset-name change touches `install.py`, `runtime_paths.PRESET_NAMES`, `auto-populate-configuration.py`, and the docs. A routing-config schema key change touches all three install paths, `platform_check.startup_check`'s rewrite, and `boot.load_routing_config` (which the server smoke test calls). An endpoint-id change in the free chains touches `install_server_config.py`'s chains *and* the daily `ora-audit-models.py` catalog check that guards them.

### Platform compatibility

Per-subsystem platform checks at commit 7a5e8f40. "Works" appears only where the path was actually run; every non-Mac surface that was not exercised is labeled untested.

- **Desktop installer (`scripts/install.py`):** macOS = works (primary tested path). Windows-native = untested (`py -3 scripts\install.py`; documented as "intended, pending Parallels clean-install test", `install-guide.md`). WSL = untested (documented fallback, not in the formal matrix). Linux-desktop = untested/best-effort (explicitly "not the primary tested desktop path"). Pure-Python 3.11+, no shell dependencies, so the command is portable in principle — but do not claim it works off-Mac.
- **`start.sh` launcher:** macOS = works. WSL = untested. Linux = untested (`xdg-open` branch present, but the whole path was not run). Windows-native uses the separate tracked `start.bat` described below rather than this POSIX script.
- **`stop.sh`:** macOS = works. WSL / Linux = works in principle (`pkill -f` is POSIX). Windows-native = committed `stop.bat` mirror present, untested.
- **`run-ora-server.sh` (launchd wrapper):** macOS = works (and untracked in git). Windows-native / WSL / Linux = n/a (`#!/bin/zsh -l`, hardcoded `/opt/homebrew/bin/python3`, launchd job — Mac-only by construction).
- **`Ora.app` / `Ora Models.app` bundles:** macOS = works. Windows-native / WSL / Linux = n/a (macOS `.app` format, hardcoded Homebrew Python shebang, `subprocess open` — no cross-platform equivalent shipped).
- **`install-server.sh` (Linux server):** Linux (Debian/Ubuntu, `apt`) = works (exercised on Hetzner CX33, Ubuntu 24.04). macOS = refuses by design (`uname` guard → use `install.py`). Non-apt Linux (Fedora/Arch) = needs-porting. WSL-Ubuntu = untested. Windows-native = n/a.
- **Python interpreter resolution:** macOS = works (`/opt/homebrew/bin/python3` then PATH in `start.sh`). Linux / WSL = works (PATH `python3`). Windows-native = untested (`start.bat` implements the `py -3` → `python` fallback, but no Parallels run has exercised it; the app bundle and launchd wrapper hardcode the Homebrew path).
- **RAM detection (`platform_check.get_system_ram_gb`, `local_models.get_total_ram_gb`):** macOS (`sysctl`) = works. Linux / WSL (`/proc/meminfo`) = works. Windows-native (`wmic`) = untested and at risk — `wmic` is deprecated/removed in recent Windows 11 builds.
- **Engine resolution (`platform_check.detect_platform`):** macOS/Apple-Silicon → MLX = works. Every non-Apple-Silicon host → Ollama at `localhost:11434` = untested on desktop (the non-Mac engine path was never exercised outside the server install, which forbids local models entirely). MLX is Apple-hardware-bound.
- **Advisory file lock (`runtime_paths.locked_file`):** macOS / Linux / WSL (`fcntl.flock`) = works. Windows-native (`msvcrt.locking`) = untested (branch present, guarded import, no test). Falls to best-effort single-process if neither primitive is available.
- **Path root resolution + `norm_key`/`within_base` (`runtime_paths`):** macOS / Linux / WSL = works (built from `expanduser("~")`, env-overridable). Windows-native = untested (`%USERPROFILE%` resolution and case/separator normalization are coded but not run).
- **Keyring credential storage (desktop):** macOS Keychain = works. Windows Credential Locker / Linux Secret Service = untested (backends exist in the `keyring` lib; not exercised here). The server path bypasses keyring entirely.
- **Server key store `~/.config/ora-server.env` (chmod 600):** Linux / WSL = works (POSIX `chmod`, `read -rs`). macOS / Windows-native = n/a (server path is Linux-only).
- **Browser auto-open post-launch:** macOS (`open`) = works. Linux (`xdg-open`) = works in principle. WSL = untested (browser handoff is documented as flaky). Windows-native = untested.
- **Local model download (`local_models.py`, MLX weights via HuggingFace `snapshot_download`):** macOS/Apple-Silicon = works (recommends `mlx-community/*` MLX-format repos). Windows-native / WSL / Linux = n/a for MLX (Apple-Silicon-only); non-Mac hosts route workhorse slots to cloud, and the server path has no local models by design.
- **Windows launcher (`.bat`):** Windows-native = present-but-untested. `start.bat` honors `ORA_HOME`, prefers `.venv`, validates an explicit port, enables oversight by default, consumes `--no-oversight`, forwards all other arguments, and binds start/stop/health checks to this checkout's process identity. `stop.bat` and `uninstall/uninstall.ps1` also ship. None has completed the clean-room Windows protocol, so these are current implemented contracts, not a supported-platform claim.

For the rationale behind the installer's shape and the retired First-Boot flow, see the vault sources *Installer — Ora* and *Paper — Installer*. Engine and slot behavior downstream of this chapter is developed in the routing and pipeline chapters.

## 2. Server & Browser-UI Runtime

- **Current browser-origin boundary.** Before application code parses a body or any view runs in either shipped Flask application, each app-wide pre-view hook calls the same server-package browser-origin decision. This covers the main chat/API host (`server/app.py`) and the standalone document-review host (`server/review.py`).
- A hostile `Origin` or cross-site fetch context receives `403` on either host regardless of method; plain-text, form, multipart, `GET`, and `HEAD` requests—including the review action form—do not bypass the decision.
- Headerless local clients and same-origin browser requests remain supported. Route-local checks may repeat the decision, but they are no longer the only protection.
- `POST /api/session/export` accepts the public Dialogue identity and optional title only. Presence of a private vault root, sessions root, raw-conversation directory, or Node-program field is a `400` before lifecycle, filesystem, or export work; tests substitute those dependencies at the internal Python seam.
- **Current metadata save boundary.** New Matrix saves and both explicit Markdown export paths preflight metadata before document-support effects. Invalid YAML/identity returns a field-level refusal without a saved-path claim. Optional-description warnings do not prevent saving: Matrix warnings accompany the MOM result and log, while both `/api/export` branches and `/api/session/export` retain warnings. The existing export status slot displays saved-with-warnings as inert text and offers Reveal only after success. Word/PDF, Print and Trace retain their separate paths.
- Project-theme responses and internal theme reads validate the kernel-reported path of an already-open file against the captured resolved project/theme paths before reading content; Flask consumes that file without reopening its pathname. The theme contract is detailed in Chapter 17 and the Project Plugin Convention §13.

The orchestrator described in the pipeline chapters is a library. It has no way to receive a prompt, show you a result, let you switch models, or resolve an oversight pause. This chapter is about the process that turns that library into a running system you can talk to: a single localhost Flask host (`server/app.py`, 15,040 lines) that serves one browser page (`server/index-v3.html`) backed by ~130 static JavaScript modules, and exposes 174 HTTP routes covering chat, clarification, conversation persistence, named configurations, model-routing slots, oversight, canvas/visual rendering, media capability, themes, and the model registry.

I am describing the installed system at commit `7a5e8f40` (PR #181, "Execution Review Phase 1-4: macOS+Windows portability retrofit"). That commit matters for this chapter specifically: it is the change that pulled the server's filesystem roots out of hardcoded `~/ora` / `~/Documents` string literals and behind a single cross-platform resolver. Where older documentation still describes the server as Mac-only string paths, trust the code — the retrofit landed. What the retrofit did not do is boot the server off a Mac; the code paths exist, the observation does not, and the Platform compatibility section holds that line.

### Problem

Without this subsystem there is no product. But "we need a UI" is not an architecture; the interesting failures are narrower.

**Lost work.** The pipeline takes minutes. A user submits a prompt, closes the laptop lid, and walks away — that is the normal case, not the exception. Ora is async-by-design. If the submission lives only in an open HTTP request or a browser tab, then a dropped connection, a tab close, or a server crash mid-run destroys the input with no record that it ever existed. The failure mode is silent data loss: the user believes they asked a question and later finds no answer and no trace.

**The spinner problem.** A synchronous streaming UI — hold the connection open, stream pipeline-stage events, render progress — couples the user's presence to the pipeline's runtime. If runs are sub-second that is fine. When runs are minutes and the user has left, streaming buys nothing and costs a live connection that will drop. The failure mode is a UI that only works if you sit and watch it.

**The empty-response drop.** A specific, real bug. Greetings, acknowledgements, system lookups, and the archived placeholder modes (`simple` / `standard`) route through `load_mode()`, which returns `""` for a mode file that no longer exists. The analytical pipeline then runs to completion and emits nothing — the user's "hi" produces `pipeline produced no response`. This is not hypothetical; the bypass guard described below exists precisely to close it.

**No control surface.** Model routing, named configurations, capability-slot assignment (which provider generates images, which generates video), and oversight resolution all need a place to live. Without HTTP endpoints backing a UI, every one of these is a config-file edit and a restart.

### Design Decision & Tradeoffs

**The decision: file-as-source-of-truth, poll-to-reconcile.** `POST /chat` does not stream. It runs the full pipeline synchronously server-side, writes the result to disk — a processed chunk file plus a `conversation.json` envelope — *before* it replies, and returns one small JSON acknowledgement: `{"status": "ok", "conversation_id": ..., "chunk_id": ...}`. The browser never holds an event stream for the pipeline. Instead it polls list and conversation endpoints on a 12-second cadence and reconciles what it finds against what it last rendered. The disk is the source of truth; the browser is a cache that catches up.

`_invoke_pipeline` states this in its own docstring (`server.py:6018`): "No SSE. No streaming. No pipeline-stage progress events. Per the V3 spec, the user does not watch the pipeline run — they submit and come back later."

**The alternative I rejected: keep the SSE-streamed live pipeline.** The pre-V3 design streamed `pipeline_stage` events over `text/event-stream` so the UI could show a live progress bar. Some of the project documentation still headlines the server as "Flask + SSE" with "SSE streaming with `pipeline_stage` events for UI progress." That description is stale. I abandoned it because it optimizes for a user who watches — and the actual user submits and leaves. A streamed connection to an absent client is a connection waiting to drop, and a dropped connection under the old model lost the run. The reframe is not manual versus agentic; it is foreground-watched versus background-completed execution, and the pipeline is background.

**What this buys and what it costs.** The gain is crash-recoverability and honesty about latency: because the input is on disk before anything else happens and the output is on disk before the reply, no single point in the request can silently drop work. The cost is real and I will not paper over it. There is no live progress — the user gets a pending marker and then, eventually, a result. And there is a reconciliation-latency floor: a result that lands 200 ms after a poll is not seen for up to 12 seconds. That 12s cadence is a deliberate tradeoff between staleness and polling load, not a measured optimum.

**Current sidebar and Knowledge Library reconciliation.** Disk remains authoritative. An unchanged expanded-sidebar poll receives the existing ETag-based 304; a collapsed sidebar does not poll and refreshes once on expansion. Overlapping conversation-list requests share the active request and coalesce into one trailing request per burst using the latest ETag; callers settle only after final reconciliation. Library has one canonical state and separate inventory, pinned-detail and render generations. Query/scope/close invalidates the corresponding finite request, and late frames/layouts cannot replace current work. A project-scope change clears identity-bound rows, pin, checks, detail, preview and actions before requesting the replacement, preserving eligible source/query/filter/group/sort choices. Same-scope refresh retains accepted state until replacement; an early frame cannot erase an identity whose source is pending. Final accounting reconciles absent IDs and List order once. Stable IDs deduplicate paging. Ineligible grouping repairs only to None; counts, facets, completeness and paging remain server authority.

Only the last List/Visual preference persists across reload; filters/grouping survive close/reopen in the same page session. Invalid/unavailable storage gives a visible fallback without retaining content or history. Both views expose native checked selection and the same bulk actions, separately from pin. Chip removal is one state transition and refresh. `OraLibraryWorkspace.open(options)` accepts validated `projectId`, `sources`, stable `provenanceId` and optional boolean `cleanBrowse` for closed or already-open Library. Invalid options, including clean browse combined with provenance, are rejected before mutation.

Overview's collection destinations use `cleanBrowse:true`: the same transition sets exact project/source scope, clears query/input, refinements/provenance, Trace, pin/selection, read/edit/preview and result identity even when scope is unchanged, and synchronizes counts, chips and controls. It preserves saved List/Visual, compatible grouping and sort, repairing only an ineligible grouping to None. Clearing provenance does not remove Extraction source grouping when Engrams remain selected. Criteria input is synchronous with no queued debounce; superseded list/preview/relationship work is aborted through existing owners, and generations invalidate late responses before one fresh inventory request. Without the flag, focus-only Sidebar re-entry preserves state without another fetch; derived-Engram entry retains exact provenance and one fetch, and refresh/pinned activation retain their existing behavior. Overview never writes private Library state.

**Shared renderer-neutral Library browse and production workspace.** GET /api/library/browser is the sole read-only inventory. Repeated/comma-delimited Dialogues, Engrams and Files are additive; omission means all and the zero-source UI state is local. Commons/omitted scope is universal admission, not a membership ID; a named project requires exact membership. Source, scope, query and corpus refinements qualify the complete eligible universe before stable ordering, facets, counts and offset/limit paging. Rows have stable source-namespaced identities, body-free metadata, unavailable fields, provenance, directional relationship summaries, preview hints and provisional editability. The complete Engram universe does not inspect every note's write access; only returned ordinary and admitted Trace rows receive exact source hydration after paging and Trace selection. The final JSON/NDJSON path deduplicates shared row objects without dropping distinct returned representations of the same identity, so an off-page neighbor retains current access hints. Preview/edit descriptors never grant authority, and Dialogues are read-only.

The additional query fields are item_type, repeatable AND tag, inclusive date_from/to, lifecycle, privacy, local_restriction, show_archived, relationship, relationship_family, provenance_id, epistemic_kind, extraction_date_from/to, file_type, folder, category and content_type. Invalid values are input errors. Item type remains Dialogue/Engram/File, distinct from canonical YAML type. Missing metadata remains in unfiltered inventory but cannot satisfy a refinement. No local-restriction value is inferred from privacy or model settings. Relationship qualification requires authoritative pre-page membership; a failed optional read leaves unrefined inventory usable and marks that requested filter unavailable.

Grouping is one shared presentation axis. A File folder comes only from provider-relative path, with Project root when empty; content type uses the supplied value or Unavailable. Complete-result grouping/title sort waits for all pages. Empty Dialogue membership belongs to the Commons group. Multi-project group occurrences preserve all memberships while totals, selection and bulk actions deduplicate canonical IDs. Unread/subject/lineage controls require the current owner record, not inferred values.

The existing project metadata record optionally stores library_file_categories: stable id, label and match over nexus/type/tags/subtype. Existing read/update owners validate and preserve unrelated fields. Exact scalar equality, all-required list membership and AND across fields apply only to the owning project. Missing config supplies no choices; malformed entries warn without hiding Files. Pinned File metadata displays nonempty string metadata_warning and category_warning from provenance.details as literal text. Its existing replacement lifecycle clears earlier warnings at the next pin; warning rendering alone does not replace the document host or disturb an edit draft. There is no rule language, regex, category editor or new store.

Opening Library hides and makes Inquiry and Aside inert without removing their DOM or resetting drafts, attachments, Framework/Analysis selection, or other staged composer state. The surface seals the upper region and physical bridge into one uninterrupted workspace: no second O, spine, console, grid, divider, toolbar band, or bordered container. Individual contextual controls occupy the live measured bridge strips without a shared background and open popovers upward. Findings and Exhibits keep their DOM, controls, and existing owners; a pin adds temporary preview layers and closing removes them.

The top field sends q only to /api/library/browser. It searches privacy-admitted Dialogue bodies through the existing lexical seam and intersects exact/fuzzy knowledge-index results with the admitted Engram inventory. It adds no semantic search or vault scan. Files return no keyword rows and an explicit unsupported/incomplete reason; metadata-only Dialogues cannot match body text. All query/refinement qualification precedes counts/paging, not a visible-page filter or fallback endpoint.

With stream=1, the same route returns finite NDJSON using Flask Response/stream_with_context and frames {event:'snapshot', stage, final, data}; absent/zero retains ordinary JSON. Data has the ordinary response shape. Frozen criteria and provider phase iteration enumerate each requested provider once, yielding qualified source/exact/fuzzy stages before subsequent slower work. Pending/failed sources and provisional accounting stay labelled; pagination/Load all are disabled until final accounting. Final data equals the ordinary complete-or-honestly-incomplete response. Malformed frames, EOF without final or failed sources retain already-admitted safe rows with incomplete status and retry. Fetch abort and generation checks terminate request-local work at finite boundaries. There is no polling, worker pool, persistent job or second inventory.

The existing native raised O changes from Submit to a selected-item anchor and restores its semantics on close. Visual uses live O/bridge geometry for a roughly 270-degree arc, keeping the downward cone clear; named controls remain reachable at narrow widths. Slots are keyed by stable ID/group occurrence: pinning, hover and late enrichment never move existing nodes, remove the pin or prepend Related nodes. Source/scope/query/group/refinement changes or explicit expansion may change the scene. Capacity disclosure and an expandable/scrollable native relationship list preserve every advertised neighbor and action beyond the arc.

Engram Browse groups by proven provenance, with epistemic kind/extraction date and project refinements where supplied. Show derived Engrams from an admitted File works even without a supported File preview; a Dialogue requires readable preview authority and metadata-only Dialogues do not offer the action. It enters that source in one request without activation, new extraction or guessed joins, and absent indexed derivation remains an honest empty result. Trace queries only selected adjacency and exact admitted Engram endpoints under current filters/archive scope; Files cannot bypass Engram privacy. Ambiguous stems stay unresolved, and hidden endpoints disclose neither IDs nor hidden counts. Include all distinct neighbors through fifty, excluding the selected node and repeated edges. Above fifty, round-robin the four established families plus unclassified last, then best incident confidence and stable ID within family; deduplicate neighbors and disclose exact admitted N more in batches of fifty. Explicit filters reset the budget. Include all stored edges among displayed endpoints, preserving multiple kinds and both directions. Neighbor pin retains context; Trace from this Engram explicitly starts another scene.

Relationship families are evidence/argument (supports/contradicts/qualifies), building/evolution (extends/supersedes/derived-from), causal/logical (enables/requires/produces/precedes) and hierarchy/abstraction (parent/child/analogous-to). Ranking maps high/medium/low to 1/.7/.3, accepts finite 0–1 numeric confidence and ranks unknown last; disclosure retains original values. Unknown kinds remain inspectable as unclassified text. Native type/direction/confidence disclosure, line patterns, labels and arrowheads supplement color; SVG stays assistive-technology-hidden and noninteractive. Similarity suggestions remain separate and acquire no typed connectors. File detail distinguishes project/folder membership, explicit metadata relationships and derived provenance; every connector requires exact admitted endpoints. Optional graph failure preserves usable inventory.

Pinning never invokes the active-Dialogue loader. A readable retained Dialogue uses non-mutating read_conversation_history_envelope / resolve_effective_conversation_history, then filter_conversation_history_for_tag(messages, '') to return only role/content of exact admitted complete exchanges, preserving nested-fork cutoffs. P2 renders turns under fixed external speaker labels; source headings/fences cannot change attribution. The 4 MiB bound applies across the whole Dialogue, with speaker-labelled literal fallback. Unknown/conflicting privacy and orphan turns cannot become content. Pin changes no last access, composer or active Dialogue and offers no Edit; Continue is the explicit activation. Metadata-only Dialogues retain generic titles and no content actions.

Preview always sends one stable Library ID, never a browser path, and reruns current read admission. Engram text uses Standard-lane privacy authentication; File text is UTF-8 from its current provider identity, including frontmatter unchanged. Stable P2 Findings hosts remain separate from metadata/relationships/status. Archive-aware preview admission and the common edit-source resolver remain separate: a readable historical identity never becomes editable. Duplicate/stale parameters, current authority, no-store content responses and existing exact-file atomic save/conflict behavior remain enforced.

A supported image File uses the same stable `files:` identity boundary. The server reruns the current project-file provider and normalized identity, requires one current regular non-symlink match, authenticates a single-frame PNG, JPEG, or WebP from its bytes, and refuses stale or unlisted identity, symlinks, unsupported or spoofed content, files over 16 MiB, images over 40,000,000 pixels, and unreadable data. It returns authenticated image data; the client renders it only in the temporary Library Exhibits preview while Findings keeps metadata and relationships. The raster path remains separate from Markdown rendering. Previewing changes no active Dialogue, composer draft, pin, filters, selection, or underlying pane owner; repinning or closing aborts the request, and the generation guard discards a late result.

Any browse-time edit offer for a Markdown project File or uniquely direct Standard Engram is provisional. Entering Edit performs final current-provider and source-authority validation while reloading the full Markdown and its SHA-256 digest, then mounts CodeMirror 6 with explicit Save/Cancel controls in temporary Findings. Save re-resolves provider and source authority immediately before acquiring the existing exact-file mutation lock and verifies that the resolved source and path still match the request's prior resolution. The lock covers only the final filesystem reread, digest and eligibility validation, and atomic write, preserving uniform LF or CRLF endings and file mode. Engram knowledge-index refresh occurs after lock release and reports separately from the saved content. A conflict or refusal writes nothing and retains the complete browser draft. A malformed response, timeout, or transport failure preserves the draft without claiming whether persistence completed. A successful save returns to Read and refetches current text; Cancel discards only the unsaved draft and refetches the canonical preview. Folded aliases, symlink-only, ambiguous, incomplete, malformed, or privacy-changing Engrams are read-only. A project File declaring `type: engram` is also read-only and must be opened through an eligible Engrams row. Dialogues and non-Markdown, missing, unwritable, mixed-ending, or lone-CR Files remain read-only; supported image preview does not make any image editable. Browse remains body-free; there is no autosave, local draft database, general editor, File-body search, SVG/PDF/animated or other-media preview, image/media editing, download, or ownership change.

Dialogue Continue, Fork, contributor, lifecycle, Related disclosure, and project-membership actions reuse their existing seams, and extension-contributed contextual actions append to those built-ins. A project File row additionally offers **Reveal in Finder**. The controller uses only that current provider row's `preview.locator.path`, posts exactly `{path}` to the existing `POST /api/fs/reveal`, and reports route success, refusal, or failure in the existing notice. The route remains authoritative for sandbox membership, current existence, and macOS support; Reveal adds no general opener, read, write, or download authorization and changes no Library scope, query/filter/paging, selection, pin, preview, active Dialogue/draft, or lower-pane ownership. A File row whose current server result also supplies an `obsidian://open` locator receives a separate **Open in Obsidian** action. Its native anchor uses that exact URI as `href` and delegates activation to the browser and operating system; Ora has no completion signal and does not claim that Obsidian accepted or completed the handoff. Dialogues, Engrams, and Files without the locator receive no such action. Open in Obsidian is independent of Reveal in Finder and changes none of the same Library, active-Dialogue, draft, preview, or pane state and ownership. **New Dialogue with checked context** translates every selected readable Dialogue and indexed atomic Engram into repeated `include_ref` values for the existing `/api/conversations/browser?purpose=creation` discovery, review-token, and create-token flow. For that discovery request only, a named Library scope is sent as `project_id`; Commons, an absent scope, and legacy `general` omit it. Every creation opening resets the ephemeral discovery scope, and neither it nor the Library selection changes the ordinary project-membership rule for the new Dialogue. Explicit contributors are promoted ahead of its ordinary result limit so all enter the review candidate set. Files and metadata-only Dialogues are named as unsupported, and the target privacy tag is conservatively derived from all checked metadata before the server revalidates each contributor.

For Dialogues, that shared browse takes one coherent envelope snapshot and composes readable structural envelopes with the separately privacy-admitted live rows. The same snapshot supplies both the complete readable identity set and any lexical query matches. If an envelope has no exchange with authoritative per-turn privacy, queryless inventory retains a generic metadata-only row with stable identity, project membership, active/inactive lifecycle, message count, and known read-only policy. It does not expose a human-readable title, tags, privacy classification, activity time, snippet, content, preview, or content locator; those fields remain unavailable until per-turn authority exists, and the conversation-level tag is never treated as a substitute. Relationship authority is unavailable or incomplete, with no invented summaries or timestamp. Such a row is excluded from body-search matches but continues to make completeness explicit. A metadata-only fallback or a missing/unreadable envelope keeps the Dialogue provider and combined universe incomplete without loosening the existing interactive or Related privacy filters.

**Shared Markdown presentation (P2 source implementation).** `server/document-surface/` builds the committed local IIFE and included-package notices in `server/static/vendor/document-surface/`; `index-v3.html` loads it before Library and Overview, and `ora-default.css` imports `components/document-surface.css`. Exact package pins and a lockfile fix CodeMirror 6, Markdown-it, DOMPurify, and the build toolchain. The product loads no CDN, npm, source maps, or network dependency. The exact-pinned `jsdom` harness is test-only and excluded from the runtime bundle and runtime notices.

`OraDocumentSurface.renderRead({host, markdown, ariaLabel})` returns a Read owner with `destroy()`; `createEditor({host, text, ariaLabel, onChange})` returns `getText()`, `setDisabled(disabled)`, `focus()`, and `destroy()`. Destruction is idempotent. The bundle visibly refuses a second live editor rather than replacing an existing draft. Source identity, paths, requests, authority, digests, saves, and indexing remain with callers; the shared layer has no store, router, source registry, or Obsidian compatibility API.

Read supports headings, paragraphs, lists, blockquotes, fenced and inline code, emphasis, tables, and thematic breaks. Markdown-it disables raw HTML, automatic linkification, typography, highlighter callbacks, and third-party plugins. Explicit `http:`, `https:`, and `mailto:` destinations alone become active anchors with fixed safe attributes. Images, embeds, relative paths, fragments, wikilinks, and file/data/javascript/Obsidian/custom schemes remain meaningful labelled inert content, with no automatic resource fetch. DOMPurify sanitizes at the HTML sink using a narrow HTML-only element/attribute allowlist; styles, event attributes, forms/controls, frames/media, arbitrary IDs/classes, SVG, and MathML are excluded. Sanitized markup is inserted without a later markup rewrite. The caller's Markdown is preserved; the renderer never strips frontmatter.

Rendered Read accepts at most 4 MiB of UTF-8 source. A larger authorized Library document falls back to literal text with an explanation and keeps its existing Edit policy. Missing bundle, parser/sanitizer failure, or invalid rendered output also yields literal source plus a visible diagnostic. Edit becomes visibly unavailable when the bundle or rendered presentation fails; a native textarea does not return as fallback. The existing served page has no effective Content Security Policy; P2 adds neither a CSP nor script/style authority, remote hosts, or a nonce system. CodeMirror's required runtime style module is mounted with the editor and removed on destruction.

Library keeps document, metadata/relationships, status, and controls in stable separate slots. Ordinary relationship/status changes and same-row refreshes preserve the same editor, full draft, original digest, selection, focus, and undo history. Save disables that editor and suppresses duplicate PUTs; failure re-enables it. Cancel, successful Save, repin, source/project-scope invalidation, disappearance of the pin, close, and terminal preview teardown destroy it and invalidate applicable work. Preview, Edit, and Save responses must pass identity and generation checks; a late PUT may have persisted server-side but cannot replace a newer surface. Engram indexing still occurs after the existing atomic write and lock release, and saved-but-unindexed remains distinct from an unwritten refusal or unknown transport outcome.

**Overview composition and widget source adapter (scoped D1/P3 source implementation).** `orchestrator/overview_widgets.py` exports `load_overview_widget_sources(...)`, the renderer-neutral, read-only adapter used by the browser's Overview response. It returns five projections in fixed order — Project priority, Oversight, Triggers, the Daily Note for the completed previous local day, and `matrix-tasks`, displayed as Tasks. Source rows retain availability, state, count, freshness, error and supported actions. The request reads `operation_matrix.list_active_project_meta` once and shares that ordered inventory and skipped-authority information with Priority and Tasks. Only real, exact-`active` projects enter either source, in canonical ranked-then-unranked order. Equal ranks and the unranked tail use ascending immutable nexus, never activity time or display name.

Source failures remain isolated except for the deliberate shared project inventory: its failure can affect both Priority and Tasks without disabling Oversight, Triggers or Daily Note. A source may be `ready`, `empty`, `partial`, `missing`, or `unavailable`, with freshness and source-specific errors rather than a fabricated successful zero. A skipped project record can make the project-dependent sources partial, one usable Paused or Operating lane can leave Oversight partial, and an absent prior-day Daily Note is missing. Tasks additionally qualifies each group as ready, empty, read-only, partial or unavailable. An authenticated absent Tasks section is zero; unresolved or unreadable Matrix authority is unknown. Partial totals are labelled known counts, and one bad Matrix does not disable healthy groups. Owning modules retain full-data authority; the shell never generates a Daily Note.

The permanent left rail opens and closes one Ora-owned full-screen Overview surface from its dedicated mid-height control. Opening preserves the exact prior workspace; closing restores that workspace rather than rebuilding or resetting it. A card exposes an action entrance only when its adapter item names an action the existing owning surface supports. Each Trigger row's server adapter advertises explicit read-only `open_scheduled`. **Open in Scheduled** closes Overview, restores the exact prior workspace, then asks the existing Oversight Scheduled controller to refetch, open, expand, and focus the exact stable Trigger card. Overview never calls or duplicates review, activate, run, pause, resume, or retire controls.

`overview-desktop.js`, its component CSS and the isolated `index-v3.html` mount render one native active-project ring and five cards inside `overviewDesktopSources`. Ring and Project priority card share the same source result from the entrance's single `/api/overview` request. Canonical DOM order determines angular slots; activity changes labelled emphasis, never order or membership. An explicit priority reorder appears on the next entrance. Membership and viewport changes may alter geometry without local re-ranking. Full ring, compact ring and centered wrapping use the same native project controls; wrapping scrolls vertically and retains every project, with full names accessible when shortened. Empty and one-project states remain useful. At roomy widths the original four cards surround the project composition and Tasks follows with working width; narrow widths use the canonical one-column flow without changing the original source order or ring geometry.

The central O/spine reuses passive vector shapes in Overview-owned, `aria-hidden`, noninteractive markup with no live-logo IDs, roles, listeners or tab stops. Native text carries project identity, priority, activity and actions without color alone. Layout settles, and reduced motion removes nonessential movement. Local Tab/Shift-Tab containment follows only visible, enabled, non-inert controls, including deepest reader focus across shadow DOM; Close is the fallback. Reflow preserves useful focus, and Escape closes Overview even in Read. No second renderer, request timer, content/order store, corpus scanner or count service is introduced.

Every valid project supplies `open_project`, `open_project_files`, `open_project_dialogues` and `open_project_knowledge`. The client requires stable project scope, a declared action and a callable destination owner. **Overview** restores workspace ownership through the existing close path, then calls `OraProjectModal.open(nexus,name)` for that project's Overview tab. **Files**, **Dialogues** and **Knowledge** likewise close before calling `OraLibraryWorkspace.open` with the exact project, respectively `['files']`, `['dialogues']` and `['engrams']`, and `cleanBrowse:true`. Destinations receive a stable visible workspace return-focus target rather than a hidden project button. Missing owners or malformed items produce local unavailability while Overview and unrelated cards stay usable.

Projects represent collections, not individual content nodes. The composition displays the known project count with partial/unavailable qualification and never promotes skipped records into a complete global total. Incomplete Dialogue inventory or relationships do not disable an otherwise valid browse entrance. Collection counts, content and freshness remain with Library after navigation; Overview does not fetch every project's corpus to decorate the ring.

**Tasks routes and source authority.** `GET /api/projects/<nexus>/tasks` returns `{ok:true,group}`; `POST` performs one strictly typed operation using `expected_digest` and only its necessary source-bound target, destination, position or value. Operations are `add`, `edit`, `delete`, `reorder`, `indent`, `outdent`, `promote`, `complete`, `reopen`, `set-date` and `clear-date`. Neither route accepts query parameters, browser paths, display-name authority or raw section replacement. Both retain the shared app-wide browser-origin refusal before the view; POST repeats that check locally. Both recheck current persisted project/folder identity and authenticate the unique regular nonsymlink Matrix under the authorized root. Authenticated tasks with missing, scalar or invalid classification remain visible read-only with a reason; unresolved identity exposes no guessed body. Mutation additionally requires writable storage and the existing list-form classification gate. Chapter 14 owns the lossless operation and write contract.

The Tasks source resolves all requested project groups through one Operation-Matrix multi-nexus pass. It retains the single-target resolver's nexus, persisted-folder, Commons and duplicate-claim rules, scans each Matrix once per call, and adds no persistent cache or local ranking. The browser receives each group with its digest and source-position task references; duplicate labels remain distinct and references expire with the full-file snapshot. Missing Matrix storage offers the existing project-opening action, not a task-driven Matrix creation path.

**Scoped Tasks interaction.** One native keyboard-operable card exposes project expansion, every completed/nested task, plain action names, labelled text/date controls and literal unfamiliar source. Hierarchy is stated semantically and in text, not just indentation. Each deliberate action sends one request and disables only affected pending/duplicate controls. Success replaces only that project group, using returned target correspondence to restore focus or choosing an adjacent/group control after deletion; it never re-renders the source host, ring, other groups/drafts or underlying workspace. Opaque content is rendered as text, never executable HTML.

Text/date drafts survive refusal, conflict and uncertainty. Explicit refresh retains the draft but requires target re-selection if correspondence is unavailable; equal labels are never a retargeting rule. A lost, malformed or interrupted mutation response may follow a completed write, so the group requires refresh before another mutation and never automatically replays the action. Requests bind Overview generation and group identity; closing, handoff, teardown or a superseding reload invalidates late responses. Abort does not prove that a write was cancelled.

Unsaved Tasks drafts live only in the existing controller's transient memory through closing/reopening until explicit Save/Cancel or page teardown. Reopening refreshes source authority before another write and does not silently rebind old drafts. The source refreshes on open, explicit refresh and its own success, with no polling or implied live synchronization. Daily Read hides the mounted browse host, and Back restores the same Tasks state; Escape closes Overview without saving. Existing workspace inertness, focus containment including shadow-root reader focus, project/Library handoffs and reduced motion remain in force.

An available, regular previous-day Daily Note retains its separate actions. Its adapter row advertises `read_note` and `open_note`, and the browser renders **Read in Ora** and **Open externally** only for that available identity; a missing or unavailable row stays non-actionable even if malformed data names the action. The external-open click disables only that button and posts exactly `{"id":"daily-note:YYYY-MM-DD"}` to `POST /api/overview/daily-note/open`; no client path or URI crosses the boundary.

**Read in Ora** sends exactly `GET /api/overview/daily-note/read?id=daily-note:YYYY-MM-DD`. The route accepts one `id`, no duplicate/extra query parameters, and no body. The browser-origin check rejects hostile Origin before file access; the server recomputes the completed previous local day and resolves only through the existing Daily Note path authority. Hosts without the required no-follow support refuse this reader safely. It binds both the parent directory and file without following symlinks, reads at most 4 MiB plus one byte, and verifies device, inode, size, and modification time before, during, and after the read. It refuses changed custody, stale identity, unsafe/nonregular/missing/unreadable/oversize data, and invalid UTF-8 before returning any note content.

The narrow leading-YAML validator uses the existing safe YAML parser and requires a mapping declaring `type: daily-note` and the matching date. Success returns exactly `{id, source: "daily-note", text}` with the body-only Markdown and `Cache-Control: no-store`; it returns no path or metadata and neither mutates, generates, repairs, indexes, nor launches a note. Malformed query, identity, or body is `400`; hostile Origin is `403`; missing is `404`; stale, unsafe, changed, oversize, invalid text/frontmatter is `409`; unsupported method remains `405`. Errors expose no path or note content. Oversize prevents this reader response, not the card preview or existing external-open action.

The ring and five cards remain mounted inside the hidden sources host behind an Overview-owned reader host. Only the pending Read action is disabled, and its abort controller plus monotonically checked generation prevent a late response from entering a closed or newer session even when the dated identity is unchanged. Valid content uses only shared `renderRead`; no Daily Note can enter Edit. **Back to Overview** destroys the reader, shows the unchanged ring/cards and Tasks drafts, and returns focus to its initiating action when possible. Close or Escape aborts and invalidates pending work, destroys the reader, restores the prior workspace/focus through the existing close path, and resets the next opening to browse. Reader failure leaves the cards usable.

Before native dispatch, the external-open route applies the shared browser-origin check, rejects extra fields and malformed identities, recomputes the completed previous local day, and refuses a stale date rather than substituting another note. It resolves the canonical path again from `daily_note.daily_dir()`: missing stays missing, while a symlink or non-directory root and a symlink or non-regular target are unsafe. The action does not read, write, create, overwrite, or regenerate note content.

On local macOS, the preferred handoff is one shell-free `/usr/bin/open -a Obsidian` call, bounded to five seconds, with the validated absolute path fully percent-encoded in an `obsidian://open?path=` URI. Exit zero means only **Open request sent to Obsidian**, not that the note appeared. Only a positive Obsidian return code is a definite refusal. Before fallback, Ora recomputes the completed previous local day and revalidates both the date and filesystem path; if either changed, it refuses rather than opening stale material. A timeout or signal-terminated preferred handoff is uncertain, so Ora names the uncertainty and tries no other application; a signal-terminated fallback is likewise uncertain rather than a definite failure. Missing, stale, unsafe, unsupported, launch, and fallback failures remain visible without command output or stack traces.

A confirmed Obsidian or fallback dispatch uses the existing close path, leaves plain-text status visible, and restores the prior Dialogue, draft, panes, workspace, and focus. Failure or uncertainty leaves the cards usable, restores the pending button, and starts no retry; an older response cannot close a reopened Overview. D1/P3 source behavior does not activate live Ora or prove native display. Saved layout, widget configuration, quick links, priority editing, connectors, a new store/scheduler, remote/wake behavior and Library replacement remain separate work. Tasks does not add a raw-section editor or advanced task-management fields.

**Second decision: analytical selection and the bypass boundary.** The Analyses picker, manual selection and lens eligibility read the same validated routing-source collection as automatic routing. Picker names, territories, aliases and Display Descriptions come from the maintained Markdown sources; required, optional, foundational and lens-owned relationships retain their categories and qualifications. A source collection that cannot compile is not advertised as a partially valid analysis catalogue. An explicit manual pick replaces the prior automatic selection metadata and uses that mode's compiled input contract at the missing-input boundary. Its saved question uses the same explicit Answer/Skip flow as automatic clarification, preserving the pending turn's configuration, model and Dialogue authority.

A pending routing or completeness question pauses the Inquiry before response execution: the server atomically saves the submitted Inquiry, question and pending identity in the Dialogue envelope before returning `status: "clarification_pending"`, and saves each question as an owned conversation chunk so it remains forkable and supports exact-turn privacy changes. V3 shows one question with **Answer** and **Skip** controls. Answer posts free text to `/api/clarification`; Skip posts to `/api/clarification/skip`. Both bind the Dialogue and exact pending identity, and ordinary chat cannot consume or replace that pending question. An insufficient answer saves a new question and identity; Skip uses the `simple` response mode with the available information. Reopening the Dialogue restores its question through `/api/clarification/pending`. A failed save restores the claimed answer or Skip choice as a locked **Retry** action, including after reopening; retry sends that exact saved choice. A running execution whose result was lost on restart is shown as interrupted, without Retry. V3 reloads the saved Dialogue after `done` or an acknowledged follow-up question, and never presents unsaved response text as a completed result. Chapter 8 describes the persistence and interrupted-execution limits.

**Third decision: single-user localhost only.** `app.run(host="localhost", ...)` (`server.py:15040`). No auth middleware, no non-localhost bind, no Content-Security-Policy header on the served page. This is a deliberate scope choice for a single-user local tool, and it is honest about its limits: the multi-machine / remote-access goal that appears elsewhere in the project's notes is **not built at the server layer**. Binding beyond localhost without first adding an auth and CSP layer would expose an unauthenticated pipeline that can write files and call paid APIs. That layer is planned, not present.

**Open problems.** The 12s poll cadence is hardcoded independently in two files — `sidebar.js` (`REFRESH_INTERVAL_MS = 12000`, line 26) and `sidebar-oversight.js` (`POLL_MS = 12000`, line 33). Changing the reconciliation interval requires touching both; there is no single source of truth for it. And the durability claim here is qualitative: the design *prevents* silent loss by ordering disk writes before the reply, but nothing measures a loss rate or a recovery rate. There is no instrumented reliability budget, and I will not invent one — the named failure mode is a mid-request crash between the pending-file write and the reply, and the guarantee is that such a crash surfaces the input as a recoverable errored chunk on next boot, not that some percentage of runs recover.

### Implementation

**Startup and self-heal (`server.py:14780`–`15040`).** The `__main__` block is where the process assembles itself, and its ordering is load-bearing. It parses two flags — `--scheduler` (task scheduler, `server.py:14784`) and `--oversight` (meta-layer daemon, `server.py:14785`). It then binds the first free port in the range 5000–5010 by trying `socket.bind` on each in turn (`server.py:14789`); the first success wins and the loser ports are skipped. If all eleven are busy, the loop falls through with `port` still at its initial `5000` and `app.run` will then fail to bind — there is no explicit "all ports busy" error path.

Port selection is immediately followed by the core job queue's one-shot paid-provider reconciliation, before active-project or Trigger migrations, any worker start, or Flask HTTP serving.
This core-owned startup seam runs whether or not the optional video feature package is installed. It reattaches only a Replicate prediction durably bound to the same existing Dialogue and local job.
An indeterminate paid contact without that exact prediction binding is failed locally and is never submitted again on restart.

After the bind, a sequence of self-heal hooks runs, each wrapped in its own `try/except` so a failure prints and continues rather than aborting boot: `platform_check.startup_check` (engine-matches-machine validation, `server.py:14797`), the API in-flight pool cap sized from `install-state.json` via `mlx_mutex.configure_api_pool_from_install_state` (`server.py:14808`), an MLX worker heartbeat every 30s for the oversight health surface (`server.py:14822`), an embedding-readiness probe against the Ollama daemon (`server.py:14833`), index/RAG-manifest regeneration (shelling out to `bash generate-indexes.sh` / `compile-rag-manifest.sh` at `server.py:14856` / `14893` when the on-disk indexes drift from the directories), OpenRouter and direct-API catalog refreshes, local-MLX-model discovery that rewrites the `local_models` section of `config/models.json`, and a Lucide icon-set rebuild. Each is best-effort by design: this is a personal tool that should boot degraded rather than refuse to boot. Both startup catalog refreshers launch with the server's running Python interpreter (`sys.executable`), including an activated virtual environment selected by the launcher. They retain the inherited environment with runtime-path overrides, seven-day freshness checks, and the 120-second Direct-API and 60-second OpenRouter timeouts; Direct-API writes its success stamp only after a zero exit status, and refresh failures still print and let startup continue.

Two of these hooks are the durability spine. The **orphan pending-submission scan** (`_scan_orphaned_pending_submissions` in `server/app.py`) walks the pending submission directory at boot. Recovery holds the existing Dialogue lifecycle and envelope-write locks, including the cross-process file lock, while preparing the errored chunk and envelope. A missing envelope may be created; an unreadable existing envelope stays byte-for-byte at its original path. A readable envelope retains its prior messages and metadata while receiving the interrupted prompt, retry identity, and error state. The chunk and envelope each use their existing atomic writer. Only after both writes succeed does the scanner move the pending record to `processed/`; a refused or failed recovery leaves that source record pending, with read or write failures logged. This is the crash-recovery half of the lost-work guarantee. The **SIGINT/SIGTERM shutdown handler** fires `session_end` hooks, clears all in-memory sidebar windows, and cleans up bash-execution state before exiting.

**Trusted feature host.** During module assembly the server gives
`server/feature_plugins.py` an explicit ordered source list. Production lists
only Video, preserving its routes, asset URLs, and lifecycle callbacks. The host
can retain multiple valid descriptors, inject each one's ordered browser assets,
and dispatch lifecycle phases across them. An absent or broken source is logged
and skipped independently; there is no filesystem discovery, installer, or
third-party loading surface.

**The `/chat` request lifecycle.** This is the path to read first for the whole subsystem.

1. `chat()` (`server.py:6297`) parses the payload and immediately calls `_log_pending_submission` (`server.py:5352`) — *before* validation, before attachment processing, before anything that could fail. It writes the full submission (input, history, mode/framework selections, attachments) to a `pending/<submission_id>.json` record. This write is the recoverable record. If it fails, the handler logs and proceeds anyway — a disk error must not be the thing that drops the user's input.
2. Attachments are partitioned: text is inlined into the prompt, images are passed separately. Multipart image uploads (via `/chat/multipart`, `server.py:6439`) land under `~/ora/sessions/<conversation_id>/uploads/`.
3. `_invoke_pipeline` (`server.py:5997`) drives the pipeline. It iterates `agentic_loop_stream` synchronously (`server.py:6091`) — the generator still yields SSE-framed chunks internally, but the server consumes them (`json.loads(chunk[6:])`, `server.py:6101`) rather than forwarding them, collecting the original producer response and last-seen stage/mode/gear for bridge state. `agentic_loop_stream` (`server.py:3828`) itself dispatches to `_pipeline_stream` (analytical, `server.py:3003`) or `_direct_stream` (bypass, `server.py:3666`). After production, `_run_pipeline_from_step2` folds the after-clock over that original text and passes its signals and the original response through `_execution_review_terminal` before emitting one response frame. The terminal may return it unchanged, revised, or with a visible mandatory-handoff failure. Browser and resumed-clarification callers receive that value exactly once; route-observation and producer-claim bookkeeping remain tied to the original response. The conversation is marked Pending for the duration and cleared in a `finally`.
4. On a final response, `_save_conversation` (`server.py:5626`) runs synchronously under the Dialogue lifecycle lock so the `chunk_id` is known before the reply. It accepts only an exact Standard, Private, or Stealth authority for this exchange and requires Stealth authority to agree with the Dialogue's creation identity. The two new envelope messages receive the same local `turn_index`, `chunk_id`, and `turn_privacy`. The raw recovery log remains in its Dialogue-owned session file and marks the pair number and privacy; the processed chunk, index manifest, and Chroma row bind the same exchange through the Dialogue, turn/pair, chunk, and privacy fields available on those representations. One Dialogue may therefore contain Standard and Private exchanges side by side. Its current composer tag does not fill in authority missing from an older exchange. The envelope and raw exchange remain authoritative content; chunks, manifests, and index rows are owned representations, and false or partial metadata cannot make an unauthenticated turn eligible. The `_save_conversation` docstring still names `nomic-embed-text-v1.5` as the embedder; per the project's own migration notes the embedder is now bge-m3 — a docstring drift, harmless to this chapter but worth flagging when you read that function.
5. On success, `_finalize_pending_submission` moves the pending file to `processed/`; on failure it is left in `pending/` for the next boot's orphan scan. The reply is `{"status": "ok", ...}` or `{"status": "errored", ..., "failure_summary": ...}`. A durably saved clarification instead returns `{"status": "clarification_pending", "conversation_id": ..., "clarification": ...}` and finalizes the submission record without saving a guessed analysis chunk. Failure marking updates a readable envelope; it does not replace an unreadable one. Existing unreadable Dialogue privacy authority is refused rather than treated as a new Standard Dialogue, and Close returns `409` when that authority cannot be read. The failure summary is deliberately informative: when the generator returned without a response and without an error, the code synthesizes a breadcrumb from the last observed stage rather than the bare "no response" string.

**Current Model response and protected-operation boundaries.** Gear-1/2 and required Gear-3/4 model-call boundaries return usable text with invocation identities or raise a typed failure; exhaustion does not create a completed assistant answer from diagnostic text. The ordinary response handler and successful clarification resumes drain request-thread Pipeline Health warnings, including one warning when recovery selected a different model, and prepend the warning note before saving the response. Parallel workers return their outcomes for that request-thread aggregation. Same-model retries emit no substitution warning, and a warning-surface failure logs without breaking usable output.

`DELETE /api/configurations/<name>` and `POST /api/retrieval/rebuild/start` complete deterministic preflight before one-shot approval. The former binds the exact custom profile, active pointer, and existing validated Free fallback when needed; the latter binds the vector-store/configuration/script state and one immutable profile/script plan. Existing locks govern their immediate rechecks before mutation or worker admission. Invalid initial targets consume no approval or start receipt; later drift, contention, launch failure, or effect failure closes with a failed receipt. Receipt infrastructure failure is surfaced, never reported as a successful operation. Chapters 4 and 8 give the respective effect boundaries.

Side effects that do not gate delivery begin only after the complete exchange is durable. In particular, background document extraction does not trust the prompt and answer retained by its worker. It reacquires the same Dialogue lifecycle lock, rereads the envelope, and authenticates the current local turn number, chunk id, content, and privacy before processing. It receives that one freshly authenticated exchange, not the preceding Dialogue history. Extraction failure is logged loudly but does not retract an answer already delivered.

The browser exposes two different privacy operations because their consequences differ. **Compose next as Standard/Private** changes only the next exchange's proposed authority. **Make displayed turn Standard/Private** identifies the exact displayed owner, reacquires the displayed and canonical owner locks in deterministic order, resolves the owner again, and then changes one complete non-Stealth exchange. Tightening to Private updates only exact Ora-owned representations and the exact managed Daily Note summary. Missing, conflicting, edited, or ambiguous material is retained and returned as incomplete; the UI must not claim that every copy changed.

The Findings export toolbar also treats browser state as a view, not evidence. **Save output to Vault**, Word, and PDF send only the owner tuple emitted with the displayed exchange; the server rereads effective history and the canonical local owner before using canonical assistant content and deriving a title. A Private output remains visibly labelled and Private-tagged. An explicit Stealth output omits privacy markers and source identity. **Save full Dialogue** authenticates every effective exchange while holding all owner lifecycle locks in deterministic order; a non-Stealth target rejects Stealth-owned turns, while a Stealth target may include canonically eligible inherited Standard or Private ancestry and uses a neutral source-free fallback name when its canonical title is unsafe.

**Cross-platform path resolution.** Every filesystem root the server touches now flows through `orchestrator/runtime_paths.py`. `WORKSPACE` honors `$ORA_HOME` before falling back to `expanduser("~/ora")` (`server.py:22`); conversation roots come from `runtime_paths.CONVERSATIONS_STR`, which resolves `$ORA_CONVERSATIONS` or `expanduser("~")/Documents/conversations` (`runtime_paths.py:27`,`38`). Because the defaults are built from `expanduser("~")` rather than literal `/Users/...`, they resolve to `%USERPROFILE%` on Windows. The module also carries the pieces a Windows port actually needs: `norm_key` / `within_base` (`runtime_paths.py:49`,`61`) do case- and separator-normalized, directory-boundary-anchored path comparison — so a protected-prefix check is not fooled by a sibling like `~/ora-project` matching `~/ora` — and `locked_file` (`runtime_paths.py:100`) is a cross-platform advisory lock that uses `fcntl` on POSIX and `msvcrt` on Windows, each imported under a guard so neither platform crashes at import (`runtime_paths.py:88`,`92`). One residual hardcode survives the retrofit inside `server.py`: `VISUAL_UPLOADS_ROOT = os.path.expanduser("~/ora/sessions/")` (`server.py:6376`) does not consult `$ORA_HOME`, so a relocated install writes uploads to the un-relocated root. That is a portability drift, not a crash.

**Config surfaces.** The tracked launcher `start.sh` exports the runtime flags — `ORA_HOME`, `ORA_RAG_SELECTION=1`, `ORA_RAG_FIT_GATE_SLOT=sidebar`, `ORA_WEB_EXTRACTION=1`, `ORA_RUNTIME_ENGRAM_PROMOTION=1`, `ORA_DELIVERABLE_SCRUB=1` and more (`start.sh:12-58`) — then launches the server backgrounded (`"$PYTHON" server/app.py --oversight &`), selecting `/opt/homebrew/bin/python3` with a system-`python3` fallback. Model routing is edited through `GET/POST /config/routing/slots` (`server.py:14042`/`14071`), which merges `{preferred, fallback}` into a single named slot within `routing-config.json` and rejects underscore-prefixed keys from client patches so annotations can't be clobbered. Named configurations live behind the `/api/configurations*` family (`server.py:13395`–`13596`): list, duplicate, new, per-slot edit, per-slot fallback edit, delete, active-get/set, and active-toggles. The active pipeline is chosen through these, not a config-file edit.

**Programming documentation-evidence forwarding (current scoped behavior).** `POST /api/programming/run` accepts an optional `documentation_review` object alongside the approved plan. The server rejects a non-object value and forwards the object unchanged to `orchestrator.programming.run_approved_programming`; it does not reinterpret, summarize, or certify the evidence. The browser sends that object only when it resumes a documentation-impacting task at the final evidence boundary, after sending the pasted raw JSON through the existing privacy screen. The HTTP layer preserves the coordinator's complete packet; Programming validates its final gate state and the independent reviewer owns the semantic verdict described in Chapter 18.

**The route surface (174 routes).** Grouped by family: chat (`/chat`, `/chat/multipart`, `/chat/queue-retry`); clarification (`/api/clarification`, `/skip`, `/pending`); conversations (list, load, fork, mark-read, close); configurations and routing slots (above); oversight (`/api/oversight/paused` at `server.py:14683`, `/api/oversight/operating` at `14716`, and per-entry `/name`, `/engagement`, `/discuss` at `14731`–`14761`); canvas/visual (`/api/canvas/save` at `6691`, `/api/canvas/load/<id>` at `6792`, visual-schema serving); media capability — nine wired image/video POST routes (`image_generates` at `server.py:11048`, then `image_edits` `11227`, `image_outpaints` `11403`, `image_upscales` `11615`, `image_styles` `11790`, `image_critique` `11987`, `image_varies` `12375`, `image_to_prompt` `12601`, and `video_generates` `12779`); themes; and the model registry (`/models` at `12932`, `/api/model-registry/picks` at `13652`). The capability image/video routes are live and wired — this contradicts the stale note elsewhere in the project docs that image generation is "locked but not implemented." That note was not superseded in place.

**Residual SSE.** Streaming survives on exactly three route bodies, all returning `text/event-stream`: `/api/document/stream` (`server.py:5084`), `/api/clarification` (`server.py:10714`), and `/api/clarification/skip` (`server.py:10805`); `/api/framework/analyze-inputs` (`server.py:5109`) sits in the same neighborhood. These are the minority path. The pipeline's own progress is not among them.

**The V3 static UI.** `/` and `/v3` both serve `index-v3.html` (`server.py:3859`, `3867`); the classic `/classic` and `/v2` routes were retired (per the `index()` comment, 2026-05-30). `index-v3.html` owns the stable Knowledge Library mount and the single mode-aware listener on the native O. `library-workspace.js` owns ordinary Library state, inventory and pinned-relationship requests, List/Visual rendering, measured geometry, preview layers, relationship disclosure, and the extension-action event; `library-workspace.css` owns its presentation. `sidebar.js` retains the launcher and existing Dialogue mutations but no longer creates a second Library overlay or calls the two-source search endpoint for Library results. It also runs the conversation-list reconciliation loop (12s, visibility-gated so a hidden tab stops polling). `sidebar-oversight.js` drives the three-supergroup accordion (Conversations / Paused / Operating) and polls `/api/oversight/paused` and `/api/oversight/operating` every 12s. `v3-conversation.js` remains the active Dialogue owner and renders assistant turns; Library pin preview does not call it.

Aside submission uses the existing privacy preparation screen. Choosing Private updates the current Dialogue’s privacy in place; it does not create another Dialogue. Its callback reads the resulting active Dialogue ID only after that process completes, preserving any main Inquiry draft. While a Dialogue is loading or a composition target is unresolved, Aside does not send the prompt. The browser sends that ID with the prompt to `/api/scratchpad` and accepts a reply only for the same ID. Each Dialogue has its own temporary display of up to ten query/answer pairs. Selection hides the previous display while loading; successful navigation restores the selected Dialogue's display, and failed loading restores the still-active one. A late reply stays with its original Dialogue's display. Successful Delete Forever discards that display and prevents an outstanding reply from recreating it.

**Partial and standalone components.** `/chat/queue-retry` (`server.py:9034`) is **partial**: it persists vision-retry requests both in-memory and to `~/ora/sessions/<conversation_id>/vision-retry-queue.json`, but its own docstring is explicit — "NO automatic retry here — a future daemon or user-triggered action will flush the queue." Persistence is built; the flush is not.

`server/review.py` remains a **separate** standalone Flask app (the Note Review interface) that binds `127.0.0.1:5005` in its own `__main__`; it is not imported by `server/app.py`. Its runtime-created review records are nevertheless part of the per-exchange privacy contract. Each carries the exact Dialogue owner, local turn, chunk, and privacy from the authenticated extraction source. Approve, edit, and reject take the same cross-process Dialogue lifecycle lock used by retag, reread the review record under that lock, and refuse if its owner changed before an approved Markdown derivative can be published.

**Failure behavior, summarized.** Every startup hook is best-effort. A pipeline crash mid-run leaves the pending file in place; the next boot surfaces an errored chunk when recovery can save the chunk and envelope, otherwise it logs the failure and retains the pending source. A save failure returns `status: errored` and marks a readable envelope without replacing unreadable authority. The oversight health surface prepends a warning to the next chat response when the daemon is down or a watcher is stale, so the user sees degradation in the conversation without a separate dashboard.

### Evidence & Verification

**What is tested.** The focused `server/static/tests/test-library-ui-quick-fixes.js` harness covers mixed-source continuity, truthful refinements/chips/grouping/counts, saved-view fallback, progressive completion/cancellation, stable Visual slots, checked selection, admitted typed Trace and fifty-neighbor expansion, derived entry, fixed Dialogue speakers, archive reading, P2 edit/draft/save/close ownership, keyboard/reduced-motion behavior and measured narrow layouts. The approved selectors in `orchestrator/tests/test_library_browser.py` cover complete-universe qualification, exact provenance and inherited projects, retained/indexed archive admission, privacy-safe complete-pair preview, typed neighbor budgets, progressive read-only snapshots and P2 authority/conflict regressions. `TestLibraryFileCategories` in `test_project_meta.py` covers the existing pointer record’s category projection, update validation and unrelated-field preservation. The retained graph/event/startup/quarantine selectors cover read-only snapshots, changed-file refresh without unchanged parsing, identity/referrer repair, cancellation/errors/direct-mutation invalidation and unchanged sibling event consumers. The visual-dispatch fence extraction has `server/static/ora-visual-compiler/tests/cases/test-v3-visual-dispatch.js` (12 assertions, per the project's test docs). The routing-slots endpoint has `orchestrator/tests/test_routing_slots_endpoint.py` (12 Python tests, verified by count in-file) and `server/static/ora-visual-compiler/tests/cases/test-visual-slots-pane.js` (24 JS assertions, per the project's test docs), part of a JS harness cited at 813/813.

**What is not tested — and this is the honest gap.** There is no server-route-level HTTP integration test inside `server/`. A grep of `server.py` for test or pytest references returns nothing in-file. Route correctness is inferred from orchestrator-side unit tests plus live probing, not from an automated suite that boots the app and exercises `POST /chat`. There is no CSP test and no auth test, because there is no CSP header and no auth. The full 174-route surface was mapped by family; the capability/media/theme sub-endpoints are asserted present by their route decorators but not each exercised by an automated request. And whether the three residual SSE endpoints are still exercised by the current V3 JS or are vestigial was not traced through the client.

**How to confirm it works live** (this is a manual path, not CI): launch via `start.sh`; `GET /health` (`server.py:3871`) returns `{"status": "ok", "endpoint": ...}`; `POST /chat` with a real prompt, then confirm a chunk `.md` lands under the conversations directory and the `conversation.json` envelope updates. For the durability guarantee specifically: `POST /chat`, kill the process mid-pipeline before the reply, restart, and confirm the submission reappears as an errored chunk with retry/dismiss (the orphan scan at `server.py:14984`).

### Expert Reading Notes

**Read these four spans first, in order.** (1) The `__main__` block (`server.py:14780`–`15040`) for startup ordering and every self-heal side effect — this is where the process's contract with its environment lives. (2) `_invoke_pipeline` (`server.py:5997`) and (3) the bypass guard inside `_pipeline_stream` (`server.py:3507`–`3541`) for the request lifecycle and the empty-response fix. (4) `_save_conversation` (`server.py:5626`) for the persistence contract — internalize that `conversation.json` is source of truth and chunks are a denormalized cache before you touch either.

**Where future changes most likely cause drift.** First, the doc-vs-code gap on streaming: any documentation that headlines "Flask + SSE" or "SSE streaming with `pipeline_stage` events" is describing the retired model; the installed `/chat` is synchronous JSON with disk persistence and browser polling. Second, the 174-route surface grows without a map, so any route-count or route-family reference goes stale quietly. Third, launcher drift: the tracked `start.sh` sets every runtime `ORA_*` flag (`start.sh:12-58`) and selects `/opt/homebrew/bin/python3` with a system-`python3` fallback, oversight-on by default — this is the committed, reproducible launcher. The author's Mac additionally runs an **untracked** `run-ora-server.sh` launchd wrapper (not in commit 7a5e8f40; a fresh clone won't have it) whose flag set can diverge from `start.sh`. Because the wrapper is untracked and machine-local, anyone reasoning from the commit should treat `start.sh` as authoritative; the divergence is a live hazard only on a machine carrying the untracked wrapper. Fourth, the 12s poll cadence duplicated across `sidebar.js` and `sidebar-oversight.js`. Fifth, the `_save_conversation` embedder docstring naming nomic while the system runs bge-m3.

**One thing to verify before you rely on it.** `sidebar-oversight.js` hedges the Approve/Deny commit path as going through "`/api/oversight/resolve` (or fall back to slash commands)," and its own header comment says the v1 build uses the slash-command path. There is no `/api/oversight/resolve` route in `server.py` — the oversight route family stops at `/discuss` (`server.py:14761`). Approve/Deny commit through the chat resolution-chain / slash-command path, not a dedicated resolve endpoint. Do not build against a resolve endpoint that isn't there.

### Platform compatibility

The macOS+Windows portability retrofit (PR #181) is what this commit *is*, so the platform story here is better than "Mac-only" — but "retrofitted so it should work" is not "run and verified off-Mac," and I label the difference. Nothing below was booted on a non-Mac surface; every non-mac cell that is not a clean-degrade path is "untested."

- **First-free-port bind (5000–5010):** macOS = works | Windows-native = untested | WSL = untested | Linux = untested. Plain `socket.bind` loop on `localhost` (`server.py:14789`); `localhost` resolves cross-platform, but full boot to a bound port was only observed on macOS. If all eleven ports are busy the loop falls through and `app.run` fails to bind — no dedicated error path.
- **Filesystem roots (`$ORA_HOME`, `$ORA_CONVERSATIONS`):** macOS = works | Windows-native = untested | WSL = untested | Linux = untested. Resolved through `runtime_paths.py` from `expanduser("~")` (`runtime_paths.py:27`,`38`), which maps to `%USERPROFILE%` on Windows. The retrofit is the reason this is no longer "needs-porting," but no non-Mac boot was run, so it stays untested rather than "plausibly-works."
- **`VISUAL_UPLOADS_ROOT` hardcode:** macOS = works | Windows-native = wrong-root | WSL = untested | Linux = works-at-default-only. `os.path.expanduser("~/ora/sessions/")` (`server.py:6376`) ignores `$ORA_HOME`; a relocated install writes multipart uploads to the un-relocated path. Drift, not crash.
- **Cross-platform advisory lock:** macOS = works | Windows-native = untested | WSL = works | Linux = works. `fcntl` on POSIX, `msvcrt` on Windows, guarded imports (`runtime_paths.py:88`,`92`). The Windows branch exists but was not exercised here.
- **Untracked launch artifact `run-ora-server.sh` (author's machine only — NOT in commit 7a5e8f40):** a `#!/bin/zsh -l` launchd wrapper hardcoding `/opt/homebrew/bin/python3`; macOS-only by construction and not part of the committed system (a fresh clone does not contain it). The tracked launcher at this commit is `start.sh`.
- **MLX heartbeat + `mlx_mutex` (Apple MLX/Metal):** macOS = works (Apple Silicon) | Windows-native = degrades | WSL = degrades | Linux = degrades. Wrapped in `try/except` (`server.py:14822`); off Apple Silicon the import fails, the hook is skipped, and the local-model path is simply absent — the server still boots.
- **Reveal-in-Finder (`POST /api/fs/reveal` → `open -R`):** macOS = works | Windows-native = clean-501 | WSL = clean-501 | Linux = clean-501. Guarded by `sys.platform != "darwin"` returning HTTP 501 "reveal-in-Finder is macOS-only" (`server.py:4948`) before it shells out to `open -R`. This one *was* ported — it degrades cleanly rather than erroring on a missing `open`.
- **Startup `bash` script calls (`generate-indexes.sh`, `compile-rag-manifest.sh`):** macOS = works | Windows-native = fails-soft | WSL = works | Linux = works. Invoked as `subprocess.run(["bash", ...])` (`server.py:14856`,`14893`); no `bash` on Windows-native means these fail, but they are inside the startup `try/except` and only skip index/manifest self-heal.
- **SIGINT/SIGTERM shutdown + threaded Flask:** macOS = works | Windows-native = untested | WSL = works | Linux = works. `signal.signal` handlers (`server.py:14975`–`14976`); SIGTERM semantics differ on Windows-native.
- **Embedding-readiness probe (Ollama reachability):** macOS = works | Windows-native = untested | WSL = untested | Linux = untested. `assert_embedding_ready` at `server.py:14833`; the module comment claims Win/Linux/Mac but only Mac was observed.
- **`index-v3.html` asset loading (absolute `/static` URLs, deferred scripts):** macOS = works | Windows-native = works | WSL = works | Linux = works. Browser-served absolute URLs are OS-agnostic and carry no filesystem-separator concern client-side.
- **CSP / auth / non-localhost bind:** all platforms = absent. No `Content-Security-Policy` header on the served page (0 matches in `index-v3.html`), no auth middleware, `host="localhost"` only (`server.py:15040`). This is a scope decision, uniform across platforms, and it is the blocker for any remote-access goal.

## 3. Settings, API Keys & Local-Model Handling

- **Current API-key store boundary.** Before approval, `POST /api/settings/api-key` requires a registered provider and one non-empty string value. Approval binds the exact raw string that the handler stores; malformed values cannot consume the approval.

### Problem

Three failure modes converge on one subsystem, and each is the kind that corrupts trust in an install rather than merely inconveniencing it.

The first is secret leakage. An orchestrator that talks to two dozen external providers holds a bearer credential for each, and each of those credentials bills the user's card. Store those keys in a JSON config file and they leak into git, into backups, into log lines, into any error message that echoes a request URL. The naive alternative — re-prompt for keys every session — trades leakage for friction so severe the product is unusable. The requirement is keys durable across restarts, absent from every file the user might share, and scrubbed from every error string that can round-trip to a browser.

The second is provider-metadata drift. The Settings UI must know each provider's display label and console URL. The boot sequence must know each provider's env-var name to bridge its key. The key-setup framework must know each provider's key prefix to validate format. The verify probe must know each provider's authentication scheme. If those four surfaces each carry their own copy of the provider table, they drift: a provider added to the UI never gets its key bridged to the env; a renamed env var silently drops a working key. This is the classic single-source-of-truth problem, and the punishment for getting it wrong is a key the user pasted correctly that nonetheless does nothing.

The third is local-model registry staleness. Ora runs MLX models — Apple's array framework for on-device inference on Apple Silicon — from `~/ora/models/`. When the user drops a new model into that directory or deletes an old one, nothing notices until a human hand-edits `config/models.json`. The V3 Models pane then shows models that are gone and hides models that are present. This was a live gap dated 2026-05-26, recorded in `local_model_discovery.py`'s own module notes: nothing in the system noticed the on-disk change until a human edited `models.json`.

A fourth, quieter need rides along: non-secret preferences — Whisper model size, default render preset, output style, capture frame rate — need a durable home that is neither an environment variable (wrong lifetime, wrong scope) nor per-conversation state (wrong persistence).

### Design Decision & Tradeoffs

Two deliberate splits and one automation choice.

**Split one: secrets live in the OS keyring; everything else lives in JSON.** API keys are written only to the operating system's credential store via the `keyring` library — service name `ora`, username `<provider>-api-key`. On macOS that store is the Keychain. Non-secret preferences live in `config/user-settings.json`, which holds no secrets — its module docstring states "Secrets (API keys) are NOT stored here; they live in the system keyring" (`orchestrator/user_settings.py:5-7`). The rejected alternative was a single encrypted config blob. It reinvents a credential store the OS already ships, audits, and integrates with the user's login session; a homegrown blob is a smaller attack surface's illusion, not its reality.

**The single source of truth for provider metadata is `provider_registry.py`.** One list, `PROVIDERS`, declares every provider's id, label, category, keyring username, env var, signup and console URLs, key prefix, activation flags, dispatch style, and verify eligibility (`orchestrator/provider_registry.py:73-328`). Every consumer derives from it: `user_settings.py` builds its keyring-username map and its UI status rows from `keyring_username_map()` and `ui_rows()`; `boot.py` builds its keyring→env bridge from `env_bridge_pairs()`; the prefer-direct router builds its rewrite table from `or_prefix_map()`. The registry's own docstring states the invariant: adding a provider is a one-entry edit here; no consumer needs touching. The rejected alternative — duplicating provider tables across the JavaScript panel, the Python boot path, and the setup docs — is exactly the drift failure mode above. The tradeoff is a single point of failure inverted into a single point of correctness: a provider omitted from `PROVIDERS` silently vanishes from the env-bridge, the verify probe, and the Settings UI at once. That is a real hazard, and it recurs under Expert Reading Notes because it is the most likely way a future edit breaks this subsystem.

**Split two: desktop and server key handling diverge on purpose.** The Mac desktop uses the keyring. The headless Linux server does not. `scripts/install-server.sh` writes `OPENROUTER_API_KEY` (and optionally `ANTHROPIC_API_KEY`) to a plaintext file at `~/.config/ora-server.env`, `chmod 600`, sourced into the process environment (`scripts/install-server.sh:190-217, 226-231`). This is a deliberate acceptance of plaintext-on-a-locked-box over the cost of wiring a headless credential daemon — the Secret Service backend `keyring` targets on Linux needs a running keyring daemon and a login session a headless VM does not have. What makes the divergence cheap is the bridge design below: the same `boot.py` code serves both hosts because it reads keys from the environment, and the environment is populated either by the keyring mirror (Mac) or by the sourced env-file (Linux). This is an open problem, not a solved one: there is no OS-backed credential store on the server path. The plaintext env-file is the current answer, and a headless-keyring option remains unbuilt. Nothing in `CLAUDE.md` documents the server plaintext path — its only keyring reference is a note about which Python interpreter ships the `keyring` package — so a reader who knows only the desktop model can wrongly assume keyring everywhere.

**Automation choice: local models are discovered, not curated.** `local_model_discovery.py` scans `~/ora/models/` at server startup and rewrites the `local_models` array in `config/models.json`. The rejected alternative — hand-curated entries — is the staleness failure mode by construction. The tradeoff is that discovery can only read what the model files declare, so a model with a malformed `config.json` is silently skipped rather than surfaced as an error. Against that, a no-shrink guard: `refresh()` refuses any write that would record fewer local models than `models.json` already holds, printing the refusal and the missing ids, so neither an unmounted models directory nor a readable-but-empty one can truncate the registry (`local_model_discovery.py::refresh`). Without that guard, a race between a mount and server startup would erase every local model from the UI; deliberate removal is the exception and passes `allow_shrink=True` (CLI `--write --allow-shrink`).

### Implementation

**Provider registry (`orchestrator/provider_registry.py`).** `PROVIDERS` is a list of 24 dicts. The derived helpers are pure functions over that list: `keyring_username_map()`, `labels_map()`, `env_bridge_pairs()` (every provider declaring an `env_var`), `direct_llm_providers()` (those with a `dispatch` value), `catalog_authoritative_providers()` (direct providers with a real pay-as-you-go path), `or_prefix_map()` (keyed by OpenRouter vendor segment for the prefer-direct rewrite), and `ui_rows()` (the no-secret payload the panel renders). `validate_key_format(provider_id, value)` is lenient by design: it returns a `(looks_ok, message)` tuple, and a key under 12 characters or missing its declared prefix returns `looks_ok=False` with a warning message that the UI surfaces but the store path never enforces (`provider_registry.py:384-399`) — the docstring ties this to the vault's [[Framework — API Key Setup]] rule that a surprising shape warns but never blocks a key the user insists on. Categories drive UI grouping through `GROUP_ORDER`; dispatch metadata (`native` vs `openai_compatible`, `base_url`, `or_prefix`, `native_service`) drives direct-vendor routing.

**Settings persistence (`orchestrator/user_settings.py`).** Load, save, and reset use `runtime_paths.CONFIG_DIR / "user-settings.json"`, under the effective `ORA_HOME` (default `~/ora`). A relocated installation does not fall back to the home installation's settings. `load_settings()` deep-merges the on-disk file over `DEFAULTS` and is forward-compatible: keys present on disk but absent from `DEFAULTS` are preserved on round-trip, so an older client cannot truncate a field a newer server wrote (`user_settings.py:158-181`). Writes go through `_write_raw()`, which writes a `.json.tmp` sibling and calls `Path.replace` — an atomic same-filesystem rename that prevents a torn write from a crash mid-save (`user_settings.py:151-155`). A `threading.Lock` serializes read-modify-write against concurrent requests. `save_settings()` type-validates known leaves before persisting (`capture.frame_rate` ∈ {24,25,30,50,60}, `whisper.model_size` ∈ {tiny,base,small,medium,large-v3}, render preset against `render.py`'s keys) and raises `SettingsError` rather than writing garbage.

The API-key surface delegates entirely to the keyring, guarded by a provider whitelist derived from the registry so the HTTP endpoints cannot write an arbitrary keyring account (`user_settings.py:274-277`). `set_api_key`, `delete_api_key`, and `api_key_present` import `keyring` lazily — the comment notes this lets tests stub it via `sys.modules`. `delete_api_key` swallows every exception because keyring backends raise different exception types for "not found," and "already absent" is success (`user_settings.py:288-296`). `list_api_key_status()` returns the registry's `ui_rows()` enriched with a live `present` boolean and nothing else — the value never leaves the keyring (`user_settings.py:310-324`).

**The keyring→env bridge (`orchestrator/boot.py:173-200`).** `_export_provider_keys_to_env()` runs unconditionally at module import (`boot.py:200`). It iterates `provider_registry.env_bridge_pairs()` and, for each `(env_var, keyring_username)`, writes the keyring value into `os.environ` only when the env var is not already set — the "pre-set env vars win" rule (comment at `boot.py:170`, enforced at `boot.py:189-190`). This one rule unifies the two hosts: on Mac the keyring populates the env; on Linux the sourced env-file has already populated it, so the bridge sees the var set and no-ops. The function is defensively wrapped: a missing `keyring` backend (`ImportError`) returns early, and a registry import failure falls back to the original web-search trio (Tavily/Brave/Exa) so the search cascade survives even a broken registry (`boot.py:176-188`). Without the bridge, every consumer that reads `os.environ` would see no key until a manual restart-time lookup — a cold-start credential outage.

At dispatch time the read path is env-first, keyring-second. `_provider_key(entry)` returns the env var if set and stripped, else falls back to `_keyring_lookup("ora", entry["keyring_username"])` (`boot.py:13713-13720`). The prefer-direct router (`_resolve_direct_endpoint`, `boot.py:13723`+) uses `or_prefix_map()` to rewrite an OpenRouter `vendor/model` id to the vendor's own API when that vendor's key is present, bypassing OpenRouter's markup. It is gated by `ORA_PREFER_DIRECT`, read at `boot.py:13701`: `_prefer_direct_enabled()` treats the value as on unless it is explicitly `0/false/no/off`. That flag is a kill switch, not a feature gate — it defaults on, so prefer-direct is installed and live, not flag-gated-off.

**Settings HTTP surface (`server/app.py`).** `GET /api/settings` returns settings, the no-secret `api_keys` rows, and provider groups (`server.py:1142-1160`). `POST /api/settings` deep-merges an `updates` dict and returns the merged state (`1163-1177`). `POST /api/settings/api-key` stores a key; `DELETE /api/settings/api-key/<provider>` removes one, validating the provider id against the registry before touching the keyring (`1810-1844`). `POST /api/settings/api-key/verify` runs `_verify_provider_key`, which makes the cheapest authenticated call that proves a key works — a free auth probe, a model-list GET, or a single search — using only stdlib `urllib` (`1847-1927`). Every branch is free or negligible; providers whose only auth proof costs a generation (image, TTS, transcription) are marked `verifiable: false` in the registry and short-circuit to "Verification isn't available" at the endpoint before any network call (`1945-1947`). The scrubbing discipline is the load-bearing detail: FRED authenticates via query string, so a malformed key can embed itself in a URL that an exception stringifies; the catch-all replaces the key with `***` before returning (`1922-1926`). The Gemini probe deliberately passes the key as a header, not a query param, for the same reason (`1884-1887`). This component closes the "secret round-trips to the browser in an error message" failure mode.

**Local-model discovery (`orchestrator/local_model_discovery.py`).** `probe_model_dir()` reads a subdirectory's `config.json` and `*.safetensors`, returning `None` for any directory lacking a config, lacking safetensors, or carrying malformed JSON — so `diffusers`, `whisper`, and `loras` subdirectories are rejected automatically (`local_model_discovery.py:238-284, 287-296`). It estimates total parameters from safetensors byte-size and quantization bits rather than parsing layers, which works across architectures and captures embeddings and vision towers for free (`111-127`). It detects MoE (mixture-of-experts — a model that routes each token through a subset of its parameters) and extracts active-params-per-token, first from the directory-name `-a<N>b` convention, then from the config's expert-routing ratio (`85-97, 129-164`). Vision capability is inferred from three signals: a `vision_config` block, an image-token field, or an architecture ending in `ConditionalGeneration` (`61-82`). `refresh(write=True)` rewrites `models.json`'s `local_models` array and stamps `_local_models_discovered_at`, preserving `commercial_models` and all other top-level keys, and refuses the write outright when discovery would record fewer models than the file already holds, unless `allow_shrink=True` (`local_model_discovery.py::refresh`). `server.py` calls it best-effort at startup, wrapped so a failure logs but does not abort boot (`server.py:15008-15024`). The CLI defaults to dry-run; `--write` persists.

**Install-side selection (`scripts/local_models.py`).** A separate concern: at install time, a RAM-tier classifier maps total system RAM to a deployment tier (`small_only` under 32 GB, `workhorse_choice` under 64, `full_local` under 128, `adversarial` at 128+) and a curated per-tier catalog, then wraps `huggingface_hub.snapshot_download` for the fetch, with `local_dir_use_symlinks=False` to avoid the Windows symlink-permission pitfall, and `HF_TOKEN` guidance for gated repos (`scripts/local_models.py:113-296, 329-362`). RAM detection branches on `sys.platform`: `sysctl` on macOS, `/proc/meminfo` on Linux, `wmic` on Windows (`92-120`). This path writes install state to `config/local-models.json`, distinct from the runtime `models.json`.

**Failure behavior, summarized.** Missing keyring backend → bridge no-ops, verify returns `None`. Malformed model `config.json` → that directory is silently skipped. Zero discovered models → `refresh` declines to write. Malformed `user-settings.json` → `load_settings` raises `SettingsError` rather than returning partial state.

### Evidence & Verification

Four test suites cover the subsystem, 110 tests total, and the counts hold against the files: `test_user_settings.py` (27 — persistence, deep-merge, forward-compat, validation, and keyring CRUD via a stubbed `keyring` in `sys.modules`); `test_provider_registry.py` (22 — derived maps, `env_bridge_pairs`, `validate_key_format`, `ui_rows`); `test_local_model_discovery.py` (37 — probe, scan, refresh, MoE active-param extraction, vision detection, and the zero-entry guard); `test_chunk8_local_models.py` (24 — RAM-tier classification, RAM estimation, and the download wrapper). Adjacent coverage lives in `test_aa_path_resolution.py` and `test_direct_catalog.py`.

Live confirmation: `config/models.json` on this machine carries 6 discovered `local_models` and a `_local_models_discovered_at` float stamp — four Qwen MLX builds (a 4B abliterated variant, a 9B, a 27B, and a 122B `-a10b` MoE), GLM-4.6V, and a 128B Mistral-Medium-3.5, matching the 2026-05-26 vision lineup. The server-path key store is checked by `scripts/install_server_smoke.py`, which asserts `OPENROUTER_API_KEY` is sourced from the env-file.

Explicitly untested surfaces. The `keyring` backend is exercised only on macOS Keychain; the Windows Credential Locker and Linux Secret Service paths are never run — the server path deliberately routes around them with the env-file. The verify probes call live provider endpoints; there is no offline mock, so the most failure-prone branch of `_verify_provider_key` — the network call itself — is uncovered by CI. `scripts/local_models.py`'s `snapshot_download` is not exercised against a real gated repo in CI. None of these paths is instrumented with success-rate or reliability metrics; there is no reliability budget measured here, and none is claimed. The named risks are qualitative failure modes, not measured rates.

### Expert Reading Notes

Read `provider_registry.py`'s `PROVIDERS` list first. It is the spine; every other surface in this chapter derives from it, and the fastest way to understand a bug in the env-bridge, the verify probe, or the Settings UI is to check whether the provider's registry entry declares the field that surface consumes. Then read `boot.py::_export_provider_keys_to_env` — it is the Mac↔server unifier, and the "pre-set env wins" rule (`boot.py:189-190`) is what lets one code path serve two credential models. Then read `server.py::_verify_provider_key` for the key-scrubbing discipline (`1922-1926`) — the pattern to preserve is header-auth-over-query-auth and scrub-on-exception.

Drift will most likely appear in four places. First, `config/user-settings.json` on disk carries `capture.default_system_audio` and `whisper.provider`, both absent from `DEFAULTS` (the `default_system_audio` checkbox was wired to nothing and removed 2026-07-01, per the NOTE at `user_settings.py:64-67`; the disk file predates that). The forward-compatible merge preserves them harmlessly, which is exactly why the drift is invisible — a reader comparing `DEFAULTS` to the live file will find keys in the file that the code no longer knows about. Second, `scripts/local_models.py`'s `TIER_RECOMMENDATIONS` catalog still lists Qwen2.5-7B/27B and Meta-Llama-3.1-70B (`scripts/local_models.py:211-296`) — the Qwen2.5/Llama-3.1 era, not the deployed 2026-05-26 vision lineup in `models.json`. The two local-model code paths — install-side selection and runtime discovery — are not reconciled; there is no step that syncs the curated catalog to what is actually installed. Third, any new provider must land in `PROVIDERS` or it silently drops out of the bridge, the verify probe, and the UI at once. Fourth, `local_model_discovery.refresh` rewrites only `local_models` and the timestamp; the `_vision_capable_defaults`, `overhead_reservation_gb`, and `local_model_directory` keys in `models.json` are managed elsewhere and the module does not document where — a reader debugging vision routing should not assume discovery owns those fields.

### Platform compatibility

- **API-key storage backend (`keyring`, `user_settings.set_api_key`/`api_key_present`):** macOS — works (Keychain, exercised). Windows-native — untested (maps to Credential Locker in principle; never run here). WSL — untested. Linux — needs-porting/unknown-untested: `keyring`'s Secret Service backend requires a running keyring daemon and login session a headless server lacks; the server path deliberately uses the plaintext env-file instead, so libsecret is never exercised.
- **Server-side key file `~/.config/ora-server.env` (`chmod 600`, `source` into env; `install-server.sh`):** Linux — works (the deployment target). WSL — works in principle (bash + POSIX perms + `source` apply); not exercised on this machine. macOS — works (POSIX). Windows-native — needs-porting: no `chmod`/POSIX permission bits and no shell `source`; a different secret mechanism would be required.
- **Settings config via `runtime_paths.CONFIG_DIR` (effective `ORA_HOME/config`) and models dir `~/ora/models/`:** macOS — works. Linux — works. WSL — untested. Windows-native — unknown-untested: `Path.home()` is cross-platform, but the hardcoded `~/ora` layout and the absolute `/Users/oracle` paths baked into `models.json` are Mac-shaped. `models.json` is gitignored and machine-specific, so discovery regenerates the absolute paths per host — the paths do not need to be portable, only re-derivable, which the startup rewrite guarantees.
- **Local MLX model inference (running the `models/` contents):** macOS (Apple Silicon) — mac-only; MLX/Metal is Apple-Silicon-exclusive. Windows-native / WSL / Linux — not-applicable: the discovery code merely reads `config.json` + safetensors (platform-neutral), but the models only *run* on Apple Silicon; the Linux server path routes every model call to OpenRouter or direct vendor APIs and hosts no local models.
- **RAM detection `scripts/local_models.py::get_total_ram_gb`:** macOS — works (`sysctl`). Linux — works (`/proc/meminfo`). Windows-native — untested and at risk: uses `wmic`, deprecated on recent Windows. WSL — untested.
- **HF `snapshot_download(local_dir_use_symlinks=False)`:** macOS — works. Linux — works in principle; not exercised in CI. WSL — untested. Windows-native — untested, though disabling symlinks is the correct portability choice for the Windows symlink-permission pitfall; it is simply not run there.
- **`user-settings.json` atomic write (`.json.tmp` + `Path.replace`):** macOS / Linux / WSL / Windows-native — atomic same-filesystem rename on every platform; no `fcntl` locking is used (in-process `threading.Lock` only). Exercised on macOS; the mechanism is stdlib and platform-neutral, but only the macOS path is actually run here.
- **Verify probes (`_verify_provider_key`, live HTTP via `urllib`):** platform-neutral (stdlib `urllib`), so no OS-specific surface — but network-dependent and unmocked; the branch is uninstrumented and untested offline on every platform including macOS.


---

# Part II — The Analytical Core

## 4. Model / Provider Configuration & Routing

### Problem

A single hardcoded model choice cannot span Ora's install spectrum. At one end sits a free-tier-only user with no local hardware, routing every call through rate-limited OpenRouter free endpoints. At the other end sits a power user on a Mac Studio with 128 GB of RAM, running several local MLX models plus a wallet full of paid API keys. Between them are hybrid installs that mix a couple of local models with a Claude or GPT key. A choice that is correct for one of these is wrong — often unusably wrong — for the others.

The problem is worse than "one size doesn't fit all," because a single request is not homogeneous. Ora's adversarial pipeline runs eight steps, and the steps have opposite cost/capability profiles. Prompt cleanup and classification want a cheap 4B model that answers in two seconds. Depth analysis wants a 60B-plus model that can actually execute an adversarial protocol. The breadth analyst wants a model from a *different* training family than depth, so the two do not share the same blind spots. The consolidator and formatter want something in between. If every step draws from the same model, you either overpay for classification or underpower analysis, and you lose the cross-family diversity that makes the adversarial pass worth running.

Four concrete failure modes follow if you do not solve this:

1. **Every model choice becomes a code edit.** Changing which model authors an article, or which model classifies intent, means editing Python. Non-programmer users cannot tune the system at all.
2. **Adversarial diversity is impossible.** With no way to say "breadth must differ from depth," the pipeline silently pairs a model against itself and reports agreement as if it were independent confirmation.
3. **A rate-limited or down provider stalls the pipeline.** One 429 from OpenRouter, or one MLX model that fails to load, and the request hangs or dies with a raw transport error shipped to the user.
4. **Concurrent MLX calls segfault the process.** The MLX runtime on Apple Silicon crashes with SIGSEGV when two threads load or invoke models on the same machine at once. Gear 4 runs depth and breadth in parallel by design. Without serialization, an all-local Gear 4 run kills the server.

A fifth, subtler failure sits underneath all of these: capability metadata drifts. A model's `vision_capable` flag, hand-entered into a config file, goes stale when a provider changes the model. The proof case was `kimi-k2.6`: a wrong `vision_capable: true` in `routing-config.json` routed image turns to a model that silently dropped the image, and the analyst ran blind with no error. Whatever fills each slot, something authoritative has to decide what that model can actually do.

This chapter describes the subsystem that decides which concrete endpoint fills each named pipeline slot — for a given gear, execution context, and user-selected configuration — and dispatches the call while surviving all five failure modes. The eight-step pipeline that consumes these resolved endpoints is covered in the Gear Pipeline & Mode Dispatch chapter; the concurrency primitives are canonicalized in [[Reference — Ora Concurrency Architecture]]; the per-slot Models pane UI is described in [[Reference — Ora Runtime Configuration]].

### Design Decision & Tradeoffs

**The decision: named slots decouple role from model; a user-selected named configuration maps each slot to an explicit primary-plus-fallback chain.**

A slot is a role in the pipeline — `depth`, `breadth`, `sidebar`, `consolidator`, `evaluator`, `step1_cleanup`, `classification`, `formatter`, `primary`. The mode file and the gear pipeline reference slots, never models. A named configuration (`config/configurations/<name>.json`) maps each slot's cell to an ordered chain: a `primary` endpoint id plus a `fallback` list of endpoint ids. The router walks that chain in order and returns the first endpoint that survives filtering. This is the Unix principle of indirection — the pipeline calls a name, and a lookup table binds the name to an implementation, so you re-point the binding without touching the caller.

The user picks a configuration. Four presets are defined — **Free**, **Budget**, **Speed**, **Premium** — baked from the model catalog on first Models-pane open (`active_configuration.bake_missing_presets`, `active_configuration.py:272`), plus any custom configurations the user builds. The active choice is a pointer file at `~/ora/data/active-configuration.json`, read on every interactive request (`active_configuration.get_active_name`, `orchestrator/active_configuration.py:52`). `PRESET_ORDER = ["free", "budget", "speed", "premium"]` (`active_configuration.py:615`) is the authoritative list.

At the reviewed commit, only three preset files are materialized on disk — `free.json`, `budget.json`, `premium.json`; `speed.json` is not yet baked. All four presets are defined in `config/configuration-presets.json` (keys `free / budget / speed / premium`), so `bake_missing_presets` will materialize `speed` the next time it runs against a present catalog; it is a defined-but-unbaked preset, not a missing one.

> Drift note, load-bearing: `CLAUDE.md` names the presets "Free / Budget / Optimum / Premium" and cites a "358-model" inventory. The code says `free / budget / speed / premium` and the shipped registry carries `model_count = 337` (`config/model-registry.json`). The Preset Speed Redesign renamed Optimum→Budget and old-Budget→Speed; the `CLAUDE.md` Models-pane paragraph was not updated. Trust the code. This chapter uses `speed`.

**The main alternative rejected: the six-bucket tier system.** The original design (`docs/design-model-configuration.md`, now marked Historical/superseded) sorted every endpoint into one of six tiers — `local-fast`, `local-mid`, `local-premium`, `free`, `mid`, `premium` — and resolved a slot by walking an ordered list of buckets. That abstraction was dissolved. Opaque tier ordering did not match how users reason. A user does not think "give me a local-premium model"; they think "use Qwen 122B for depth, and if that's busy fall back to GLM." The bucket walk hid the specific model behind a tier label, so the Models pane could not show, and the user could not choose, the actual endpoint. Named configurations invert this: the chain names concrete ids, the UI renders them, and the user edits them directly.

**The tradeoff.** Explicit chains are verbose — a `gear4.depth` cell can list six fallback ids — and they can reference an id that is absent from `routing-config.json::endpoints[]`. When a registry refresh reshapes endpoint ids, a configuration written against the old ids silently degrades: the router logs the unknown id and continues the cascade to the next fallback (`router.py:1044-1061`). This is config-vs-catalog drift, and it is the live system's sharpest edge (see Open problems). The bucket system had the opposite property — tiers were stable, so a refresh could not orphan a slot — but it paid for that stability by hiding the model. The team judged explicit-and-fragile better than opaque-and-stable, because the failure is loud in the logs and the fix (`sync_endpoints_from_catalog.py`) is mechanical, whereas the opacity was a permanent usability tax.

**Pre-execution downgrade and current-request recovery are distinct.** When a required slot cannot resolve, the router can try lower gears before model work starts (Gear 4 → 3 → 2 → 1); a `STOP` or exhausted cascade still refuses. Once invocation starts, bounded retries and the selected cell's configured fallback repair that current request. Neither mechanism guarantees an answer: exhausted invocation returns a typed failure, not an error string dressed as successful output.

**Curated registry authoritative for capability flags.** Rather than trust the hand-entered `vision_capable` in `routing-config.json`, an offline pipeline builds `config/model-registry.json` from OpenRouter, LiteLLM, Chatbot Arena, and Artificial Analysis, and `model_registry.overlay_routing_config` merges its capability values onto the endpoint dicts at load (`boot.py:1375`). The registry wins. This closes the `kimi-k2.6` class: a wrong flag in `routing-config.json` cannot propagate into routing because the overlay overwrites it. The tradeoff is a second source of truth that must be refreshed; when the registry is missing or malformed the overlay is a documented no-op and the routing-config values stand.

**Feature-owned provider rows do not create routing.** A loaded trusted feature
may atomically add an owner-bound provider declaration after collision checks.
This makes its no-secret row and keyring presence available to Settings and lets
that feature read only its declared named credential. It does not create a model
endpoint, catalogue entry, preset, or routing fallback; those remain explicit
configuration work in the mechanisms described below.

**Open problems acknowledged in the design:**

- The legacy bucket-walk code still exists in `router.py` (`resolve_endpoint`'s fallback branch at lines 338-392, and `_get_bucket_order` at 1097-1147). It is dead: `routing-config.json` has no `pipelines` block, so `_resolve_config_name` always resolves a configuration name and the bucket branch is never reached. It is a reader trap and a latent bug surface, pending removal (labelled Chunk 12).
- `vision_capable` lives in three places — `routing-config.json` endpoints, `config/models.json`, and the registry overlay — reconciled by `vision_capable_for_endpoint`. The source-of-truth consolidation is incomplete.
- Non-Mac support is untested end to end (see Platform compatibility).

### Implementation

**Entry points.** Two callers reach the router. Project tools call `model_dispatch.invoke_chat(system_prompt, user_prompt, slot=..., context=...)` (`orchestrator/model_dispatch.py:42`) — the stable, project-agnostic public API; MSI's `article_generator.py` is the reference consumer. The gear pipeline inside `boot.py` calls `get_slot_endpoint(config, slot, context, config_name)` (`boot.py:1512`) and, for the depth/breadth pair, `resolve_gear4_endpoints` (`boot.py:1593`). Both paths converge on the singleton `Router`.

**The singleton and its load.** `boot._get_router()` (`boot.py:1389`) builds one `Router` and caches it; a failed load is marked `False` and not retried until `reload_router` clears the marker. On construction the Router reads `routing-config.json`, then does three things that matter (`router.py:_build_lookup_tables`, 145):

1. Indexes endpoints by id.
2. Merges discovered local models from `config/models.json` into the in-memory endpoint table (`_merge_models_json_local_endpoints`, 165). This is the seam that lets a model you picked in the Models pane actually resolve at dispatch time even when `routing-config.json` has drifted behind the discovered set. It is runtime-only; discovery remains the source of truth for the installed local set. An entry's `status` becomes `active` and `enabled` becomes true only when its `model_path` exists on disk (`router.py:203-204`) — a model listed but not present cannot be dispatched.
3. Builds case-insensitive endpoint aliases (`_build_endpoint_aliases`, 218). The sharp edge is MiniMax: the catalog id is `minimax/minimax-m3` but the direct-provider endpoint registers as `minimax/MiniMax-M3`. Configurations get edited from both sides, so a case-equivalent id resolves instead of falling through the chain.

`load_routing_config` (`boot.py:1342`) is separate from the Router and is what `invoke_chat` calls; it reads the file and applies the registry overlay before handing the config to `get_slot_endpoint`.

**Active-configuration precedence.** `_resolve_config_name(config_name, context)` (`router.py:728`) derives the effective configuration with this precedence, most-specific first:

1. An explicit `config_name` passed by the caller (a per-run override) wins over everything.
2. For `interactive` context: the active project's `default_model_profile`, if set and resolvable, overrides the account-wide default — a project default is "closer" than the global one. Best-effort; a failure never breaks chat.
3. Otherwise the account-wide active pointer, `active_configuration.get_active_name()`.
4. For `agent` / `autonomous` context: the static map `DEFAULT_CONFIG_FOR_CONTEXT` (`router.py:45`) — both resolve to `background-default`. Automated processes are not user-steered through the active pointer.

The operative distinction here is foreground vs. background execution: interactive chat consults the user's live selection; a background pipeline run resolves against a fixed default. This is not "manual vs. agentic" — both run the same resolver; they differ only in whether a human is currently steering the pointer.

**The chain walk.** `_resolve_from_configuration` (`router.py:995`) is the live resolution path. It loads and caches the named configuration, maps the slot to a cell path via `_slot_to_cell_path` (`router.py:811`), and walks `primary` then each `fallback` id. For each candidate it applies, in order:

- **Unknown-id skip.** If the id has no matching endpoint, log and continue to the next fallback (the config-vs-catalog drift path, 1044-1061).
- **Enabled / active filter.** Skip endpoints that are disabled or not `status == "active"`.
- **Exclusion filter.** Skip ids already assigned this gear (used for diversity).
- **Same-machine block.** Skip a local endpoint on a machine already committed to another local model this gear — the MLX parallel constraint.
- **Mutex skip.** If a local endpoint's per-machine MLX mutex is currently held, skip it in favour of the next chain entry. If every remaining entry is ineligible and the only survivors are busy locals, return the first busy local so the caller's `call_model` queues at the mutex — the "no API fallback, so we wait" path (`first_busy_local`, 1041/1093).
- **Circuit-breaker skip.** If `endpoint_health.is_in_cooldown(id)` is true, skip (1088).

The first candidate that survives all filters is returned as a v2 endpoint dict, converted to the v1 shape `call_model` expects by `_to_v1_endpoint` (`router.py:1149`).

**Slot-to-cell mapping (current Model boundary).** Utility slots map to `cells.utility.<slot>`; `sidebar` aliases `utility.step1_cleanup`. Analysis runs use the exact `cells.analysis.gear3` or `gear4` depth/breadth cell. Consolidation, verification, and formatting use their separate `post_analysis` cells. The generic `primary` alias reads `gear3.depth`, but the direct Gear-2 runner does not use it: `resolve_single_pass_endpoint` returns an endpoint together with its actual cell identity. Gear 1 uses `classification`; Gear 2 first resolves `fast` to `utility.gear2_rag_lookup`, then may resolve `utility.step1_cleanup` within the same named configuration. Only an unnamed legacy request may finally use the active endpoint.

**Current-request Gear-1/2 fallback.** `run_single_pass_with_tools` preserves that actual cell, gear, configuration, and vision requirement. Each endpoint receives one same-model retry on transport failure, error output, or empty output; if still unhealthy, the exact cell chain advances with all attempted endpoint IDs excluded, so it cannot cycle back. The direct-call minimum is one character after cleanup: a substantive one-word answer is valid. Successful output carries the attempted and selected endpoint/model identities. Exhaustion raises `ModelInvocationFailure` before completed output routing or persistence.

**Gear cascade and diversity.** `execute` (`router.py:476`) tries the requested gear, calls `resolve_gear` for every slot the gear needs, and on any unfilled slot drops a gear and retries. `resolve_gear` (394) applies adversarial diversity: for the `breadth` slot it excludes the `depth` id so breadth lands on a different model. When diversity is enforced it deliberately does *not* fall back to the same model — it lets the cascade find a wider pool at a lower gear instead. A per-config `diversity_override: false` disables this, for single-model demo configurations like `qwen-9b-only` where every slot is intentionally the same 9B endpoint (proving Ora's pipeline lifts one small model); without the override, gear 4 would downgrade all the way to gear 2 because breadth could never differ from depth.

**Effective gear is separate from parallel safety (current Model boundary).** `Gear4EndpointResolution` carries both `effective_gear` and `parallel_safe`. A router-selected effective Gear 3 delegates to `run_gear3`; `parallel_safe=false` alone does not. Serialized Gear 4 still runs the complete two-lane Gear-4 contract. The per-machine local-model mutex remains the GPU serialization boundary.

**Current RAM and selected-endpoint cache protection.** The resilience probes return `None` for failed, malformed, or unsupported RAM readings; they never substitute 128 GB. The resource assessment reports that uncertainty. Unknown RAM or model-memory facts serialize the local analyst pair without changing Gear 4 or authorizing eviction. Before each analyst attempt, including retries, fallback, and supplemental calls, the admission guard checks the actual endpoint against active calls and earlier local attempts, using `ram_resident_gb` plus `ram_overhead_gb` and current available RAM, not legacy slot estimates. It waits for conflicting active calls and requires release of an earlier local model only when two distinct local identities have a known combined requirement above 80% of available RAM. Failed local attempts remain possible cache residents even if that lane later recovers on an API. MLX removes cached entries matching the model path; an already-absent entry is a successful release. Ollama sends that model's unload request to its selected server. Failed required release produces a typed `resource_release_failed` refusal before the unsafe attempt; existing configured recovery may still use a safe alternative, such as an API. Unrecovered release failure cannot be bypassed by downgrading to Gear 3, and the admission guard remains active for later Gear-4 stages and retries after a release failure. API pairs, same-model pairs, unknown facts, and below-threshold pairs do not trigger cache release.

**Dispatch.** `call_model(messages, endpoint, images)` (`boot.py:13043`) routes on `endpoint["type"]`:

- **api** — wrapped in `mlx_mutex.track_api_call(id)`, a non-blocking in-flight counter for observability plus an optional sized concurrency cap. It does not block on contention; API providers handle their own concurrency.
- **local** — wrapped in the blocking `mlx_mutex.acquire(machine_id)` (`boot.py:13077`), so only one local call per machine runs at a time. This is the SIGSEGV guard. `call_local_endpoint` (`boot.py:14178`) then resolves the engine: the endpoint's `engine` field defaults to `ollama`, an explicit `engine` is honoured, and `engine == "auto"` resolves to `mlx` only when `platform.system() == "Darwin"` and `platform.machine() == "arm64"`, otherwise `ollama` (14185-14189). MLX runs in-process via `mlx_lm` with a module-level model cache; Ollama posts to `localhost:11434`.
- **anything else** — returns `"[Error] Unknown endpoint type"`.

**Failure behavior feeds the circuit breaker.** `call_model` reads the return string and records the outcome to `endpoint_health` (`boot.py:13093-13105`). A response starting with `"[Error"` is a failure; crucially, an *empty* response is also a failure (13099) — the `kimi-k2.6` proof case returned `content=None` with no exception, and treating empty as success meant the breaker never tripped and the router kept dispatching to the broken model. `endpoint_health` (`orchestrator/endpoint_health.py`) is a per-endpoint circuit breaker: **3 failures within a 60-second rolling window trips a 300-second cooldown** (`_failure_threshold = 3`, `_failure_window_seconds = 60.0`, `_cooldown_seconds = 300.0`, lines 47-49). During cooldown the router's chain walk skips the endpoint. After cooldown the next call is allowed through as a half-open probe; success clears the breaker, failure re-extends the cooldown (`is_in_cooldown`, 110). These three numbers are the trip mechanism, not a reliability budget — nothing in the code measures achieved success rates; the failure counts and cooldown are the only instrumented health signal, and all three are tunable via `configure()`.

**When resolution or invocation fails, the caller refuses honestly.** `get_slot_endpoint` returns `None` for an exhausted named chain rather than selecting the first active catalog entry. Required Gear-3/4 endpoint failures and exhausted invocation attempts are typed failures. `ModelInvocationOutcome` remains text-compatible but carries attempted/selected endpoint and model identities, substitution state, and any typed failure; a failed outcome contains no candidate prose. Provider exceptions and error/empty responses therefore cannot become analysis, critique, consolidated text, or a completed answer just because they were wrapped in a string. The low-level provider/circuit-breaker convention above remains internal to these call boundaries.

A successful identity-changing recovery records one `model_substitution` warning per request context, with attempted and selected identities; same-model retries record none, even if the provider channel changed. Gear-4 outcomes are aggregated on the request thread after worker completion so the ordinary response collector receives the warning. Failure of the health surface logs loudly and does not discard a usable result.

**Startup rewrite of the local set.** At server boot (`server/app.py:15009`), `local_model_discovery.refresh(write=True)` scans `~/ora/models/` for subdirectories with a valid MLX `config.json` and `.safetensors` files, derives id / ram_gb / type / vision_capable / recommended_roles, and rewrites `config/models.json`'s `local_models` array — preserving `commercial_models` and every other key. A no-shrink guard refuses to write whenever discovery finds fewer models than the file already records (`local_model_discovery.py::refresh`), protecting against a momentarily unmounted or half-populated models directory; deliberate removal passes `allow_shrink=True`. `active_configuration.bake_missing_presets()` creates any missing preset configs on Models-pane open.

**The offline registry pipeline.** Separate from per-request routing, a refresh pipeline builds the capability data. `scripts/sync_model_registry.py` (the 124KB primary builder) pulls OpenRouter + LiteLLM + Arena + Artificial Analysis and writes `model-registry.json`; `refresh-catalog.py`, `refresh-openrouter.py`, and `refresh-direct-apis.py` refresh the catalog and provider listings; `sync_endpoints_from_catalog.py` keeps `routing-config.json::endpoints[]` in step with the ids configurations reference. These are triggered from the server (`/api/model-registry/refresh`, `/reach/start|status`) or run as CLI. `check-model-refresh-drift.py` is a CI guard that fails if a refresh run dirtied tracked seed files.

**Live config edits reach the singleton without restart.** The V3 Models pane POSTs to `server/app.py` (`/api/configurations/*`, `/config/routing/slots`), which write the configuration/routing files and call `boot.reload_router()` (`boot.py:1406`) plus the `active_configuration` setters. `router.reload()` (`router.py:265`) re-reads the file and rebuilds the lookup tables, clearing the configuration cache so edits take effect on the next call; on a read/parse failure it keeps the prior in-memory config so the running pipeline does not degrade.

**Protected Model Profile deletion (current boundary).** The four presets and the reserved `user-pipeline`/`background-default` profiles are refused. A custom deletion first requires a regular non-symlink target and a readable valid active pointer. Deleting the active custom additionally binds the existing regular Free profile and validates its current allocation shape; this is not a live reachability or complete-fallback qualification. The route approves those exact target/pointer/Free identities. The sink repeats the full preflight under the existing process lock and target, pointer, and applicable Free file locks before unlinking the target and atomically writing the pointer to `free`. Removal and pointer fallback share one protected receipt scope. A failure records actual post-state; there is no rollback if deletion succeeded before a later pointer write failed.

**Config surfaces.** `config/routing-config.json` (endpoints, machines, capability slots including `slots.vision_input`, diversity, and the legacy `slot_assignments`/`gear4_overrides`/`buckets` fields); `config/configurations/<name>.json`; `config/models.json` (gitignored, machine-specific; auto-managed `local_models` + hand-kept `commercial_models`); `config/model-registry.json` and `config/model-catalog.json`; `config/configuration-presets.json` (preset algorithm params); `config/family-classification.json` (same-family warning). Environment: `ORA_HOME`, `ORA_API_POOL_SIZE`, and `install-state.json::profile` for API-pool sizing (Solo `0` = uncapped, Hybrid 8, Organization 32; `mlx_mutex.PROFILE_API_POOL_SIZES`, 62).

**Vision fallback.** `resolve_vision_fallback` (`router.py:913`) is the resolution path for an image-bearing turn where the slot must be able to read the image. It checks a global `slots.vision_input` chain (`_vision_input_chain`) first; when that chain is configured it *is* the vision fallback and the method refuses to advance into the slot's generic chain, because a `vision_capable` flag is not sufficient to trust an entry — image *delivery* is per-service, and the 2026-06-01 evaluation surfaced a breadth entry that was flagged vision-capable but whose service path never attached the image. When no global chain is present it falls back to the cell's per-cell `vision_substitute` field (the legacy path, still in the code). The two coexist; the global slot takes precedence but has not fully replaced `vision_substitute`.

### Evidence & Verification

**Unit tests** live under `orchestrator/tests/` and run with:

```
cd ~/ora && /opt/homebrew/bin/python3 -m unittest discover -s orchestrator/tests
```

The routing-relevant suites, all present in the tree: `test_model_router` (core slot resolution + gear cascade), `test_router_config_name` (named-config resolution + precedence), `test_router_chain_walk` (fallback-chain reporting + circuit-breaker state in the refusal path), `test_router_vision_input` (global vision-input slot), `test_router_reload` (reload keeps prior config on failure), `test_slot_inheritance` (cell inheritance), `test_active_configuration` (63 test methods: presets, active pointer, slot setters, lifecycle), `test_model_registry` + `test_runtime_configuration_overlay` (registry overlay + `compute_picks`), `test_model_dispatch` (`invoke_chat` typed errors), `test_mlx_mutex` + `test_call_model_mutex` (per-machine mutex, API pool, `call_model` integration), `test_endpoint_health` (cooldown/probe breaker), `test_local_model_discovery` + `test_chunk8_local_models` (MLX scan + rewrite safety guard), `test_provider_registry`, `test_direct_catalog`, `test_vendor_catalog_registry`, `test_sync_endpoints_vendor_auth`, `test_refresh_catalog`, `test_refresh_openrouter`, `test_visual_routing` + `test_routing_slots_endpoint` (capability-slot routing + Models-pane endpoints).

**How to confirm resolution by hand.** `model_dispatch.get_slot_info(slot, context=...)` (`model_dispatch.py:135`) returns the endpoint a slot would resolve to without spending a call — the fastest way to see what a configuration actually binds. The circuit-breaker state for any endpoint is readable via `endpoint_health`. To confirm the local set, `local_model_discovery.refresh()` runs dry (no `write=True`) and prints what it found.

**Instrumented facts, verified against the installed tree at 7a5e8f40.** `config/model-registry.json`: `model_count = 337`, `generated_at = 2026-06-15`, `last_probe_at = null`. `config/routing-config.json`: 547 endpoints, one machine (`studio-128`), no `pipelines` block (confirming the bucket-walk path is dead), `buckets` key still present (the dead data). `config/configurations/`: 8 files — `free`, `budget`, `premium` (the three baked presets; `speed` was defined in `configuration-presets.json` but not yet baked — since then the installer bakes all four during step 7, so a fresh install has no unbaked preset), plus `user-pipeline`, `background-default`, `background-speed`, `qwen-9b-only`, and one `experiment-optimum-premium-consolidator`. (There are no `campaign-*` configuration files in the committed tree.) `active_configuration.PRESET_ORDER = ["free", "budget", "speed", "premium"]`.

**Explicitly untested / unverified surfaces:**

- **End-to-end resolution off macOS.** The Ollama fallback and the pure-API path have no non-Mac test evidence. Every "works" claim in the Platform matrix that is not macOS is "untested," not "broken."
- **Keyring off-Mac.** `user_settings` stores keys via the `keyring` library under service `"ora"`; only the macOS Keychain backend has been exercised.
- **The empirical reach-probe has not run on the shipped registry.** `last_probe_at = null` means the probe that would populate per-endpoint measured latency/reachability has not been run against this registry; the circuit breaker is the only runtime health signal.
- **`direct_catalog` and `vendor_catalog_registry` gates default ON but their live consumption is unverified.** `direct_catalog._enabled()` reads `ORA_DIRECT_CATALOG` defaulting `"1"` (ON), and `vendor_catalog_registry.enabled()` reads its flag defaulting `"1"` (ON). Both are labelled *partial* here not because a flag defaults off, but because they feed the *offline* registry-build pipeline and their runtime effect on live routing was not traced line-by-line; the AA/Arena/LiteLLM fetch logic inside the 124KB `sync_model_registry.py` was verified only at the wiring/subcommand level.
- **`call_api_endpoint` provider-specific request building** (`boot.py:13404`) was not traced per service; the per-service image-delivery claim in `resolve_vision_fallback`'s docstring is taken at its word.
- **The committed config JSONs are authoritative here; the live working tree drifts from them.** A clean checkout of 7a5e8f40 is clean — the committed `model-registry.json` / `model-catalog.json` / `routing-config.json` are what the figures above are read from. On the author's running machine those same files show as `M` (modified) because the catalog/registry refresh rewrites them at runtime; that auto-managed working-tree drift is machine-local and not part of the commit, so this document reports the committed values, not the dirty working copy.

### Expert Reading Notes

**Read in this order.** Start with `router._resolve_from_configuration` (`router.py:995`) and `_slot_to_cell_path` (811) — this is the entire live resolution path in two functions. Then `_resolve_config_name` (728) for the active-config precedence. Then `boot.get_slot_endpoint` (1512) for the v1-slot → v2 mapping, including the `formatter` branch and the single-slot depth/breadth lookup that tries the gear-4 cell before gear-3. Then `boot.call_model` (13043) and `call_local_endpoint` (14178) for dispatch, the mutex acquisition, and the `engine == "auto"` platform gate.

**Ignore the bucket-walk path.** `router.py` lines 338-392 (`resolve_endpoint`'s fallback branch) and 1097-1147 (`_get_bucket_order`) reference `self._buckets` and `pipelines[context]`, which no longer exist in the config. In production these return empty/None and are never reached, because `_resolve_config_name` always resolves a name. Do not mistake them for live logic. The module docstring still describes the router as "bucket-based" — that docstring is stale; the live engine is named-configuration cell chains.

**Where future changes most likely cause drift:**

1. **Config-vs-catalog id drift.** A configuration primary or fallback that references an id absent from `routing-config.json::endpoints[]` is silently skipped and logged (`router.py:1044-1061`). A registry/endpoint refresh that reshapes ids can quietly degrade a preset's chain. `sync_endpoints_from_catalog.py` is the coupling that must hold; if you change how ids are minted, run it and re-check the presets.
2. **Three `vision_capable` sources.** `routing-config.json` endpoints, `config/models.json`, and the registry overlay all carry it, reconciled by `vision_capable_for_endpoint` (`router.py:688`, `boot.py:1443`). Change one, check the other two. `models.json` is the intended source of truth; the migration is incomplete.
3. **The slot-to-cell contract is duplicated.** `router._slot_to_cell_path` and `boot.get_slot_endpoint`'s slot dispatch must agree on the slot vocabulary. Add a slot in one place only and it silently resolves to the wrong cell.
4. **Single-machine assumption.** `DEFAULT_MACHINE_ID = "studio-128"` is hardcoded in `router.py:41`. Multi-machine Bonjour discovery from the design doc was never built. Any local endpoint without an explicit `machine` is assumed to be on the one Studio.
5. **`CLAUDE.md` is stale on preset names and model count** (Optimum vs Speed; 358 vs 337) and still frames routing as bucket-based ("the classification slot resolves from the local-fast bucket"). Trust `active_configuration.PRESET_ORDER`, `config/model-registry.json::model_count`, and the named-configuration code, not that prose.

### Platform compatibility

Ora is developed and run on a single Apple Silicon Mac Studio. Every "works" below means "exercised on macOS." Non-Mac surfaces are marked from the code's platform gates and the absence of off-Mac test evidence — not from having been run.

| Subsystem | macOS | Windows-native | WSL | Linux |
|---|---|---|---|---|
| **MLX local inference** (`call_local_endpoint` engine=`mlx`/`auto`) | works | n/a | n/a | n/a |
| **Ollama local fallback** (`localhost:11434`) | works | untested | untested | untested |
| **`mlx_mutex` SIGSEGV serialization** | works | n/a | n/a | n/a |
| **API-only / cloud routing** (OpenRouter + direct APIs) | works | untested | untested | untested |
| **API key storage** (`keyring`, service `"ora"`) | works | untested | needs-porting | untested |
| **Config/model paths** (`~/ora`, `~/ora/models` scan) | works | needs-porting | untested | untested |
| **Local-model discovery scan** (`.safetensors` glob) | works | untested | untested | untested |
| **Server startup + refresh subprocesses** | works | untested | untested | untested |
| **MLX heartbeat + machine id `studio-128`** | works | n/a | n/a | n/a |

Per-item notes:

- **MLX** runs in-process via `mlx_lm` against Apple Metal and is Apple-Silicon-only. `engine == "auto"` resolves to `mlx` only under `Darwin` + `arm64` (`boot.py:14185-14189`); everywhere else `auto` resolves to `ollama`. Marked **n/a** off-Mac because MLX cannot run there at all.
- **Ollama** is the only local path on non-Apple hardware. It is plain HTTP to `localhost:11434` (`call_local_endpoint`) and cross-platform in principle, but has no off-Mac test evidence. Marked **untested**, not "works."
- **`mlx_mutex`** guards the MLX Metal segfault. It is irrelevant where MLX is not the engine — an API-only install never takes the blocking `acquire`. Marked **n/a** off-Mac because there is no MLX to serialize.
- **API-only routing** is pure HTTP plus keyring — the zero-local-hardware path, and the most portable in principle. It is nonetheless **untested** off macOS.
- **Keyring** uses the `keyring` library: macOS Keychain on Mac; Secret Service (Linux) or Windows Credential Locker elsewhere in principle. WSL headless typically lacks a Secret Service backend, so it is marked **needs-porting**; native Windows and Linux are **untested**.
- **Paths** use POSIX `~` and forward slashes throughout (`Path.home()/'ora'/'models'`, `ORA_HOME` defaulting `~/ora`), with no `%USERPROFILE%` handling. Native Windows is **needs-porting**.
- **Local-model discovery** is a filesystem glob with no Apple calls, so it is portable in principle — but only MLX models are useful output, and MLX is Mac-only, so the scan finding anything usable off-Mac is moot. Marked **untested**.
- **Startup catalog refresh subprocesses** use the running server interpreter (`sys.executable`), preserving the launcher's Python selection without requiring a Homebrew path. Native Windows and other off-Mac execution remain **untested**.
- **`studio-128`** is `DEFAULT_MACHINE_ID`, a single hardcoded Mac Studio. Multi-machine discovery was not built, so the machine layer is **n/a** off the one supported machine.

## 5. Gear Pipeline & Mode Dispatch

### Problem

A single language model cannot reliably catch its own errors. The model that fabricates a citation, misframes a decision, or quietly answers a different question than the one asked will not flag that mistake on self-review. The blind spots are correlated: the same weights that produced the error also grade it, so "check your work" returns the same wrong answer with more confidence. This is the failure mode the whole subsystem exists to prevent — an analysis that is wrong in a way its own author cannot see.

There is a second, cheaper failure. If every prompt runs the same pipeline, a greeting, a capital-city lookup, and a wicked-problem analysis all pay the same latency and cost. A five-to-fifteen-minute adversarial cascade on "hello" is not thoroughness; it is waste. And routing that guesses wrong in the other direction is worse: an ambiguous prompt either interrogates the user with clarifying questions they already answered, or silently commits to the wrong analytical frame and produces a confident answer to the wrong problem.

So the subsystem has two jobs that pull against each other. It must spend heavily where judgment is required and independent review actually breaks correlated errors, and it must spend nothing where the prompt is trivial or already unambiguous. The routing decision that separates those cases has to be predictable — a routing layer that is itself a non-deterministic model call reintroduces exactly the unreliability it is meant to contain.

### Design Decision & Tradeoffs

Two coupled moves answer those two problems.

**Deterministic mode-and-gear routing.** A four-stage pre-routing pipeline classifies each prompt to one of 64 analytical modes across 21 territories (T1–T21, verified in `architecture/territories.md`), and the selected mode file's `## DEFAULT GEAR` heading — not a model's opinion — fixes the pipeline shape. "Gear" is the pipeline's cost/quality tier: Gear 1 is a direct single-model pass with no retrieval; Gear 2 is a single pass with RAG for factual lookups; Gear 3 is a sequential adversarial cascade on two models; Gear 4 is the parallel adversarial cascade. The distribution across the installed 64 modes is concrete and lopsided: 1 mode declares Gear 1, 2 declare Gear 2, 3 declare Gear 3, and 58 declare Gear 4 (confirmed by grep over `modes/*.md`). Judgment-heavy work is the common case; the cheap gears are the exceptions that must opt out explicitly.

The rejected alternative was the retired Mode Classification Directory, which ran an LLM intent-classification pass over a flat mode list. The current deterministic routing authority is the maintained Markdown source set, not a model-generated classification or a separately authored registry. Each mode owns its identity, Display Description and adjacent Selection/Activation Guidance; territory and cross-mode sources own global questions, answer choices and defaults. `orchestrator/routing_sources.py` compiles the whole collection before exposing records. It validates mode, lens, territory, deferred-candidate, question and molecular-companion references, retaining typed active/deferred/territory/fallback/action destinations and qualified alternatives. Named predicates bind only the finite implemented detectors: prose is never evaluated as an expression. Word boundaries, case normalization and bounded typo comparison remain mechanical. The tradeoff is still explicit: authored vocabulary and questions need maintenance, but that maintenance now has one owner instead of diverging registry tables, code aliases and metadata parsers.

**Independent-model adversarial review.** For the 58 Gear-4 modes, two analysts — a depth stream and a breadth stream, ideally on different model families for genuine independence — analyze in parallel, then cross-evaluate, revise against each other's critique, and verify each other's revised drafts before a consolidator and formatter produce the deliverable. The reason is the Problem above: independent review catches what self-review cannot, because a second model does not share the first's correlated blind spot.

The rejected alternative was single-model-plus-self-critique — one model that drafts and then critiques its own draft. It is roughly half the cost. It was rejected because it does not break correlated errors; the self-critique inherits the same blind spot that produced the error. The tradeoff for choosing real adversarial independence is steep and stated plainly in `architecture/pre-routing-pipeline.md`: five to fifteen minutes of wall-clock latency and roughly 2x model cost for a Gear-4 turn.

**Open problems.** Bounded retries, configured endpoint advancement, and the circuit breaker can recover transient model failures and provider outages, but do not guarantee availability when the permitted chain is exhausted. The verifier is a gate, not an editor: a correction it computes (a fixed date, a fixed citation) is captured in the trace but never reaches the user, because the consolidator and formatter read the reviser's draft directly — an acknowledged architectural gap, not a bug (detailed below). Deterministic routing also remains limited by authored signals and input detectors. An unresolved route is represented honestly as a saved question handled through the Answer/Skip lifecycle in Chapters 2 and 8; numeric corpus results rather than determinism alone establish routing quality.

### Implementation

The runtime entrypoint is `boot.py::run_pipeline` (line 9053). The flow, in order: a risk gate, then slash-command and runtime short-circuits (the meta-layer `/instance`, `/render`, `/framework` commands intercept here before any model call), then Phase A prompt cleanup, then `run_pre_routing_pipeline` (line 5513), then the gear-dispatch switch (lines 9338–9394).

**The four stages.** `run_pre_routing_pipeline` runs Stages 1–3; Stage 4 is the gear execution that follows.

- *Stage 1 — pre-analysis filter* (`stage1_pre_analysis_filter`). Matches the compiled mode-owned vocabulary while retaining the existing greeting/direct-response and Gear-2 factual-retrieval boundaries. Only equivalent complete signal records coalesce; the same phrase may legitimately name different destinations, as with causal and structural systems dynamics. A factual retrieval request without judgment cues retains its dedicated `factual-lookup` dispatch and that mode's Default Gear.
- *Stage 2 — sufficiency analyzer* (`stage2_sufficiency_analyzer`). Uses the compiled identities, signals, questions, authored answers and explicit defaults to select destinations or return a specific unresolved question. Explicit names and plural selections retain their actual destination identities and order, including through a later canonical follow-up that says to keep the choice already supplied. No-territory ambiguity, within-territory distinctions and cross-territory questions remain observable rather than becoming a best-guess selection; when a targeted boundary remains unresolved, the same targeted question remains pending instead of widening to the generic all-territory question. Both pre-mortem methods stay available and follow the object being examined: asking to stress-test a launch or action plan selects the action-plan pre-mortem, while a system or design selects the structural-fragility counterpart. Red Team without that object uses its owning mode's question; it is not an undefined catch-all. A deferred destination remains deferred rather than becoming an executable mode merely because it was offered.
- *Stage 3 — input completeness check* (`stage3_input_completeness_check`). Reads the selected mode's compiled input contract and lighter-sibling relationships, applies the existing input detectors, and identifies required missing material. A reference to an earlier artifact resolves to substantive earlier artifact content, not a later acknowledgement or other non-artifact utterance. A lighter-analysis choice is preserved when it needs the same material, with that missing requirement stated explicitly; neither option is represented as ready to run without it. A plural routing result retains its selected identities and their completeness results, not a claim that the analyses have executed.
- *Stage 4 — mode execution.* Compiled active modes require a valid `## DEFAULT GEAR` before admission. The generic text extractor's legacy missing-heading fallback is not a way to admit an invalid routing source. Gear 1 resolves classification, and Gear 2 resolves the dedicated RAG-lookup cell or its same-configuration cleanup fallback, carrying the actual cell through current-request retries (Chapter 4). Gear 3 calls `run_gear3`; Gear 4 calls `run_gear4`, which distinguishes a real router-selected effective Gear 3 from merely serialized Gear 4. There is no separate Gear 5. This routing cutover preserves the seven authored molecular companions; it does not certify or redesign molecular execution.

The same compiled collection supplies mode loading, educational naming, server picker/manual metadata and lens eligibility, completeness/lighter-sibling lookup, `scripts/ora-test` mode listing/validation and the implementation verifier's source inventory. The signal registry, Mode Registry, mode index and reverse lens-to-mode catalogue are generated views, never independently writable runtime authorities. Loading is process-local at the existing boundary: a fresh process reads changed source bytes, with no persistent compiler cache, watcher, service or new dependency. Framework-internal selection and project-specific article eligibility remain separate policies and retain their existing named IDs and analytical section interfaces.

**Prompt assembly — the injection model.** Every pipeline step's system prompt is built by `build_system_prompt_for_gear` (line 8467). This is the load-bearing coupling in the whole subsystem, so its contract is worth stating precisely. The function takes a `step` — one of the six members of `_PIPELINE_STEPS` (line 8358): `analyst`, `evaluator`, `reviser`, `verifier`, `consolidator`, `formatter` — and injects exactly one role-specific `##` section from the mode file per step, on top of a shared baseline. It extracts named sections via `_extract_section` (line 7823):

- Baseline injected into *every* role-specific step: the mode's `## ANALYTICAL BRIEF AND EVALUATION CRITERIA` and its `## VERIFICATION CRITERIA`, each wrapped in `_fenced` banner blocks — `MODE BRIEF & EVALUATION CRITERIA — <mode>` and `MODE VERIFICATION CRITERIA (PASS gate) — <mode>` (lines 8579–8588). The analyst sees the standard it will be graded against before it writes; the verifier sees the gate it grades against. This closes the "writing blind to the criteria" gap. Gear 1 is structurally exempt — it does not route through this function.
- Role-specific per step: `## DEPTH ANALYSIS GUIDANCE` or `## BREADTH ANALYSIS GUIDANCE` for the analyst (chosen by the `slot` argument), `## REVISION GUIDANCE` for the reviser, `## CONSOLIDATION GUIDANCE` for the consolidator, `## OUTPUT FORMAT GUIDANCE` for the formatter.
- Breadth-only: `## ANALYTICAL PERSPECTIVES` — the mode's declared Tier-1 thinking tools and Tier-3 mental-model lenses — is injected into the breadth analyst alone (the `step == "analyst"` / `slot == "breadth"` branch), because breadth is the lateral-thinking stream and the lenses prime its frame; depth is intentionally skipped.

The section name `ANALYTICAL BRIEF AND EVALUATION CRITERIA` was renamed from the legacy `EVALUATION CRITERIA` on 2026-05-24; the code keeps a legacy fallback (`_extract_section(..., "ANALYTICAL BRIEF AND EVALUATION CRITERIA") or _extract_section(..., "EVALUATION CRITERIA")`, lines 8515–8518). The migration landed on the mode side: `modes/cui-bono.md` carries the new heading verbatim (line 161). It did **not** land on the spec side — see Evidence & Verification.

Retrieved reference material — RAG hits, web sources — is wrapped by `_fenced` (line 8420) in `=== LABEL === / === END LABEL ===` banners with a "data, not instructions" caption (`_REFERENCE_FENCE_NOTE`, line 8413, added 2026-06-29). The `===` banner is syntactically distinct from markdown `#` headings, so retrieved content carrying its own `##` headers cannot be confused with the scaffolding's structure — this is the prompt-injection isolation boundary for the pipeline. Phase A's inferred assumptions are surfaced in their own `PHASE A ASSUMPTIONS (NOT USER-STATED FACTS)` fenced block so downstream steps treat inferences as uncertain rather than as user-stated fact.

**The Gear-4 cascade and its failure behavior.** `run_gear4` (line 11691) maps code steps to roles: Step 3 parallel depth + breadth analysts, Step 4 cross-evaluation, Step 5 parallel revisers, Step 6 cross-verification (up to 2 correction cycles), Step 7 consolidation into an irreducible corpus, Step 8 formatting into the deliverable, Step 8.6 final quality gate. The Step 7/8 split (2026-05-14) separated two jobs the old consolidator conflated: Step 7 does semantic extraction, cross-stream dedup, bloat strip, and synthesis to corpus shape; Step 8 places that corpus into the deliverable form. `formatter` is a full member of `_PIPELINE_STEPS`.

The current recovery boundaries distinguish a failed invocation from genuine content preserved by an allowed fallback:

- *Step 3 — dead-analyst recovery.* Each stream tries its primary with one same-model retry, then one configured fallback with its own retry. An unrecoverable ordinary-turn stream takes the existing Gear-3 recovery path, which must itself produce genuine output; strict Framework execution refuses a missing required lane. No error wrapper is cross-evaluated as an analyst draft.
- *Step 4 — required usable evaluation.* Evaluator calls retain bounded retry/fallback, but an exhausted failure stops the stage. The success guards run before the historical no-feedback fallback blocks, so those blocks do not turn a failed invocation into completed review.
- *Step 5 — reviser recovery.* Reviser calls retain bounded retry/fallback. After exhaustion, ordinary Gear-3/4 turns can preserve that stream's healthy original analyst draft in the expected revision envelope, with the failed revision and unapplied critique explicitly recorded for downstream verification. Strict Framework execution stops before this fallback. Preserving a genuine draft does not promote diagnostic text into an answer or claim that revision succeeded.
- *Step 6 — three-way verdict.* PASS / FAIL / BROKEN. PASS and BROKEN both unblock the cycle (re-revision cannot fix a verifier that itself errored); only FAIL triggers re-revision, capped at 2 cycles. BROKEN never counts as real verification — a `step6-cycleN-<slot>-verifier-BROKEN-not-verified` contingency lands in the health record so trend data reflects how often verification actually happened. This replaced a retired auto-PASS-on-exception path (the docstring labels it "silent failure #9").
- *Step 7 — substantive consolidation.* The consolidator uses its configured post-analysis cell, bounded recovery, and a 30-character stage floor. Exhausted failure raises before the old longer-draft fallback block; it does not relabel one stream as completed consolidation.
- *Step 8 / 8.5 — formatting and leak scrub.* Required formatter calls and corrective formatter calls must return usable outcomes; exhaustion stops them before failed text can enter delivery. The structural-leak gate retries a process-meta leak and Step 8.5 deterministically strips residual pipeline-leak lines.
- *Step 8.6 (Gear 4) / 6.5 (Gear 3) — final quality gate.* The verification-slot judge grades a usable candidate against the mode and output contracts. Gear 4 permits one redo per implicated producer kind (ANALYSIS reconsolidates then reformats; FORMATTING reformats), with reinspection; Gear 3 has its bounded revision redo. PASS confirms the inspected candidate. A broken judge is a typed failed call, not verification success. Ordinary turns preserve a substantive candidate in full on unresolved FAIL/BROKEN and mark it unverified with a warning where its format permits; structured openings remain byte-identical, and status still records shipped-with-warning. Strict Framework execution instead withholds the candidate. Neither path promotes the judge's failure text into the deliverable.

`run_pipeline` can prepend the resource-degradation signal. Model substitution is separately collected once on the request thread after worker outcomes return (Chapter 4). Parallel execution uses `ThreadPoolExecutor`, while the per-machine mutex and the selected-endpoint cache decision may serialize local work without changing Gear 4. Only a true effective-gear selection or the explicit unrecoverable-analyst recovery path invokes Gear 3; a scheduling hint alone cannot do so.

**Config surfaces.** `config/routing-config.json` is the single source of truth for endpoints, capability routing, `slot_assignments`, and gear-4 overrides. The `slot_assignments` block names eight gear-independent model slots — `depth`, `breadth`, `evaluator`, `consolidator`, `classification`, `step1_cleanup`, `sidebar`, and `rag_planner` — and any model can fill any slot. Note that there is no `verifier` slot and no `formatter` slot in `slot_assignments`: the verifier and the quality-gate judge resolve to the post-analysis `verification` slot through the router (`resolve_post_analysis_slot("verification", ...)`, lines 1539–1540), and the formatter/consolidator draw on the analysis slots. Env flags read at runtime: `ORA_DELIVERABLE_SCRUB` (Step 8.5), `ORA_MODEL_TOOL_SELECTION` (the model-driven tool escape hatch), `ORA_RAG_SELECTION` (RAG contamination gate), `ORA_PIPELINE_TRACE` (forensic traces). Both `ORA_DELIVERABLE_SCRUB=1` (line 47) and `ORA_RAG_SELECTION=1` (line 18) are exported by `start.sh`, so those are on in the installed system.

**Status precision on two features.** The **Step 8.5 deliverable scrub** is not flag-gated-off: the scrub is guarded by `_env_flag("ORA_DELIVERABLE_SCRUB")` at its gear-4 call site (line 11648; `_env_flag` is defined at line 6973 and is true when the var is `1`), and `start.sh` exports `ORA_DELIVERABLE_SCRUB=1`, so the scrub runs by default in the installed system and is disabled only by explicitly setting it to `0` for debugging (implemented, default-on). The **model-driven tool escape hatch** is genuinely partial: `_model_tool_selection_enabled` (line 8363) defaults `ORA_MODEL_TOOL_SELECTION` to `"0"`, and `start.sh` does not export it — flag-gated-off, so partial. The current Gear-2 `fast` resolution is installed through the named configuration's dedicated `utility.gear2_rag_lookup` cell; it is not dependent on a legacy `slot_assignments.fast` entry. If cleanup supplied the endpoint instead, that actual cell owns its retries.

### Evidence & Verification

**Tests.** `orchestrator/tests/test_pre_routing_pipeline.py` covers Stages 1–3, the dispatch announcement, the `run_pre_routing_pipeline` composition, and the corpus runner’s admission, comparison, and report-preservation behavior. Gear-4 reliability has dedicated suites — `test_gear4_analyst_recovery.py`, `test_gear4_degradation.py`, `test_gear4_no_short_circuit.py` — alongside `test_quality_gate.py`, `test_boot_prompt_assembly.py`, `test_perspective_loader.py`, `test_deliverable_scrub.py`, and nine `test_style_*.py` files exercising the Output Style composition path. This describes test coverage; it does not establish a full-suite or live-provider pass.

The routing corpus runner (`scripts/run-corpus-routing-test.py`) requires explicit absolute `ORA_HOME` and `ORA_VAULT` bindings, confirms that Ora is the checkout containing the runner, and refuses a differently initialized runtime root or home-default fallback. It checks the complete corpus and both vault/runtime Modes sources, admitting every Stages 1–3 requirement, fixture variant and answer checkpoint before importing the pipeline, measuring prompts, printing percentages or writing a report. Refusal returns exit 2 with source-located `INACCESSIBLE SOURCE`, `INVALID CORPUS`, `STALE OR INVALID TARGET` or `UNSUPPORTED MEASUREMENT` diagnostics; no unsupported clause is silently weakened or removed from the scored corpus. `--validate-only` performs admission without pipeline execution or report writes. Measurement calls the real pre-routing pipeline with inert supplied content, earlier messages and clarification answers. Manual picks and saved selections use the existing in-process server boundary with current configuration, model and conversation/privacy authority fields; effect services and terminal analysis are substituted, while routing and applicable completeness checks remain real. A guarded observation refuses unexpected network, process or storage effects. Required question alternatives, exact destinations and order, deferred/degradation offers, named missing fields and validated source/value are compared with actual outputs. A fault-injected initial classification earns no credit; a recovery answer is sent only after the actual required question, and absent required continuation remains a failure. Each stage counts an applicable case once and passes it only when all applicable variants and checkpoints pass. Reports disclose each injected or not-applicable reason, measured denominators and cascade non-execution; an upstream failure never removes a required later outcome from the denominator, and a zero denominator is labeled “not measured.” Stage 4 prose remains preserved but unmeasured: neither routing nor a captured analysis handoff proves analysis execution. The current report is atomically replaced at the default vault path or the explicit `--report` destination before success output; admission, observation or replacement failure preserves its preimage, with observation/report failure returning exit 1. Contained evidence uses explicit task/source-fixture roots and an enforced network-denied sandbox; merely setting environment flags is not evidence of containment. The routing verifier's 90% requirement for each of Stages 1–3 is unchanged. Admission alone is not a baseline, and a numeric baseline below that standard is not routing acceptance.

**Forensic traces.** When `ORA_PIPELINE_TRACE` is active, every step writes a health payload and any fired contingency under `data/pipeline-traces/<conv>/<turn>/`, including a `step-health.json` recording each step's ok/degraded state and the named contingency (for example `step8_6-quality-gate-*`, `step6-cycleN-<slot>-verifier-BROKEN-not-verified`). This is the primary way to confirm a Gear-4 turn actually ran the adversarial pair rather than silently degrading — the trace distinguishes "verified" from "verifier BROKEN" so a turn that shipped without real verification is visible after the fact. Reliability here is qualitative: the traces record *whether* each step degraded, but there is no instrumented success-rate or per-step reliability budget in the code. Any percentage would be invented; none is measured.

**Trace-completeness currency note (2026-07-16, post-pin).** PR #269 (implementation commit `99638ef3`, merge commit `241a31c0`) extends the forensic layer well beyond the state described at this document's `7a5e8f40` pin. This is a scoped current-state note for that landed subsystem, not a re-audit or re-pin of the rest of the chapter.

The current trace boundary covers the real server and CLI entrypoints, explicit direct-entry calls, framework milestones, and the production paths for Gears 1–4. It records pre-routing, context/RAG and web consultation, model-call configuration and usage, claim-evidence assembly, verification, fallback/retry paths, lifecycle outcomes, and the terminal value returned after output routing or server delivery. `step-terminal-output.json` is deliberately written after routing/persistence and includes route, output target, and `persisted`; a failed persistence response is therefore evidence with `persisted=false`, not a falsely successful terminal record. Gear 4→3 fallback markers and the last `step-health.json` correct the manifest to the Gear that actually executed.

`trace-manifest.json` preserves history instead of normalizing it away. `expected_steps` is the normally required path; `actual_steps` is the literal on-disk `step*.json` inventory; `derived_artifacts` holds summaries such as step health and cost; `missing_steps` is restricted to required work absent from a completed turn; `skipped_steps` covers optional non-execution and not-reached work on exceptional exits; `replaced_steps` names normal stages displaced by a fallback; `contingency_steps` names real exceptional stages that ran; and `unexpected_steps` contains real stages belonging to none of those declared sets. `stage_replacements` records which contingency displaced which normal stages. Direct and short-circuit paths have their own truthful expected sets rather than inheriting an impossible full-pipeline template.

Tracing remains observational and fail-open: trace start, step writes, terminal capture, inventory refresh, and manifest finalization catch their own failures and cannot change the production return value. Raw turn directories intentionally contain exact forensic inputs and outputs. The browser and HTML-export trust boundary is different: recursive safe projections replace every raw string/byte sequence with type, length, and SHA-256 metadata, hash unrecognized keys, refuse unsafe paths/symlinks/oversized files, and emit fixed error codes. Private Dialogues retain local evidence but are labeled `redaction_level=private`; browser/export surfaces omit their raw content and diagnostic turns retain the private routing tag. Stealth suppresses trace creation, and protected Delete Forever purges any Ora-managed residue. Trace Walk inspection and export are read-only with respect to production behavior and original evidence; pin/unpin changes only retention metadata, and **Investigate** creates a separate same-Dialogue P-Debug turn rather than replaying or modifying the traced execution.

**Confirmed drift (trust the code).** Two remaining documentation-versus-code divergences are real and load-bearing enough to name:

1. `frameworks/book/f-evaluate.md` and `f-revise.md` still reference the mode section `## EVALUATION CRITERIA` (f-evaluate lines 3, 11, 206, 211). The modes and `boot.py` (lines 8515–8518) renamed it to `## ANALYTICAL BRIEF AND EVALUATION CRITERIA` on 2026-05-24. The code keeps a legacy fallback, so this is functional but doc-stale — a reader following the F-stage spec will look for a section name the mode files no longer use.
2. `boot/boot.md` line 59 reads "Seventeen modes, four gears." The installed system has 64 modes. The gear count is right; the mode count is stale.

Additionally, `f-quality-gate.md` is wired (Gear-3 step 6.5, Gear-4 step 8.6) but is absent from `pre-routing-pipeline.md`'s stage description and from the F-stage inventory — an undocumented pipeline step relative to the architecture spec. And `architecture/` holds 9 files (`trusted-web-sources.md` is the ninth), while `CLAUDE.md` Decision K enumerates only 8.

**Explicitly untested surfaces at the chapter baseline.** The model-driven tool escape hatch (`ORA_MODEL_TOOL_SELECTION` default off, unexercised in normal operation) and every non-macOS platform (see below) retain their stated limitations. The current Model notes describe source-level Gear-1/2 and Gear-3/4 behavior, not a new live-provider or platform qualification. The broader `manual_mode_selection` and `/chat` bypass claims remain outside this scoped update.

### Expert Reading Notes

Read in this order. Start with `architecture/pre-routing-pipeline.md` for the *why* of the four stages and `routing_sources.py` for the compiled source boundary. Then read `build_system_prompt_for_gear` to see what each step's prompt contains, `run_gear4` for the cascade and its contingency table, and `run_gear3` for the sequential variant and the step-6.5 gate. Source-level routing measurement does not qualify those model-driven execution stages.

The single highest drift risk is the coupling between mode-file `## SECTION` names and the `_extract_section` calls at lines 8509–8523. A renamed section does not error; it silently returns empty and drops that step's guidance. The `EVALUATION CRITERIA` → `ANALYTICAL BRIEF AND EVALUATION CRITERIA` rename already left `f-evaluate.md` and `f-revise.md` stale, and the code only survives it because someone added a legacy fallback. If you rename or add a mode section, grep every `_extract_section` call and every `f-*.md` spec before assuming it took effect. This is the place future changes most likely cause silent regressions.

Second: the F-stage spec files in `frameworks/book/` are documentation that must track `boot.py`'s step wiring, and they have drifted before (`f-verify.md` was corrected 2026-05-22 after the Step 7/8 split left it describing a post-consolidation verifier that no longer exists). Treat the specs as intent and the boot.py step map as truth. Note in particular the verifier's known gap: `f-verify.md` states plainly (lines 7, 26) that the verifier's `### Verified Final Output` is captured in the trace but **not propagated** — the consolidator and formatter read the reviser's `revised_depth` / `revised_breadth` directly. A correction the verifier computes never reaches the user. That is by design, but it surprises readers who assume verification edits the output.

Third: the pipeline's "8 steps" and its 6 named pipeline roles are different countings. `_PIPELINE_STEPS` has six roles (analyst, evaluator, reviser, verifier, consolidator, formatter); the "8 steps" language counts Phase A cleanup as step 1, context assembly as step 2, and the quality gate at 6.5/8.6. Do not try to reconcile them into one sequence.

Fourth: `orchestrator/dispatcher.py` is named like a mode/gear dispatcher but is the *tool* dispatcher (permission gating, path validation, audit logging). The actual gear dispatch is the switch inside `boot.py::run_pipeline` at lines 9338–9394. This mis-maps easily from the file list.

Finally, treat `CLAUDE.md`'s mode and file counts as unreliable: 56, 58, 60, 64, and 65 all appear across the docs. The verified numbers at 7a5e8f40 are 64 modes (65 files in `modes/`, one being `INDEX.md`) with a 1/2/3/58 Gear-1/2/3/4 distribution, and 9 files in `architecture/`.

### Platform compatibility

The installed system runs on macOS on Apple Silicon and is verified only there. Per-subsystem checks:

- **Inference engine (depth/breadth analyst calls, Gears 3/4).** `platform_check.py::detect_platform` routes `Darwin` + `arm64` to MLX and everything else to Ollama (the `recommended_engine` branch at line 31). MLX is Apple-Silicon-only. **macOS: works. Windows-native / WSL / Linux: n/a for MLX** — those platforms are routed to Ollama by design, but that Ollama path for the gear analysts was **not run off-Mac and is untested.**
- **Parallel-analyst execution (`run_gear4`).** Uses stdlib `concurrent.futures.ThreadPoolExecutor` (portable) plus a Mac-specific MLX mutex that serializes same-machine local calls. **macOS: works. Windows-native / WSL / Linux: untested** — the ThreadPoolExecutor is portable in principle; the MLX serialization is the Mac-specific part and does not apply elsewhere.
- **RAM detection (`platform_check.get_system_ram_gb`).** Has explicit branches for Darwin (`sysctl`), Linux (`/proc/meminfo`), and Windows (`wmic`), with a 16 GB fallback (lines 40–59). **macOS: works. Linux / Windows-native: code path exists but untested. WSL: untested** (would take the Linux branch).
- **Root paths (`runtime_paths.py`).** Built from `expanduser('~')`, env-overridable via `ORA_HOME` / `ORA_VAULT`, with `norm_key` using `os.path.normcase` + `realpath` for Windows-safe prefix checks. **macOS: works. Windows-native / WSL / Linux: untested** — cross-platform in construction, POSIX-shaped in its defaults, never actually run off-Mac.
- **Interpreter and launcher.** `start.sh` is a bash launcher and retains Mac-oriented interpreter preferences and explicit feature exports. The separate current `start.bat` honors `ORA_HOME`, prefers the checkout virtual environment, falls back to `py -3`/`python`, validates the port, enables oversight by default, consumes `--no-oversight`, forwards other arguments, and binds process/health handling to the current checkout. **macOS: works. Windows-native: implemented but untested** — `start.bat` enables the Execution Review loop but omits the POSIX launcher's additional feature-default exports, so Windows can still run a reduced or differently configured pipeline; no clean-room Windows run supports a “works” label. **WSL / Linux: untested** — the POSIX launcher is plausible there but not verified.
- **Server startup (`server.py` → `boot.py` pipeline, localhost:5000, Flask).** **macOS: works. Windows-native / WSL / Linux: untested** — localhost binding is portable in principle, but the server has not been started off-Mac. (The `/chat` pipeline is synchronous JSON with browser polling, not SSE — see the Server chapter; SSE survives only on three residual endpoints.)
- **Trace writes (`data/pipeline-traces/` via `pipeline_trace`).** Path-based writes through `runtime_paths`. **macOS: works. Windows-native / WSL / Linux: untested** — filesystem case-sensitivity differs on Windows; `normcase` handles the prefix checks but the write path itself was not exercised off-Mac.

No non-macOS surface is claimed to work; every one above is labeled untested or needs-porting where it was not actually run off-Mac.

## 6. Framework Loading & Execution

- **Current standalone document-review boundary.** The Note Review app at `server/review.py` remains a separate localhost process, but its app-wide pre-view hook calls the same server-package browser-origin decision as the main host.
- A hostile form POST to `/action/<filename>` returns `403` before the action handler reads `request.form` or touches review, staging, or rejected files; headerless local and same-origin requests remain compatible.

### Problem

An Ora framework is a long adversarial-analysis specification — Process Formalization Framework v2.1, for one, runs to thousands of words of layered instructions. If I hand that whole document to a model as a single prompt, three failure modes follow. The model truncates: it reads the first layers, produces something plausible, and never reaches the verification criteria at the bottom. The model wanders: with no enforced checkpoints, milestone three's output drifts from what the user actually asked in milestone one, and nothing catches the drift until a human reads the final artifact. And the model conflates: it collapses a five-stage procedure into one undifferentiated pass, losing the reproducibility that made the framework worth writing down. A framework that is only prose is a framework the model is free to ignore.

There is a second, orthogonal risk. The directory `~/ora/frameworks/book/` holds 39 markdown files. Most are not user-facing frameworks. Eight are internal Gear-4 pipeline-stage specs — `f-analysis-depth.md`, `f-evaluate.md`, `f-consolidate.md`, and their siblings — that the gear pipeline loads automatically and that make no sense invoked standalone: running `/framework f-verify "…"` would ask the model to perform a per-stream verification pass against nothing. If file existence in `frameworks/book/` were treated as permission to invoke, every one of those internal stages, plus support specs like `phase-a-prompt-cleanup.md` and `supplemental-rag-protocol.md`, would be exposed through the same `/framework` command that runs Problem Evolution. That is a namespace-hygiene failure: the invocation surface must be a curated subset of the file surface, not its mirror.

This subsystem solves both problems. It parses spec prose into an executable milestone graph, runs that graph as milestone-bounded gear passes with a drift check at each boundary, and gates which files a user may invoke through a hand-maintained allow-list.

### Design Decision & Tradeoffs

**The milestone is the unit of execution.** The parser (`framework_parser.py`) reads a framework's `## MILESTONES DELIVERED` section and extracts each `### Milestone N:` (single-mode) or `#### Milestone N:` (multi-mode) block into a `Milestone` dataclass, binding its properties inline: the endpoint it produces, the verification criterion that defines success, the layers it covers, the gear it runs at, the milestones it requires first, and a drift-check question. The executor (`milestone_executor.py`) then runs the framework as a sequence: for each milestone, assemble a structured handoff packet, run it through one gear pass, persist the deliverable, and check it for drift before advancing.

The decision I want to defend here is reuse over parallel machinery. A framework execution *is* adversarial analysis — depth pass, breadth evaluation, revision, consolidation, verification. Ora already has that machinery in `run_gear4`. So one milestone equals one `run_gear4` call over a structured handoff packet (`_run_through_gear_pipeline`, `milestone_executor.py:565-607`). I rejected the alternative of building a separate framework-specific review path. A parallel path would have duplicated the entire gear pipeline's adversarial logic and then drifted out of sync with it the first time someone tuned the gear-4 consolidation step. Instead the framework layer is thin: it decomposes the spec into milestones and lets the existing pipeline do the analytical work. The tradeoff is that a framework can only be as adversarial as the gear it declares; a milestone marked `gear: 1` gets a single-pass model call with no cross-evaluation (`milestone_executor.py:586-607`).

**Multi-mode routing is deterministic-first, LLM-last.** Frameworks like Problem Evolution declare several operating modes (PE-Init, PE-Iterate, PE-Review, PE-Spawn). Picking the right one for a given user request is a routing problem, and I did not want to pay a model call for it in the common case. `select_mode` is a four-tier cascade (`milestone_executor.py:734-793`): an explicit mode prefix as the first token of the input (consumed, cheap, exact); an in-input mention of a mode name anywhere in the text; only then a small-model routing classifier on the sidebar slot; and finally a default to the first declared mode. The first two tiers are pure string matching. The classifier — the only tier that costs a model call — fires only when no textual signal is present, and it degrades to the first-mode default when no endpoint is available (`_llm_select_mode` returning `None`). This is the same design principle as a fast-path/slow-path cache: spend the expensive resource only on the inputs that actually need it.

**Invocability is a curated allow-list, not file existence.** `framework_invocability.py` loads `config/framework-invocability.json`, which declares three disjoint sets: `invocable_frameworks` (11 entries, runnable through `/framework`), `pickable_frameworks` (11 entries, the same public set surfaced in the picker UI), and `internal_only_frameworks` (10 entries). The loader unconditionally folds the eight Gear-4 F-* stage IDs into the internal set regardless of the JSON (`framework_invocability.py:86-91`), so even a mis-edited config cannot accidentally expose a pipeline stage. `resolve_user_invocable_framework` maps a user token through aliases (`cff` → `corpus-formalization.md`, plus `deep-research`, `knowledge-artifact-coaching`, `off`, and `pff`) and raises `FrameworkInvocabilityError` with a specific message when the token is internal-only or unregistered (`framework_invocability.py:124-149`). The rejected alternative — glob `frameworks/book/` and expose everything — would have made the F-* internal stages invocable and coupled the user-facing surface to an implementation-detail directory.

**Config binding was designed as opt-in, and remains unwired.** `framework_config.py` implements `${config.<key>}` substitution and `<!-- ora-project-extension: <id> -->` overlay splicing, so a project-bound spec can carry project-specific values (a ChromaDB collection name, say) without hardcoding them. The design decision was to keep this a separate opt-in layer — `compose_framework_spec` — so the default execution path stays project-neutral and the executor keeps parsing raw spec text. That decision is sound. But the honest status is that `compose_framework_spec` has no production call site: grep finds it only in `framework_config.py` itself and its test file, and `framework_config` is imported by nothing in production (verified against `orchestrator/` and `server/` at 7a5e8f40). The module's own docstring says Chunk 4 — the migration of `document-processing.md` — "switches that one framework's call sites over" (`framework_config.py:43-46`); that migration did not land. This is partial: coded, unit-tested, and orphaned. The runtime path in `execute_framework` calls `parse_framework_file` on raw text and never touches the config binder (`milestone_executor.py:254`).

**The strict parser is a deliberate tradeoff that leaves DCA non-executable.** The parser fails loudly: a framework whose `## MILESTONES DELIVERED` section is absent raises `FrameworkParseError` rather than silently producing an empty milestone list (`framework_parser.py:100-101`). I prefer loud failure — a spec that doesn't conform should not run half-formed. The cost is that Decision Clarity Analysis (`decision-clarity-analysis.md`), named as one of the six canonical frameworks, has no `## MILESTONES DELIVERED` section. Parsing it raises `FrameworkParseError: decision-clarity-analysis: no '## MILESTONES DELIVERED' section found` (verified by direct invocation at 7a5e8f40), and it is absent from all three lists in `framework-invocability.json` (verified: `grep -c` returns 0). So the operative status is precise: PEF, PIF, PFF, CFF, and OFF parse and route as multi-mode frameworks (all five verified to yield their declared mode lists), but DCA is an analytical specification only — it cannot run through the milestone executor. The canonical framework suite is closed at six members; five are executable, DCA is not.

**Open problems.** Three are load-bearing. First, `DRIFT_DETECTED` is log-and-continue: when the drift check flags that a milestone wandered, the executor records the reasoning and proceeds anyway (`milestone_executor.py:321-324`); pause-and-surface-to-user is deferred. Second, per-framework default configuration via `framework-routing.json` is coded but inert — the file contains only an underscore-prefixed `_example_pef` stub that the loader ignores, so the lookup always falls through to the Router's auto-derivation (`config/framework-routing.json`; lookup at `_lookup_framework_default_configuration`, `milestone_executor.py:141`). Third, the orphaned config-binding layer above.

### Implementation

**Dispatch chain.** Both `orchestrator/boot.py` and `server/app.py` detect the `/framework ` prefix and route to `run_framework_command` (`boot.py:9158-9194`, `server.py:3253-3290`). Each call site is gated by an Execution Review Phase 2 check before dispatch (the comment reads "Execution Review Phase 2 (condition 4)" at both sites). `run_framework_command` (`milestone_executor.py:994`) is the top-level entrypoint: it calls `parse_framework_command` to split `/framework <name> [--config <ConfigName>] [<query>]` into a resolved filename, a query, and an optional config name. The filename is resolved through `resolve_user_invocable_framework`, so an internal or unregistered token is rejected here with a bracketed error string. Every failure mode in this function is caught and returned as a bracketed string rather than raised (`FileNotFoundError`, `FrameworkParseError`, `NotImplementedError`, catch-all `Exception`), because the chat UI must always receive a renderable response. A framework that fails to parse produces `[Framework parse error: …]` in the transcript, not a stack trace.

An empty query is a deliberate fork, not an error: `run_framework_command` treats it as a usage message, while the chat handlers route empty-query invocations to interactive multi-turn elicitation (documented at `parse_framework_command`, `milestone_executor.py:890`).

**Execution.** `execute_framework` (`milestone_executor.py:205-411`) parses the file, resolves the effective config name (explicit arg → `framework-routing.json` default → `None`, at which point the Router auto-derives from execution context), then branches on `is_multi_mode`. Multi-mode frameworks call `select_mode`; the chosen mode's milestones are pulled from `milestones_by_mode`. If the selected mode is mechanical (C-Instance, C-Validate, O-Render), the executor short-circuits before creating scratch or calling any model and returns a redirect (`milestone_executor.py:272`, `_build_mechanical_redirect` at 693). This prevents a specific failure: a mechanical mode's real job is to create a file on disk via a runtime function; routed through the gear pipeline, a model would instead hallucinate an artifact. The redirect points the user at the matching slash command (`/instance`, `/validate`, `/render`).

**Mechanical corpus validation.** The corpus parser treats malformed YAML in either frontmatter or a fenced `sections:` block as an invalid corpus. `/instance`, `/validate`, and `/render` surface that parse failure and stop before creating an instance or rendered artifact or emitting `CorpusInstanceCreated`, `CorpusValidated`, or `OFFRendered`; metadata-free legacy corpus templates and valid YAML continue through the existing paths.

For a live mode, the executor creates a `ScratchSession` (`scratch.py`), emits a `FrameworkStarted` oversight event, and iterates the milestones. Each milestone runs through `_run_milestone` (`milestone_executor.py:416-456`), which builds the handoff packet once and retries the gear pass up to `MAX_RETRIES = 3` times with `2**(attempt-1)` backoff (`milestone_executor.py:449-451`). On success it writes the deliverable to scratch and runs the drift check; on the third failure it raises `MilestoneExecutionError`, which `execute_framework` catches to mark the scratch session failed — preserving it for inspection — and emit `MilestoneBlocked` plus a failed `FrameworkComplete` (`milestone_executor.py:369-411`). On overall success, scratch is cleaned up and the last milestone's deliverable becomes the final output.

**Handoff packet.** `_build_handoff_packet` (`milestone_executor.py:461-527`) assembles the user message for the gear pass from: the original user input; prior milestone deliverables read from scratch, keyed on the milestone's `required_prior` list; the current milestone header; the layer instruction bodies for the layers this milestone covers (looked up through `_collect_layer_bodies` at `milestone_executor.py:530`, which tries exact label, punctuation-stripped label, then integer match); the output specification; the verification criterion; and any conditional layers. When a milestone references layers whose `## LAYER` blocks are absent, the packet does not silently proceed — it inlines a visible note recording the gap. This matters because the packet, not the raw spec, is what the model sees; a missing layer body would otherwise be an invisible omission.

**Gear invocation.** `_run_through_gear_pipeline` (`milestone_executor.py:565-607`) wraps the packet in a `context_pkg` and dispatches by declared gear: `run_gear4` at gear ≥ 4 (default), `run_gear3` at gear 3, single-model pass below. It deliberately bypasses Phase A cleanup and mode classification — the handoff is framework-generated structured content, not raw human input, so re-running input hygiene on it would be wrong. The `config_name` threads through to the gear functions so a named configuration routes the whole pass.

**Runtime document-processing ownership.** Dialogue-closeout extraction is not another framework mode, but it reuses the document-processing path after `server/app.py` has authenticated one current complete non-Stealth exchange. Every approved staging note and every human-review record created from that run carries the Ora-managed derivative kind plus the exact source Dialogue, source chunk, local turn, and Standard or Private authority. A Private derivative also retains its controlled Private tag. Stealth is refused before extraction, and the extractor never receives preceding history as substitute evidence for a missing current owner.

The standalone review app preserves runtime ownership through disposition. Approve, edit, and reject take the source Dialogue's cross-process lock and reread the record. They compare only immutable Dialogue/chunk/turn coordinates; when those still match, disposition uses the reread record's current privacy, so a retag completed before lock acquisition is safe. Ordinary non-turn records with none of the runtime markers—derivative kind, manager, turn privacy, source chunk, or source turn—remain compatible. Any marker makes an ownership claim; partial or malformed runtime ownership, and conflicting immutable coordinates, are refused.

**Drift check.** `_run_drift_check` (`milestone_executor.py:627-669`) runs on the sidebar slot (`DRIFT_CHECK_SLOT = "sidebar"`, a small cheap model). It prompts a drift-detection auditor with the user's original input, the milestone deliverable, and the milestone's drift question, demanding a structured `STATUS:` reply parsed by `_parse_drift_response` (`milestone_executor.py:672`). Three outcomes: `IN_SCOPE`, `DRIFT_DETECTED`, or `DRIFT_CHECK_SKIPPED` — the last when no drift question is declared, no endpoint is available, or the model call raises. The check names the failure mode it prevents: silent scope creep across a multi-milestone procedure.

**Oversight events.** Every boundary emits through the in-process bus (`FrameworkStarted`, `MilestoneComplete`, `FrameworkComplete`, `MilestoneBlocked`), each carrying the selected mode. The emit function is lazily imported and stubbed to a no-op when the oversight infrastructure isn't loaded (`milestone_executor.py:243-249`), so the executor runs standalone. Standalone invocations (no `project_nexus`) still emit but are filtered out downstream by the oversight router.

**Config surfaces.** Four: `config/framework-invocability.json` (the allow-list); `config/framework-routing.json` (per-framework default configuration — stub-only today); the `--config <ConfigName>` flag on the command; and `ORA_HOME`, honored only by `framework_config.py:60`. Note the workspace-root divergence: `framework_parser.py:20` hardcodes `~/ora/frameworks/book` and `scratch.py:22` hardcodes `~/ora/scratch`, both via `os.path.expanduser` and both ignoring `ORA_HOME`, while `framework_config.py:60` respects it. A relocated install would find the config binder and the parser disagreeing about where the workspace lives.

**A one-shot self-persistence side effect.** `_maybe_persist_self_mindspec` (`milestone_executor.py:155-202`) fires only when the framework is `mindspec-interview` and the mode is `MSI-Self`: it archives the full self-portrait to `~/ora/mindspec/self-spec.md` and writes a projected assistant-directives block into `~/ora/mind.md` (via `mind_projection`, falling back to the verbatim spec on projection failure). It is best-effort and never raises. This is the one place the executor writes outside scratch, and it is narrowly gated so an agent or character spec can never clobber the user's values.

### Evidence & Verification

**Passing test suites** covering this subsystem: `test_multi_mode_dispatch.py` (28 tests) covers the four-tier priority chain, the LLM-classifier response parsing, catalog construction, end-to-end `execute_framework` with a mocked gear pipeline, mode-tagged oversight events, and the mechanical-mode redirect, plus live-parse smoke tests against the canonical CFF and OFF files. `test_framework_invocability.py` (4 tests) covers the allow-list resolution and alias handling. `test_framework_config.py` (35 tests) covers `parse_config_interface`, `substitute_config_refs`, `splice_extension_overlays`, and `compose_framework_spec` end-to-end against synthetic frameworks. `test_chunk3_per_process_config.py` (10 tests) and `test_router_config_name.py` (26 tests) cover named-configuration threading. These counts are the `def test_` totals in each file at 7a5e8f40; the subsystem carries no instrumented aggregate suite number.

**Live parser proof.** Running `parse_framework_file` against each canonical framework at 7a5e8f40 confirms: `problem-evolution` → `['PE-Init', 'PE-Iterate', 'PE-Review', 'PE-Spawn']`; `process-inference` → `['P-Infer', 'P-Debug', 'P-Decompose', 'P-Formalize', 'P-Feasibility']`; `process-formalization` → `['F-Design', 'F-Convert', 'F-Render', 'F-Audit']`; `corpus-formalization` → `['C-Design', 'C-Modify', 'C-Instance', 'C-Validate']`; `output-formalization` → `['O-Design', 'O-Modify', 'O-Render', 'O-Audit']`. The negative case is equally confirmed: `decision-clarity-analysis.md` raises `FrameworkParseError: decision-clarity-analysis: no '## MILESTONES DELIVERED' section found`, and `grep -c decision-clarity config/framework-invocability.json` returns 0.

**How to confirm end-to-end.** Invoke `/framework problem-evolution PE-Init walk through the new project` in chat: the explicit prefix consumes `PE-Init`, the remaining text becomes the effective input, and `format_execution_result` surfaces the selected mode with a mode-selection line in the header. A `/framework f-verify …` invocation returns a bracketed `FrameworkInvocabilityError` message — proof the internal-stage boundary holds. A `/framework cff "create instance for May"` invocation returns the mechanical-mode redirect rather than running the gear pipeline.

**Explicitly untested surfaces.** `framework_input_gap.py` has no dedicated unit-test file in `orchestrator/tests/`; its two analysis paths (structured `## Setup Questions` and LLM-fallback `## INPUT CONTRACT`) are verified only indirectly through its `server/app.py` call site at `/api/framework/analyze-inputs`. `compose_framework_spec` is unit-tested but has zero production call sites — its behavior in situ is unverified because nothing in production invokes it. The `framework-routing.json` per-framework default path is exercised by no real mapping — only the ignored `_example_pef` stub exists, so the non-trivial branch of `_lookup_framework_default_configuration` never fires in production. There is no instrumented reliability metric on milestone execution: `MAX_RETRIES = 3` is a fixed retry count, and the fraction of milestones that pass drift, retry, or fail is not measured. Non-macOS platforms are entirely untested.

### Expert Reading Notes

Read `framework_parser._parse_properties` and `_extract_milestones_section` first. The entire executor depends on that regex schema — properties are parsed from `- **PropertyName:** value` bullets, and a spec that deviates from that exact shape silently loses the property (the value defaults to empty string, and the milestone runs with a missing endpoint or verification criterion). If you are debugging a milestone that runs but produces the wrong deliverable, suspect a malformed property bullet before anything else. Then read `milestone_executor.select_mode` (734-793) and `_build_handoff_packet` (461-527) — those two functions determine which milestones run and what the model actually sees.

Four drift hotspots, where future changes are most likely to cause silent breakage:

1. **Stale docstrings.** `milestone_executor.py:19-24` still claims single-mode is "the MVP target" and multi-mode routing is "not yet wired in (an executor TODO)." That is false at 7a5e8f40 — `select_mode` and full multi-mode dispatch are implemented and tested. Trust the code, not the docstring.

2. **Orphaned config binding.** `framework_config.py`'s docstring describes a Chunk-4 migration of `document-processing.md` that never landed. `compose_framework_spec` is dead in the runtime path. If a new framework adds a `## CONFIGURATION INTERFACE` section expecting `${config.*}` substitution, the executor will not perform it — it parses raw text. This is the single most likely place for a future contributor to build on an assumption the runtime doesn't honor.

3. **DCA in the canonical six.** Anyone reasoning about "the six frameworks" as a uniform executable set is wrong: DCA is a spec-only analytical framework. Check the invocability JSON before assuming a framework runs.

4. **Workspace-root divergence.** `framework_parser.py:20` and `scratch.py:22` hardcode `~/ora` paths; `framework_config.py:60` honors `ORA_HOME`. Any relocation work must reconcile these three, or the parser and the (currently dormant) config binder will read from different roots.

### Platform compatibility

- **`FRAMEWORKS_DIR` (`framework_parser.py:20`, `os.path.expanduser("~/ora/frameworks/book")`)** — macOS: works. Linux: untested; `expanduser` resolves `~` on POSIX, but the hardcoded `~/ora` layout has not been run off Mac. WSL: untested, same basis. Windows-native: needs-porting — the hardcoded `~/ora` layout and forward-slash path have not been exercised off POSIX.
- **`framework_config.FRAMEWORKS_DIR` / `ORA_HOME` override (`framework_config.py:60-61`)** — macOS: works. Linux / WSL: untested. Windows-native: untested. Note the intra-subsystem inconsistency: this module honors `ORA_HOME`; `framework_parser.py` and `scratch.py` do not. On any platform where the install is relocated via `ORA_HOME`, the parser and config binder resolve different roots — though since `compose_framework_spec` is unwired in production, this is latent, not active, today.
- **`framework_invocability.CONFIG_PATH` (`framework_invocability.py:16-20`, `Path(__file__).resolve().parent.parent / "config" / …`)** — macOS: works. `pathlib` relative resolution is cross-platform in principle, but not actually run off Mac. Linux / WSL / Windows-native: unknown-untested.
- **Scratch writes (`scratch.py:22`, `SCRATCH_ROOT = ~/ora/scratch/<execution_id>/`) and the failure-message path (`milestone_executor.py:968`, hardcoded `~/ora/scratch/…`)** — macOS: works. Linux / WSL: untested. Windows-native: needs-porting — same hardcoded POSIX layout as the parser.
- **Local model dispatch for drift check and LLM mode-select (sidebar slot)** — macOS: works. Linux / WSL / Windows-native: the *local* slots are Apple-silicon-only. Ora's local models run on MLX/Metal. Off-Mac, only cloud slots are available; when no endpoint resolves, the drift check returns `DRIFT_CHECK_SKIPPED` and `_llm_select_mode` returns `None` (the cascade then defaults to the first declared mode). Framework execution itself does not require a local model — it degrades to reduced routing precision and skipped drift checks, not failure. This degradation path is coded but has not been exercised off Mac.
- **`compose_framework_spec` overlay reads (`framework_config.py`, utf-8, `OSError`-guarded)** — macOS: works in unit tests. Linux / WSL / Windows-native: untested. Effectively n/a in production today: the entire config-binding path has no production caller, so no platform actually exercises it at runtime.
- **Untested off-Mac overall:** Windows-native, WSL, and Linux have not been run for this subsystem end-to-end. Any "works" above is scoped to macOS; POSIX `expanduser`/`pathlib` semantics are the *reason to expect* Linux/WSL portability, not evidence of an executed run.

## 7. Output Styles & Personal Values

- **Current Style/Theme deletion boundary.** Style deletion requires its callable store/settings handlers, a regular style store, a regular-or-absent settings file, the exact profile, and valid settings shape before review. Theme deletion similarly requires one valid regular index entry and its non-symlink directory. Their existing protection receipts still bind the affected files and terminal state.
- Project-theme asset serving, list previews, duplication, and export share the project-theme opener described in Chapter 17; it refuses outward-symlink redirection before content is read.

### Problem

An analytical pipeline that produces the right answer in the wrong voice has still failed the user. But voice is not one knob. A user wants two independent controls: *how* the assistant talks — tone, arrangement, diction, the genre it writes in — and *what values* it holds — its directness, its willingness to disagree, its ethical red lines. Bolt these together into a single "personality" and you get a system where changing the writing style silently changes the ethics, and where a request for a warmer tone leaks into a request for softer verdicts.

Two failure modes must be prevented specifically.

The first is granularity collapse: injecting the same voice instruction into a two-line conversational reply and a 3,000-word deliverable. A fast reply at gear 1 needs a disposition — is this assistant blunt or diplomatic, warm or cool. A produced artifact at gear 4 needs that *plus* an arrangement schema, a craft floor, and a diction glossary. One undifferentiated blob is either too heavy for the quick reply or too thin for the deliverable.

The second, and the more dangerous, is style contaminating substance. If the same voice layer that shapes prose also reaches the evaluator or the verifier — the adversarial steps whose entire job is to catch errors — then a style mismatch can lower a verdict. A user who asked for a "challenging" demeanor could, through that alone, get findings marked down. Style is secondary to substance. The system has to encode that ordering mechanically, not merely hope for it.

The values layer has its own failure mode: clobbering. A user might spend two minutes in a guided wizard, then months later run a multi-hour structured interview, then hand-edit the result. If those three authoring paths write the same file without knowing about each other, each overwrites the last and the user loses work silently.

This chapter covers the subsystem that answers all four: the prompt-injection voice/values layer, distinct from and secondary to the gear pipeline described in the pipeline-architecture chapters. It is authoritative for what the vault documents as the Output Styles pane; see [[Reference — Ora Output Styles Pane]] and [[Specification — Output Style Registry]].

### Design Decision & Tradeoffs

The concern splits into two orthogonal, independently-toggled layers with an explicit precedence chain baked into every injected block. In the module's own words (`orchestrator/style_assembly.py:13`) the ordering is:

```
values floor > completeness floor > craft floor > substance/findings > style
```

That ordering is not decorative — a condensed form of it ships verbatim inside the injected text, so the model reads its own precedence constraint. The gear-2 demeanor block emits `PRECEDENCE: values floor > completeness > craft > substance > style` (`style_assembly.py:164`); the gear-3/4 style block emits `PRECEDENCE: values floor > completeness > craft > findings > style` (`:175`). The precedence is *instructed*, not mechanically gated — a prompt line, not a runtime guard — and that is a weaker guarantee than a hard gate. What *is* mechanical is where the block gets injected and which steps see it.

Output Styles are gear-conditional. At gears 1-2 (fast conversational replies) the system injects a compact `## DEMEANOR` block, framed *high* in the prompt, before substance — because at that granularity the disposition *is* the instruction. At gears 3-4 (produced output) it injects the full `## STYLE` block, appended *after* the step's substance, and only on the producing/shaping steps: analyst, reviser, consolidator, formatter. The evaluator and verifier are excluded outright (`orchestrator/boot.py:8711`). This is the mechanical answer to the substance-contamination failure mode: a style mismatch physically cannot reach the step that renders a verdict, because the injection code never runs there.

Personal values live in a single user-authored `mind.md`, appended by the boot loader as an authoritative block, gated on one setting. It is one file with one save destination that equals its load source — no parallel copies to drift.

Persona, Output Styles, and `mind.md` are separate layers rather than one unified persona blob. Persona establishes the speaking identity; Output Styles shape delivery at the gear and pipeline stages described above; `mind.md` supplies the user's authoritative personal values. For interactive work, Persona resolution keeps its ordinary precedence: a project Persona, then the global default, then packaged Ora. The Scheduled/Oversight email-draft composer is the deliberate exception: it disables project precedence and uses the global default unless the user explicitly selects another available Persona. Each saved draft records and displays the resolved Persona identity. If an explicitly requested Persona is unavailable, draft creation refuses the request instead of silently substituting another sender. Creating the draft changes no authority: it neither activates nor approves a Trigger, contacts an email provider, nor permits sending. Keeping these layers separate prevents a writing-style edit from changing the user's values or a project context from silently changing the identity attached to scheduled correspondence.

The tradeoff on the values side is three authoring paths for one file: a two-minute guided wizard, a raw editor, and a projection from a multi-hour structured interview. Three writers on one file is exactly the clobbering hazard. It is paid for with a provenance-marker state machine: a composed `mind.md` carries an HTML-comment marker declaring who wrote it — `<!-- ora-mind-guided: … -->` (`orchestrator/mind_guided.py:36`) or `<!-- ora-mind-projected: … -->` (`orchestrator/mind_projection.py:55`) — while the stock default file carries only a plain text marker line (`*Default configuration…*`, `mind.md:3`) that reads as un-customized. The write endpoints read the marker before overwriting and refuse to clobber a hand-edited or interview-authored file without explicit `confirm_overwrite` (`server/app.py:1339`). This lets a 2-minute wizard and a multi-hour interview coexist on the same file without either silently eating the other.

**Open problems.** Two are load-bearing and are flagged plainly here.

First, the situational-delta and register-switching chain is designed and coded but unwired at runtime — status *partial*. `style_assembly.py` fully implements one-rung demeanor swaps (`resolve_demeanor`, `:98`) and a conversational-register overlay (inside `compose`, `:218`), and both are unit-tested in isolation. But the runtime context package hardcodes `style_register: "written"` and `style_deltas: None` (`orchestrator/boot.py:7767-7768`), and no code path sets them otherwise. The register-derivation is explicitly deferred in the module's own docstring ("once the resolution chain is wired", `style_assembly.py:206`). In production this surface is dead: every injected style resolves as written-register with no deltas. It is not a bug; it is an unbuilt half of a designed feature, and the documentation must not present it as working.

Second, the values floor is instructed, not enforced. The precedence line tells the model to hold values above style, but nothing measures whether it did. There is no instrumentation on this — no counter, no per-step budget. A guarantee stronger than "the prompt says so" does not exist yet.

### Implementation

The subsystem has a clean seam: a standalone, side-effect-free assembly core (`style_assembly.py`, `style_store.py`) that only reads files and returns strings, and the boot-layer wiring that decides *when* and *where* those strings get injected. This mirrors the Unix discipline of a pure function plus a caller that owns the side effects.

**Style assembly (the pure core).** `orchestrator/style_assembly.py` parses four read-only markdown source files under `frameworks/book/`:

- `style-registry.md` — 12 built-in genre profiles (explainer, technical, how-to, journalism, business, white-paper, academic, legal, marketing, reference, narrative, conversational), each a `## id` heading plus a `yaml` block (verified: `grep -c '^## '` = 12).
- `style-demeanor-axes.md` — 7 demeanor axes, each with 3 rungs low-to-high: warmth (cool/even/warm), force (gentle/measured/forceful), energy (calm/steady/lively), outlook (skeptical/balanced/affirming), playfulness (straight/dry/playful), directness (diplomatic/plain/blunt), agreeableness (accommodating/candid/challenging) — plus a device-overlays section (sarcasm, irony, hyperbole, understatement) toggled independently. The file carries 8 `## ` headings: the 7 axes plus the device-overlays block.
- `style-arrangement-schemas.md` — 12 ordered slot-list schemas (answer-first, motivation-first, inverted-pyramid, and so on).
- `style-craft-floor.md` — 5 craft-floor bullet lines that outrank style inside the full block.

The parsing uses PyYAML to read the registry's yaml blocks. The import is guarded: if PyYAML is absent, `load_registry` raises `RuntimeError` (`:57`) — and the callers swallow that (below), so a PyYAML-less environment silently *disables* Output Styles rather than crashing the pipeline. That is a deliberate degradation, and it is exactly the failure mode "missing dependency takes down the whole assistant" that the guard prevents.

`compose(style_id, register, gear, deltas, base, custom_entries)` (`:201`) is the entrypoint. It merges any custom profiles over the built-in registry, then branches on gear: `gear <= 2` returns `compose_demeanor_block` (disposition lines + the values-floor line + the completeness line, no arrangement); `gear >= 3` returns `compose_style_block` (demeanor + craft floor + arrangement schema + diction glossary + completeness). The gear-2 branch is where the conversational-register overlay *would* apply — it reads `entry["conversational"]` and merges the alternate demeanor per-axis — but since no caller sets a non-written register at runtime, the overlay is inert today.

**Style store (custom forks).** `orchestrator/style_store.py` persists user-forked profiles to `data/custom-styles.json` (STORE_PATH at style_store.py:25; the file is created on first custom-profile write and is absent on a fresh checkout). It mirrors the model-configuration persistence idiom deliberately: read-only framework files stay canonical, user edits live in the data dir. Writes are atomic — write to `STORE_PATH + ".tmp"`, then `os.replace` (`:95-98`) — so a crashed write never leaves a half-written store. Every reader degrades to an empty dict on a missing or corrupt file (`load_custom_profiles`, `:74`): a bad custom profile can never break the picker or the pipeline. A `PATCHABLE` whitelist (`:48`) means an endpoint can never inject an arbitrary key into a stored entry; a patch to `demeanor`/`devices`/`glossary`/`format` deep-merges key-by-key so one axis can be changed without resending the block.

**The two injection sites (the wiring).** `orchestrator/boot.py::_compose_output_style` (`:8432`) is the single funnel. It reads `style_id` off the context package, merges custom profiles, and calls `style_assembly.compose`. Critically, the *whole function is wrapped in a bare except that returns `""`* (`:8464`) — style is secondary to substance and must never break the pipeline, so any failure (missing file, malformed yaml, absent PyYAML) yields a no-op, not an exception.

It is called from exactly two places:

1. Gears 1-2, framed high (`:8539`): `if 0 < _osf_gear <= 2:` — the compact demeanor block goes near the top of the prompt.
2. Gears 3-4, after substance (`:8711`): `if _osf_gear >= 3 and step in ("analyst", "reviser", "consolidator", "formatter"):` — the full style block is appended after the step's substantive content. The step tuple is the mechanical exclusion of evaluator and verifier.

**Resolution chain.** `_resolve_effective_style_id(config)` (`:7088`) computes the turn's default style before any one-off, in precedence order: active project's `default_style_id` → project container-record `output_style` (read via `project_meta` for a Projects-feature project with no plugin manifest) → the `styles.default_id` account setting → legacy engine-config default → `None`. It is best-effort throughout — every lookup is in a try/except that falls through, so a project-lookup failure never breaks step-2 assembly.

**Config surfaces.** `orchestrator/user_settings.py` defines two account-wide controls (`:105-115`): `styles.default_id` (string; `""` = no style) and `styles.use_custom_values` (bool). Against the upstream inventory this is worth stating exactly: `default_id` ships defaulting to `"explainer"`, not empty — Output Styles are on out of the box, styled with the explainer genre — and `use_custom_values` ships `False`, so values injection is off by default. The settings docstring also states the register is determined by the pipeline path, not a stored toggle; that intent is consistent with the unwired-register open problem above, and the runtime hardcode is why the pipeline-path derivation does not yet happen.

**Per-turn overrides.** `/style <id>` and `/style off` are parsed in `parse_user_command` (`:14509`). `_is_known_style_id` (`:14485`) validates the id against the registry and custom store, and — matching the degradation philosophy — *accepts* (returns `True`) when the registry can't be read, so a one-off isn't silently eaten in a PyYAML-less environment. An unknown id leaves the input intact, so a `/style` typo passes through as ordinary text rather than being consumed.

**Honne/tatemae audience toggle.** `server/app.py::_apply_style_audience` (`:5970`) implements a separate axis: an input-toolbar toggle (`server/static/js/style-audience-toggle.js`) that marks a turn `internal`, folding the active project's `interaction_style` onto `style_id` for that turn (how Ora talks *to* you, versus the default output style). An explicit `/style` one-off already on the context wins over it. This swaps the *whole* style_id; it is **not** the written-vs-conversational register axis — two different knobs a reader could easily conflate.

**Personal values injection.** `load_boot_md` (`orchestrator/boot.py:1316-1324`) appends `mind.md` as an authoritative block only when `styles.use_custom_values` is on and the file exists. Before appending, `_filter_private_values` (`:95`) strips any `## Private Context` section unless the conversation is tagged `private` or `stealth` — so private life-context directives never reach an ordinary conversation. The heading constant `PRIVATE_VALUES_HEADING` (`:87`) is pinned to the projection module's whitelist by a unit test (`test_mind_projection.py:164-165`), so the two can't drift apart. One implementation seam worth flagging: `boot.py` derives its `WORKSPACE` from `os.path.expanduser("~/ora/")` (`:110`) and builds `MIND_MD` from it (`:112`), while `style_store.py` and `style_assembly.py` derive their `WORKSPACE` from `__file__` (`style_store.py:24`, `style_assembly.py:25`). The `__file__`-derived paths let a git worktree's tests write the worktree's own `data/`; the `expanduser` path in boot always points at the live install regardless of checkout.

**Three authoring paths for `mind.md`.**

- Guided wizard — `orchestrator/mind_guided.py` (595 lines). A deterministic, no-model question registry: `QUESTIONS` (`:51`) holds 12 questions across 5 sections (Communication Preferences, Intellectual Posture, Ethical Boundaries, Work Patterns, Standing Principles). `compose(answers, free_text)` (`:530`) builds a complete `mind.md`, splicing in fixed non-configurable lines from `_FIXED` (`:411`) — anti-sycophancy pushback calibration and source discipline — that the user cannot toggle off. The design constraint is explicit in the docstring: the wizard covers only the value dimensions that measurably change model output when injected; the MindSpec library's psychometric variables are prompt-inert and deliberately excluded. Backs `GET/POST /api/mind/guided`. 31 tests.
- Raw editor + create-from-template — `POST /api/mind` (`server.py:1237`), 128 KB cap (`_MIND_MAX_BYTES`, `:1200`), atomic tmp+`os.replace` write. Create-from-template refuses to overwrite a customized file (`:1251`).
- Interview projection — `orchestrator/mind_projection.py` (263 lines). A self-spec is a first-person portrait of how the user operates; the assistant needs second-person imperative directives. `project_self_spec` (`:178`) makes one model call (`model_dispatch.invoke_chat`, breadth slot) driven by `PROJECTION_SYSTEM_PROMPT` (`:73`), which transforms the portrait into whitelisted `## `-sections: What to Challenge Me On, How to Deliver Pushback, My Red Lines, Mission Alignment, Off Limits, Private Context (`ALLOWED_SECTIONS`, `:60`). `validate_directives` (`:159`) enforces the whitelist and 200–8000-char bounds (`MIN_CHARS`/`MAX_CHARS`, `:70-71`). `compose_projected_mind_md` (`:229`) assembles the final file as guided-base plus a projected block delimited by the projected marker. Backs `POST /api/mind/project`. 29 tests.

The projection's source resolution (`server.py:1383`) reads `mindspec/self-spec.md` when present, otherwise back-fills it from a raw-interview `mind.md` (one that exists, is customized, and carries neither guided nor projected marker) — archiving that content to `self-spec.md` first. On this checkout `self-spec.md` does **not exist** (status *planned*): the mindspec directory holds only `default-mindspec.md` (50 KB canonical library) and `mind-template.md`. The projection is fully coded and tested, but its live trigger requires first running `/framework mindspec-interview` to write the self-spec. The endpoint docstring claims the interview writes it "since 2026-07-01" (`server.py:1373`); that claim could not be verified from the files alone.

**Where `mind.md` sits relative to its neighbors.** `mind.md` is the user's *values*, injected conditionally and authoritatively over the default Mind Seeds in `boot/boot.md`. It is not the interview library (`mindspec/`) and not the boot system prompt. The guided wizard and the projection both write `mind.md`; the interview library is read-only reference material that feeds the projection.

**Normal operation vs. failure.** Normal path: a turn resolves a style_id, `_compose_output_style` renders the gear-appropriate block, it splices into the step prompt at the correct site; if values are enabled, `mind.md` (privacy-filtered) appends to boot content. Failure paths all degrade to empty: missing PyYAML → styles disabled, pipeline unaffected; corrupt `custom-styles.json` → empty custom store; malformed profile → `compose` raises, `_compose_output_style` returns `""`; unknown `/style` id → passes as text; missing `mind.md` or values-off → no values block. No single failure in this subsystem can break the analytical pipeline. That is the whole reliability posture, and it is qualitative — none of these degradations is instrumented with counters or success-rate metrics; the named failure mode (styles silently going dark) is caught by the guards, not measured.

### Evidence & Verification

The subsystem carries 123 unit tests across 12 files, all present and verified at this checkout:

| File | Tests | Surface |
|---|---|---|
| `test_style_assembly.py` | 9 | registry/axes/schema parse; gear branch; delta math |
| `test_style_store.py` | 8 | CRUD; atomic write; corrupt-file degradation; PATCHABLE whitelist |
| `test_style_injection.py` | 7 | the two boot injection sites + step gating |
| `test_style_resolution.py` | 7 | the effective-style-id precedence chain |
| `test_style_oneoff.py` | 6 | `/style <id>` and `/style off` parsing |
| `test_style_cards.py` | 4 | picker card labels |
| `test_style_trace.py` | 2 | trace capture |
| `test_style_audience.py` | 7 | honne/tatemae folding |
| `test_style_mindmd.py` | 3 | values-block injection + private filter |
| `test_mind_guided.py` | 31 | question registry, composer, marker parse, overwrite guard |
| `test_mind_projection.py` | 29 | prompt, whitelist validation, projected-marker preservation, private-heading pin |
| `test_mind_endpoints.py` | 10 | the six HTTP endpoints, provenance derivation |

To confirm the subsystem works, run the style and mind suites (`python3 -m unittest orchestrator.tests.test_style_assembly …`). To confirm the substance-first exclusion mechanically, read `boot.py:8711` — the step tuple is the proof that evaluator and verifier never receive a style block. To confirm degradation, corrupt `data/custom-styles.json` and confirm the picker still loads (empty store).

**Explicitly untested / unverified surfaces:**

- The situational-delta / register chain is unit-tested in isolation but has no runtime driver (`style_register` hardcoded `"written"`, `style_deltas` `None`). Its end-to-end behavior is dead in production. The 9 assembly tests exercise `resolve_demeanor` and the conversational overlay directly; nothing exercises them through a live turn, because nothing can.
- The `mind_projection` end-to-end path depends on a real `self-spec.md` that is absent on a fresh install. The 29 tests mock the model call; the live trigger (a completed MindSpec interview writing the self-spec) is not verifiable from this checkout, and the "since 2026-07-01" claim in the endpoint docstring is unconfirmed.
- All file I/O and path derivation are exercised only on macOS (M4 Max). See platform compatibility below.

### Expert Reading Notes

Read in this order. First `style_assembly.compose()` (`:201`) for the gear branch — this is the entire style-shape logic in one function. Then the two `boot.py` injection sites, `:8539` (gears 1-2, high) and `:8711` (gears 3-4, after substance, producing steps only) — this is where the substance-first ordering physically lives, and the step tuple at `:8711` is the load-bearing exclusion. Then `_resolve_effective_style_id` (`:7088`) for the precedence chain. For values, read `load_boot_md:1316-1324` and `_filter_private_values:95`.

Where future changes most likely cause drift:

- The register/delta chain. If someone wires `style_register` or `style_deltas` into the context package (`boot.py:7767-7768`), the currently-dead conversational overlay and delta math go live at once, and the demeanor a user sees at gears 1-2 will change. Any change there needs the assembly tests re-run against a live turn, not just in isolation.
- Two `WORKSPACE` derivations. `boot.py` uses `expanduser("~/ora/")` (`:110`) for `mind.md`; `style_store`/`style_assembly` use `__file__` (`:24`, `:25`). A refactor that "unifies" them could break either the worktree-isolation the `__file__` form gives tests, or the live-install targeting the `expanduser` form gives boot. Verify both before consolidating.
- The `PRIVATE_VALUES_HEADING` ↔ `ALLOWED_SECTIONS` pin. `boot.py:87` and `mind_projection.py:66` must agree on `"Private Context"`; the unit test at `test_mind_projection.py:164-165` enforces it. Rename one without the other and private directives could leak into ordinary conversations.

Known documentation drift, so you don't chase ghosts:

- `style_assembly.py`'s module docstring is stale. Lines 8-10 claim "Nothing in the pipeline imports it yet … [the wiring] sit[s] on top of this later." This is false at 7a5e8f40: `boot.py::_compose_output_style` imports and calls `compose()`. Trust the code; the wiring exists.
- `config/interface.json` does not hold the Output Styles tab. It is layout + theme only. The tab is a `settings-panel.js` / `styles-pane.js` construct. Do not repeat the interface.json premise.
- The `styles.default_id` default is `"explainer"`, not empty. Styles ship on.
- The full-word precedence line (`values floor > completeness floor > craft floor > substance/findings > style`) lives only in the module docstring (`:13`). The lines actually injected into prompts are condensed variants (`:164`, `:175`) and differ between the gear-2 and gear-3/4 blocks (`substance` vs `findings`). Quote the injected form, not the docstring form, if the exact string matters.

Treat the vault's [[Reference — Ora Output Styles Pane]] (2026-07-01) as canonical over both code comments and this chapter for the pane's UI; the `Fiction register` section it lists is a *deferred* placeholder, not built.

### Platform compatibility

All verification for this subsystem was performed on macOS (Apple M4 Max, macOS 26.3). Every non-macOS surface below is labeled from code inspection, not from an off-Mac run; where it was not actually executed elsewhere, it is marked untested.

- `mind.md` / `custom-styles.json` / `self-spec.md` path resolution (WORKSPACE derived from `__file__` in `style_assembly`/`style_store`; from `os.path.expanduser("~/ora/")` in `boot.py`; server paths via `os.path.join`). **macOS: works. Windows-native: untested. WSL: untested. Linux: untested.** All paths are built with `os.path.join` and are separator-agnostic, so they are likely portable, but only exercised on macOS. The two distinct WORKSPACE derivations (noted above) are a portability seam to check off-Mac.
- Atomic writes (tmp + `os.replace` in `style_store._save_all`, the mind endpoints, and `mind.md` writes). **macOS: works. Windows-native: untested. WSL: untested. Linux: untested.** `os.replace` is atomic within a filesystem on both POSIX and Windows, so behavior should hold, but it was not run off-Mac. No `fcntl` or symlink use in this subsystem.
- PyYAML dependency (`style_assembly.load_registry`). **macOS: works. Windows-native: untested. WSL: untested. Linux: untested.** Pure-Python, no native deps. The import is guarded (`:57` raises if absent); a missing-PyYAML environment silently *disables* Output Styles via the exception-swallowing in `_compose_output_style` and the accept-on-failure in `_is_known_style_id`, rather than crashing. Confirm PyYAML is present in any non-Mac deploy env, or the whole style layer goes dark without an error.
- `mind_projection` model call (`model_dispatch.invoke_chat`, breadth slot). **macOS: works. Windows-native: untested. WSL: untested. Linux: untested.** The projection transform is provider-agnostic (any breadth-slot model). Only the *local-model* backing inherits the MLX / Apple-Silicon constraint; a cloud breadth slot removes even that dependency. The Mac-specificity here is the local-inference layer, not the projection code.
- Server endpoints (Flask, localhost:5000 — the six `/api/mind*` and `/api/styles*` routes). **macOS: works. Windows-native: untested. WSL: untested. Linux: untested.** Standard Flask with a portable localhost binding; not specifically exercised off macOS for this subsystem.


---

# Part III — Knowledge, Tools & Media

## 8. Vault, RAG, Conversations & Persistent State

- **Current destructive-state preparation.** Protected mutations do not ask for approval until their exact durable target is usable.
- A Stealth Delete Forever request binds the Dialogue's logical identity, deletion state, and authoritative tag, then compares that exact approved state again while holding the lifecycle lock immediately before installing the tombstone.
- **Session-export privacy boundary.** `POST /api/session/export` refuses any request containing a private vault-root, sessions-root, raw-conversation, or Node-program HTTP field before entering Dialogue lifecycle or touching filesystem/export paths. Those dependencies remain substitutable only through the internal Python seam.

### Problem

A stateless model cannot supply continuity by itself. Ora must reconstruct the exact history a Dialogue branch may see, add explicit and discovered sources without violating privacy, fit complete evidence units inside the selected endpoint's real request limit, and deliver the same server-authoritative context to every processing path.

The hard cases are structural. A fork must see the ancestor prefixes that existed when each branch was made, never later ancestor turns. Explicit contributors must stay ordered, cutoff-safe, fairly represented, and visibly accounted for when missing, withheld, or deferred. Global retrieval must not rediscover the current Dialogue, its ancestry, a contributor, or a contributor's ancestry through another row. The stored retrieval document must preserve the full exchange even though the vector is intentionally oriented around context plus the user's prompt.

Lifecycle language must also match consequences. Standard and Private Close is reversible hiding, Exit Stealth is navigation, and Stealth Delete Forever is an Ora-managed purge that leaves surviving descendants detached. Explicit exports and copies already held by providers, Git, backups, or other external systems are outside that purge boundary.

### Design Decision & Tradeoffs

Five decisions carry the subsystem.

**The server owns effective history.** Each envelope stores local messages only. A true fork starts with `messages=[]`, local numbering at 1, the direct parent, and an immutable `fork_point_message_count` in that parent's local coordinates. Read-time recursion applies every ancestry cutoff before appending the child's local turns. Later ancestor turns cannot leak, the parent remains unchanged, and `fork_point_chunk_id` is legacy metadata only. Raw exchanges are authoritative; there is no accepted-decision store or inference pass.

**Context is packed as complete units against the endpoint, not a nominal character cap.** The Dialogue maximum is 200,000 tokens, but the safe request can be smaller after required payload, output allowance, retry headroom, images, provider constraints, and safety reserves. An unknown endpoint fails small. Recent local turns and the fork frontier lead, then promoted contributor units, older history, and global RAG. Initial 50/40/10 planning shares for history/contributors/global are soft; unused space returns to a common pool. No turn or indexed note chunk is truncated mid-unit.

**Explicit and global sources remain different lanes.** Contributors are ordered, identity-deduplicated, and uncapped. Dialogue contributors resolve recursively and cutoff-safely; atomic-note contributors use indexed hierarchical-content-provenance chunks. All references remain inventoried as available, missing, withheld, or deferred, and source-fair scheduling prevents one large contributor from crowding out the rest. Global RAG is lower priority and excludes the current Dialogue, all current ancestors, all contributors, and all contributor ancestors source-wide.

**Storage and semantic orientation are separate.** A conversation row's stored document is Context + User + Assistant. Its separately supplied vector input is bounded Context + User. The assistant is therefore returned with the retrieved evidence but does not steer the embedding. Live writes and historical replay target the same contract. Existing rows still require validated replay before this split is fully materialized across the historical collection; this chapter does not claim that production cutover has occurred.

**Aside help is an isolated public corpus, not another memory lane.** Aside may retrieve only five repository-tracked files from `help/`: `user-guide.md`, `accessible-overview.md`, `install-guide.md`, `install-manual.md`, and `install-recovery.md`. They use the logical owner `help`, and collision checks refuse a collection or alias owned by another corpus. Vault notes, Dialogue rows, and private RAG never enter this search. Retrieved excerpts exist only in the current Aside model call; they are not appended to conversation history or written back to any index. The tradeoff is intentionally limited coverage in exchange for a boundary a user can understand and audit.

Privacy belongs to each complete Inquiry/Findings pair, not the Dialogue. Ordinary history needs adjacent halves with the same valid privacy; Dialogue and local turn are reconstructed from the canonical envelope and message order, without requiring matching persisted turn or chunk fields. A Dialogue may mix Standard and Private turns, and its composer tag never fills missing or conflicting older privacy.

Access is cumulative: Standard may use Standard; Private may also use Private; Stealth may use all three. Ordinary history and contributors silently filter missing, conflicting, or too-strict pair privacy. Library/search use surface-specific pre-content predicates. Full owner authentication is reserved for fork creation, current-pair extraction and review derivatives, runtime-indexed derivatives, retagging, and current/full export.

An archived Dialogue contributes only complete, cutoff-safe pairs with matching permitted privacy; archived notes and global rows stay excluded. A note or row claiming to be an Ora-managed derivative needs its complete exact owner tuple. A partial or false claim cannot fall back to Standard or restore missing content.

### Implementation

**Protected retrieval rebuild (current Model boundary).** `retrieval_rebuild_start` prepares an immutable inactive-profile plan before approval: supported provider, non-empty model, exact positive-integer dimension, valid active retrieval configuration, and a readable regular non-symlink `re-embed-local.py`. The approval binds provider/model/dimension/profile and script identity, plus the exact vector-store, Chroma configuration, and script pre-state. `_spawn_reembed` rechecks both job availability and the complete plan under the existing rebuild mutex before claiming worker state. Immediately before subprocess launch, the protected effect reauthenticates those storage identities; the command carries explicit profile/provider/model/dimension arguments and `--activate-on-success`. No filesystem lock is held over the store for the entire rebuild.

A plan mismatch or competing job after authorization closes as failed without overwriting the competing job. Thread-start failure releases the claimed state; worker failure reports its actual outcome and receipt. Process success reports `requires_restart`, not historical-row validation or a completed production migration. These receipt and admission safeguards do not themselves roll back a partially executed rebuild.

**Envelope and effective-history resolution.** `orchestrator/conversation_memory.py` owns the envelope and effective history. Ordinary and visual writes distinguish an absent envelope from one that cannot be decoded, read, or validated as an object with a messages list. Only absence permits a new envelope. An unreadable existing file remains at its original path, unchanged; the writer reports failure instead of moving it aside or replacing it with an empty history. Error marking follows the same preservation rule. Valid writes retain the existing lock and atomic-replacement path. Startup recovery uses those same mechanisms and retains its pending source until the recovered chunk and envelope are both saved.

**Clarification persistence.** The existing envelope stores the pending question, continuation inputs, captured turn privacy, model/profile authority, original file-output request and chunk destination, and a digest of the runtime configuration and routing sources. The initial Inquiry/question pair and pending identity are written together through the envelope's locked atomic writer. Each question is also saved through the existing raw/chunk/index writer and its acknowledged chunk is attached to that same pair before pause success. A failed question save retains the pending exchange; Answer or Skip finishes that exact save before running analysis. Its original submission identity remains on that saved question, so a delayed submission-marker move cannot make startup recovery mislabel the pause as interrupted. Answer and Skip hold the Dialogue lifecycle lock while checking the pending identity and message count, claiming the action, producing any result and saving it; Close/Delete cannot reorder those writes. Resume rereads eligible Dialogue history and contributors, and passes that history plus the original attachment-presence facts into routing and completeness rechecks. The pending envelope retains the start of its question chain; continuation refuses changed privacy anywhere in that chain before using its saved original prompt or earlier answers. Fresh execution refuses a changed runtime snapshot; every resume refuses a stale identity, closed/deleted Dialogue or incompatible claimed answer. Completeness checks use the original supplied text and actual answers, separately from display labels and inserted paragraph spacing. The pending route retains each selected analysis’s completeness result, so a lighter choice replaces the analysis whose question was shown, keeps its own required-input check and preserves the other selected analyses. An answer that still lacks required information atomically appends that answer and its next question with a new pending identity, without reporting completion. Optional trace initialization and finalization failures are logged and do not prevent continuation.

A successful generated response is checkpointed in the pending envelope before file-output routing and chunk saving. Continuation honors the original `/save` or `/saveboth` request and `output_destination`; file-routing failure retains the generated result for retry, and successful routing is checkpointed before chunk saving. The pending identity gives the chunk a deterministic name so a matching saved chunk can be reused. Question and answer chunks have distinct deterministic identities and raw reservations. Artifact turn numbers follow the local canonical Dialogue order, including questions and later ordinary exchanges; inherited fork history does not count as a child’s local turn. Before each raw transcript write, the pending envelope checkpoints the exact session, pair and append position. The manifest also reuses its matching chunk entry on retry. The raw writer atomically appends or reuses that exact exchange, so a failed chunk write and subsequent retry—even after process-cache loss—cannot duplicate exported or imported conversation content. The resumed exchange records the actual answer, or an explicit skipped-clarification marker, and is appended synchronously to the envelope; the server acknowledges its content, chunk and privacy before removing pending state and emitting `response` and `done`. Save failure keeps the item retryable. A durably checkpointed result resumes saving without another model run; if its checkpoint write failed, the same process retains the result for retry. A restart after execution was marked running but before its result became durable is refused as interrupted, not automatically replayed. The GET pending endpoint restores saved questions, the claimed action and answer, and whether retry is available after reopening or restart; unsubmitted browser answer drafts remain in memory only. There is no separate clarification store or background completion writer.

Effective history yields only adjacent complete Inquiry/Findings pairs with matching valid privacy, then derives owner and local turn from the canonical envelope and order for downstream actions; ordinary history does not authenticate persisted owner, turn, or chunk agreement. Orphans, malformed privacy, and disagreements stay stored for recovery but do not enter model history.

New forks store no copied parent messages; recursive reads enforce each parent cutoff. Fork creation separately validates every inherited complete pair's reconstructed Dialogue/turn owner and matching saved chunk/privacy. The child starts at local turn one while parent, project, contributors, canonical content, recovery data, and source identity stay intact. Stealth purge detaches children; it strips a legacy copied prefix only on an exact match, preserving ambiguous local content without authorizing it for a weaker lane.

**Contributor resolution and inventory.** Conversation references resolve by stable identity and recursively rebuild cutoff-safe history. An ordinary contributor pair needs matching permitted privacy; its envelope supplies owner and order, not persisted turn/chunk agreement. A Dialogue tag cannot fill a gap. Atomic notes use indexed HCP chunks; Ora-managed derivatives require their complete source-exchange tuple.

Deduplication precedes fair scheduling. The private bundle inventory records missing, withheld, and deferred status internally. Only selected provenance/source labels face the model, and ordinary trace exposes no candidates, so an unavailable contributor need not produce a visible notice.

**Context assembly.** The server packs complete units against an endpoint-safe budget, prioritizing recent local context/fork frontier, contributors, older history, then global results. Before any consumer sees content, `filter_conversation_history_for_tag` removes a pair with unknown or too-strict privacy.

Dense and lexical conversation retrieval use exact `turn_privacy` predicates plus a second row check before candidacy. Knowledge retrieval groups rows by path and admits a claimed derivative only when every row has one matching eligible owner tuple, before Chroma distance. SQLite lexical joins admitted paths or exact turn privacy before scoring.

Library and search surfaces apply their privacy/ownership rule before title, snippet, body score, or distance, with a stale-backend backstop. Global exclusions remain source-wide and ranking cannot restore an ineligible candidate. This governs calls when made; Phase 4 automatic global semantic retrieval is not active.

The interactive Library browser applies project (including explicit Commons), inclusive activity dates, privacy, lifecycle, relationship, and local-restriction filters on the server before limiting. Rows carry structured privacy, provenance, lifecycle, and relationship state; `closed` is only the authority for active/inactive, indexed archive remains distinct, and no current authority supplies a local-restriction value, so that facet is explicitly unavailable with zero counts. Parent, direct child, and sibling are distinct; contributor, incoming direct-related, and shared exact-project relationships are added only from stored references and project IDs, never from inferred Engram lineage. A process-local stat-keyed parsed-envelope snapshot serves existing Dialogue inventory callers: unchanged inventory reads parse no envelopes, one changed envelope reparses itself, empty Library browse skips body-regex cleaning, and the Library consumes the inventory's already parsed envelope/effective history rather than reopening every file.

The shared Library adapter reuses the privacy-admitted retained Dialogue snapshot, enumerates Engrams nonsemantically in one knowledge-collection read, and takes one complete inventory of each registered project's Files, including folders beyond 500. Engram alias/missing-file/modified-time reconciliation shares one filesystem enumeration per parent, preserving POSIX target collapse and Windows full-leaf canonicalization. Indexed source_file/source_path/source_document, exchange/chunk fields, subtype and processed date establish extraction provenance only through exact admitted Dialogue/File joins. Multiple proven sources remain; conflicting/missing joins stay unresolved. Proven source-project context combines with Engram membership for filtering without changing YAML. Returned ordinary page and admitted Trace rows alone receive exact Engram access hydration; current preview/Edit requests still reauthenticate their source.

Each eligible Markdown File supplies bounded frontmatter once, including read-only files; optional metadata failure warns without removing the row. No body search, fabricated binary metadata, broader symlink access or extra inventory is introduced. Relationship detail reads SQLite without constructing a writer or creating sidecars for a settled database. Freshness compares eligible stat inventory with committed per-file states; it reports last successful completion, current incomplete reason and the unchanged-signature observation limit. A direct mutation, corrupt cache, failed read or racing state cannot be reported as full coverage. Preview descriptors remain routing hints; browsing never returns bodies or changes canonical content.

Show archived defaults false and consistently controls archived Engrams plus indexed archived Dialogues across browse/query, provenance, relationships and preview, not Edit. The archive provider reuses complete _browser_latest_archive_rows(limit=None) and exact/fuzzy/source-wide admission seams in one coherent read-only snapshot, with explicit incomplete source errors and retained/indexed-source deduplication. Specialized callers retain their own policy. Indexed preview requires verified unambiguous complete-pair boundaries and current per-turn privacy, preserving stored text under external speaker labels. It never uses an unchecked chunk-file fallback, raw reconstructed envelope or guessed assistant role; ambiguous/unpaired/unknown-private records stay metadata-only or incomplete, without activation or healing.

**Conversation indexing.** `orchestrator/conversation_chunk.py` defines live/replay exchange records. A live envelope pair receives one Dialogue, local turn, chunk, and privacy owner; raw, chunk, manifest, and Chroma representations bind the corresponding available fields. Chroma stores Context + User + Assistant but embeds bounded Context + User. Its turn-privacy predicate and row check run before document or distance leaves the collection.

Document-processing output promoted into the knowledge corpus uses a stricter derivative owner: `artifact_kind: conversation_runtime_derivative`, `managed_by: ora`, stable source Dialogue, source chunk, positive local turn, and exact privacy, with the controlled Private tag when applicable. If any row for a path claims that ownership, every row for the path must authenticate the same owner and be eligible. A lone source field, conflicting tag, malformed turn, or mixed owner excludes the whole path rather than falling back to ordinary-note privacy.

Metadata remains denormalized and filterable; authoritative content remains in the envelope and raw exchange. Retagging changes only representations that still prove the exact old owner. It never rewrites canonical text, fork lineage, project association, contributor order, raw recovery identity, or a user-authored note merely because that note mentions the same source. `docs/conversation-file-format.py` is the field reference, not a claim that Pydantic validates runtime writes.

**Protected privacy repair preparation.** `python3 -m orchestrator.tools.chroma_source_rebuild protected-inactive-copy` is a separately invoked operator CLI for preparing a metadata-only repaired conversation collection. Its explicit JSON authority plan names distinct existing source and fresh inactive target physical collections and supplies complete Private and Standard row-ID arrays, each bound to its expected count and sorted-ID digest. The authority must cover the source collection exactly. Missing or unknown fields, duplicate or overlapping IDs, count/digest drift, unknown or omitted source IDs, unsafe names, a pre-existing target, conflicting source privacy metadata, invalid payload/profile/vector shape, or source/target identity drift are refused before it can be treated as success.

The current source collection is the strict survival allowlist. The command creates the target in the same Chroma store and copies its exact IDs, stored retrieval documents, stored vectors, collection metadata, and all nonprivacy row metadata; it changes only `turn_privacy`, `tag`, and `tag_private`. Exact audits cover IDs, documents, collection/nonprivacy metadata, and stored vectors, with the existing Chroma-copy tolerance of no more than one float32 ULP per vector component. If any later check fails, cleanup first rechecks the created target's identity and removes only that target. The path does not embed, replay, contact a network, read or change runtime configuration, stop Ora, backfill envelopes, promote the target, or cut over the active mapping.

For the present 40,177-row corpus, the owner has authorized 4,107 Private and 36,070 Standard rows. That decision knowingly classifies 16,534 propagation-only Private rows as Standard, preserves all 698 faithful empty-sided turns without inventing missing text, and excludes 884 recovery-only rows because they are absent from the active source allowlist. This is authority and repository capability, not a completed data operation: no live inactive copy, envelope backfill, cutover, or deployment has occurred.

**Current-turn extraction.** After a response and its envelope pair are durable, the background worker reacquires the process-local Dialogue lifecycle lock and rereads canonical local history. It must match the requested current turn number, chunk id, content, and exact non-Stealth privacy before running. The runtime pipeline receives only that one authenticated pair; prior history cannot be smuggled in as a substitute source. An authentication or extraction failure is logged without invalidating the delivered exchange.

Approved staging notes and pending review records preserve the same exact owner tuple. `server/review.py` acquires the source Dialogue's cross-process lifecycle lock for approve, edit, and reject, rereads the record, and refuses an owner change. Displayed-turn retag uses that lock too, so publication and retag serialize instead of racing a stale privacy label into the vault.

**Aside help retrieval.** `orchestrator/help_retrieval.py` owns the exact five-file whitelist, deterministic heading-aware chunks, indexing, and lexical fallback. Chunks are bounded at 1,600 characters; returned excerpts are bounded at 760 characters each and the assembled help context at 3,200. Startup refreshes the help-owned Chroma rows only when the embedding pipeline is ready. It upserts changed deterministic chunks, deletes stale help-owned rows only after a successful pass, and aborts rather than crossing an ownership collision. If Chroma or embeddings are unavailable, lexical ranking reads the same five files directly. `server.app` adds the selected excerpts to the `/api/scratchpad` system context for that request only. Help failure is logged and the ordinary Aside answer continues without excerpts.

Aside requires the selected Dialogue's `conversation_id` and keeps at most five complete query/answer pairs in that Dialogue's existing `SidebarWindow`. The per-window transaction covers help lookup, history read, model call, and append so overlapping requests preserve causal order. The existing Dialogue lifecycle lock spans the transaction; a deleted Dialogue returns `410`, and Delete Forever cannot clear its window and then receive a late append. Status and clear address the same window through `panel_id` equal to that Dialogue ID and use the same lifecycle coordination. Purge clears that exact in-memory window; shutdown clears all windows. The separate legacy pipeline sidebar continues to use its existing panel keys. Aside accepts only prompt and Dialogue identity and carries no Run or transfer fields. Successful responses echo the Dialogue ID and report `persisted: false` and `authoritative: false` inside the surface contract. This model context is separate from the browser's ten-pair display described in Chapter 2; neither is durable Dialogue history.

**Supplemental handling.** A supplemental request never runs a fresh query or appends a result beyond the budget. It promotes unseen, already-validated deferred contributor/global whole units and repacks the same call boundary. It stops on a repeated gap, no new eligible unit, no whole-unit fit, or resolution; there is no numeric request cap. The first three conditions yield a local `COVERAGE GAP`.

**Lifecycle and navigation.** Standard/Private Close sets `closed: true`; retained Dialogues disappear from the ordinary sidebar and remain restorable in Manage. The sidebar shows the current project's non-Stealth Dialogues plus only the active Stealth Dialogue. Exit Stealth selects the latest readable direct parent, even if that parent is Stealth, or creates a fresh Standard Dialogue; it does not close or delete. Delete Forever invokes protected Stealth purge even with descendants, which detach and retain only local turns. Ora-managed state is the purge boundary; deliberate exports and external/provider/Git/backup copies remain.

Paid Replicate video and style work belongs to this same live Dialogue lifecycle. Delete Forever treats the shared queue as a synchronous pre-purge barrier.
Every bound prediction must already be terminal or receive provider-cancelled or terminal confirmation before any purge or success response.
On failure Ora retains `jobs.json` and the exact provider/Dialogue/job binding, unwinds the temporary server tombstone for retry, and emits no Delete Forever success receipt.

Composer privacy and displayed-turn privacy are separate. Changing the composer affects only the next exchange. Retagging a displayed non-Stealth turn first authenticates its current complete owner. Tightening Standard to Private preflights and updates only exact-owned conversation rows, manifest and failure records, processed and raw representations, runtime derivatives and review records, knowledge rows, and its managed Daily Note projection; missing, edited, conflicting, or ambiguous state is untouched and returned as incomplete, and the canonical envelope changes only after that propagation succeeds. Relaxing Private to Standard changes the envelope first so any stale copy remains over-protective, then reports `reconciliation_required` if exact owned copies cannot all follow. Stealth cannot be retagged.

**Current-output export.** The browser supplies no title or content, only the exact owner tuple attached to the displayed complete exchange. The server resolves effective membership, rereads the canonical local owner while its process-local lifecycle lock is held, and matches owner, turn, chunk, privacy, and content before deriving a title from the canonical Findings text. Private Markdown remains Private-tagged and visibly labelled; Private Word/PDF content is visibly labelled. Explicit Stealth output contains neither a privacy marker nor a source identity.

The Markdown writer safely encodes its owned YAML scalars, so punctuation such as a colon or quotation mark in a source-derived title remains a string. It resolves the persisted project folder or explicit Commons destination and validates the proposed frontmatter in memory before creating a destination directory, collision reservation or temporary file. Missing or invalid project authority never falls back to a guessed folder or Commons. Dated slug filenames, content/privacy presentation, collision limits and atomic replacement remain owned by the existing exporter.

**Full-Dialogue export.** The exporter discovers every canonical owner in effective history, acquires both lifecycle-lock layers for all owners in deterministic identity order, discovers again, and refuses a changed owner set or deleted owner. Every displayed Inquiry/Findings pair must match its canonical owner, local turn, chunk, privacy, and content. A Standard or Private target rejects any Stealth-owned exchange. A Stealth target may include canonically eligible inherited Standard or Private ancestry along with its own Stealth turns; the published artifact omits source and per-turn privacy markers and falls back to a neutral source-free name rather than leaking an unsafe title.

Both explicit HTTP entrances use that same exporter. It derives title/stem, Master Matrix-backed nexus, tags and source dates, then validates before creating the Sessions directory or ignore file, invoking a renderer or writing SVG sidecars. Empty/multiple nexus, specialized filename/H1, provenance and exact conversation prose remain valid under the Dialogue owner's contract. The generic frontmatter builder's automatic capture/chunking callers are unchanged. This preflight prevents invalid metadata from starting supporting effects; it is not a new whole-export transaction or a guarantee that every later renderer/storage failure has no effect.

**Failure behavior.** An unresolved endpoint budget fails small. Ordinary history/contributors silently omit missing, conflicting, or ineligible pair privacy; Library/search rows may likewise be withheld without a visible notice. Ora-managed runtime derivatives with partial, malformed, conflicting, or ineligible owner claims fail before content or distance crosses lanes. OpenRouter embedding responses fail before any vector reaches a caller or Chroma unless the response is a complete one-to-one indexed set for the submitted batch: every row must be an object with a unique integer index in range, and shuffled rows are restored to input order. Every vector must be a list of finite numeric, non-Boolean components at the configured dimension; missing, duplicate, malformed, out-of-range, non-finite, or dimension-mismatched data aborts the batch.

Extraction logs authentication failure. Retag returns incomplete propagation/reconciliation; current/full export and runtime-owned review refuse invalid owners; Stealth purge reports layer failures. Normal turn/save, current-output export, and extraction coordinate through process-local Dialogue locks; envelope writes additionally take the per-conversation write lock and shared cross-process file lock. Retag, runtime-owned review, full export, and purge also take the shared `runtime_paths` cross-process lock. Optional retrieval may degrade without inventing availability. Phase 3 still neither activates Phase 4 global semantic recall nor repairs protected rows during reads. The protected-copy CLI is a separately invoked preparation path; until a separately authorized live operation builds and validates the target and changes the mapping through an appropriate cutover, the live index remains unchanged.

### Evidence & Verification

Verify this subsystem through behavior, not construction totals:

- A new fork has an empty local message list and local turn 1; effective history ends at every recorded ancestry cutoff even after ancestors receive later turns.
- Every consumer class receives the same server-authoritative history contract.
- Mixed Standard/Private turns remain distinct, and missing or conflicting pair privacy is never filled from the Dialogue tag.
- Contributor order/deduplication, fair scheduling, full-ancestry privacy, archive exclusion, missing/withheld/deferred inventory, and source-wide global exclusions hold together.
- Library, lexical, and vector candidates are authority-scoped before content or distance is exposed, including exact-owner knowledge derivatives.
- Packing respects the endpoint-safe budget, retains complete units, reuses unused 50/40/10 planning space, and fails small for an unknown endpoint.
- A retrieved conversation row returns Context + User + Assistant while its supplied vector is derived from bounded Context + User.
- Supplemental promotion changes selection only among already-validated deferred units and terminates without a numeric retry cap.
- Current-turn extraction/review derivatives preserve one owner tuple; retag reports incomplete propagation/reconciliation and exports refuse mismatch.
- Close/restore, Exit Stealth, protected purge, descendant detachment, and exact-only legacy prefix stripping produce their stated durable state.
- A plugin or video operating question retrieves the current user guide from the five-file help corpus; an unrelated query returns no help excerpt. The same source allowlist applies to indexed and lexical retrieval, and an Aside request persists neither prompt nor answer.

Ordinary `context_coverage` output is deliberately numeric: `budget`, `lanes`, `source_counts`, and optional `physical_calls`, `deferred_unit_count`, and `deduplicated_unit_count`. It has no promotion or terminal-status field and reveals no unit/source identities. The sensitive supplemental forensic trace separately retains bounded gap, query-term, and why-it-matters text plus outcome fields; private mechanics retain candidate identities for resolution and diagnosis.

The historical collection and the presence of the protected-copy CLI are not evidence of migration completion. Before declaring the stored-document/vector split operationally materialized, replay existing rows through the live contract and validate identity, counts, content shape, privacy, exclusions, and query behavior against an isolated target. Before declaring the privacy repair live, execute the authority-bound inactive copy under separate mutation authority, independently verify it, and perform a separately designed and authorized cutover.

### Expert Reading Notes

Read the boundaries in this order:

1. **Complete pair before Dialogue defaults.** The envelope owns history and pair privacy; composer/Dialogue tags cannot fill gaps. Full owner authentication belongs to fork creation, current-pair/review derivatives, runtime indexes, retag, and current/full export. Chunks/index rows are rebuildable.
2. **Eligibility before content or relevance.** Exact privacy, complete ancestry, archive state, and source-wide exclusions determine whether a unit may be read or compete. Similarity, lexical match, provenance, false metadata, and reranking cannot restore an ineligible unit.
3. **Endpoint budget before allocation.** The 200,000-token maximum is narrowed by the actual endpoint contract. Planning shares are soft and units stay whole.
4. **Stored document before vector shorthand.** Assistant content is present in the stored document even though the vector orientation excludes it.
5. **Raw commitment before interpretation.** Never manufacture an accepted-decision memory. Attribute a decision only when the user actually committed in the authoritative exchange.
6. **Help corpus before general RAG.** Aside help is a separate five-file public shelf. Do not broaden its allowlist to the vault, Dialogue collection, or private documentation merely to improve recall.

The highest drift risks are copied-parent forks, Dialogue-wide privacy fallback, a nominal cap that ignores provider reserves, contributor-count limits, row-only instead of source-wide exclusion, privacy checks after content or distance retrieval, partial derivative ownership, stale review/retag races, browser-authored export content, assistant-omitted stored documents, supplemental fresh queries, trace identity disclosure, and language that confuses Close or Exit Stealth with Delete Forever.

### Platform compatibility

The installed path is exercised on macOS/Apple Silicon. The context contract itself is provider-neutral, but these supporting surfaces retain platform dependencies:

- Local Ollama embeddings and a local reranker require their daemons and models; cloud endpoints require network access and provider credentials.
- `config/chromadb.json` binds embedder, vector dimension, and physical collection names. Every machine must keep those three consistent; dimension mismatch must fail visibly.
- ChromaDB state and the default session/conversation/vault paths are relocatable through runtime paths but remain unverified as a complete Windows-native, WSL, or Linux deployment.
- Atomic replacement is portable on one filesystem. Normal turn, current-output export, and extraction locks are process-local. Retag, runtime-owned review, full export, and Stealth purge add `runtime_paths` cross-process advisory locks; POSIX is exercised on macOS, Windows untested.
- Ora-managed Stealth purge cannot erase explicit exports or copies held by a model provider, Git remote, backup system, or other external service.
- Help indexing uses the configured embedding and Chroma services when available, but deterministic lexical fallback is local and platform-neutral. The files themselves are the canonical corpus; an unavailable index must not disable Aside.

## 9. Tool Dispatch, the Capability Gate & Execution Telemetry

- **Current gate-order and file-integrity boundary.** The dispatcher completes deterministic handler-contract and target preparation before the generic gate can consume one-shot authority.
- File read/write/edit calls require typed fields and one canonical allowed regular-or-absent target as appropriate. A write's existing ancestors must be directories; missing parent directories may still be created. That state is checked before the gate and immediately before the handler. Edits and overwrites are irreversible; only new-file creation may remain reversible.
- File write/edit use `runtime_paths.atomic_write_text` and retain an existing file's mode. A failed atomic swap leaves the old bytes intact. Overwrites advertise no rollback, and blocked/error write/edit results produce failed receipts and telemetry with `mutated=false`. Successful read text with an error-like prefix remains eligible source evidence.
- Credential calls require a declared `ora` account and an action of `status`, `store`, or `delete`; store additionally requires a non-empty string value. Invalid calls fail before gate entry.
- A blocked call reserves its dedup key only while the existing Paused card and pending authority are being written and bound. A concurrent identical call reports publication in progress, not a delivered card. Publication promotes the key to the published process-local dedup set; every failure releases the reservation, allowing an exact retry.

> **2026-08-20 G1.22 currency note.** The chapter's older composite pin remains useful for the surrounding Execution Review design, but this note supersedes its descriptions of command execution, native network reach, and MCP startup. `bash_execute` now accepts one command only, prepares one immutable argument vector, rejects shell grammar, resolves the executable against the same reduced environment used at execution, classifies Git/download/path effects semantically, and carries that prepared object through approval to `shell=False` execution. Public web and provider calls validate public destinations and redirect hops; credentialed provider requests are exact-origin and no-redirect, Jina refuses credential-bearing, signed, or sensitive-query URLs and records whether forwarding occurred, and Replicate output URLs are materialized instead of being retained as durable signed references. The vault-filesystem, Playwright, and GitHub MCP capabilities are all preserved: exact repository-local packages and Chromium are pinned, each child receives a fresh minimal environment, discovery exposes only the intersection of the child's catalog and the exact per-tool policy map, JSON-RPC requests are size-bounded and response-ID-correlated, and shutdown cleans up the children. Arbitrary Playwright code/evaluation tools are denied. The Playwright route hook is defense in depth rather than complete egress confinement: redirect hops are not delivered to the hook by the pinned browser, Chrome performs DNS independently after validation, and WebRTC and WebTransport are outside the hook.

### Problem

An orchestrator that lets a model choose what to do is only as safe as the boundary between its decisions and the world. Ora's pipeline reaches reality through many seams: it calls external models, reads RAG corpora, writes files, launches MCP subprocesses, invokes paid image and video APIs, runs user-installed shell hooks, and fetches the open web. Without a common enforcement-and-audit layer, six failure modes are unguarded, and each is independently sufficient to cause harm.

First, **auto-approve carrying a dangerous action.** The dispatcher has a permission mode (approve-each, auto-approve). If enforcement lives inside the permission mode, then flipping to auto-approve — a convenience toggle — silently authorizes irreversible deletes, secret reads, and unknown actions. A permission mode is a UX affordance; it must not be the security boundary.

Second, **unaudited execution.** A tool that runs and leaves no record cannot be reviewed after the fact. When a session does something surprising, "what did it actually touch, in what order, with what result" must be reconstructable from a durable log, not from the model's own narration of what it believes it did. Model narration is not evidence; it is a claim by the same process under review.

Third, **secret leakage into logs.** The instrument built to prevent the first two problems becomes the third if it records a retrieved API key, a password in a command argument, or a token embedded in a git URL. An audit log that captures secrets is a secret store with worse access control.

Fourth, **silent failures dissolving into empty strings.** A RAG read that returns nothing, a Step-1 parse that fails, a capability slot every provider refuses — each can degrade to `""` and flow downstream as if the stage simply had nothing to say. The user sees a thinner answer and never learns a subsystem broke.

Fifth, **non-reproducible tool selection during evaluation.** Comparative evaluation runs the same prompt through different model configurations. If tool selection is itself a model decision, the tools fired vary per run and per model, and the comparison is contaminated — you are no longer measuring the model, you are measuring the model plus its improvised tool use.

Sixth, **ungoverned web reach.** Reaching the open web from inside an analysis raises three distinct risks: leaking the prompt to a search provider, double-charging paid fetch/image APIs on retries, and injecting unattributed web text into a grounded answer with no provenance.

This chapter's subsystem — the dispatcher seam, the capability gate, the tool-event recorder, the capability-slot registry, the MCP client, the three web-reach modules, and the per-turn trace/health surfaces — exists to close all six. Its core (`orchestrator/tool_events.py`) is Phase 1 of the Execution Review architecture: the always-on instrumentation-and-enforcement substrate.

### Design Decision & Tradeoffs

**One always-on module for manifest, recorder, and gate.** The capability manifest (the risk vocabulary), the tool-event recorder (the audit write), and the pre-execution gate (the enforcement decision) all live in `tool_events.py`, deliberately in one module because they share one lifecycle (`tool_events.py:1-34`). The alternative — three modules, wired independently — was rejected because the three must never drift apart: the gate decides using the same axes the recorder stamps, and a recorder that logged a different classification than the gate enforced would make the audit trail lie about what was enforced. Colocation is the mechanism that keeps the enforced classification and the recorded classification identical by construction.

**Three risk axes layered on the dispatcher's function axis, not replacing it.** The dispatcher already classifies every tool as read/write/execute — the *function* axis. This subsystem adds three orthogonal axes on top and leaves the function axis untouched (`tool_events.py:8-15`): *mutability* (`read` → `reversible_write` → `external_write` → `irreversible`), *sensitivity* (`public` → `private` → `sensitive` → `secret`), and *egress* (`none` → `local` → `external`). The reason for orthogonality: "can I undo it," "how secret is what it touches," and "does data leave the machine" are independent questions, and collapsing them into one severity scalar loses the distinction that lets the gate block an irreversible-but-public action and an innocuous-but-secret action for different, legible reasons. The rejected alternative — a single risk score — cannot express "reversible write to a secret file," which must be gated on sensitivity while a reversible write to a public file passes.

**The gate runs before execution and independently of the permission mode.** This is the release-blocking decision. `gate()` is called at the dispatcher seam *before* the tool runs, and it does not consult the dispatcher's approve-each/auto-approve setting at all (`tool_events.py:836-850`). Auto-approve can never carry an irreversible, unknown, or secret action, because the gate that blocks those never sees the permission mode. The permission mode governs the *routine* read/reversible tools; the gate governs the *dangerous* ones, and the two are separate code paths on purpose.

**Fail closed on the unknown.** An action absent from the manifest is classified `irreversible` + `secret` + `external` + `unknown: True` and gated (`tool_events.py:99-106`, `manifest_axes` at 194-199). The tradeoff is explicit and accepted: a newly added tool whose axes nobody declared will be blocked until someone classifies it. That is the safe failure — a false block costs a human approval; a false allow costs whatever the unclassified action did. This is the Unix `umask`-style default: deny first, grant deliberately.

**Deterministic tool selection, with a default-off model-driven escape hatch.** Tool selection splits into two lanes. The deterministic lane (`tool_selector.py`) derives parameters mechanically from the prompt — a mode declares tool needs in a `## TOOLS` section, and `web_fetch` fires on exactly the URLs present in the prompt (`tool_selector.py:70-71`, docstring at 26). This is reproducible: the same prompt fires the same tools regardless of which model runs, which is what makes it safe during comparative evaluation. The model-driven escape hatch (a model requesting read-category tools mid-analysis) exists but is gated behind `ORA_MODEL_TOOL_SELECTION`, which defaults to `"0"` (`boot.py:8376`). It is built and wired, not active. That makes it **partial**, not implemented — a flag that defaults off is a capability the installed system does not exercise.

**Pattern B web evidence over Pattern A tool-use round-trips.** All three web-reach modules inject retrieved evidence into the system prompt as a context block rather than exposing web search as a tool the model calls in a round-trip (`claim_verification.py:14-22`). Pattern B was chosen over Pattern A for provider-agnosticism (a local model with no tool-use protocol still gets the evidence), traceability (every injected chunk carries a provenance tier), and cost control (the fetch happens once, deterministically, not once per model retry). The tradeoff: the model cannot adaptively decide it needs one more search mid-generation. For Ora's grounded-answer goal, deterministic-evidence-then-generate beats adaptive-search-during-generate, because the former is auditable and the latter is not.

**Capability slots generalize the hard-coded vision-extraction routing.** Rather than hard-code each image/video provider into the dispatcher, `capability_registry.py` lifts the existing `vision_extraction` routing pattern into a named-slot registry: `capabilities.json` declares N slots with input/output contracts, and `routing-config.json` declares a preferred→fallback provider chain per slot (`capability_registry.py:13-21`). `invoke()` contract-validates inputs before any provider runs and walks the fallback chain on fallback-triggering error codes. The alternative — provider conditionals scattered through the dispatcher — was rejected because it made adding a provider a dispatcher edit and gave no uniform place to record the invocation or express a fallback chain.

**The tradeoff of the whole design:** fail-closed enforcement plus a single instrumentation seam adds friction and concentrates correctness risk. Every blocked action needs a human approval. Every action pays a record cost. And the entire enforcement boundary lives in one module — a bug in `gate()` is a bug in the security boundary. I accept this concentration deliberately: one auditable seam I can test exhaustively beats enforcement scattered across every call site, where coverage is impossible to reason about.

**Open problems.** Four are real and unresolved. First, the action manifest, the exact MCP per-tool policy maps, and the protected-path lists must still be kept in sync with new actions and protection-worthy files; an unrecognized MCP tool is not exposed and an unclassified action fails closed, but a newly protection-worthy file omitted from `_PROTECTED_FILES` remains ungated. Second, the secret-path patterns are POSIX-dotfile-shaped and will under-classify Windows credential conventions (`%APPDATA%` stores) at the gate — a correctness gap the moment anyone ports off Mac. Third, the evidence-runner that would let the gate verify a check declaration before allowing it is declared vocabulary only — the runner itself is Phase 5 and not built. Fourth, the Playwright route hook cannot see redirect hops, cannot bind Chrome's subsequent DNS resolution to Python's validation result, and does not cover WebRTC or WebTransport; complete browser egress confinement would require a separately approved proxy, firewall/network sandbox, or maintained browser interception fork.

### Implementation

**The dispatcher seam.** `dispatcher.py` is the single wiring point where axes are derived and the gate and recorder fire. For an MCP call, `tool_events.mcp_policy` resolves the exact namespaced tool plus its actual parameters into call-specific axes and authority scopes; an absent or malformed policy fails closed rather than borrowing a server-wide default. File targets still escalate through `resolve_path_sensitivity`, and protected configuration still escalates mutability. For `bash_execute`, the seam prepares the command once and binds that exact prepared object — argument vector, working directory, reduced-environment digest, executable identity, semantic selectors, and read/write scopes — to both the gate and the later execution. It calls `tool_events.gate(...)` before any effect and records the real result after execution. The credential-store channel remains the one sanctioned secret path: a matching standing allow can bypass the per-call prompt while still being logged existence-only. The gate runs *here*, before the tool, so no downstream permission mode can reach around it.

**The gate decision.** `gate()` (`tool_events.py:836-933`) computes a `block_why`: unknown → blocked; `irreversible` → blocked; `secret` → blocked; and, for model-facing surfaces only, `sensitive` → blocked (851-863). If nothing blocks, it returns `allowed` immediately. If something blocks, it tries three escalations in order: consume a matching one-shot approval token if one was previously granted (884-889); ask a live human via an `interactive_approver` callback when one is wired — the terminal approve-each prompt or a browser callback (893-912); otherwise deny immediately and write a Paused queue entry (916-933). The third path is a *record, never a wait* — the gate does not block the thread waiting for approval; it denies now and leaves an entry the user can approve later, at which point a re-issued matching call passes once. A per-process dedup set (`_queued_hashes`, 788-803) ensures an agentic retry loop enqueues one Paused card, not one per retry.

**Approval tokens.** A grant mints a one-shot token keyed by `(action, args_hash)` with a default 3600-second TTL (`tool_events.py:574`, `grant_approval` 610-634). Consumption marks it used and stamps `used_at` (`check_and_consume_approval` 738-771). Conversation-scoped tokens require an exact conversation match — a context-less call cannot consume a token minted for a specific conversation (751-757). The read-modify-write is wrapped in a cross-platform exclusive lock (`_with_approvals_lock` → `runtime_paths.locked_file`) plus atomic `os.replace`; the lock is retained rather than replaced by atomic-write alone because grant and consume both mutate the same file and atomic replace does not prevent a lost update between concurrent grant/consume (601-607, `_save_approvals` 593-598). The standing-allow variant (637-657) is the credential-store exception: revocable, scoped per service, logged existence-only.

**Risk tier (Execution Review Phase 2).** The per-turn risk tier rides in the turn context (`set_turn_context(..., risk_tier=...)`, `tool_events.py:375-390`) so the gate and every recorded event see it without a dispatcher signature change. Because the tier is known only after mode dispatch — which runs after the context is seeded at step 2 — `update_turn_risk_tier` (393-405) attaches it in place. `record()` stamps it onto every event (528-532), and `consume_token_by_fingerprint` (681-712) implements the Phase-2 task gate, consuming a token by `(action, args_hash)` alone (the fingerprint itself binds the conversation, so the mint and the resume may legitimately differ). This is verified by `test_risk_gate.py` (93 tests) and `test_risk_gate_pipeline.py` (6).

**Turn context propagation.** The context (`trace_dir`, `conversation_id`, `stealth`, `surface`, `risk_tier`) is stored in a `ContextVar` (`tool_events.py:371`). The pipeline seeds it at step 2 and the server at the `_direct_stream` head. It propagates to Gear-4 worker threads via `contextvars.copy_context` (through `boot._submit_with_context`, `boot.py:44`) and to child project-tool processes via `ORA_*` environment variables read back by `get_turn_context` when no context var is set (408-416). This is why a tool-event fired deep inside a parallel Gear-4 stream still knows which conversation it belongs to.

**Two redaction layers.** Sensitivity-based redaction (`_redact_for_record`, `tool_events.py:463-490`): a `secret` event is reduced to existence-only (`content_redacted: "secret-existence-only"`, args and reads dropped); a `sensitive` event keeps only length descriptors. Content-based scrub (`scrub_content`, 359-366) runs regardless of sensitivity, matching OpenAI/Anthropic-style keys, GitHub PATs, AWS access-key ids, Slack tokens, bearer headers, userinfo credentials in URLs, and `password=`/`token=` assignments (`_SCRUB_PATTERNS` 348-356). The path classifier (`resolve_path_sensitivity`, 324-340) matches boundary-anchored patterns — `\.env($|\.)` catches `.env` and `prod.env` but not `.venv`; patterns are anchored so `tokenizer.json` does not match "token" (289-305). Paths under known private roots (workspace, vault, conversations, `~/Documents`, `~/sites`, scratch) classify `private`; unrecognized territory classifies `sensitive` — conservative by default (317-340).

**Telemetry health.** `record()` never raises (`tool_events.py:508-569`): on failure it calls `_note_failure`, which increments a counter, records the last error, prints to stderr, and best-effort emits a `ToolEventRecordingFailure` on the oversight bus (426-439). `get_telemetry_health()` (442-446) reports `incomplete: True` once any failure has occurred, and every subsequent event is stamped `telemetry_incomplete: True` (544-546). The failure mode this prevents: a silently-dropped audit line that makes the log look complete when it is not. When telemetry breaks, the log says so, on every line thereafter. `ORA_TOOL_EVENTS=off` disables the *write* only (516) — the gate still runs and still fails closed. The dual sink routes to the active turn trace dir when a turn is live, else `data/tool-events.jsonl` (`_sink_path` 502-505). Writes use `os.open(..., O_WRONLY|O_APPEND|O_CREAT, 0o644)` with UTF-8-encoded lines capped at `MAX_LINE_BYTES` (8000), truncating to a keep-set of structural fields when over (549-567).

**Capability-slot registry.** `load_registry` (`capability_registry.py:744`) reads `capabilities.json` and `routing-config.json`'s `slots` block (runtime-overlay-aware), merges project-declared slots (`merge_project_capability_slots` 674), resolves slot inheritance (`resolve_slot_inheritance` 565), and auto-registers `local_diffusers` best-effort (879-880). Six integration modules bind real handlers via `register()` — `gemini_images`, `openai_images`, `openrouter_images`, `stability`, `replicate`, and `local_diffusers`. `invoke()` (361-524) validates required inputs before any handler runs, fills optional defaults, builds the provider chain (preferred→fallback, or a length-1 chain when an explicit `provider_id` disables fallback), and walks it: on a `CapabilityError` whose code is in the slot's fallback-triggering set and with providers remaining, it advances; otherwise it breaks and re-raises the last error with the full `attempts` chain attached, so the caller can render "Slot 1 refused for content_policy → Slot 2 unavailable → no providers left." Every invocation records one `capability_slot` tool-event with the provider tried, the attempts list, and the exit reason (`_record_tool_event` 527-558). `config/capabilities.json` defines 14 slots; the image and video families are wired via integrations, while project-specific slots (e.g. `column_authoring_paid`, `image_generates_barb_cartoon`) have their provider wiring outside `~/ora` in the project's `ora-project/`, so their runtime provider status is per-install.

**MCP client.** `mcp_client.py` accepts only the three pinned stdio declarations in `config/mcp-servers.json`: vault filesystem (`@modelcontextprotocol/server-filesystem` 2026.7.10), Playwright (`@playwright/mcp` 0.0.79), and GitHub (`@modelcontextprotocol/server-github` 2025.4.8). Startup resolves their exact Node entrypoints under `mcp-runtime/node_modules`; it never invokes `npm`, `npx`, or a runtime package download. The desktop and server installers apply `mcp-runtime/patch-playwright.cjs` after installing the pinned packages, then provision the locked Playwright Chromium through the repository-local Playwright CLI. Startup refuses an unpatched connector. Each child starts in its designated working directory with a fresh allowlisted environment; the GitHub token is inherited only by the GitHub child, and a missing token skips that child without removing the other two capabilities. Discovery namespaces tools as `mcp_<server>_<tool>` and exposes only tools present in both the child catalog and the exact policy map. Requests are serialized and bounded, replies must carry the exact outstanding JSON-RPC ID and shape, discovery/schema/catalog and stderr retention have explicit limits, tool errors remain failures, and shutdown closes streams, terminates or kills stragglers within bounded waits, joins reader threads, and clears the catalog. A server that cannot start fails soft and does not block Ora startup. HTTP transport is unsupported; pinned stdio is the only accepted transport.

For GitHub repository creation and forks without an explicit organization, the dispatcher prepares the actual child before review and binds its non-secret launch identity in the existing protected request, without changing the raw argument hash. The approved call uses that exact child and its prepared arguments; replacement makes the approval stale, and execution never silently recovers onto another child after approval. A child lost after the start receipt produces a failed effect. This preserves the implicit account destination across review and execution without recording a credential fingerprint or claiming to verify a GitHub login. Explicit repository/organization arguments do not require remote-content snapshots. Browser target preflight rejects missing, non-string, or empty required element targets, incomplete drag targets, and malformed form-field targets before the gate; optional no-target tools, empty form lists, and upload cancellation remain supported. Supported Page-relative typed interactions additionally bind the exact child, Page, document, and native element or intrinsic target before approval. The pinned connector retains those targets across native waits, retries, and dialog handoffs and reauthenticates them through its final native send, including keyboard focus, file chooser, and history targets. At the final native guard, native-fill insertion or deletion requires the complete bound text target to remain selected; supported email and number controls, whose selection Chromium hides, are reselected on that exact bound, focused control there. Slow typing keeps the narrower focus/containment rule. A modified click retains both the original control-rooted focus outcome and a separately bound retargeted button/link receiver for trailing modifier keyup; unrelated focus remains rejected. It preserves native interaction behavior without switching to another Page or replacement document; arbitrary browser code remains denied. Chromium can still reload after the final connector check and before applying an already-sent action, so browser-atomic execution is not claimed.

**Web reach at steps 2/5/8.** `web_consultation.py` (F-Consult) runs at Step 2: parallel intent searches, source tiering (whitelisted/corroborated/single/excluded), per-chunk provenance, optional prompt-sanity and conflict passes; dependency-injected, enabled by default via `routing-config` (`web_consultation.enabled: true`; wired at `boot.py:7459`). `web_extraction.py` escalates when high-trust snippets are too thin: it deep-fetches full pages, chunks, fit-gates, and folds a `### DEEP EXTRACTIONS` block into web context — fail-*closed* when nothing survives the fit gate (`web_extraction.py:382-388`). It is gated on `ORA_WEB_EXTRACTION` — code default off (`boot.py:7042`, `_env_flag`) but force-exported `=1` by the tracked launcher `start.sh:26`, so it is **active on a normally-launched installed system**. `claim_verification.py` runs a pre-flight for Step 5 (Reviser) and Step 8 (Verifier): it parses the evaluator's `## FLAGGED CLAIMS`, runs challenge queries in parallel via `web_search_structured` through the configured provider cascade, and injects `## FLAGGED CLAIM EVIDENCE` into the reviser's and verifier's prompts — Pattern B (`claim_verification.py:1-22`, wired at `boot.py:11278/12008/12012`). The shared `network_policy` now normalizes destinations, refuses credentials in URLs and non-public address resolutions, and requires explicit validation of each allowed redirect hop. Authenticated OpenRouter and Replicate calls are bound to their exact provider origin and do not forward credentials across redirects; public provider-returned assets are fetched without the provider bearer. Jina remains available for useful public pages, but refuses credential-bearing, signed, or sensitive-query source URLs and emits durable forwarding/refusal metadata. Replicate's asynchronous output walker materializes URL values and URL-shaped dictionary keys into local artifacts before job state is retained.

**Search provider cascade and its two environment overrides.** Which provider actually answers a search is decided by `search_cascade_order` in `routing-config.json`, walked in order by `_search_text` (`orchestrator/tools/web_search.py`). A provider whose API key is absent is skipped; a provider that errors *or* returns an empty result falls through to the next tier, so a free primary can sit in front of a paid fallback that only answers what the primary misses. Two environment variables override the configured behaviour per deployment, because `ORA_ROUTING_CONFIG_PATH` replaces the whole routing-config file rather than merging into it, and forking a 500KB config to change one list invites silent drift:

- `ORA_SEARCH_CASCADE_ORDER` — comma-separated provider names (for example `ddg,brave`) replacing `search_cascade_order` for that process. Matching is case-insensitive. Names that match no registered provider are dropped with a stderr warning naming them, and the remaining names are used; only when nothing at all is recognised is the whole override discarded and the routing-config value used instead. Note what that fallback is worth: the shipped `routing-config.json` holds the same list as the built-in default, so falling back does not avoid a paid-first cascade — it restores one. That is why the resolved order is now logged on every path, whichever source supplied it.
- `ORA_SEARCH_SEMANTIC_AUGMENT` — `0`/`1`, overriding the `semantic_augment` block. This exists because semantic augmentation is a **second, independent search per query** — `_gather_raw` calls `_search_text` again pinned to the semantic provider — and `search_cascade_order` does not govern it. Without this override, pinning a free keyword cascade for cost reasons would still bill one semantic call per query, with nothing in the cascade setting to indicate it.
- `ORA_SEARCH_MAX_QUERIES` — the maximum number of search queries a single fan-out may issue. `0`, unset, or malformed means unlimited, so a machine that never sets it keeps its existing behaviour. The ceiling matters because fan-out width is chosen by a model — the intent list at Step 2, the flagged-claim list at Step 4.5 — and neither has a natural bound; production has recorded a single fan-out issuing 213 searches. Enforcement is at the fan-out sites, which know what they are dropping and record it in their trace signals, and it fails **open**: excess queries are dropped and the run continues, since both callers already treat missing web evidence as a coverage gap rather than an error.

- `ORA_SEARCH_MAX_QUERIES_<STAGE>` — a per-stage ceiling that overrides the global one, currently honoured for `UNFLAGGED`. Not every fan-out is worth the same: verifying the claims an evaluator actually flagged is the check the pipeline has always run, while the Step-5.5 unflagged scan is a speculative second pass that hunts for more and fans out twice per gear-4 run on top of it. They cost the same per query and are not worth the same, so `ORA_SEARCH_MAX_QUERIES_UNFLAGGED=3` can bind the speculative pass while flagged claims keep the global 12. Unset or malformed defers to `ORA_SEARCH_MAX_QUERIES`, so a typo cannot silently uncap the more expensive stage; an explicit `0` means that stage is deliberately unlimited.

- `ORA_SEARCH_DDG_CONCURRENCY` — how many DuckDuckGo requests may be in flight at once; default 3, and `0` restores unbounded behaviour. DuckDuckGo is reached by scraping its HTML endpoint and throttles on **burst** concurrency far more than on steady volume, while the pipeline's fan-outs size their thread pool to the number of items — so a single claim-verification pass could open a dozen simultaneous connections. Production recorded 121 `operation timed out` responses plus connection resets in two hours that way, and because a failed tier cascades, each of those became a request to the next, billed provider. Pacing converts those timeouts back into results, which both restores coverage and removes the spend they were causing.

The cascade order is read once per process and cached, so changing it takes effect at restart rather than on edit. The cap is read from the environment on every call and is not cached, but since a process's environment does not change under it, that distinction only matters to tests. Provider selection is logged when a later tier answers and when a configured tier is inactive for want of a key — previously both were silent, which meant a cascade could quietly degrade, or quietly bill, with nothing in the logs to say which provider had served a given query.

**Forensic trace and health banner.** `pipeline_trace.py` writes per-turn step JSON/MD, RAG-failure logs, and a step-health summary under `data/pipeline-traces/<conv>/<ts>/`, gated by `ORA_PIPELINE_TRACE` (default on, `pipeline_trace.py:58-66`). `_resolve_ora_home` derives the root from `__file__` rather than a literal `~`, avoiding the observed bug where an unset `HOME` produced a literal-tilde directory (35-56). Stealth conversations create no trace directory at all (`start_trace`, 75-101). `pipeline_health.py` is the silent-failure surface: any pipeline step calls `record(category, message)` into thread-local storage (`_tls`, 32-47), and `collect_and_clear` drains it at turn end to a chat banner (50-63). This is what turns "empty RAG dissolved into an empty string" into a warning the user actually sees.

**Lifecycle hooks.** `hooks.py` loads definitions from the effective `runtime_paths.CONFIG_DIR/hooks` directory. Its `fire_hooks` function runs their shell scripts for pre_tool/post_tool/session_*/pre_compact via `subprocess.run(command, shell=True, ...)`, using `runtime_paths.WORKSPACE` as the working directory and recording each execution as a tool-event with `enforcement: boundary_only`. The shipped `session-cleanup.json` imports `bash_execute.cleanup_all` from `orchestrator/tools` relative to that working directory, so a relocated installation uses its own hook configuration and cleanup module even when a separate `~/ora` exists. Only `pre_tool`, `post_tool`, and `pre_compact` may inject output. A `pre_tool` injection reaches the next model receiver exactly once while structured tool results and deterministic consumers stay valid; a `pre_compact` injection reaches the first system context exactly once (creating one if absent) and persists through success and every fail-soft return. Because `session_start` and `session_end` have no receiver, definitions requesting `inject_output` for either are refused before execution; ordinary non-injecting session hooks continue. Ora cannot see inside these `shell=True` subprocesses, so their axes are honestly marked boundary-only. User-installed hooks are pre-authorized, while installing hook JSON remains a protected-config write (`config/hooks` is in `_PROTECTED_PREFIXES`, `tool_events.py:256`), closing the "one allowed write buys arbitrary execution" escalation.

**URL import.** `url_import.py` wraps `yt-dlp` to download public media into a per-conversation library. `YTDLP_BINARY = shutil.which("yt-dlp") or "/opt/homebrew/bin/yt-dlp"` (48) — `which` finds a binary on any platform, but the fallback and the not-found guidance (`brew install yt-dlp`, 149) are Homebrew-specific and Mac-shaped.

**Config surfaces.** `config/capabilities.json` (slot contracts; protected), `config/mcp-servers.json` (the three pinned stdio declarations plus exact per-tool policies; protected), `mcp-runtime/package-lock.json` (exact MCP dependency graph), and `config/routing-config.json` (`web_consultation.enabled`, slot chains; protected). Env flags: `ORA_TOOL_EVENTS`, `ORA_PIPELINE_TRACE`, `ORA_MODEL_TOOL_SELECTION`, `ORA_WEB_EXTRACTION*`, `ORA_RISK_TIER`.

### Evidence & Verification

The 2026-08-20 G1.22 pre-channel acceptance used focused behavioral checks rather than repinning the whole chapter. The command/native-network slice passed 126 tests with 3 skips plus 186 parameterized subtests, and its execution-provenance suite passed 39 tests. The final MCP correction passed 91 tests plus 64 parameterized subtests; the wider preceding MCP run passed 251 tests plus 41 parameterized subtests. An independent MCP review returned accepted after checking that all three capabilities remained available, policy was per tool and call, arbitrary Playwright code was denied, errors stayed failures, and no child survived cleanup. These counts establish only the pre-channel boundary. The original Order-16 hardening is merge `88f971ae6067289fbfadd6f681a481481d063919`; the focused ordinary-use follow-up is separate and landed at Ora merge `0c602b439fb62b1892611cd1ec4c41a4b9587ed1` (PR #410). Full G1.22 remains yellow and open for live channel, browser-transport, and supported-platform proof. No reactive memory watchdog, CPU/per-file/scratch quota, or extra HTTP/Jina/global MCP cap is present or being added absent ordinary-use evidence.

The subsystem carries roughly 400 unit tests across the target files, confirmed by `grep -c 'def test'` at 7a5e8f40: `test_tool_events` (52), `test_dispatcher_gate` (44), `test_capability_registry` (34) + `test_capability_registry_fallback` (25), `test_web_consultation` (44), `test_web_extraction` (21), `test_claim_verification` (35), `test_tool_selector` (22), `test_mcp_catalog` (15), `test_url_import` (13), `test_risk_gate` (93), `test_risk_gate_pipeline` (6) — 404 in total. (The inventory listed a `test_capability_registry_routes_rerun` file and a smaller risk-gate count; at this commit that file is absent and the risk-gate suite is larger — trust the tree.)

To confirm the gate fails closed: run `test_dispatcher_gate` and `test_risk_gate`. The gate's block logic (`tool_events.py:851-863`) and its independence from the permission mode are the properties under test; the `FAIL_CLOSED` default (99-106) is exercised by feeding an unclassified action and asserting a block.

To confirm telemetry flags its own incompleteness: force a `record()` failure and read `get_telemetry_health()` — `incomplete` flips to `True` and every subsequent event carries `telemetry_incomplete: True` (544-546). This is the check that distinguishes a complete log from a log that merely looks complete.

To confirm slot fallback records an attempts chain: `test_capability_registry_fallback` drives a Slot-1 refusal into a Slot-2 attempt and asserts the re-raised error's `.attempts` captures the full chain (`capability_registry.py:361-524`).

**Explicitly untested surfaces.** There is no dedicated test file for `pipeline_trace.py`, `pipeline_health.py`, or `hooks.py` at this commit (confirmed by directory listing) — the trace atomic-write, the stealth-skip path, and the hook telemetry-recording path are exercised only indirectly through pipeline tests, not in isolation. `ORA_MODEL_TOOL_SELECTION` is default-off and not exported by any launcher, so the installed system does not exercise it; `ORA_WEB_EXTRACTION` is code-default-off but force-exported `=1` by `start.sh:26`, so a normally-launched installed system does run web extraction. The evidence-runner has a validated check vocabulary (`validate_check_declaration`, `EVIDENCE_RUNNER_DEFAULTS`, `tool_events.py:983-1005`) but no runner — an undeclared check is un-runnable by construction, and the runner is Phase 5 (980-981). No reliability budget is instrumented anywhere in this subsystem: there is no per-step success-rate metric, no gate-block-rate counter beyond the raw event log, and no measured coverage percentage. The failure modes above are prevented qualitatively — the gate blocks the class, the recorder logs the class — and the only quantitative instrument present is the telemetry-failure counter, which counts recorder failures, not action success rates.

**Post-pin trace verification.** The dedicated tests absent at `7a5e8f40` now exist in `orchestrator/tests/test_trace_manifest.py` and `test_trace_walk_ui.py`. The PR #269 acceptance run exercised both import orders and the full suite with exact baseline failure/error signature parity. Its behavioral cases invoke real single-analyst, external-consolidation, fallback/retry, server/CLI direct-entry, lifecycle/disconnect, flagged-claim evidence, supplemental-RAG, web-consultation, terminal-routing, private-projection/export, and writer-failure paths. This statement is limited to the trace-completeness landing; it does not revise the other untested surfaces named in this chapter.

A note on Execution Review status, because it is easy to overstate. Phases 1–4 are landed in `~/ora` main at 7a5e8f40 (the commit is titled "Execution Review Phase 1-4: macOS+Windows portability retrofit"). Phase 5 — the evidence-runner — is not. It sits in an unmerged worktree (`ora-worktrees/phase5-impl` on branch `execution-review-phase5-impl`) at the code-review gate; it is not merged to `~/ora` main, not installed, not landed. The installed system's Execution-Review coverage ends at Phase 4. The evidence-runner vocabulary you see in `tool_events.py` is the Phase-4 declaration of what Phase 5 will validate — declared now precisely so an undeclared check cannot later slip past a runner that does not yet exist.

### Expert Reading Notes

Read these five things first, in this order. First, `tool_events.gate()` and the `FAIL_CLOSED` default — this is the release-blocking security boundary, and everything else is scaffolding around it. Second, `_redact_for_record`, `_SCRUB_PATTERNS`, and `resolve_path_sensitivity` — the leak-prevention layer that keeps the audit log from becoming a secret store. Third, the dispatcher seam — the single place policy is resolved and the gate and recorder fire; its `PreparedCommand` and `mcp_policy` paths are the current command/MCP authority boundary. Fourth, `capability_registry.invoke`, `resolve_provider_chain`, and `load_registry`'s routing-config overlay resolution — the slot dispatch and fallback machinery. Fifth, `mcp_client`'s pinned-registry validation, fresh-environment construction, exact-ID request handling, discovery intersection, and cleanup — together these are the load-bearing MCP boundary; the reader thread is only one part of it.

**Where future changes most likely cause drift.** First, the `capability_registry.py` module docstring (lines 30-33) still declares "Stub-only in WP-7.0.1 ... ships with the registry, the dispatcher, and an empty provider table." That is stale: six integration modules now register real handlers. Trust the code — the provider table is populated at load. Second, `CLAUDE.md` gives this subsystem essentially no coverage: the security/telemetry substrate (the gate, the recorder, the dispatcher wiring, the MCP client, the web modules, the trace/health surfaces, hooks) is absent from the architecture map, which mentions only `capability_slots` and only in the plugin-convention and Visual-tab sections. The doc-code gap here is a whole-subsystem omission, not a line-level lag — do not read `CLAUDE.md`'s silence as absence of the subsystem. Third, the `ACTION_MANIFEST`, the exact MCP tool maps in `mcp-servers.json`, the package lock, and the `_PROTECTED_*` path lists must be kept in sync with new actions, MCP package versions, tools, and protection-worthy files; a discovered MCP tool absent from policy is not exposed and a new unclassified action fails closed, but a newly protection-worthy file left out of `_PROTECTED_FILES` remains ungated. Fourth, the secret-path patterns are POSIX-dotfile-shaped and will under-cover a Windows port. Fifth, the two default-off flags are partial-by-policy, not unbuilt — do not delete them as dead code; they are wired and tested, waiting on a deliberate operator enable.

For the framework contracts these modules implement, see [[Reference — Capability Invocation Contracts]] (slot input/output contracts), [[Reference — AI Agent Security Architecture]] (the gate's threat model), and the F-stage specs [[Specification — F-Consult]], [[Specification — F-Revise]], and [[Specification — F-Verify]] for the web-reach injection points at Step 2 (consultation), Step 5 (claim-verification reviser pre-flight), and Step 8 (claim-verification verifier pre-flight).

### Platform compatibility

The commit at 7a5e8f40 is titled a "macOS+Windows portability retrofit," and the code reflects genuine portability *intent* — but intent is not evidence. No test at this commit simulates Windows or Linux path semantics — no test exercises `sys.platform`, `normcase`, or backslash-separated paths against the classifiers — so every non-macOS surface below is labeled **untested**, never "works." macOS Apple Silicon is the sole verified target.

- **Path handling — `_matchable` / `_cmp_key` / protected-path and secret-path matching** (`tool_events.py:61-77`, 254-282): macOS — works. Windows-native / WSL / Linux — untested. The code forward-slashes and normcase-folds keys explicitly so that `/`-anchored patterns and `startswith` prefix checks hold on backslash-separated, case-insensitive Windows paths; this is written for portability but has no off-Mac test evidence.
- **Secret/sensitive path patterns** (`tool_events.py:289-313`): macOS / WSL / Linux — works (POSIX dotfile conventions: `.ssh`, `.aws`, `.env`, `id_rsa`, `.pem`). Windows-native — untested and known to under-cover: Windows credential conventions (`%APPDATA%` credential stores, DPAPI blobs) are not in the pattern set, so a Windows port would under-classify sensitive paths at the gate. This is a correctness gap, not just an untested one.
- **Tool-event sink write — `os.open(..., O_WRONLY|O_APPEND|O_CREAT, 0o644)`** (`tool_events.py:563`): macOS / WSL / Linux — works. Windows-native — untested; `os.open` ignores most mode bits on Windows but the append write still functions.
- **Approvals file locking — `runtime_paths.locked_file` + atomic `os.replace`** (`tool_events.py:601-607`): macOS — works. Windows-native / WSL / Linux — untested. Portability depends on the `runtime_paths.locked_file` cross-platform lock abstraction, which is out of scope for this chapter and unverified off-Mac.
- **MCP stdio reader threads + binary UTF-8 protocol bounds** (`mcp_client.py`): macOS — works. Windows-native / WSL / Linux — untested. The pump avoids `select.select` on Windows pipes, while byte limits are enforced before decoding; this is design-intent for Windows correctness, not verified behavior.
- **Pinned repository-local MCP packages and Chromium** (`mcp-runtime/package-lock.json`, `config/mcp-servers.json`; vault-fs/playwright/github): macOS — works. Windows-native / WSL / Linux — untested. Startup uses exact local Node entrypoints rather than `npx`; installation requires a compatible Node runtime and provisions the locked Chromium, but neither install nor launch has been exercised off-Mac. Individual servers fail soft when their pinned local runtime or required credential is unavailable.
- **`local_diffusers` capability provider (MLX/Metal)** (auto-registered in `capability_registry.load_registry`): macOS Apple Silicon — works. Windows-native / WSL / Linux — n/a: MLX/Metal is Apple-Silicon-only; on other platforms the import fails and the registry silently falls through to API providers.
- **`web_fetch` browser tier (local Playwright/Chrome) used by web_consultation/web_extraction**: macOS — works. Windows-native / WSL / Linux — untested; depends on a browser install and was not exercised off-Mac.
- **Pipeline-trace / tool-event sink directory under `ORA_HOME`** (`pipeline_trace.py:35-56`): macOS / WSL / Linux — works; `_resolve_ora_home` derives from `__file__` to avoid a literal-`~` directory when `HOME` is unset and refuses `~` paths. Windows-native — untested but filesystem-agnostic in principle.
- **`yt-dlp` URL import binary resolution** (`url_import.py:48`, 149): macOS — works. Windows-native — needs porting: the fallback path `/opt/homebrew/bin/yt-dlp` and the `brew install yt-dlp` guidance are Homebrew-specific. WSL / Linux — untested: `shutil.which` would find a system-installed binary, but the fallback and error message are Mac-shaped.

## 10. Visual Intelligence

### Problem

Ora's substantive analytical output has two coupled forms: prose carries the full reasoning, and a visual companion makes its relationships inspectable. For systems dynamics, decision structure, causal attribution, and argument mapping, prose alone hides structure that a diagram exposes immediately. A causal loop with three reinforcing and two balancing links is a five-line drawing and an unreadable page of English. Three failure modes stand between "the model reasoned well" and "the user saw a correct, readable, accessible diagram," and each one, left unaddressed, silently degrades the product.

First, **producer unreliability and revision drift.** The renderable contract is a strict, typed JSON envelope (`ora-visual`), but a producer can omit it, emit malformed JSON, or draw against prose that a later pipeline stage replaces. Gear 4 therefore contributes prose only; a branch diagram cannot survive consolidation merely because its labels still look plausible. Gear 2 and Gear 3 may contribute a candidate only when it remains paired with the exact prose revision selected for delivery. The terminal authority must recover or construct the visual from that final prose rather than trusting any diagram found earlier in the turn.

Second, **dishonest charts.** An LLM asked to draw a chart defaults to the dishonest form: truncated y-axes that exaggerate a trend, chartjunk, low-contrast palettes, unlabeled series. These are not rendering bugs; they are the model's learned priors. A chart that renders cleanly and lies is worse than no chart, because it launders a distortion through an authoritative-looking artifact.

Third, **inaccessibility.** A rendered diagram is, by default, a pixel blob to a screen-reader user and a dead end to a keyboard user. Without a deliberate accessibility layer, every visual the system produces is invisible to that audience.

Without this subsystem, visuals either fail silently, disagree with the delivered reasoning, erase canvas work, or appear misleading and inaccessible. The Visual Intelligence subsystem exists to prevent those failures while leaving the prose answer intact.

### Design Decision & Tradeoffs

The central move is one **terminal visual authority over a strict semantic envelope**. Producers may offer candidates, but the final accepted prose is canonical. The authority validates candidates against that prose, holds producer envelopes outside text sent to later models, and uses a bounded selection and recovery sequence:

1. Honor an explicit user-selected image provider or visual type.
2. Accept a schema-valid candidate only when it is paired with the selected prose revision; deterministic Mermaid, DAGitty, and Structurizr recovery is limited to that same revision.
3. If no specialized candidate survives, synthesize from the final prose and then build the existing clause-grounded `concept_map` as the universal fallback. Numeric grounding and structural integrity remain hard blocks; redundancy receives one improvement attempt and then remains a nonblocking warning.
4. If bounded recovery still produces no valid envelope, persist a visible `failed` outcome with stage and reason. The prose remains delivered and unchanged.

The alternative I rejected is **making the analytical model emit render-ready SVG directly.** It is tempting: one round-trip, no schema, no compiler. I rejected it because SVG from a model is non-deterministic (the same reasoning yields a different drawing each time), unauditable (there is no typed structure for an honesty gate to inspect — you would be reduced to regexing pixels), and it forecloses the accessibility layer (alt-text and keyboard navigation need to know that *this* mark is a stock and *that* one is a flow). The strict-envelope-plus-swappable-producer design keeps a single auditable contract at the center and lets the messy, model-dependent part live at the edges where it can fail open.

Honesty is enforced by an **adversarial Tufte gate**, `visual_adversarial.py`, which runs 15 `_t*` rules plus LLM-prior-inversion, grounding, and rendered-artifact checks. Critical findings reject the visual candidate; they do not replace it with a table or pretend the visual succeeded. The authority continues through its bounded grounded recovery, then persists a conspicuous failure if nothing valid survives. This is the component that prevents truncated axes and chartjunk from being laundered through an authoritative-looking artifact.

Rendering has two explicit environments over one vendored compiler. Interactive V3 compiles in the browser and installs native Excalidraw objects or compiler-rendered artifacts. Agent and autonomous producers have no client, so the Node/jsdom CLI renders the first valid surviving envelope to a persisted SVG. The normal visual path launches no server-side browser; Playwright use elsewhere in Ora is unrelated to visual rendering.

The tradeoff is real and worth naming. This design pays for model-independence, honesty, accessibility, editable native output, and ownership-safe regeneration with bounded repair calls, a substantial vendored library set (Vega, Vega-Lite, Mermaid, D3, dagre, Ajv, viz-js, and a hand-written Structurizr parser), and a Node runtime for headless producers. A single-round direct-SVG design would be lighter and faster. I traded latency and disk for auditability and correctness, because for analytical diagrams a wrong-but-fast answer is a liability, not a feature.

**Open problem.** Emission telemetry makes candidate validity observable, but there is still no live reliability budget or success-rate threshold. Provider and model quality therefore remain environment-dependent even though malformed, ungrounded, and unrenderable outcomes now fail visibly rather than disappearing.

### Implementation

**Runtime entry.** `_run_visual_hook(response, context_pkg)` is the terminal authority for direct pipeline output, framework one-shots, and milestone execution. The assistant-message record begins in `building`; the hook either leaves valid envelopes for the client, materializes a headless artifact, establishes a narrow `not_applicable` exception, or records `failed`. Visual-hook availability is still guarded, but absence of the optional hook cannot remove the prose response.

**Data flow inside the hook** (braiding why with how):

- **Candidate isolation and revision pairing.** Gear 4 branch envelopes are discarded because consolidation follows them. Gear 2 and Gear 3 candidates are held outside downstream prose and reattached only when paired with the selected final revision. `_visual_recovery_texts` mines that selected prose only; it does not search superseded pipeline steps.
- **Validation and selection.** `_visual_process_response` validates every fenced block against the final prose, including schema, structural, numeric-grounding, and adversarial checks. Numeric grounding recognizes conventional three-digit comma, horizontal-space, and apostrophe groups in integers, signed decimals, and percentages in both prose and numeric visual labels. It compares equivalent values, including explicitly marked percentages, without joining separate numbers. Unsupported or mismatched numeric claims reject the visual block while preserving the prose. Every valid survivor remains available to interactive dispatch. Critical failures are trace evidence, not permission to alter the prose.
- **Bounded recovery.** The authority first attempts deterministic recovery from the selected revision, then interactive synthesis for configured modes, then the deterministic clause-grounded concept-map builder. Each successful candidate re-enters the same validation path. If all stages fail, the outcome is `failed` unless the turn positively qualifies for a narrow `not_applicable` reason.
- **Diagnostics and durable outcome.** Per-envelope diagnostics land in `step-visual-hook.json`. An exception from the primary visual processor never releases its input as a fallback: it strips complete unreviewed `ora-visual` blocks, visual-suppression markers, and any residual unterminated `ora-visual` opener while preserving the remaining prose and ordinary fenced code in source order. It stores a blocked diagnostic and a `failed` `visual_hook` outcome, and records `processor_exception` in the turn trace when one exists. If only the optional redundancy-improvement processor raises after the original visual passed review, the unreviewed replacement is discarded, the approved original remains, and the response plus its diagnostics, outcome, and, when tracing exists, trace disclose the failed attempt. The browser changes `building` to `ready` only after actual insertion; unsupported operations and dispatch errors use the Exhibits pane's nonblocking error surface and persist `failed`. Reopening a stalled `building` turn retries its durable envelope or records failure.

**Headless render.** `_materialize_noninteractive_visual` selects the first valid surviving envelope, calls `_render_visual_svg_cli`, and persists `visual-artifact.svg` plus `visual-artifact.json` beside the turn trace. `_render_visual_svg` delegates directly to the same Node/jsdom CLI. This is the load-bearing renderer for agent and autonomous producers and the SVG path used by vault export; it launches no browser.

**Dormant legacy helpers.** `_render_visual_svg_browser`, `_rasterize_svg_light`, and `_maybe_review_and_refine_visual` remain defined, but `_maybe_review_and_refine_visual` has no production caller. The old Playwright screenshot-critique loop is therefore not part of normal turn processing or export, and its mere presence must not be read as an active fallback.

**Client compiler.** `server/static/ora-visual-compiler/index.js` exposes `compileWithNav`: validate against the Ajv 2020 schema → dispatch through `dispatcher.js` → apply the accessibility layer (`alt-text-generator.js`, `aria-annotator.js`, and `keyboard-nav.js`) → run the rendered-artifact honesty pass for overlap, truncation, and contrast. A blocking artifact finding clears the artifact and becomes a dispatch failure; the client does not substitute a table or prose representation. The Findings prose remains intact, and the assistant message records the failure. The 15 renderer files cover 22 diagram kinds because Mermaid and Vega-Lite are multi-kind; `annotated_image` remains the separate Konva image-overlay path.

**Canvas and V3 dispatch.** `server/static/js/v3-visual-dispatch.js` extracts every parseable surviving envelope and hands the complete ordered set to `OraPanels.visual.onBridgeUpdate`; `v3-canvas-mount.js` serializes installation so every block lands in source order. Native structural types become editable Excalidraw objects, while position/value and deferred DSL types become assistant-owned rendered artifacts; Konva remains the fallback editor and SVG consumer. Four ownership fields — assistant visual identity, generation revision, semantic element identity, and original-generation fingerprint — distinguish untouched assistant output from user edits. `replace`, `update`, and assistant-issued `clear` remove only untouched assistant-owned material. User-created elements, uploads, viewport, and history survive; a directly modified native generation survives as a complete generation, and a modified assistant annotation group survives atomically with its attached generation. The dispatch key prevents a rerender of the same conversation turn from applying the set twice.

**Canvas file format.** `orchestrator/canvas_file_format.py` is the server-side reader/writer for `.ora-canvas` files (gzip-wrapped canonical JSON), a byte-identical mirror of the client `canvas-serializer.js`, used for vault export/autosave and server previews.

**Vision extraction.** `orchestrator/visual_extraction.py` (WP-4.3) invokes a vision-capable model on a user image, parses the result into `spatial_representation` JSON, validates with a single repair pass, and scores confidence — the inbound direction (image → structure) complementing the outbound envelope→SVG.

**Media adjuncts** (a related, Mac-bound cluster, not the diagram path):
- `plugins/video/backend/render.py` is the optional feature's **FFmpeg video** render engine. It remains separate from envelope→SVG visual rendering.
- `plugins/video/backend/media_capture.py` wraps FFmpeg for screen, microphone, and camera capture; AVFoundation on macOS is the working host path.
- `orchestrator/transcription.py` wraps `whisper-cli` (whisper.cpp) — ffmpeg 16 kHz mono extract → whisper-cli JSON → hallucination filter.
- `plugins/video/backend/waveform.py` shells `ffmpeg showwavespic` to a small PNG. Chapter 11 owns the plugin boundary and current portability detail.

**Library inventory with licenses** (versions and license text read from each `vendor/*/VERSION` and `LICENSE`): Vega 6.2.0 (BSD-3-Clause), Vega-Lite 6.4.2 (BSD-3-Clause), Mermaid 11.14.0 (MIT), D3 7.9.0 (ISC), dagre 0.8.5 (MIT — header "Permission is hereby granted, free of charge…", Copyright 2012-2014 Chris Pettitt), Ajv 8.18.0 2020-12 bundle (MIT), viz-js 3.26.0 (ships a Third-Party Notices file covering its Graphviz/Emscripten sublicenses — confirm before redistribution), structurizr-mini (hand-written parser + renderer, Context/Container only), and Konva (MIT, Anton Lavrenov) for the canvas. All vendored locally; no CDN; CSP-safe.

**Config surfaces.** `config/visual-schemas/specs/` holds the 23 JSON-Schema-2020-12 specs (verified count) plus example envelopes and guides. `config/mode-to-visual.json` (`_schema_version` 0.2, `_last_updated` 2026-06-13) sets per-mode `visual_types`, `relation_to_prose_default`, and `adversarial_strictness`; its `_comment` states it is synced manually to the vault reference "until generation is automated" (`config/mode-to-visual.json:2`). Environment flags: `ORA_VISUAL_RECOVER_SIBLINGS` (restore accept-any-sibling recovery), `ORA_VISUAL_RECOVER_ANY` (recover an unrequested diagram in interactive contexts; default off).

**Failure behavior separates prose continuity from visual integrity.** Prose delivery fails open: a hook, recovery, compiler, insertion, or persistence error does not remove the text answer. Visual integrity remains fail-closed: an invalid or dishonest artifact is not marked ready. Bounded recovery ends in a persisted, visible `failed` outcome with stage and reason; no table/prose substitution hides the missing visual.

### Evidence & Verification

**Python suites** (`orchestrator/tests/`): `test_visual_validator`, `test_visual_adversarial`, `test_visual_phase2_rules`, `test_visual_synthesis`, `test_visual_fallback`, `test_visual_extraction`, `test_visual_routing`, `test_visual_e2e`, `test_visual_merged_input`, `test_canvas_file_format`, `test_media_library_transcript`, `test_render_two_pass`, `test_waveform`. Recovery is exercised through the fallback and e2e suites.

**JS harness**: `server/static/ora-visual-compiler/tests/` with 39 case directories (verified: `ls tests/cases/` = 39), run via `node run.js`; plus a V3-dispatch case (`tests/cases/test-v3-visual-dispatch.js`, cited at 12 assertions in the module's audit note). Exact aggregate pass counts were not re-run in this pass — CLAUDE.md cites figures at various dates; treat those as stale until re-run.

**Live-fire**: a Tier A run across the visual-bearing modes was executed during development on 2026-04-18.

**How to confirm it works.** In an interactive turn with more than one valid envelope, confirm that every surviving block is installed in source order and that the assistant message moves from `building` to `ready` only after insertion. In an agent or autonomous turn, confirm that only the first valid envelope becomes `visual-artifact.svg` plus `visual-artifact.json`, with `renderer: node-jsdom-cli` and no browser launch. Force bounded recovery to exhaust and confirm that the prose remains present while the assistant message and Exhibits pane show the persisted `failed` stage and reason.

**Explicitly untested surfaces.** Live provider/model interpretation remains environment-dependent. The retained Playwright raster/critique helpers have no production caller and therefore provide no runtime coverage. **All non-macOS server platforms are untested** (see below). Video suggestion and preview evidence belongs to the optional feature in Chapter 11, not to the visual compiler. Reliability remains qualitative: there is no instrumented reliability budget, success-rate metric, or per-step percentage.

### Expert Reading Notes

Read `orchestrator/boot.py::_run_visual_hook` first for terminal selection, bounded recovery, durable outcome assignment, and noninteractive handoff. Then read `server/static/js/v3-visual-dispatch.js` and `server/static/js/v3-canvas-mount.js` for all-block interactive dispatch and ownership-safe installation; `orchestrator/visual_recovery.py` for selected-revision recovery; `orchestrator/visual_adversarial.py` for honesty and grounding; and `tools/render-envelope.js` for the Node/jsdom headless renderer.

**Drift hotspots, in priority order:**

1. **Terminal authority must remain terminal.** Every output path, including framework one-shots and milestone execution, must pass the final selected prose through `_run_visual_hook`; Gear 4 branch envelopes must not leak back in after consolidation.
2. **Interactive and noninteractive multiplicity intentionally differ.** V3 installs every valid surviving block; `_materialize_noninteractive_visual` selects only the first. A shared helper that silently collapses both paths would change user-visible behavior.
3. **Ownership metadata is one safety mechanism, not decoration.** Assistant visual identity, generation revision, semantic element identity, and original-generation fingerprint jointly decide whether regeneration may replace material. Dropping any field can erase or strand user-modified work.
4. **`config/mode-to-visual.json` is manually synced** to the vault reference. Canonical-versus-operational drift remains possible.
5. **The 23 schemas ↔ 16 renderer files ↔ 22 synthesizable kinds mapping is non-obvious** because Mermaid and Vega-Lite are multi-kind and `annotated_image` is a separate Konva path. Hold the mapping, not the raw file counts.
6. **The Playwright renderer and screenshot-critique helpers are dormant.** They have no production caller; do not infer active behavior from their definitions. `render.py` is video, not diagrams. The noninteractive diagram renderer is the Node/jsdom `render-envelope.js` path.

### Platform compatibility

The interactive compiler, Excalidraw emitter, and retained Konva canvas are browser JavaScript with vendored, CSP-safe libraries. Noninteractive rendering is the Node/jsdom CLI and launches no browser. Server-side evidence remains macOS-only; every off-Mac label below stays conservative.

| Subsystem | macOS | Windows-native | WSL | Linux | Evidence / note |
|---|---|---|---|---|---|
| Client compiler + native Excalidraw emitter + accessibility review | works | works | n/a | works | Browser JS, vendored libraries, no CDN. `server/static/ora-visual-compiler/`. OS-independent by construction. |
| Excalidraw/Konva canvas dispatch and ownership preservation | works | works | n/a | works | Browser JS in `v3-visual-dispatch.js`, `v3-canvas-mount.js`, and `visual-panel.js`. |
| Noninteractive envelope→SVG render (Node + jsdom CLI) | works | untested | untested | untested | `_render_visual_svg_cli` and `render-envelope.js`; relies on `node` on PATH and the bundled jsdom/compiler dependencies. This is the normal agent/autonomous and vault-export path, not a Playwright fallback. |
| FFmpeg video render (`render.py`) | works | untested | untested | untested | `FFMPEG_BINARY = shutil.which("ffmpeg") or "/opt/homebrew/bin/ffmpeg"` (`render.py:71`). PATH lookup is portable; the fallback is a Homebrew-on-Apple-Silicon path. `drawtext` requires the homebrew-ffmpeg tap on Mac (per project memory). |
| Video-render font resolution (`_resolve_font_file`) | works | needs-porting | needs-porting | needs-porting | `render.py:247` candidates are all `/System/Library/Fonts` and `/Library/Fonts`; the final fallback is a hardcoded Mac path that will not exist elsewhere. macOS-only. |
| Screen/mic capture (`media_capture.py`) | works | not-implemented | n/a | not-implemented | macOS `avfoundation` only; the Windows branch raises `NotImplementedError("Windows capture lands in Phase 10")` (`media_capture.py:159, 259`). No Linux `x11grab` path exists. |
| Transcription (`transcription.py`, whisper.cpp) | works | untested | untested | untested | `whisper-cli` via PATH with `/opt/homebrew/bin/whisper-cli` fallback. whisper.cpp builds cross-platform, but binary path and models layout assume Mac. |
| Waveform (`waveform.py`) | works | untested | untested | untested | ffmpeg `showwavespic`; same ffmpeg-binary assumptions as above. |
| `WORKSPACE` / path form (`~/ora`, vault `~/Documents/vault`) | works | needs-porting | untested | untested | `os.path.expanduser("~/ora")` — tilde expands cross-platform, but a `%USERPROFILE%\ora` layout is untested and the vault path is Mac-specific. |
| JS test harness (`tests/run.js`, jsdom) | works | untested | untested | untested | `cd tests && node run.js`; node/jsdom are cross-platform but run only on macOS. |

No non-macOS server surface is claimed to work without evidence. The one surface with a *definite* off-Mac status is `media_capture.py`: its Windows path is coded-but-stubbed (raises `NotImplementedError`) and Linux is absent, so it is "not-implemented," not merely "untested." Cross-reference the Audio/Video Media & Timeline Editing chapter for the adjacent Mac-bound media-capture and render/export tooling.

## 11. Audio/Video Media & Timeline Editing

### Problem

A user may need ordinary audio/video transcription, or a complete editing surface
for capture, media organization, timeline work, preview, suggestions, and render.
Those are related experiences but different ownership boundaries. Transcription
is general Dialogue input. The editor is substantial and removable; making the
core import it everywhere would prevent Ora from starting cleanly without video.

The editing layer must also avoid destructive edits and unsafe deletion. A
timeline should refer to original media rather than rewrite it, and Delete Forever
must stop every video worker which could still write into the Dialogue before the
managed purge begins.

### Design Decision & Tradeoffs

Three decisions define the current layer.

**Video is the only configured production source in a plural first-party host.**
`server/feature_plugins.py` receives an explicit ordered source list and loads
the built-in Python implementation `plugins.video` only when the configured
`video` directory exists. By
default that checked descriptor-and-assets root is `<ora>/plugins/video`.
`ORA_FEATURE_PLUGINS_DIR` changes the parent of that checked root, but it does
not change the Python import: implementation code still comes from
`plugins.video`. The override is the removal-test seam, not alternate-package
discovery. The feature runs in the Ora process because its workers must share
the conversation lifecycle lock and deleted-state boundary. A separate service
would need a new cross-process safety protocol before it could preserve the same
purge guarantee. The accepted cost is that trusted first-party plugin code
shares the server's process.

**Registration is complete before installation.** Each package returns a validated
descriptor containing its source-matched id, static root, ordered scripts and
styles, routes, optional owner-bound providers, and Close/Delete lifecycle
callbacks. It receives a fixed
`FeaturePluginContext`, not the Flask application. Validation accepts only the
expected source id and static root, confines assets below that root,
requires declared files to exist, and rejects duplicate endpoint names. Only
then are routes, the static asset route, and page tags installed. An absent or
broken package is logged and returns no active descriptor; the core keeps
running. Validation precedes route installation, although Flask routes already
added before an unexpected mid-loop failure are not transactionally rolled back.

**Editing is non-destructive.** The plugin's media library stores protected
references and metadata rather than modifying a user's source file. Timeline
edits are normalized JSON state. Render resolves a snapshot of timeline and media
references into an FFmpeg command, leaving sources unchanged. This keeps undo and
re-edit cheap, at the cost of a dangling reference when an external source is
moved or removed. FFmpeg filter and codec availability remains the principal host
compatibility risk.

This is not generalized discovery, a public SDK, a marketplace, an installer,
version negotiation, dependency resolution, sandboxing, hot loading, or an
injection point into the analytical pipeline. Project Plugins remain the separate
external-project subprocess convention described in Chapter 17.

### Implementation

**Loader and contract.** `server/feature_plugins.py` defines `FeaturePlugin`,
`PluginRoute`, `FeaturePluginContext`, descriptor validation, route installation,
the plugin asset route, and asset-tag injection into `server/index-v3.html`.
The explicit context supplies two paths plus seventeen lifecycle, safe-file,
mutation-policy, settings, declared-credential, metadata, telemetry, and
async-capability helpers. It
is the current contract; the earlier seven-helper proposal is obsolete.

`plugins/video/__init__.py` registers the descriptor and three lifecycle
callbacks. `plugins/video/routes.py` declares all 31 plugin routes. They cover
capture; media library, URL import, protected-reference removal, transcript,
waveform and suggestions; timeline; preview; render and watermark upload; and
video capability/state calls. `plugins/video/runtime.py` holds the provided
context so package modules do not import `server.app`.

**Plugin-owned server modules.** The package owns its capture, media-library,
preview, render, timeline, URL-import, video-suggestion, and waveform backends
below `plugins/video/backend/`. The suggestion framework and schema are also
plugin-local. Removing the package therefore removes both the suggestion
implementation and the instructions which define its output.

**Core-retained services.** `orchestrator/transcription.py` and
`orchestrator/vault_transcript.py` remain general Dialogue services.
`orchestrator/job_queue.py` and its core routes/lifecycle remain shared by
non-video work. `server/static/transcribe-input.js` retains Inquiry-pane A/V
drop. `server/static/js/v3-capability-async.js` remains the shared browser
bootstrap; video consumes it through the context's async-capability registry.

**Paid async-provider contract.** Replicate `video_generates` and `style_trains` require an existing live Dialogue. Before provider contact, the shared queue durably records the `submitting` transition.
Before polling, it records the exact prediction ID with the same Dialogue and local job binding. Restart reattaches to that ID without resubmission.
Submission and queued cancellation are lock-ordered, so a queued cancellation either wins before any provider call or becomes durable intent on the active job.
Polling, terminal changes, cancellation, and artifact materialization authenticate the surviving Dialogue/job owner; artifacts are written only while that exact owner remains live.

**Browser installation.** After complete validation, the host replaces the
single `<!-- ORA_FEATURE_PLUGIN_ASSETS -->` placeholder with every loaded
descriptor's ordered styles and scripts. `plugins/video/static/video-plugin.js` registers four host
seams: the Exhibits `video` pane through `OraPanes`; Video editor, Browse Dialogue
media, and Send canvas image controls through `OraMounts`; the media browser
through `OraBrowseOverlays`; and **Settings → Video** through
`OraSettingsSections`. It also registers `/video` and releases pane ownership on
new-Dialogue or Dialogue-selection events. With no descriptor, the placeholder
is empty and none of those registrations execute.

**Media and render flow.** The plugin library records conversation-owned
references and metadata. Protected media-reference removal compares the approved entry with the current cached entry under the existing media-library lock and returns `409` without removing a substituted entry, while the route retains core state-file reauthentication immediately before the protected effect. Timeline state is normalized on read and write, and
render receives a snapshot of the timeline and library rather than reading a
moving edit. FFmpeg performs trimming, padding, overlays, watermarking, mixing,
encoding, preview frames/proxies, waveform work, and capture where the host
supports it. `plugins.video.backend.render.RenderManager` starts FFmpeg in its
own daemon thread and exposes render state to the plugin routes. The core shared
job queue remains available for non-video work; it does not execute this render.

**Lifecycle.** Close invokes `on_release`: finished capture, import, and render
records plus timeline/library caches are released, while active work and owned
media remain. Delete Forever invokes `on_quiesce` before core purge to stop or
tombstone capture, import, preview, render, and library work; after the purge,
`on_clear` removes timeline state, owned upload staging, and render lookup state.
Lifecycle errors are logged and collected without falsely claiming that an
optional callback succeeded.

Transcript and suggestion routes hold the existing conversation read scope from entry lookup through transcript file I/O, JSON parsing and normalization, optional suggestion derivation, and final response construction; Delete Forever therefore waits for an admitted read to finish, while a Dialogue it has made unavailable returns `410` without transcript or suggestion content.

**Failure and removal.** A missing source or an import, registration,
descriptor, provider, or route-install failure logs a specific error, skips that
source, and continues loading later explicit sources. An unexpected mid-route-loop
failure can leave routes Flask already accepted until restart; the loader does
not claim transactional rollback. Removing or renaming `plugins/video` and
restarting removes its 31 routes, assets, settings, controls, pane, overlay, and
slash command while leaving transcription, Inquiry A/V drop, and the shared job
queue operational. Removing code does not itself delete previously written media.

### Evidence & Verification

The landed implementation proves the boundary through focused behavior: startup
with the package present, startup with its root absent, rejection of invalid
descriptors and routes, plugin-only route and asset ownership, all four browser
registries, removal of the core `/video` registration, and the distinction
between Close release and Delete Forever quiesce/clear.

The accepted extraction also keeps shared services live when the plugin is
absent. The non-destructive removal test points `ORA_FEATURE_PLUGINS_DIR` at a
parent with no `video` child and restarts Ora; this changes only the checked
descriptor/assets root, so the built-in `plugins.video` import is never attempted.
Removing or renaming the bundled directory exercises the same absent state.
Confirm Ora boots and video routes/assets/UI are absent, then confirm Inquiry A/V
drop, transcription, and the shared job queue remain. A real FFmpeg render is
still the appropriate end-to-end check for host codecs, filters, fonts, device
capture, and output media; documentation or route tests cannot prove those host
capabilities.

### Expert Reading Notes

Read the boundary before the backend: `server/feature_plugins.py`, then
`plugins/video/__init__.py`, `routes.py`, and `runtime.py`; then the four browser
registries and `video-plugin.js`; finally the backend module relevant to the
behavior under review.

The highest drift risks are a plugin importing `server.app`, a new route or asset
bypassing descriptor validation, browser UI added directly to the core page,
Close killing active work, Delete Forever purging before quiesce, video-only
schemas moving back into core, or shared transcription/job infrastructure moving
into the plugin. Treat the 31-route descriptor, explicit context fields, browser
seams, and three lifecycle callbacks as one contract when changing any part.

### Platform compatibility

The feature boundary is platform-neutral Python and browser code, but the work it
exposes retains host dependencies:

- **Plugin loading and browser registration** — standard Python import, Flask,
  and browser JavaScript. Exercised on macOS; untested on Windows-native, WSL,
  and Linux. An unavailable package is skipped on every platform.
- **FFmpeg / ffprobe** — the plugin backends prefer binaries on `PATH` and retain
  Homebrew fallbacks. Software codecs are portable once present, but filters,
  fonts, and installed codec sets vary. macOS is exercised; other platforms are
  untested or need porting where a Mac font path is assumed.
- **Screen, microphone, and webcam capture** — macOS uses AVFoundation and is the
  working path. Windows-native and Linux capture remain unimplemented or
  unverified; WSL has no claimed desktop capture surface.
- **Local Whisper transcription** — core transcription works on macOS with
  `whisper-cli` and locally installed models. Other hosts are untested; the cloud
  transcription alternatives are network-dependent rather than OS-specific.
- **Atomic timeline/library state and subprocess cancellation** — use portable
  Python primitives, but only the macOS concurrency and process behavior is
  exercised. Windows handle and termination semantics remain unverified.
- **Removal behavior** — does not depend on FFmpeg: no valid descriptor means no
  video routes or assets. Core startup, transcription, Inquiry A/V drop, and the
  shared job queue remain the required cross-platform fallback.


---

# Part IV — Supervision & Governance

## 12. Meta-Layer Observation and Explicit Project Oversight

### Problem

Long-lived projects still need reality-contact signals: a milestone checkbox can change without evidence, a corpus reference can go stale, and a child project's progress may matter to its parent. Those facts justify observation and durable event records. They do not justify an autonomous judgment or transition engine.

### Design Decision & Tradeoffs

The installed project-oversight router is observational. File events and exact time-caused deadlines feed the existing event bus. `oversight_router.process_event` records an event, suppresses Stealth persistence, prevents fan-out recursion, and may surface a child event to its parent. It never invokes Process Coherence, parses a framework verdict, mutates project state, or dispatches Programming work.

Process Coherence v5.0 is instead an explicitly invoked project/framework judgment. When a caller chooses it at a boundary configured by Oversight Configuration v3.0, it returns exactly `PROCEED`, `REVISE`, or `ESCALATE`. The caller retains responsibility for any permitted next action. These labels are unrelated to Programming's `CONTINUE`, `FIX`, `DONE`, and `ASK USER` outcomes.

This removes the former `ORA_OVERSIGHT_LIVE` fork, automatic context-to-model route, seven-directive parser, REVISE counter, and verdict-action dispatcher. There is no dormant live mode to enable and no simulated-supervision warning.

That observational boundary does not mean Ora has no Trigger facility. **Scheduled Triggers are separate, explicitly authored activation objects** built on the same exact-event and persisted-deadline substrate. A Trigger binds an already registered project tool, a user-invocable framework, or the narrow manual-only email action; it does not turn the oversight router into a judgment engine, create a Programming Run, or restore the retired governed-Process stack.

### Implementation

- **Event/deadline daemon.** oversight_daemon.py runs the existing OS file-event lane, persisted exact-deadline lane and bounded watchdog. Time is used only when it is the actual cause. The dispatcher launches the vault event pipeline with its running Python interpreter and exact paths in a temporary JSON file removed on return/failure. Existing Resources force-indexing and per-branch error reporting remain; this does not certify native Windows execution. Before ordinary actionable filtering, eligible raw graph events include deletions and root MSI News; sibling Git/cloud/Trigger/judgment consumers keep their existing filtered input. Hidden/repository internals, .trash and Archive remain graph exclusions. Graph failure is isolated and logged. After listeners start, existing bootstrap reconciliation performs finite cancellation-aware graph catch-up, without another watcher, timer, daemon or scheduler.

The graph's one additive relationship_files table retains vault-relative path, stem, observed device/inode/size/mtime-ns/ctime-ns, digest, H1 claim, archived flag and original normalized declarations. No bodies, extraction provenance, event history, persistent reverse-reference table or retry queue are stored. Unchanged stat state avoids catch-up reads; explicit events hash even unchanged signatures, and unchanged bytes skip parsing. Idempotent rename remove/add can reuse content; transient cached referrer resolution preserves title precedence, earliest-stem claim winners, same-stem union and deterministic confidence collisions. Cache, compiled edges and status commit together under immediate writer ordering, with file-state rechecks preventing stale startup bytes from overwriting events.

Startup takes an initial stat inventory, processes finite batches releasing the writer between them, and performs a final stat comparison. Errors/races retain useful old data and incomplete coverage instead of treating unreadable files as deleted. Existing metadata carries cache version/completeness and direct-mutation invalidation generation, so final catch-up cannot erase a concurrent invalidation. sync_from_vault remains canonical repair, reusing valid parses but comparing adjacency to restore deleted rows and remove noncanonical direct rows. Explicit rebuild verifies content and reconstructs coherent derivatives or stays incomplete. Direct add/remove and quarantine SQL cleanup invalidate coverage. Historical archived edges remain; new-archived-target guards stay intact. Markdown is the recovery authority. A missed change preserving the entire stat signature requires a later event or content-verifying rebuild; no real-corpus timing or cryptographic freshness proof is claimed.
- **Event bus.** `oversight_events.emit` normalizes and records non-Stealth events, then invokes registered observational handlers without allowing one handler failure to break the bus.
- **Router.** `oversight_router.py` produces `logged_only`, `fan_out_audit_only`, or `stealth_suppressed`. Fan-out records carry the existing recursion sentinel and cannot fan out again.
- **Parent visibility.** `oversight_relationships.py` may append one managed child-update derivative to a parent's Decision Log. The parent is informed; no parent-side judgment is invoked.
- **Durable utilities.** `oversight_actions.py` retains lock-protected managed Decision Log derivatives, reversible privacy/delete support, the existing human queue, and action-log utilities used by active callers. Every Paused-queue addition, metadata rewrite, and entry removal—including lifecycle purge—is serialized by the queue's shared cross-process sibling lock and committed through a unique same-directory temporary that is flushed, fsynced, and atomically replaced. An interruption before replacement preserves the prior bytes. It contains no Process Coherence verdict parser or dispatcher.
- **Health.** `oversight_health.py` reports actual daemon/thread or telemetry degradation. It no longer reports a configuration-disabled judgment path.
- **Explicit frameworks.** The canonical behavior of project-level judgment lives in `Framework — Oversight Configuration.md` v3.0 and `Framework — Process Coherence.md` v5.0, not in the event router.

### Trigger routes, UI, and execution contract

`orchestrator/triggers.py` exposes four causes: explicit manual request, an OS file change under a live watched root, a named-timezone calendar occurrence, and successful completion of another Trigger. The corresponding actions are a registered project tool, a user-invocable framework, or `email_send`; email remains manual-only. Literal shell commands are refused. A calendar Trigger also requires a written reason that time itself is the cause and discloses the local-app boundary: Ora acts only while it is running, with no cron, launchd, interval scan, sweep, or clock fallback.

The browser places this facility in the **Scheduled** group inside Oversight rather than restoring the old Trigger Manager. Its default `/api/triggers` request lists drafts and active or paused Triggers; retired entries are excluded unless a caller explicitly requests them. `sidebar-oversight.js` shows the active count, lane-availability or repeated-restart warnings, Ora's separate internal-deadline count, recent firing history, and the next due time. The `+` form declares a cause and one registered action. The email-draft composer resolves its visible sender identity from the global default Persona with project precedence disabled, unless the author explicitly chooses another available Persona. The saved draft displays that resolved Persona; an unavailable explicit choice is rejected rather than silently substituted. Draft creation alone neither activates nor approves the Trigger, contacts the provider, nor grants send authority. Before activation, **Review and activate…** renders the material action—including exact project-tool arguments or stdin, framework input and its resolved Project nexus/profile when present, or the email action—along with the specification digest, bound program identity, calendar justification when present, and intermittency notice; activation succeeds only for that reviewed version. The same visible card can inspect its bound message, run once, pause, resume, retire, or—before provider contact—cancel and retire an approved email action.

For a project-tool action, activation is revocable standing authority for that one exact Trigger specification. The approved binding covers the Project nexus, canonical registered root, manifest, registered command, declared interface, resolved executable and script identities, and arguments/stdin digest. The exact authenticated absolute executable path becomes the child argv's first element at both the project-tool and project-slash subprocess boundaries; Ora never asks the subprocess launcher to resolve the manifest spelling again. The root is also the child working directory and `ORA_PROJECT_ROOT`, so re-registering identical bytes elsewhere is a binding change. Every event carries the specification and admission identity it observed. An active firing compares both under the Trigger-store lock before authority or claim, then passes immutable action, binding, specification, and admission snapshots to the worker and supplies the binding to `invoke_project_tool(expected_binding=...)` for its final pre-spawn check. Pausing, retiring, or editing prevents a new claim; resuming mints a new admission identity, and an edit after claim cannot substitute later draft work. A manual draft or paused project-tool run is outside activation and instead uses the existing Paused one-shot approval path. Both forms create authenticated System Protection start and terminal receipts without adding a generic Trigger entry to `tool_events` standing allows.

The server exposes the complete surface directly: `GET /api/triggers` and `/api/triggers/actions`; `POST /api/triggers`; `GET` and `PATCH /api/triggers/<trigger_id>`; `GET .../review` and `.../inspect`; and `POST .../activate`, `.../lifecycle`, `.../rollback`, and `.../run`. These are ordinary localhost APIs backed by the Trigger service, not aliases for Programming or Process Coherence.

**Local routine descriptor (current scoped foundation).** Existing Trigger create, read, list, and activation-review views, plus `/api/triggers/actions`, carry a `routine` object. Expanded Scheduled cards and **Review and activate…** render those server-supplied facts rather than browser-owned promises: execution target `local`; authority bound to the exact action binding (and action kind on a Trigger-specific view); firing-receipt and error-debug availability; remote execution unavailable; idle-sleep protection advertised only when the host supports it (currently when `/usr/bin/caffeinate` is executable on macOS), applying only while an action is actively running and ending with that action; and no ability for Ora to wake a sleeping Mac. The browser adds no execution capability, and the existing Trigger lifecycle, firing-receipt, and error controls are unchanged. This remains response metadata on the existing service; it does not create a remote runner, wake adapter, new service, store, schema, or daemon.

**macOS active-run sleep boundary.** Inside the spawned action process, `_active_run_sleep_protection` begins immediately before `_execute_action` and starts `/usr/bin/caffeinate -i -w <action-process-pid>`. If the helper is unavailable, cannot launch, or exits before the assertion is established, the action refuses to start unprotected. Normal success, action error, provider-callback error, and cancellation leave the action scope and terminate/reap the helper before a result or error crosses back to the parent. Timeout, a parent callback failure, or action-process death terminates and reaps the action boundary; `-w` also binds assertion release to that process's death. Other platforms continue the action without the macOS helper and make no sleep-protection claim. This prevents ordinary idle system sleep only while real Trigger action work is executing; it neither keeps the machine awake merely because Ora is open nor wakes a sleeping machine.

Durability is ordered around the point where work becomes externally observable:

- A watched-file publication carries a stable `publication_id`. Under the existing emitter lock and the event log's cross-process file lock, `oversight_events.emit` removes any unterminated append fragment and scans the complete records first. A durable delivery marker is authoritative: the emitter fsyncs the log, does not prepare or recreate a router plan, invokes no handler or sink, and performs only the same narrow idempotent router acknowledgment before allowing the watcher checkpoint to advance. That acknowledgment treats an already-completed or retention-pruned claim as success; a malformed or identity-mismatched surviving claim still fails. When no marker exists, the installed production router claims the delivery before a new event row is appended and freezes the complete original publication payload plus the first resolved parent nexus, canonical PED destination, rendered child-update content, and synthesized event/action payloads. This preparation writes only the internal claim; it performs no router-log, parent-PED, or fan-out audit effect. Only after that complete plan is durable does the emitter fsync one complete event line. Every retry reauthenticates and reuses the exact claimed payload and parent plan: a crash before the event row or changed child routing metadata cannot retarget it, and an unavailable original PED leaves the same delivery claimed and retryable. A handler or sink failure propagates while that frozen claim remains nonterminal, and startup recovery exempts only this claim type so the watcher rescan can resume it. After every router, parent-PED, and fan-out audit effect succeeds, the router durably records an effects receipt inside the still-claimed row; a retry that sees the receipt returns success without replaying those idempotent effects. A separate delivery marker in the event log is then fsynced, and only afterward does the emitter acknowledge the matching claim as completed.
- Every firing first validates its exact admission authority and claims one idempotent `EventLedger` identity. The claimed worker input is immutable even if the editable Trigger later changes. A calendar deadline persisted before admission identities existed is reported once as stale after upgrade: its action does not run, and the active Trigger arms its successor with the current authenticated payload. On startup, the daemon restores interrupted event claims and replays unfinished Trigger-completion deliveries before arming active calendar Triggers.
- Before a successful source firing is marked complete, the Trigger service persists every active dependent delivery with that dependent's exact specification and admission identity. After source completion, one exact proof must authenticate the same source event id, firing type, Trigger, specification, admission, and non-empty completion time. The service durably binds that proof to every matching staged delivery before attempting any child. A proof-save failure therefore runs no child and the proof-less internal row is omitted from list/detail projection rather than falsely reporting source success. On startup, replay takes its pending snapshot under the Trigger-store lock and releases it, reads any still-live completed source row under the separate ledger lock, derives the same proof, and then atomically backfills all matching deliveries under only the Trigger-store lock before child attempts or later source pruning. The same proof predicate governs replay and projection. Once proof is durable, an unfinished delivery survives restart even after the source ledger row is pruned. Trigger list/detail responses then project it separately from firing history as `pending_completions`, exposing only its source identities and times plus `pending_retry` or `blocked_by_trigger_change`; private proof and admission digests remain internal. The Scheduled card shows a collapsed pending/blocked badge and an expanded explanation, while `/trigger list` and `/trigger show` render a separate **Pending completions** section. A changed or reactivated dependent is identified as targeting an earlier approved version: Ora does not run the current action, does not synthesize a firing, and leaves the existing delivery visibly pending.
- Immediately before provider I/O, the action child requests contact permission and blocks until the parent durably records `provider_contacted` and acknowledges it. A callback failure or parent loss therefore permits no provider work while the ledger still claims pre-contact cancellation.
- Real action work crosses a spawned process boundary with one whole-firing deadline (`ORA_TRIGGER_FIRING_TIMEOUT_SEC`, default one hour). On POSIX, Ora sends termination signals to the known action process group and verifies that group and its wrapper are gone. A descendant can create a new session and leave that group, so a deadline reached after the action-start barrier remains durably claimed as `termination_unacknowledged`: Ora writes no terminal protected receipt, releases no overlap guard, and restart recovery keeps later firings blocked even when the recorded root and group are absent. On Windows, the held wrapper is assigned to a uniquely and globally named, non-inheritable Job Object with kill-on-close and die-on-unhandled-exception enabled and breakaway disabled before the parent durably records the Job identity and acknowledges action start; global-name creation failure or any pre-existing same-name object refuses before assignment and starts no action work. The persisted identity binds the exact root PID and Windows creation time, current active count and PID/creation identities, accumulated observed membership, assignment and containment flags, and the noninherited sole-owner handle facts. The existing duplex connection keeps the wrapper held after success, error, result-transfer failure, and during parent unwind. Ora persists actual Job membership before releasing a completed wrapper, then terminates the Job; timeout and cleanup terminate the still-held Job directly. Kernel Job membership continues to contain a late descendant after its intermediate parent exits, so no point-in-time parent graph is called complete. Terminal evidence and the in-process running-claim release wait for a zero `ActiveProcesses` snapshot captured through the original owner handle, durably saved through the existing ledger callback, and a dead wrapper. Restart may trust that persisted owner-zero fact; otherwise, any successfully reopened same-named Job remains ambiguous—even when empty or apparently matching—because a name can identify a recreated object, so Ora neither terminates it nor releases the claim. Authenticated not-found releases only when the validated global name and assignment, containment, kill-on-close, non-breakaway, noninheritance, and sole-owner facts prove that loss of the original handle killed every member. Any other setup, query, termination, persistence, close, open, or recovery ambiguity leaves the protected start receipt and firing claim nonterminal and blocks another firing.
- General Oversight health evaluates telemetry completeness before returning a clean result. The Scheduled panel itself warns only when an active Trigger's deadline lane is down or when a relevant event lane is repeatedly restarting; firing-evidence or telemetry degradation belongs to the general Oversight health surface. Health warnings do not themselves block the rest of Ora.

### Evidence & Verification

The active tests exercise Stealth suppression, observational routing, fan-out recursion prevention, managed Decision Log derivative preservation/removal, event/deadline liveness, system-protection boundaries, Trigger authoring/lifecycle, exact-event and deadline firing, process-bound timeout cleanup, watcher-publication de-duplication, completion-delivery replay, and health degradation. Runtime behavior—not old routing tables or historical papers—is authoritative.

### Expert Reading Notes

Read `oversight_router.py::process_event` for the complete router authority boundary, `oversight_events.py` and `oversight_daemon.py` for event/deadline observation, `triggers.py` for the separate activation/firing contract, and `oversight_actions.py` only for its remaining durable utilities. Historical documents that describe automatic Process Coherence invocation, seven directives, governed Process Runs, or `ORA_OVERSIGHT_LIVE` do not describe the installed system. Historical claims that all Trigger facilities were removed are also too broad: only the old process-bound Trigger surface is gone.
## 13. Execution-Review Architecture

### Problem

An orchestrator that can touch reality — spawn a shell, `git push`, fetch a URL, call an MCP server, run a project tool, write a file — has two failure modes that no amount of model self-narration can catch, because the model is the thing being trusted.

The first is the auto-approve escape hatch. Ora's dispatcher has a permission mode. When a user (or a background configuration) sets it to auto-approve, every tool call the model emits runs without a human in the loop. That is the intended ergonomics for a long analytical turn — you do not want to approve forty file reads by hand. But the same setting will carry an irreversible action (`rm -rf`, a force-push, a `curl | sh`) or a secret-sensitivity read (a credential file, a private key) straight through to execution. The permission mode is a single dial, and turning it to "fast" turns off the brake on the one action class where the brake matters most. This is a privilege-escalation-by-convenience race: the dangerous call and the harmless call travel the same path, gated by the same dial.

The second is the reviewer grading the wrong artifact. When a downstream verifier — human or model — reads a turn's output and sees "I verified the tests pass and the migration is safe," it is reading a *producer claim*, a sentence the producing model wrote about its own work. A fluent claim out-argues a silent failure. If the model never actually ran the tests, the claim is indistinguishable in the text from the case where it did. The reviewer grades the model's homework using the model's own answer key. Without an independent record of what the turn *mechanically did* — what it mutated, what sources it read, what left the machine, how sensitive any of it was — there is no ground truth to check the narration against. The model grades itself, and confident prose wins.

Execution Review is the always-on instrumentation-and-gating layer that makes what a turn did to reality mechanically observable, so that neither failure mode depends on trusting the narration. I will define the load-bearing terms as they arrive; the first is *turn*, meaning one user-input-to-final-output cycle of the pipeline, which is the unit everything here is scoped to.

### Design Decision & Tradeoffs

The architectural move is to split what-was-declared from what-was-observed onto two clocks, and to make the before-clock deterministic and fail-closed.

**Two clocks.** The *before-clock* runs before the turn executes: a deterministic risk classifier assigns a *risk tier* (one of `light` / `standard` / `high-risk` / `irreversible`, an ordered severity estimate computed with no model call), and a fail-closed capability gate stands at the single point where the model's tool calls enter execution. The *after-clock* runs after the turn: it folds the mechanical tool-event log — one JSON line recorded per action at the invocation boundary, never from narration — into a small set of observed signals, and compares them against what the before-clock declared. Declaration and observation never share a code path, so a divergence between "I said this was a read-only turn" and "the log shows an external write" is structurally recordable rather than something the model can paper over.

**Fail-closed at the single chokepoint, independent of permission mode.** The gate lives at `dispatcher.py:735`, the one model-facing call site through which every dispatched tool passes. It runs *before* the existing permission check and *independently of the permission mode*. An action classified as irreversible, secret-sensitivity, or — critically — *unknown* is blocked regardless of whether the dispatcher is set to auto-approve. The rationale is the Unix principle of a chokepoint: you enforce an invariant at the one place all traffic crosses, not at every caller. The manifest's default for an unclassified action is fail-closed: unknown means irreversible-and-secret means blocked (`FAIL_CLOSED`, `tool_events.py:101-106`). The tradeoff is real and I will not soften it — a novel tool with no declared axes is refused until someone classifies it, which trades convenience for the guarantee that auto-approve can never carry an unaudited capability. That is the correct trade for this class of risk; a system that fails open on the unknown case has no gate at all.

**Structural separation of evidence from judgment.** The after-clock's output is a packet whose lanes come in two kinds, and the separation is enforced by the type system rather than by discipline. An `EvidenceLane` is a mechanical, verifiable check that yields a real verdict. A `JudgmentLane` is interpretive — a taste call — and *by construction has no `verdict` or `sufficient` field to hold a verdict* (`execution_packet.py:81-88`). Taste cannot masquerade as a passing check because there is no slot for it to occupy. The renderer then orders the packet evidence-first and producer-claim-last, and labels the producer claim as unverified text (`render_for_review`, `execution_packet.py:236`). The reviewer sees what the turn mechanically did before it sees what the turn said about itself.

**The rejected alternative: a parallel review pipeline.** The obvious design is a second Gear-3/Gear-4 adversarial pass that reviews the first turn's work — a separate reviewer stage with its own model calls. I rejected it. It doubles the pipeline surface, it invites the reviewer to re-derive the producer's claims from the same text (grading the homework again, one layer up), and it is expensive. Instead the packet is a *richer form of the existing terminal deliverable*, built at the same `run_pipeline` terminal the deliverable already exits through — same machinery, richer output. There is one construction site per gear surface, not a parallel apparatus.

**The rejected alternative: model-driven risk classification.** I also rejected asking a model to rate the turn's risk. A model-scored tier reintroduces the original problem — the thing being gated writes its own gate input — and adds latency and nondeterminism to the before-clock, which must run on every turn. The before-clock is therefore a deterministic pattern classifier: turn-head regex floors (Stage A) plus an optional mode-declared `## RISK TIER` heading (Stage B), no model call (`classify_tier`, `risk_gate.py:293`). The tradeoff is that a regex table is coarse and will misclassify novel phrasings. I accept that and mitigate it two ways: the tier is a *floor* that fails safe upward (the failsafe tier on any classifier error is `high-risk`, `risk_gate.py:67,337`), and the whole Stage-A pattern table is flagged in-code as PROVISIONAL and un-calibrated (`risk_gate.py:20,211`). It is retunable, not settled.

**Catalog versus contract — the distinction that keeps a green run honest.** The deepest design commitment sits at the boundary between what is landed and what is not. A *catalog* (`.ora/evidence.yaml`, Phase 5) says what checks *exist* in a repo. A task-specific *Evidence Contract*, produced at planning time, says what checks *this task* must pass. The distinction is load-bearing: a green evidence run means nothing you knew to check broke — it does not mean the right problem was solved. Conflating "all declared checks passed" with "the task succeeded" is the same category error as the fluent producer claim, one level up. The architecture keeps them separate so that an empty evidence-lane list is treated as *not sufficient* — evidence owed — rather than vacuously passed (`all_evidence_sufficient`, `execution_packet.py:97-105`). This is the honest-by-construction stance; it is also, as the next section makes plain, the part that is designed and implemented-in-a-worktree but not merged and not installed.

**Open problems.** The `enforcement_model` label — which says whether a gate actually constrained the action or merely observed it — is hardcoded `in_harness` in the packet builder with an explicit TODO (`execution_packet.py:352-360`). It is honest for a core in-harness gear turn and wrong for any orchestrated, opaque-shell, or MSI-custom path that later produces a packet; correct enumeration is owned by a future phase. The declared-versus-observed output-type consistency note is wired end-to-end but dormant, because no mode declares an `output_type` — it stays `unknown` on every live turn (`consistency_note`, `execution_packet.py:155-174`). And the risk-tier floors are provisional. None of these are latent bugs; they are named incompletions.

### Implementation

The subsystem is three modules plus two shared substrates, wired at two lockstep seams. I will braid the why into the how.

**Phase 1 — recorder, manifest, gate (`orchestrator/tool_events.py`, gate wired at `dispatcher.py:735`).** Three things share this module deliberately, because they share one lifecycle — the always-on instrumentation substrate. First, the *capability manifest*: three orthogonal axes layered on top of the dispatcher's existing function axis without touching it — `mutability` (`read` / `reversible_write` / `external_write` / `irreversible`), `sensitivity` (`public` / `private` / `sensitive` / `secret`), `egress` (`none` / `local` / `external`) — plus an `enforcement_model` label and the fail-closed default for unknown actions (`tool_events.py:87-106`). Second, the *recorder*: `record()` writes one JSON line per action invocation, mechanically, at the boundary — never from narration. It writes to the active pipeline-trace turn directory when a turn is live, else to the global sink (`data/tool-events.jsonl`, resolved via `runtime_paths`). The write uses `os.open` with `O_APPEND` for atomic concurrent appends (`tool_events.py:563`). Recording is two-layer redacted: a per-path sensitivity check plus a content scrub, and a secret-sensitivity event is reduced to existence-only (`_redact_for_record`, `tool_events.py:463`). Third, the *gate*: `gate()` (`tool_events.py:836`) runs before execution and independently of the permission mode. A blocked action is denied immediately and queued to the oversight Paused queue; approval mints a one-shot token that lets exactly one re-issued matching call through (`grant_approval` / `check_and_consume_approval`, `tool_events.py:610,738`). At `dispatcher.py:735` the call site invokes `tool_events.gate(...)` and, when `decision.allowed` is false, returns `decision.message` — the model gets a gate refusal string, not the tool's effect.

The failure contract here is the reliability spine: recording is best-effort and never raises, but is never silent — a failed write sets a telemetry-incomplete flag that is stamped onto every subsequent event and surfaced via `get_telemetry_health()` (`tool_events.py:422-454`); failures also ride the oversight bus through `_note_failure`. Gate decisions fail closed regardless of whether their recording or queueing succeeded. `ORA_TOOL_EVENTS=off` disables the *write* for test suites — but the docstring is explicit and the code enforces it: the gate still runs and still fails closed even with recording off (`record` docstring and guard, `tool_events.py:508-520`). This is the component that prevents the auto-approve-carries-a-dangerous-action failure mode. The failure surface is qualitative and only partly instrumented: a write failure is counted and flagged (telemetry-incomplete), but there is no success-rate or per-step reliability figure — none is instrumented, and the guarantee is the named contract (never-raises, never-silent, fail-closed), not a measured number.

**Phase 2/3 — tier, hold, criteria, after-clock fold (`orchestrator/risk_gate.py`).** The before-clock is `classify_tier` / `assign_tier` (`risk_gate.py:293,710`): Stage A matches turn-head regex floors seeded from the gate vocabulary (bash irreversible flags, `curl`/`wget` egress), Stage B reads an optional mode `## RISK TIER` heading; the higher floor wins, irreversible over external-write, with no model call. An `irreversible`-tier turn is *held before the executor runs* unless a valid task token is present — `evaluate_hold` (`risk_gate.py:814`) returns a plain-text pause reply and writes a durable Paused card, and it fails closed to a hold on any error. This is the hard boundary: post-hoc routing is permitted only in reversible tiers, so an irreversible action is stopped before it happens, not audited after. `apply_criteria` (`risk_gate.py:935`) runs an acceptance-criteria pass for standard-and-above tiers, failing closed to `WARN` (standard) or `HOLD` (high-risk and above) rather than silently dropping to light execution.

At risk-gated entry points that have both an exact caller string and a cleaned or clarification-enriched request, `assign_tier` classifies the two forms independently under the same mode floor, sticky tier, trivial-text status, and explicit `/risk` override, then enforces whichever result is more severe. Cleanup therefore cannot erase a risk signal. The exact caller string also binds an irreversible hold and its one-use approval token: case, whitespace, and wrapper text remain identity-bearing, so a token cannot admit a merely similar request. Downstream criteria and execution, and each path's existing trace and hold-description behavior, otherwise remain unchanged. Foreground, server, direct/fallback, typed and picker-selected Framework, and clarification-resume paths share this dual-form rule; a picker-selected Framework also remains part of the existing structural identity. Synthesized paths that genuinely have one risk input retain single-form classification. This adds no approval store or alternate gate—the existing Paused card and token lifecycle is unchanged.

The after-clock is `fold_route_observed` (`risk_gate.py:548`), which reads the tool-event log scoped by `conversation_id` and turn timestamp and produces the observed signals: `any_mutation` and `max_mutability` (Phase 2's signal 1), and `source_read_suspected` with its channels and public-safe candidate reads (Phase 3's signal 2 — did the output make claims that imply it read a source it did not disclose). `record_route_observed` (`risk_gate.py:983`) is the terminal recorder that writes the divergence. Note one subtle threshold: `source_read_suspected` is driven partly by whether the output is *substantive*, gated on `_MIN_SUBSTANTIVE_OUTPUT_CHARS = 24`, and the candidate list is capped at `_MAX_SOURCE_CANDIDATES = 64` — both flagged PROVISIONAL in-code (`risk_gate.py:114-128`).

**The single-fold contract.** `record_route_observed` folds the log *once* and returns the signals; the terminal then builds the packet *separately* from those returned signals. There is no second fold. Any refactor that folds again double-counts mutations — this is called out as a drift class and it is worth internalizing before touching either seam.

**Phase 4 — lanes, packet, renderer (`orchestrator/execution_packet.py`).** `route_lanes` (`execution_packet.py:174`) takes the already-folded signals and routes them into `EvidenceLane` / `JudgmentLane` instances. `EvidenceLane.sufficient` is tri-state — `None` means unfilled, which means evidence-owed, which means *not* sufficient; `True`/`False` only once a runner judges it (`execution_packet.py:65-77`). `JudgmentLane` structurally omits the verdict field. `build_execution_packet` (`execution_packet.py:322`) consumes the folded signals plus the in-gear text-review verdict threaded on `context_pkg['execution_review']` and assembles a polymorphic `ExecutionPacket` with reserved blocks for fields a later phase fills. `construct_and_write` (`execution_packet.py:436`) writes `execution-packet.json` trace-local.

**The two wiring seams, in lockstep.** The before-clock and after-clock are invoked at both primary gear terminals. In `boot.py` (foreground/terminal): `assign_tier` (~9279), `evaluate_hold` (~9289), `apply_criteria` (~9304), `record_route_observed` (~9296, ~9416), `construct_and_write` (~9429), plus the task-gate reply handler (~9123). In `server/app.py` (background/server): the same sequence (~2837 / 2845 / 2857 / 2852 / 2968 / 2980, task-gate reply ~3113). The two seams must stay in lockstep — a change to one that is not mirrored in the other silently splits foreground and background behavior. (`server.py` carries additional worker and dirty-state entrypoints, ~3264 and ~3737, that wire the same functions; when auditing coverage, grep the function names, not the offsets — the line numbers here are at 7a5e8f40 and drift with edits.)

**Config surfaces.** `ORA_TOOL_EVENTS=off` (disable recording, gate still fires); `ORA_RISK_TIER` and the `/risk <tier>` sticky-plus-override and `## RISK TIER` mode heading (tier control); `ORA_STEALTH_CONTEXT` (stealth suppression at write, with gate decisions exempted so a blocked action is still logged existence-only, `tool_events.py:524-541`); `config/mcp-servers.json` axes and `config/capabilities.json` slots (the manifest source). The manifest fail-closed default and the PROVISIONAL tuning constants are the surfaces a tuner will touch first.

**Normal-operation vs failure behavior, summarized.** On a normal turn: tier assigned, no hold, tools gated per-call, log written, after-clock folds, packet written trace-local. Only an acceptance-kind verifier item becomes an obligation: it blocks `criteria_met`, enters the existing revision route, and must be corrected, resolved or waived through the existing Paused authority, or escalated at the fixed stop; diagnostic, exploratory, and preference suggestions remain advisory. Required human handback uses the existing redacted, reference-only Paused path and fallback; only a path that accepts it counts as delivery. If both fail, Ora rewrites the existing `ExecutionPacket` with `human_handoff.status: pending`, `retryable: true`, and `human_reached: false`, then returns the response with a visible notice that no human was reached and the handoff remains retryable; it never claims success or adds a waiver store. On a gate block: the tool never runs, the refusal string returns, the block is queued and logged. On an irreversible turn: held before execution with a Paused card. On any recording failure: work proceeds, telemetry-incomplete flag set, health surface reflects it. On any classifier error: failsafe to `high-risk`. Nothing in this subsystem raises into the user's turn; everything degrades to safe-and-noted. These are contract guarantees, not measured reliability rates — no success-rate is instrumented.

### Evidence & Verification

The subsystem carries 206 dedicated unit tests: `test_tool_events.py` (52), `test_risk_gate.py` (93), `test_risk_gate_pipeline.py` (6), `test_execution_packet.py` (55), counts confirmed at 7a5e8f40. The portability retrofit added `test_portability.py`, now 65 tests, of which PR #181 contributed +17 Windows-simulation cases.

The landing gate is a full-suite parity diff, not a green-checkmark. Each phase ran the entire test suite and compared a sorted list of FAIL/ERROR names against a recorded environmental baseline of 22 failures and 5 errors (`baseline-failures.txt`, 27 lines), requiring *zero new* failures — the same 22F/5E set, name-for-name. PR #181's own commit message records the result at 7a5e8f40: 3738 tests, 22F/5E identical to baseline, zero new. This is the right discipline for landing instrumentation into a large tree: you prove you broke nothing, against a known-imperfect starting point, rather than pretending the tree was green.

**How to confirm the gate is live.** `dispatcher.py:735` calls `tool_events.gate` and returns `decision.message` when the decision is not allowed — a blocked call visibly returns a gate string instead of a tool result. The fail-closed default is confirmable at `tool_events.py:101-106`. The single-fold contract is confirmable by reading `record_route_observed` (returns signals) against the terminal's separate `construct_and_write` call.

**Explicitly untested surfaces — stated plainly.** `render_for_review` and `_format_artifact_for_review` are TESTED-BUT-UNWIRED: the docstring says so, and it is true — no live verifier consumes a packet. They are proven only by a test that feeds their output into the evaluator prompt shape (`execution_packet.py:14-30,236-243`). The evidence-first review the architecture exists *for* does not run in production; only the packet is built and written trace-local. The §6 consistency note is dormant on every live turn because `output_type` is `unknown` — a doc must not imply misroute detection is active; it is exercised only in tests. The evidence lanes are never sufficient, because the runner that fills them is not merged (below). And every Windows and Linux behavior is *simulation*-tested via `test_portability.py` — `ntpath`, `PureWindowsPath`, `os.name` monkeypatching, subprocess env assertions — and has never been run on a native Windows or Linux host. Simulation coverage is real coverage of the branching logic; it is not evidence the code runs off-Mac.

**The Phase-5 boundary, stated exactly.** Phases 1–4 are landed on `~/ora` main (current tip 7a5e8f40, PR #181). Phase 5 — `evidence_runner.py`, the `.ora/evidence.yaml` catalog reader, live sandboxed check execution, the planning-stage Evidence Contract, the dirty-state modes — is **implemented in an unmerged worktree at the code-review gate; it is not committed, not merged to `~/ora` main, and not installed.** The HANDOFF's top block records it as "DESIGN APPROVED + IMPLEMENTED 2026-07-04," implemented in `~/ora-worktrees/phase5-impl` on branch `execution-review-phase5-impl` off `82553162`, with its own `test_evidence_runner.py` (58 tests); the HANDOFF's explicit standing instruction is "NO commit until [the code-review re-review] approves." Its Evidence-Contract wiring at the planning seam is described in the worktree's own material as flag-gated (`ORA_EVIDENCE_CONTRACT`, default OFF) in one revision and as running unconditionally in a later revision — an unresolved internal detail of unmerged code, not installed behavior either way. I verified that `orchestrator/evidence_runner.py` is absent from the pinned tree at 7a5e8f40 (`git ls-files` at that commit returns nothing). (Phase 5 has since merged post-pin as PR #185, one commit past 7a5e8f40, which adds `evidence_runner.py`; this document describes the system AT 7a5e8f40, where it is not present.) So on the *installed* system, Execution-Review coverage ends at Phase 4. The Phase-5 runner vocabulary is pre-seeded into the landed tree — `EVIDENCE_RUNNER_DEFAULTS` (`tool_events.py:983`), `validate_check_declaration` (`tool_events.py:994`), and `evidence.yaml` in `_PROTECTED_BASENAMES` (`tool_events.py:267`) — but I confirmed these have *zero non-test consumers* in the landed code. They are a landed vocabulary with no landed caller. Do not read `phase5-impl.diff` or the `phase5-impl/` worktree as installed behavior.

### Expert Reading Notes

Inspect these first, in order, because this is where a confident reader goes wrong.

**The implemented-versus-landed line.** Before believing any Phase-5 claim, grep the landed tree for consumers of `EVIDENCE_RUNNER_DEFAULTS` and `validate_check_declaration` — there are none outside tests — and confirm `orchestrator/evidence_runner.py` is absent from git history. The runner exists only in an unmerged, uncommitted worktree at the code-review gate. Internalize the catalog-versus-contract distinction: a green evidence run means nothing you knew to check broke, not that the right problem was solved. A reviewer who forgets this will over-trust the very packet the architecture built to be honest about its own limits.

**The two lockstep seams.** `boot.py` (~9123–9432) and `server.py` (~2837–3113, plus the ~3264 and ~3737 worker/dirty-state entrypoints) both wire the before-clock/after-clock/packet sequence. They must move together. A change to one seam that is not mirrored in the other silently diverges foreground (terminal) from background (server) turns — the exact race the chokepoint design was meant to eliminate, reintroduced at the wiring layer. Grep function names, not line offsets; the offsets moved between 82553162 and 7a5e8f40 and will move again.

**The MSI-custom-gear bypass.** MSI's custom gear paths (`msi_run_gear4`, `article_generator`, `backfill`, `gear3_orchestrator`) build their own prompts and do not flow through the `run_pipeline` terminals. Risk tiers and packets therefore do not reach MSI-authored turns. This is a known drift class documented in the project's own convention notes; do not document Execution-Review coverage as universal without checking whether a given surface actually crosses one of the wired seams.

**The reversible-flag gotcha — do not "fix" it.** The `reversible=(tier != 'irreversible')` flag is the post-hoc-routing gate, not a consequence measure. Post-hoc routing is permitted in reversible tiers; irreversible is held before the executor. So `high-risk == reversible` is *correct* — a high-risk turn is dangerous but routable-after, while only irreversible is held-before. A reader who "corrects" high-risk to non-reversible breaks the routing model.

**The enforcement_model hardcode.** `build_execution_packet` hardcodes `enforcement_model: "in_harness"` with a TODO (`execution_packet.py:352-360`). It is honest for a core in-harness gear turn. The moment an orchestrated, opaque-shell, or MSI path produces a packet, that label is wrong until the future-phase enumeration lands. This is where the next change most likely introduces a false claim.

**The PROVISIONAL surfaces.** The Stage-A pattern table, `DEFAULT_TIER='standard'`, `FAILSAFE_TIER='high-risk'`, the token TTLs, `_MIN_SUBSTANTIVE_OUTPUT_CHARS=24`, and `_MAX_SOURCE_CANDIDATES=64` are all flagged in-code as un-calibrated. Treat them as retunable defaults, not settled thresholds, and retune from data rather than trusting the current values.

**The namespace collision.** `server/review.py` is *not* this subsystem. It is an unrelated Phase-10 document-extraction human-review web app (localhost:5005, `data/review-queue/`) that imports none of `tool_events` / `risk_gate` / `execution_packet`. The name collides with "Execution Review"; the code has nothing to do with it. Do not wire the two together, and do not cite `server/review.py` as evidence of anything here.

Finally, the primary architecture map lags the code: `CLAUDE.md` has no dedicated Execution-Review section. The authoritative sources are the landed code and the `~/ora-worktrees` spec and HANDOFF. Where those disagree with `CLAUDE.md`, trust the code.

### Platform compatibility

The primary tested target is macOS on Apple Silicon (M4 Max, macOS 26.3). Every non-macOS cell below is either simulation-tested or untested; none is "works" off-Mac without the qualification stated.

- **Check-execution sandbox backend (`code_execute._sandbox_backend`, `code_execute.py:40-48`).** macOS: works — `sandbox-exec` is the only implemented backend and returns `enforcement_model: orchestrated`. Windows-native: needs-porting — `_sandbox_backend` returns `None` off-darwin, so `code_execute` fail-closes/gates. WSL: needs-porting (same off-darwin path). Linux: needs-porting (same). Phase 5's Rev-2 design proposes an `ORA_EVIDENCE_SANDBOX` wrapper plus native Linux `unshare -rn`, but that is in the unmerged worktree, not installed.
- **Direct `bash_execute` argument-vector execution (`bash_execute.py`, `PreparedCommand`).** macOS: works. Windows-native / WSL / Linux: untested. The command is never passed to a shell: platform-appropriate narrow quoting is parsed into one argument vector, shell operators and expansion are refused, and the same reduced environment and executable identity used for classification are rechecked before `shell=False` spawn. Windows parsing is covered by simulation tests but has not been run on native Windows; WSL/Linux use direct process execution and do not require a POSIX shell for this path.
- **Advisory file locking on approvals and sticky JSON (`runtime_paths.locked_file`, `runtime_paths.py:100`).** macOS: works — POSIX `fcntl.flock` branch. Windows-native: untested — the `msvcrt.locking` branch exists and is guarded, but is simulation-tested only, not run native. WSL: untested. Linux: works — POSIX branch.
- **Protected-path and secret/sensitive regex matching (`tool_events._matchable` / `_cmp_key`; `runtime_paths.within_base` / `within_any_base` hardened in PR #181).** macOS: works. Windows-native: untested — forward-slash-plus-normcase folding and the boundary-anchored path helper are coded and Windows-simulation-tested (`ntpath`/`PureWindowsPath`), not run on native Windows. PR #181 fixed a real prefix-sibling boundary bug in the live file read/write gates (`dispatcher.validate_path` and `file_ops._validate_path` treated a mere-prefix sibling directory as "inside base") that was latent on POSIX and active on Windows, replacing raw `startswith(base)` with the shared boundary-anchored helper. WSL: works. Linux: works.
- **Trace-local sinks and packet paths (`record()` `os.open(O_WRONLY|O_APPEND|O_CREAT, 0o644)`, `tool_events.py:563`).** macOS: works. Windows-native: untested — `O_APPEND` is cross-platform, but the `0o644` mode argument is a no-op on Windows; not run native. WSL: works. Linux: works. All roots resolve via `runtime_paths` (no hardcoded `/Users/...` remains in the subsystem after PR #181's slash-command and server.py cleanups).
- **SCRATCH_DIR containment for sandboxed check execution (Phase 5, unmerged).** macOS: the per-run read-side re-allow is macOS-`sandbox-exec`-specific and is the only mechanism that works, and it is not landed. Windows-native: needs-porting. WSL: untested. Linux: untested. Off-Mac containment is entirely Phase-5 design-and-worktree, not installed.
- **MLX/Metal local inference** is Apple-only and sits upstream of this subsystem; Execution Review itself is pure Python and does not touch MLX, but the enclosing orchestrator is Apple-Silicon-tested and untested elsewhere.

The net statement: on the installed system at 7a5e8f40, Execution Review's enforcement (the `code_execute` sandbox) is macOS-only; its recording, gating, tiering, and packet logic are pure-Python with cross-platform *code* whose non-macOS branches are simulation-tested only. No non-macOS surface has been run native. Cross-reference the reference spec *Ora Execution Review Architecture* for the full build spec, and the platform substrate is shared with the roots-and-locking layer described in the runtime-paths portability discussion.

## 14. Operation-Matrix (Mission / Objectives / Milestones)

### Problem

A project needs a durable strategic hierarchy: a Mission that states why the work exists, Objectives that name the focal areas the work organizes around, and Milestones that are verifiable, cadence-specific checkpoints a third party can confirm. The project-management modal's "Mission & Goals" tab has to read and write that hierarchy. Three failure modes threaten it.

First, drift. If the strategic content lived in two places — a copy in the project's JSON record and the "real" one somewhere else — the two copies would diverge the moment either side is edited out of band, and the system would then have to decide which copy is authoritative. That reconciliation is a standing source of bugs: the same class as a cache that outlives the data it caches.

Second, clobbering. A project's Operation-Matrix is a hand-authored Obsidian markdown file. Beyond the three MOM sections it carries Obsidian Bases queries, a `## Problem Solving` section, a `## Spawned Activity Registry` table, and whatever else the user put there. A naive "read the fields, rewrite the file" edit destroys every one of those sibling sections. The write must touch only the bytes it owns.

Third, the vault-less environment. The canonical vault lives on the user's Mac. Cloud-ora — the headless Linux server that runs the news pipeline — has no vault at all. Any code path that reaches for the matrix on the server must degrade to "nothing here" rather than raise, or every server-side call site that touches a project inherits a crash.

This subsystem — `orchestrator/operation_matrix.py`, its Tasks-specific companion and the project routes in `server/app.py` — owns strategy reads/writes and the separate basic Matrix Tasks service. Both use the original Markdown, preserve unrelated content and report unavailable storage. The Tasks and shared-writer paragraphs below are scoped P3 source currency; they do not establish live activation or requalify unrelated historical chapter claims.

The Project Plugin boundary remains `project_registry.py` plus `ora-project.json` manifests, documented in [[Reference — Ora Project Plugin Convention]]. Operation-Matrix owns Matrix content, including Tasks, while project plugins expose project-specific capabilities. Sharing nexus and pointer identity does not merge those responsibilities.

### Design Decision & Tradeoffs

The decision: the vault markdown file is the single source of truth for MOM, and the project JSON record stores no copy of it. There is no second copy, so there is nothing to reconcile.

The Matrix is a nexus-authenticated Markdown file under `<vault>/Matrix/`, with classification-specific strategic headings and a separate Tasks section when one exists. The modal reads the original file on open and writes provided strategic fields back there. The project pointer owns identity, display metadata, priority and profile preferences; it contains no second copy of Mission, Objectives, Milestones or task text. Its allowed-update fields must preserve that division. A conventionally named new Project Matrix is only one layout; Operation and Passion filenames and headings are not converted to it.

The alternative rejected: cache the MOM in the project JSON for read speed. It is the obvious optimization — the pointer file is already loaded to resolve the display name, so folding the MOM in would save a filesystem hit. It reintroduces exactly the drift the design exists to prevent, and it violates the vault-canonical rule the repository is built on: the vault is the source of truth for markdown content, and `~/ora` copies are operational, never authoritative (repo `CLAUDE.md`, "Vault Canonical Rule"). A JSON cache of vault prose is a second authority for the same bytes. That is the bug, not the feature.

Writes are section-targeted splices, not rewrites. `_replace_section` (`operation_matrix.py:195-206`) finds the region between a target `## H` heading and the next `## ` heading (or end of file) via `_section_bounds` (lines 143-158), and replaces only that region. Every other byte — frontmatter, Bases queries, sibling sections — is carried through untouched by string slicing: `text[:head_start] + block + text[section_end:]`. A missing MOM section is *inserted*, not appended blindly: `_insert_index` (lines 177-192) places it to keep Mission → Objectives → Milestones order and puts the whole MOM block ahead of any non-MOM section. This is the "leave every other byte untouched" guarantee, and it is the direct answer to the clobbering failure mode.

The tradeoff accepted: no read cache. Every `mom` GET re-reads and re-parses the file from disk. Resolution authenticates frontmatter nexus and the persisted physical folder identity, always scanning `Matrix/*.md` so a conventionally named file cannot conceal another file claiming the same nexus. A historical filename or a changed display name cannot choose authority. Duplicate claims are an explicit ambiguity error, not a reason to choose the first file. The scan's large-vault performance has no measured ceiling here.

Physical folder names are validated persisted identities, not freshly sanitized display names. An unusable legacy identity requires the existing explicit migration path; reads and Tasks edits do not silently rename it. Native Windows filesystem operation remains unqualified by this scoped source change.

### Implementation

The existing MOM, assist and file-index endpoints remain in `server/app.py`; the Tasks GET/POST routes add a separate narrow action contract, with Matrix logic held by Operation-Matrix.

**Read path.** `GET /api/projects/<nexus>/mom` obtains the current project record through `project_meta.read_project_meta`, validates its persisted `folder_name`, and calls `operation_matrix.read_mom` with that identity. Query display names cannot choose a file. The result includes both parsed `milestones` and raw `milestones_raw`, with `milestone_form` distinguishing Project checkboxes from Operation prose. Operation recurring and aspirational headings retain their separate source sections; Passion Practices and Directions of Travel are not converted to Milestones. The legacy Milestone parser and renderer normalize their structured form, so they are not a lossless Tasks editor.

**Resolution.** `resolve_matrix_path` accepts only a unique frontmatter-nexus match inside the Matrix directory and validates a supplied persisted folder identity without sanitizing it into a different path. Missing storage remains missing; a convention-name collision belonging to another nexus is never authority to overwrite it. `commons` and legacy `general` are synthetic and have no Matrix. The existing YAML parser determines frontmatter ownership; a missing parser or malformed declaration cannot authorize a guessed file.

**Write path.** `POST /api/projects/<nexus>/mom` accepts the existing Mission, Objectives and Milestones fields; persisted project metadata supplies physical `folder_name` and the human-facing title. Its classification gate requires schema-valid list-form `project_type`, including Project, Operation and Passion, rather than inferring type from filenames or restricting all saves to Projects. `write_mom` retains this sequence:

1. Refuse a synthetic Commons/default nexus. A request with no provided MOM fields returns the current read without restamping it.
2. Resolve the path; if missing, `create_if_missing` and a valid persisted folder identity permit the existing exclusive creation path. Compose and strictly validate its universal metadata, authenticated nexus, list-form Project classification, frozen filename/current display H1 and three empty MOM sections before making a directory or opening a destination. The owned template includes `tags`; optional-description absence warns. A missing vault is never manufactured, and a collision cannot replace another project's file. The project-pointer creation route itself creates no Matrix.
3. Splice each provided field. `None` leaves a section untouched; raw milestones win over structured milestones. Operation bodies return to their existing recurring/aspirational headings instead of being rewritten as Project checkboxes.
4. Maintain the existing `date modified` frontmatter convention without serializing all frontmatter.
5. Write atomically and re-read, returning the fresh MOM.

**Coordinated atomic writes.** `write_mom` holds the existing `runtime_hygiene.mutation_path_locks` lock across its complete reread, splice and replacement. Tasks uses that same path lock, so an overlapping cooperating MOM save cannot restore an older Tasks snapshot. `_atomic_write` delegates to the established `runtime_paths.atomic_write_bytes`, preserving mode instead of using a fixed temporary sibling. The existing MOM endpoint gains no digest parameter and retains its content/classification, exclusive-creation, missing-vault, collision, projection and no-field no-op semantics. This is cooperation among Ora writers, not a lock on Obsidian or a cross-application transaction.

**Best-effort degradation.** A missing vault or ordinary absent/unreadable MOM file returns the existing empty or `None` result, allowing a vault-less server to report unavailable storage. Ambiguous nexus claims and invalid persisted folder identity instead raise typed Matrix errors that the routes expose as a conflict requiring correction. Best effort does not mean silently choosing a file when authority is unresolved.

**Strict metadata and explicit inspection.** `orchestrator/project_documents.py` inspects proposed text without writing. Its typed refusal reports fields/reasons, not source prose or unrelated private paths. It requires leading unique-key YAML, universal fields and their shapes, valid calendar dates and the authenticated owner's identity rules; it preserves extensible types, specialized metadata and valid existing nexus identities. Optional descriptions warn without generation or backfill. New-document preflight does not gate old MOM/Tasks edits, migrate existing frontmatter or alter their shared locks and byte splices.

From an Ora source checkout, `python -m orchestrator.project_documents check --ora-root <existing-ora-root> --vault <existing-vault-root> --project <nexus>` checks only that persisted project's Markdown and authenticated Matrix. Alternatively use `--file <vault-relative-markdown-path> --owner <ordinary|matrix|output|chat>` instead of `--project`. Both roots and exactly one selection form are required. Selecting an owner requests checks, not authority to impersonate a producer. Ordinary notes use filename/H1 agreement; specialized rules require authenticated ownership. Unsafe, symlinked, unreadable or uncertain selections remain reported individually while healthy selected files are checked. Output names relative documents, fields/reasons, totals and completeness. Exit 0 means complete with no errors (warnings allowed), 1 means document errors, and 2 means incomplete selection/authority. There are no repair, restamp, index, model or persistent report effects.

**Explicit folder orientation.** `python -m orchestrator.project_orientation --ora-root <existing-ora-root> --vault <existing-vault-root>` prints a shallow Markdown preview. `--write` alone requests replacement of that vault's root `Directory Map.md`. Project records come from the selected Ora root. The map lists real immediate vault folders and registered project folders, not individual files or nested inventory. Configuration/trash/dependency exclusions, no-symlink/outside-root checks and stable case-insensitive ordering apply; missing registered storage is labelled unavailable.

Descriptions are literal existing guidance from `AGENTS.md`'s Key Directory Structure or a nonempty explicit description in an authenticated Matrix's frontmatter. Missing/invalid/unavailable descriptions say “No recorded description”; personal note bodies, Mission, models and Registry prose are not description sources. One multi-nexus Matrix snapshot supplies unique safe relative links, with per-project ambiguity/unreadability preserved rather than guessed. Larger-index links require an existing owner's actual safe path; the generator creates no file/title index, registry, cache or watcher.

The map includes universal metadata, `type: directory_map`, H1 `Directory Map`, generation time, covered root, shallow coverage and visible incompleteness. Preview has no writes; partial inputs produce partial output and an incomplete-result exit. A requested write validates proposed metadata and uses the existing atomic byte writer; an unsafe destination or an existing unrecognized user file is refused, and a failed replacement leaves the previous map intact. A generated marker inside the single file identifies replaceable output. Rebuilding is explicit, never startup-, export- or timer-driven, and the map's type does not change indexing policy. AI entry guidance explains missing/old maps while retaining the Registry's separate role. Map contents inherit source privacy and confer no provider-upload permission. Source acceptance uses synthetic fixtures only; live generation and activation remain separate operational actions.

**The mom-assist endpoint** — `POST /api/projects/<nexus>/mom-assist` (`server.py:4715-4787`) — is the one path that touches a model, and it is strictly read-only: it drafts suggestions into the modal's editable fields and never writes the matrix. It calls the cheap `sidebar` slot through the standard model seam (`_call_small_model_with_system`, lines 4660-4685) with a doctrine prompt (`MOM_ASSIST_SYSTEM`, lines 4616-4640) that encodes the going-concern rules from the [[Framework — Mission, Objectives, Milestones Clarification]]: Mission as a cycle-shaped Service Statement, every recurring milestone carrying a specific cadence, at least one maturity gate. The response is parsed into MISSION / OBJECTIVES / MILESTONES blocks by `_parse_mom_blocks` (lines 4688-4712), tolerant of code fences and case. The failure taxonomy is explicit and never 500: the Commons → 400 before any model call (lines 4729-4731), model unavailable → 503 (lines 4767-4770), unparseable draft after a one-shot retry → 502 (lines 4774-4777). The retry exists because small models occasionally wrap output in fences or skip a label; one retry, then give up cleanly.

**The files-index endpoint** — `GET /api/projects/<nexus>/files` (`server.py:4805-4846`) — returns a read-only index of the project's vault output folder (via `project_meta.list_project_files`), pins the Operation-Matrix file as a separate `matrix` entry, and attaches an `obsidian://open` URI to each entry via `_obsidian_uri_for` (lines 4790-4802). The default response remains the first 500 files; optional `offset` and `limit` parameters expose deterministic later pages, while `total`, `next_offset`, and completeness fields describe the one enumerated inventory. File management defers to Obsidian by design decision (Q2 LOCKED in the docstrings): Ora indexes and links, Obsidian edits. The sibling reveal action — `POST /api/fs/reveal` (lines 4914-4954) — is macOS-gated: it first sandboxes the path to the vault root or the Ora Exports/Resources folders (403 on escape, lines 4934-4941) and checks existence (404, line 4944), then returns 501 `reveal-in-Finder is macOS-only` on any non-Darwin platform (lines 4946-4948) before shelling out to `open -R` (line 4951).

**Config surface.** One knob: `ORA_VAULT_PATH` (or the alias `ORA_VAULT`) overrides the vault root, which otherwise defaults to `~/Documents/vault` (`vault_root`, `operation_matrix.py:44-52`). `vault_root()` is also imported by `export.py` and `nexus_rename.py`, so it is a shared primitive, not a private helper — a change to its resolution logic reaches beyond this subsystem.

**Optional Library File categories.** The existing project record accepts `library_file_categories` through its normal read/update owners. Each entry has a unique lowercase slug `id`, a nonempty `label`, and a nonempty `match` over nexus/type/tags/subtype. Exact scalar equality, all-required list membership and AND across fields apply only to the owning project. Malformed stored entries yield `library_category_warnings` and are omitted without hiding Files; malformed updates use the existing project-metadata error and preserve unrelated fields. No category editor or separate store is introduced.

**Canonical active-project read seam (current scoped foundation).** `operation_matrix.list_active_project_meta(pointer_dir=None, *, skipped_authority=None)` supplies Overview's Project priority and Tasks sources. The Overview request calls it once and shares its records and skipped-authority information. It calls `project_meta.list_project_meta(pointer_dir)` exactly once, then filters that returned sequence in place without sorting it again: synthetic default/Commons and every status other than exactly `active` are omitted. Valid nonnegative ranks sort ascending; equal ranks and the unranked tail use ascending immutable nexus. Activity timestamps remain recorded/displayed but cannot move projects, and display-name changes do not affect order.

`project_meta._priority_sort_key` owns that order for the project list, reorder response, Library project provider and active-project adapter; consumers do not sort locally. Existing project management remains the explicit rank writer, preserving identity validation and the omitted-to-unranked reorder contract. The fallback order changes once from its former recency basis when D1 takes effect; reading or rendering writes no metadata and adds no second priority authority.

Overview marks incomplete project inventory honestly. Its Tasks projection uses one per-call multi-nexus resolver pass, owned by Operation-Matrix, to apply the same frontmatter, persisted-folder, Commons and duplicate-claim rules as single-target resolution while scanning each Matrix once. Mutations still re-resolve the individual current authority. Neither helper creates persistent cache, another priority order or a waiting/blocked filter.

**Tasks section and insertion.** Tasks authority is one real case-insensitive level-two `Tasks` heading outside frontmatter, backtick/tilde fences and strategic projections, ending at the next real H2 or EOF. The scanner respects fence delimiter lengths, so examples stay literal. Duplicate Tasks headings and malformed or protected placement prevent the affected write rather than selecting the first section. Unfamiliar source remains inspectable; the service retains exact UTF-8 spans, newline style, existing checkbox spelling, comments, headings and opaque Markdown instead of reconstructing a simplified task list.

An authenticated Matrix with no Tasks section is a valid empty collection. Reading writes nothing. First successful Add inserts `## Tasks` after a well-formed strategic projection's END; otherwise after the full last recognized strategic section; otherwise at EOF with blank separation. Project anchors are Mission/Objectives/Milestones. Operations also recognize both recurring/aspirational headings and their legacy short forms; Passions use Practices/Directions of Travel with Mission/Objectives. This does not infer strategic meaning for unfamiliar headings, create MOM, convert classification or move other sections. Stray, duplicate or unterminated projection delimiters refuse insertion without changing content. Templates and `_new_matrix_text` are unchanged; Tasks never creates a missing Matrix or vault.

**Targets and preserved blocks.** A browser target is a server-validated source-position reference bound to the whole-file digest and resolved identity, not a label, persistent ID or unchecked list index. Duplicate labels remain distinct; old references expire with their snapshot. A movable task block includes its checkbox line, clearly attached indented continuation material and contiguous descendants with their continuations. Uncertain material stays section-owned and immovable. The group exposes literal source so a refusal can be understood without discarding or silently attaching that material.

**Task operations.** Each request performs one named operation with strictly typed arguments and rejects unknown fields, stale references and impossible placements before any write. A no-op returns current state without rewriting or restamping.

- Add inserts one incomplete task at an explicit root, sibling or child position. It follows unambiguous local style, otherwise using `- [ ] ` and two spaces for new nesting without normalizing existing lines.
- Edit changes only the complete checkbox-line label shown in the editor. Child tasks and continuation notes stay unchanged; inline notation stays unless deliberately edited. Embedded newlines are refused.
- Reorder carries the complete block among siblings under its current parent. Indent places it under the preceding eligible sibling; Outdent raises it one level; Promote takes it to the root. Only required indentation prefixes change, preserving relative descendant and continuation structure.
- Delete removes only a leaf without descendants or unfamiliar attached content. The refusal explains that children must first be moved/promoted or attached material deliberately edited in the original Markdown. It never deletes a subtree or silently promotes children.
- Complete changes only the selected checkbox and adds today's server-local `✅ YYYY-MM-DD` when undated, retaining an existing unambiguous completion date. Reopen clears that checkbox and its recognized completion marker. Neither cascades to children.
- Set/clear completion date owns only the recognized trailing marker, accepts an actual calendar date and leaves completion state unchanged. Other date-like text remains untouched. Ambiguous or conflicting completion metadata is preserved; only actions requiring ownership of that field are refused.

Root outdent, first-sibling indent, cycles, destinations inside the selected subtree, subtree splitting and ambiguous ownership are refused with an operation-level explanation while safe edits remain usable. This caution applies to the affected action, not a blanket exclusion of Operation or Passion layouts. Due dates, recurrence, dependencies, estimates, assignment, advanced queries, raw Tasks replacement and arbitrary-vault checkbox scans remain outside the built-in manager.

**Task write authority and outcomes.** Both Tasks routes reauthenticate a unique exact-nexus Matrix using the current persisted project/folder identity, authorized root and readable regular nonsymlink file. Mutation additionally requires writable state and the existing schema-valid list-form classification. Authenticated content with absent, scalar or invalid classification is read-only with a reason; unsafe or unresolved identity is unavailable. The client cannot supply a path, filename, classification, display name or replacement section as authority.

Under the shared path lock, mutation re-resolves the Matrix, rereads exact bytes, compares the expected full-file digest, validates the operation and rechecks identity/digest close to the mode-preserving atomic replacement. Only a real change updates the existing modified-date line convention; unrelated frontmatter is not serialized. A changed digest or rebound identity returns a visible conflict with `saved:false` and no write. After replacement the service rereads and authenticates the actual result before reporting success, returning the new group/digest and target correspondence. If replacement may have landed but confirmation fails, the outcome is unknown rather than a definite failure. The browser must refresh before another mutation after that uncertainty or an interrupted response, never automatically replay.

### Evidence & Verification

Twenty-four tests pass across three suites, run at HEAD `7a5e8f40`:

- `test_operation_matrix.py` — 13 tests. Resolution by name and by frontmatter-nexus scan; `read_mom` on an existing file and on a missing one (returns empty); the splice-preserves-siblings invariant (`test_write_mission_preserves_rest`); milestone round-trip both structured and raw, with `milestones_raw` winning; the Commons-is-synthetic no-op guard; matrix creation from template; missing-section insertion. Tests inject a `vault=` temp directory via `tempfile.TemporaryDirectory`, so no real vault is touched.
- `test_mom_assist_endpoint.py` — 7 tests. The degradation taxonomy: unparseable → 502, model-unavailable → 503, the Commons → 400; and block parsing of MISSION/OBJECTIVES/MILESTONES. The model is mocked.
- `test_projects_mom_endpoints.py` — 4 tests. The mom GET/POST HTTP endpoints end to end.

The "no drift" property is verifiable without running anything: read `project_meta.py:42-49` and `:200-203` and confirm no MOM field appears in either `_DEFAULT_SLOTS` or `_UPDATABLE_FIELDS`. If a MOM field is ever present in either, the design's central invariant is broken.

Explicitly untested surfaces:

- **The `_bump_modified` insert-before-fence branch.** `test_bump_modified` (`test_operation_matrix.py:162-166`) uses a fixture whose frontmatter already carries a `date modified:` line (line 27, `date modified: 2025-09-12`), so it exercises the `re.sub` *replace* branch (`operation_matrix.py:336-340`). The *insert* branch — when no such line exists, inserting before the closing `---` fence (lines 342-343) — is a live code path with no test hitting it.
- **The frontmatter-nexus fallback scan at scale.** `test_resolve_by_frontmatter_nexus` covers correctness against a small fixture; behavior and cost against many `Matrix/*.md` files are not exercised.
- **All non-macOS filesystem behavior.** Every test runs on macOS against a `tempfile` directory. `os.replace` atomicity, the `~/Documents/vault` default, and `_safe_name` sanitization are unverified off-Mac.
- **Real model draft quality.** The mom-assist tests mock the model. Whether a real `sidebar`-slot draft adheres to the strict three-block OUTPUT CONTRACT is not measured — only the parse and degradation paths are tested.

### Expert Reading Notes

Read `operation_matrix.py` and `matrix_tasks.py` together for resolution, strategic splicing, lossless Tasks operations and shared locking, then their project routes in `server/app.py`. The original Milestone parser's structured round-trip does not establish Tasks byte preservation; follow each service's actual path.

Before trusting the "no drift" property, open `project_meta.py` and confirm that `_DEFAULT_SLOTS` and `_UPDATABLE_FIELDS` carry no MOM field. This is the single most important architectural fact in the subsystem, and it is also the fact most likely to be silently broken by a future change: the natural instinct, when someone wants "faster MOM reads," is to add a cached field to the pointer record. That one edit voids the invariant. Watch that whitelist.

When debugging identity, inspect the persisted folder identity and exact nexus claims. Display names and historical filenames are not alternative authorities, and every resolver must retain duplicate-claim detection even when a likely filename exists.

Where changes most likely cause drift: `_replace_section` and `_section_bounds` are the byte-preservation core. Any edit to the heading-matching regex (`_H2_RE`, line 140) or the section-boundary logic changes what counts as "the section" and therefore what gets clobbered. The splice guarantee is a regex away from breaking. Add a preserves-siblings assertion to any test that touches those functions.

For portability, inspect persisted-folder validation and the shared lock/atomic helpers rather than introducing another filename sanitizer or writer.

### Platform compatibility

Every test in this subsystem ran on macOS against `tempfile` directories. The per-subsystem platform posture:

- **`vault_root()` default `~/Documents/vault`** (`operation_matrix.py:44-52`): macOS — works (exercised). Windows-native / WSL / Linux — untested. `os.path.expanduser` resolves `%USERPROFILE%` on Windows and `$HOME` on Linux, so the mechanism is portable in principle, but the `~/Documents/vault` default assumes a macOS-style Documents directory and has been exercised only on macOS. Overridable via `ORA_VAULT_PATH` / `ORA_VAULT`.
- **Shared lock and atomic replacement:** Matrix writers use the established runtime path-lock and atomic-byte helpers. P3's isolated source checks do not qualify native Windows, WSL or Linux filesystem behavior, and the advisory lock does not coordinate non-Ora editors.
- **Persisted folder identity:** physical names come from the existing portable identity validator, while display labels remain separate. An invalid legacy identity requires explicit migration; this source update is not a migration or native-platform acceptance.
- **`obsidian://open` URI construction** (`server.py:4790-4802`): macOS — works (exercised). Windows-native / WSL / Linux — untested. The URI scheme is cross-platform (Obsidian registers it on all desktop OSes) and the code uses only `relative_to` + `urllib.quote` with no OS-specific branch, but it was verified only on macOS.
- **PyYAML frontmatter parse** (`operation_matrix.py:36-39`, `82-88`): macOS / Windows-native / WSL / Linux — no platform concern. Pure-Python with a graceful `ImportError` fallback; no OS-specific behavior.
- **Reveal-in-Finder sibling endpoint** (`server.py:4914-4954`): macOS — mac-only by design (shells out to `open -R`). Windows-native / WSL / Linux — n/a: explicitly gated, returns 501 `reveal-in-Finder is macOS-only` on any non-Darwin platform. This is not part of the core MOM engine, but it is the reveal action wired to the files-index entries this subsystem produces; a Windows or Linux port would need `explorer /select` or `xdg-open` respectively.

## 15. Documentation-Code Parity & Vault-Repo Mirror Governance

### Current implementation and enforcement boundary

Documentation parity is now a same-task, five-repository obligation. The machine-readable ownership and discovery map in [[Reference — Documentation-Code Parity Configuration]] resolves code paths in the vault, Ora, ora-ai-app, ora-ai-org, and MSI to stable documentation surfaces. A behavior, user or operator contract, configuration, route, output, or material failure-mode change updates the owning vault canonical in the same coordinated task. When an owner names a section, that section—not an unrelated edit elsewhere in the same file—must change. A true no-impact change instead carries exactly one `Documentation-No-Impact: <surface-id>` trailer in that repository's final task commit, and the independent reviewer validates that claim. Duplicate, surplus, cross-repository, and otherwise unused trailers are rejected. Every unfamiliar production path still receives its repository's conservative catch-all owner, so it owes either that canonical disposition or reviewed no impact rather than escaping as “unmapped.”

The authoritative deterministic gate is Ora's read-only `scripts/verify-implementation.py --check documentation-integrity --verbose`, supplied with explicit worktree roots and base commits for all five repositories. It checks declared ownership, typed references, task dispositions, registered propagation, lifecycle invariants, and the exact accepted-finding baseline. A vault base from before activation is refused rather than treated as a bootstrap escape. Verbose output prints each root, base, and final head for the coordinator's later identity comparison. The gate never falls back to live checkouts, never runs `--check all`, and does not claim that prose is semantically accurate. The independent reviewer owns that comparison.

Registered Ora documentation mirrors are rebuilt by stripping the vault YAML block and its one following blank line; the rest remains byte-identical. This propagation covers Technical Documentation, Accessible Overview, Using Ora, and the Thinking Tools singleton. Registered public pages use the existing cross-site publisher and reverse-parity assertion. Seven local hooks are installed: a blocking `pre-push` hook in each of the five repositories, plus the Ora and vault fail-open `post-commit` framework-pair queue hooks. The pre-push hook requires the complete explicit five-root task context for an owned-code push. The post-commit audit classifies only the byte-exact accepted E-093 and Video/D35 states as accepted external findings; any new, changed, no-longer-exact, verifier, or queue-write failure remains red without blocking the commit. These are bypassable local controls, not remote certification. Git cannot make five repositories atomic, so the coordinator still withholds every participating merge until the combined gate and semantic review pass. Recovery is Git revert in reverse landing order; the hooks do not roll back changes.

> **Testing is evidence for the changed behavior, not a second project. The task scope lock's named check list is the exact testing ceiling: run every named check and nothing additional. Do not run a full suite, full build, benchmark, broad audit, `--check all`, or an extra reassurance check. If a newly discovered material risk cannot be judged by that list, stop and revise the scope with the user before running another check. Stop when the named checks pass and material review is satisfied. The completion report must list every check actually run.**

### Semantic-review and landing evidence

Before any merge, the coordinator holds one complete evidence packet: all five cumulative repository diffs; the global-instruction and Programming-skill changes; the affected surface identifiers; exact canonical-section changes; every no-impact trailer with its rationale; registered propagation results; the verbose five-root gate output; and the authorized focused-test output. The independent reviewer compares actual behavior with each changed owning section or expressly accepts that surface's no-impact rationale. An accepting review gives exactly one explicit verdict per affected surface. Missing evidence, a missing verdict, or a rejected surface cannot be converted into acceptance by a green mechanical gate.

After the gate and semantic review pass, the coordinator confirms each clean branch head still equals the head printed by the gate. A later correction invalidates the evidence and requires the affected focused checks, five-root gate, and review again. Only those exact reviewed heads may be pushed with `--no-verify`, and only to avoid repeating the DCP pre-push check that just certified them. If another pre-push protection exists, the bypass is not valid. All remote branches and pull requests must be ready before the first merge.

### Vault auto-sync backup transport

The macOS vault auto-sync path is backup transport, not certification of a coordinated code task. It preserves the existing order—local commit, `pull --rebase --autostash`, remote push—and uses `git push --no-verify` deliberately so the installed five-root DCP pre-push hook cannot strand ordinary vault edits on one machine. A failed commit, rebase, or push stays loud in the sync log and is retried only when the next exact vault event arrives; the code does not report a failed backup as successful.

This is a narrow transport exception, not a general manual-push instruction. It neither certifies the vault state nor exempts a later code-changing task from section ownership, propagation, the five-root gate, or independent semantic review. The next coordinated task evaluates any canonical state its changed surfaces consume.

### Problem

Ora keeps authoritative prose in the vault while loading operational copies from `~/ora/`. The vault markdown carries YAML frontmatter plus body; many runtime copies carry the body only. That split is deliberate—the runtime does not need Obsidian frontmatter—but the downstream file remains physically editable. Without enforcement, a developer can change a runtime copy for fast iteration and lose the edit at the next vault→Ora regeneration, or change the vault canonical without regenerating the runtime copy and leave Ora executing stale instructions. The Vault Canonical Rule fixes which side wins; the parity mechanism makes that direction enforceable rather than aspirational.

There is a second, worse failure mode than lag: documentation that describes behavior the code never implemented. A doc claims a RAG provenance score, or a verifier that edits the consolidated output, or a runtime-configuration file that was retired months ago — and the reader trusts it. This is the missing-feature class, and it is more corrosive than staleness because a stale doc is merely old while a fictional doc is actively wrong about what the system does. For a technical documentation project specifically — the deepest source of truth an engineer consults — a fictional claim is a defect that propagates into every downstream decision.

This chapter's own deliverable is subject to exactly this hazard. The technical documentation is meant to live as a vault-canonical file paired with a `~/ora/docs/` mirror. The instant that pairing exists, both copies become writable replicas of each other, and without a governing mechanism they will diverge the first time either side is edited in isolation. So the parity apparatus is not incidental to this document — it is the thing that keeps this document honest over time.

### Design Decision & Tradeoffs

The apparatus is two-tier, and the split is the central design decision. Some parity questions are mechanically checkable; most are not.

**Tier 1 — the Documentation-Code Parity framework specification.** The model-driven framework is specified but not installed or callable. Its five modes are DCP-Sweep, DCP-Audit, DCP-Reconcile, DCP-Specify, and DCP-Deprecate. A person may execute that specification for an explicit semantic audit or reconciliation campaign, but no schedule or hook invokes it automatically. A purely mechanical differ remains insufficient for semantic parity: a byte diff cannot determine whether documentation describes behavior the code never computes.

**Tier 2 — deterministic checks for mechanically decidable claims.** The focused `documentation-integrity` category owns the same-task, five-repository contract. The `drift` category owns exact body parity for 15 registered vault-to-Ora pairs, and `framework-pairs` owns the complete manifest-backed framework lifecycle. Narrow companion scripts still check capability-slot equality, router configuration equivalence, tracked model-refresh state, and portability hazards. These are exit-code contracts, not substitutes for semantic review. Where parity reduces to set equality, declared-reference existence, lifecycle state, or byte equality, code is cheaper and more reliable than a model; where meaning is at issue, deterministic tooling stops and the reviewer decides.

**Direction-of-truth is fixed: vault→ora/site.** The vault is canonical; the `~/ora/` and site copies are downstream mirrors. This removes merge ambiguity entirely. Drift always means the copy lags the canonical, never that the two disagree and a human must adjudicate which wins. A two-way-authoritative design was not chosen because it reintroduces exactly the merge-conflict resolution problem the fixed direction eliminates.

**Task enforcement is deterministic; semantic campaigns remain explicit.** The five-root finalization check follows the code-changing task itself. A full-state model-driven audit still requires an explicit campaign. There is no daily or weekly fallback.

**Open limit, stated honestly.** The ownership map and focused verifier make registered relationships mechanically checkable, but they do not measure semantic completeness. A clean gate means the declared surfaces, references, dispositions, lifecycle states, and derivatives are internally consistent. It does not prove that the documentation chose the right concepts or describes the behavior truthfully; independent review remains load-bearing.

### Implementation

**Implemented entrypoint and data flow.** The task coordinator identifies five explicit task worktrees and their pinned base commits, resolves every changed production path through the ownership registry, updates each owed named canonical section or records its exact no-impact trailer, and regenerates registered derivatives. After the approved focused checks and final branch commits, it runs `scripts/verify-implementation.py --check documentation-integrity --verbose` once as the authoritative finalization check. The command requires all ten root/base arguments; it does not infer related worktrees or fall back to live checkouts. The independent reviewer then receives the complete evidence packet and gives a semantic verdict per surface. Only after the coordinator confirms the current heads still match the printed heads does the bounded DCP-specific push and coordinated merge sequence begin. The gate is state-based and read-only. The separate DCP framework layers describe a model-driven campaign that is specified, not installed as a runtime framework.

**Installed enforcement.** `scripts/install-dcp-hooks.sh` manages seven hooks from tracked source: five blocking `pre-push` wrappers, one for each participating repository, and two fail-open `post-commit` audit wrappers in Ora and the vault. The installer requires five explicit repository roots, rejects duplicate repository identities, refuses to overwrite foreign hooks, and supports read-only inspection and managed removal. A pre-push wrapper exports its repository identity and then requires the task coordinator's complete roots and bases. The two post-commit wrappers run the narrower framework-pair audit and may update the existing queue; because they run after a commit exists, they cannot block that commit. Their signal is deliberately narrow: exact accepted E-093 and Video/D35 evidence is labelled accepted, while a changed accepted finding, a new finding, or a verifier/queue failure remains visibly unsuccessful.

**No scheduler.** Neither the deterministic task gate nor the five model-driven framework modes run on a clock. Historical Drift Reports, the Decision Log, the Escalation Queue, and the Run History under the resolved vault's `Administration/DCP/` directory are records from explicit campaigns and fail-open audits, not evidence of a live scheduled routine. Full-state semantic work remains an explicit campaign.

**The pairing convention** fixes the direction of truth at vault→Ora/site and registers each deterministic transformation rather than guessing from prose. The current `DRIFT_PAIRS` table contains 15 canonical/runtime pairs: architecture mirrors and selected operational framework copies. It includes `Registry — Signal Vocabulary Registry.md` under the correct Registry prefix, omits the retired runtime-configuration file, and has grown beyond the earlier architecture-only list. `ARCHITECTURE_PAIRS` remains only a compatibility alias for callers that imported the old name. The same verifier separately reads the complete framework-pair manifest and the ownership registry, so the 15 direct drift pairs are not presented as the whole documentation surface.

**The mechanical checks are deliberately distinct:**

*`documentation-integrity`* is the authoritative coordinated-task gate. It reads the five explicit worktrees at their five explicit bases, resolves changed code through the ownership/discovery registry, requires a canonical change or a complete no-impact disposition, checks typed references and registered derivatives, applies the accepted-finding ratchet, and confirms every required repository was read.

*`drift`* checks the 15 registered canonical/runtime bodies under the established normalization rule. *`framework-pairs`* checks the broader manifest-backed framework inventory, including paired, no-runtime-twin, and specified-not-built lifecycle states. These narrower categories remain useful diagnostics and post-commit audit inputs; neither replaces the five-root task gate.

The capability, configuration-equivalence, model-refresh, and portability scripts protect their own narrower contracts. They are supporting checks, not a second enforcement topology, and their presence or call sites do not determine whether documentation-impact review is active.

**Normal operation** is one passing verbose five-root result, one accepting per-surface semantic review, mechanical identity between current heads and the heads printed by the gate, the bounded push of those exact heads, and only then coordinated merges. Missing roots or bases, a pre-activation vault base, a missing or section-wrong disposition, a surplus no-impact trailer, a broken typed reference, stale propagation, an invalid lifecycle state, or an unaccepted baseline change fails the gate. An unreadable repository is a failed task contract rather than a clean partial result. The coordinator does not treat a green narrow linter as five-repository certification or a green mechanical result as semantic acceptance.

### Evidence & Verification

**Historical DCP records are not schedule evidence.** The vault's `Administration/DCP/` directory contains dated Drift Reports, a Decision Log, an Escalation Queue, and a Run History. Those artifacts show that explicit campaigns and audits ran. There is no DCP scheduler, recurring sweep, daemon, or clock fallback in the installed mechanism.

**The five-root contract is explicit in the verifier.** `documentation-integrity` is a dedicated category rather than an alias for `crossref` or an item smuggled into `--check all`. Its command-line parser requires `--<repository>-root` and `--<repository>-base` for vault, Ora, app, org, and MSI; supplying only part of that set is an error. The loader then verifies each base as a full commit, requires it to be an ancestor of the task head, and records every repository as read before evaluating the coordinated state.

**The direct drift registry is current.** `DRIFT_PAIRS` contains 15 entries. It names the Signal Vocabulary Registry under its real `Registry —` filename, contains no runtime-configuration pair, and covers both architecture mirrors and selected runtime framework copies. The legacy `ARCHITECTURE_PAIRS` name is a compatibility alias to this current table, not a second stale registry.

**The installed hook topology is visible in one source.** `scripts/install-dcp-hooks.sh` calls its hook manager five times for `pre-push`—Ora, vault, app, org, and MSI—and twice for `post-commit`—Ora and vault. Its `--check` mode compares each installed wrapper with the exact wrapper the current source would render. The pre-push source runs the focused documentation-integrity contract for an owned-code push; the post-commit source remains the narrower fail-open queue audit.

The post-commit source captures changed paths and the triggering SHA before unsetting the repository-local variables returned by `git rev-parse --local-env-vars`. This preserves the committing identity, including linked worktrees, while preventing inherited Git settings from overriding the verifier's cross-repository reads. Explicit Ora/vault roots, recursion protection, accepted-finding classification, and real failure warnings remain intact. If the variable list cannot be read, the hook logs an operational failure and returns its fail-open status without invoking the audit. Both installed post-commit wrappers execute this one tracked source.

**Enforcement boundary — stated plainly.** The seven hooks are installed local controls and can be bypassed. There is no remote cross-repository enforcement and no atomic five-repository commit. The explicit finalization check and coordinator merge hold remain authoritative even with hooks installed. DCP-Sweep, Audit, Reconcile, Specify, and Deprecate remain model-driven specification modes rather than installed runtime capabilities.

**This document is itself a registered parity surface.** `Reference — Ora Technical Documentation.md` is the vault canonical and `docs/technical-documentation.md` is its Ora body-only mirror. The ownership registry declares the `ora_body_only` transformation, and the focused gate strips the canonical YAML plus exactly one separator blank line before requiring every remaining byte, including the trailing newline, to match.

### Expert Reading Notes

**Inspect first: the ownership block and `documentation-integrity`.** The ownership/discovery registry defines what changed code owes; the verifier shows how that declaration is enforced against five explicit roots and bases. Read the `DRIFT_PAIRS` table next to understand the 15 direct body-parity relationships, but do not mistake that table for the full ownership surface.

**Registered facts outrank prose discovery.** The verifier reads the machine-readable ownership block, pair manifest, typed references, and propagation declarations. It does not search ordinary prose for filenames or code symbols and then pretend that matching text proves a relationship. A new surface or transformation belongs in the owning registry, after which the deterministic gate can enforce it.

**Body-only installed-documentation mirrors use propagation, not the 15-pair table.** Technical Documentation, Accessible Overview, Using Ora, and the Thinking Tools singleton are declared as `ora_body_only` surfaces in the ownership registry. The focused gate applies their exact frontmatter-stripping rule. Thinking Tools remains outside the framework-pair manifest because it is a Reference singleton, not an active Framework row. Keeping these surfaces out of `DRIFT_PAIRS` is a separation of mechanisms, not a workaround for a broken table.

**A passing gate is not semantic proof.** The scripts can establish that a declared canonical changed, that a typed endpoint exists, and that a derivative matches its source. They cannot establish that prose describes the right behavior, default, limitation, or failure mode. Independent review remains the only semantic judgment in the same-task path.

**Where future changes most likely cause drift.** Adding a new top-level code area without an owner, changing a transformation without regenerating its derivatives, or treating a no-impact trailer as self-validating. The gate catches the first two only when the registry truthfully describes the surface; the reviewer owns the third. There is no scheduler to catch work later, so the same-task gate is the protection.

### Platform compatibility

The installed topology is verified on macOS. The focused verifier no longer embeds the author's `/Users/oracle/...` checkout paths: its Ora root comes from `ORA_HOME` or the script's repository, its ordinary vault root comes through `runtime_paths`, and the authoritative documentation-integrity path requires five explicit worktree roots and base commits. That removes the former user-specific-root defect. Native Windows, WSL, and Linux execution of the complete five-repository gate and hook topology remains unverified.

- **Five-root verifier contract.** macOS: works. Windows-native / WSL / Linux: untested. Root arguments are ordinary resolved paths and Git operations use argument arrays rather than a shell command, but no off-Mac end-to-end result is claimed.
- **Hook installer and wrappers.** macOS: works. WSL / Linux: untested. Windows-native: needs-porting because the installed hooks and installer are POSIX `sh` scripts; Git for Windows may provide `sh`, but that path has not been verified.
- **Case-sensitive vault names.** macOS: works. Linux / WSL: untested and case-sensitive. Registered paths carry their exact `Reference —`, `Registry —`, `Framework —`, and `Specification —` spelling, so a mismatch fails rather than being silently normalized.
- **Supporting linters.** Their portability varies independently of DCP. A Homebrew interpreter example or a Mac-specific portability rule in a supporting script does not reintroduce hard-coded live roots into the five-root documentation-integrity contract.
- **Model-driven framework specification.** Platform-neutral as prose, but not installed or callable on any platform. No scheduler or platform-specific campaign runner is part of DCP.


---

# Part V — Operate & Extend

## 16. Update, Recovery, Troubleshooting & Diagnostics

### Problem

An install is the one operation that runs before the system it depends on exists, on a machine whose state you do not control. Four things go wrong in practice: the network drops mid-download, Python 3.11+ is missing or is the wrong interpreter, the model catalog comes back empty, or the OpenRouter key is bad. A naive installer that re-runs from the top on failure either redoes expensive work (catalog refresh, model registry sync) or wedges — and without a resume affordance the only recovery is a full reclone. That is the failure mode the recovery tooling exists to prevent: **irrecoverable partial install**.

There is a second, more honest problem: Ora has no packaged updater. There is no `.pkg`, no `.msi`, no Sparkle feed, no self-update code anywhere in the tree. "Update" is a `git pull` of the framework library followed by a re-run of the idempotent installer. I state this plainly because CLAUDE.md does not mention the update/recovery subsystem at all and its Commands section shows only `./start.sh` — the recovery surface is under-documented relative to its runtime weight, and pretending an updater exists would be the worse error.

At runtime, three independent things degrade silently. Models and endpoints fail intermittently — a 5xx, a transport error, a local model that will not load — and a router that keeps hammering a broken endpoint just converts one failure into many while burning retry budget. That is **retry-storm-against-a-dead-endpoint**. A pipeline step can produce structurally-wrong output — the cleanup model narrates instead of emitting a cleaned prompt, and the rest of the pipeline runs against the narration — which is **silent semantic degradation**: the user gets a confident wrong answer with no signal. And the observational daemon can stop beating, leaving event/deadline liveness unreported — **unnoticed-observer-failure**.

Finally, Ora is macOS-Apple-Silicon-first. Unguarded Homebrew-Python shebangs, `os.fork` detach paths, bash-subprocess index regeneration, and an MLX-Metal heartbeat all break on a Windows-native port — and they break *quietly* unless something enumerates them. That is **silent-Mac-assumption-breaks-the-port**.

Each of these gets a dedicated, named safety net. None of them is a dashboard.

### Design Decision & Tradeoffs

**Idempotent state-file installer over transactional rollback.** `scripts/install.py` runs an ordered pipeline of nine steps — `preflight`, `dependencies`, `converters`, `profile`, `catalog`, `registry_sync`, `autopopulate`, `smoke_test`, `external_api_walkthrough` (`install.py:1877-1885`) — and records each completed step name into `install-state.json`. On failure it halts, logs `INSTALL_HALTED at step '<name>'. Fix the issue above and re-run with --resume.` (`install.py:1893`), and exits 1. `--resume` skips any step already in `steps_completed`. `--dry-run` previews without writing. `--reset` clears state and exits.

The alternative I rejected is a transactional installer that rolls back every file write on failure. Ora's install writes into `~/ora` (catalog JSON, model registry, populated configs, downloaded Pandoc/Typst binaries), installs Python and npm packages, and can create the vault — a true transaction would need to journal and reverse third-party package-installer side effects, which is not worth the machinery for a single-user install. The one deletion it does perform is narrow and self-limited: a vault creation that fails part-way removes only the empty directories that same run made. The tradeoff is explicit and sharp: **`--reset` forgets progress but does not roll back files.** It `unlink`s `install-state.json` and `install.log` (`install.py:512-519`) and nothing else. Re-running after a reset relies on each step being individually idempotent, not on the filesystem being clean. An operator who expects `--reset` to undo a half-written registry will be surprised; the recovery doc's nuke-and-reclone is the real "start clean" path.

**Git-pull-and-re-run as the update path.** No auto-updater is the deliberate current state, not an oversight I am papering over. The upside is that update carries no signing/notarization/feed infrastructure and inherits the installer's idempotency for free. The open problem is that this is undocumented as an update procedure — it lives implicitly inside `docs/install-testing.md`'s "Rolling-main install" language. The honest fix is to either build a packaged updater or write down "git pull + re-run installer" as the supported, named procedure. Today it is neither.

**In-band chat-banner health over a separate dashboard.** All three runtime health surfaces prepend a markdown banner to the *next chat turn* rather than exposing a `/health` endpoint or a status page. The rationale: the user is already in the conversation; a health signal that requires them to look somewhere else is a health signal they will miss. The tradeoff is that a degraded state is only observed on the next turn — there is no push, no alert, no polling surface for an external monitor.

**Three parallel health surfaces over one health bus.** `endpoint_health` (circuit breaker), `pipeline_health` (per-thread in-pipeline warnings), and `oversight_health` (heartbeat staleness) are three separate modules that deliberately mirror each other's shape — each exposes a collector plus a `format_warnings_as_chat_note`. I chose parallel modules over a unified event bus because the three failure domains have nothing in common at the data layer: a circuit breaker is per-endpoint global state, pipeline warnings are per-request thread-local state, and oversight staleness is derived from files on disk written by a separate daemon. A unified bus would force a lowest-common-denominator schema on three genuinely different lifetimes. The tradeoff is duplicated formatting code and three things to keep in sync instead of one.

**Portability linter with a hard exit code over per-platform CI.** `scripts/check-portability.py` statically walks the tree, regex-matches Mac/POSIX-only constructs, downgrades platform-guarded/docstring/allowlisted hits, and exits 1 on any CRITICAL. It is pure stdlib so it runs anywhere as a gate. The rejected alternative is spinning up Windows and Linux CI runners to catch these at test time. The linter is cheaper and catches constructs before they ship, but it is a heuristic — it reasons about source text, not runtime behavior, so it both misses dynamically-constructed Mac assumptions and over-flags guarded ones, which is why it carries an allowlist (`ALLOWED_MAC_ONLY_FILES`) and a platform-guard downgrade. **Open problems, stated honestly:** the 12 CRITICAL findings are a real, unpaid Windows/Linux porting backlog; the `mlx_worker` heartbeat is inapplicable-but-unguarded on non-Apple hosts; and the two startup bash index scripts fail silently on a bash-less host. All four are unresolved at 7a5e8f40.

### Implementation

**Installer state machine.** `install.py` defines `STATE_PATH = REPO_ROOT / "install-state.json"`, `LOG_PATH = REPO_ROOT / "install.log"`, and `COMPLETION_MARKER = "INSTALL_COMPLETE: 0 warnings, 0 errors"` (`install.py:123-125`). `main()` parses `--profile/--dry-run/--reset/--resume` (`install.py:1855-1858`). On `--reset` it calls `reset_install`, which under dry-run logs `[dry-run] would clear install-state.json and install.log` and otherwise unlinks both files (`install.py:512-519`) — note again that no installed artifact is removed. Otherwise it seeds `started_at`, loads `steps_completed`, and walks the nine-step pipeline; a step already in `completed` under `--resume` logs `✓ Step '<name>' already completed; skipping`, and any step returning falsy triggers the `INSTALL_HALTED` message and `sys.exit(1)` (`install.py:1888-1894`). Success writes `COMPLETION_MARKER`. The recovery doc orchestrates around exactly these two artifacts: grep `install.log` for `INSTALL_COMPLETE`, inspect `install-state.json` for the last completed step.

**Manual fallback and server path.** When the installer itself is broken, `help/install-manual.md` reproduces the Solo desktop install by hand (clone → `pip install -r requirements.txt` + the pinned MCP runtime → vault skeleton → `converters.py` → `refresh-catalog.py` → `sync_model_registry.py` → `auto-populate-configuration.py budget` + preset bake → free smoke config → optional live smoke → launchd or `start.sh`) with Windows PowerShell variants (`py -3`, `$env:USERPROFILE`). The manual MCP steps apply `node mcp-runtime/patch-playwright.cjs` after `npm ci --ignore-scripts` and before installing the pinned Chromium; this is the same required patch used by the automated installers. The Linux server path is `scripts/install-server.sh` (apt-get, ollama, `.venv`, sourcing `~/.config/ora-server.env`); `uninstall/uninstall.py` (present on disk, `--dry-run`/`--include-models`/`--include-conversations`) provides the clean-room reset the test protocol calls for.

**Endpoint circuit breaker.** `orchestrator/endpoint_health.py` holds per-endpoint state in a module-global `_state` dict (`endpoint_health.py:43`) guarded by `_registry_lock` (`endpoint_health.py:33`). Tunables default to a 60-second failure window, a 3-failure threshold, and a 300-second cooldown (`endpoint_health.py:47-49`), mutable via `configure()`. `record_failure` appends a timestamp; once the rolling-window count hits the threshold it sets `cooldown_until = now + _cooldown_seconds`, and a failure while a probe is pending re-extends the cooldown — the half-open behavior (`endpoint_health.py:82-94`). `record_success` clears state. The router consults the breaker inside its chain walk at two sites: `router.py:387` (`if endpoint_health.is_in_cooldown(ep_id)`) and `router.py:1088` (`is_in_cooldown(resolved_ep_id)`), each with a try/except covering both bare and package-qualified imports (`router.py:383-388, 1084-1088`). The module docstring pins the defaults to "concurrency spec section 5" — a vault reference (`Reference — Ora Concurrency Architecture`) that can drift from the code constants if the spec is retuned. These constants are provisional and configurable; treat the numbers as the current default, not a measured optimum.

**Per-thread pipeline warnings.** `orchestrator/pipeline_health.py` stores warnings in `threading.local()` (`pipeline_health.py:32`) because each chat request runs on its own Flask worker thread and warnings must not bleed across concurrent turns. A pipeline step calls `record(category, message, **extra)` (`pipeline_health.py:35`); the chat handler calls `collect_and_clear()` (`pipeline_health.py:50`) at turn end and `format_warnings_as_chat_note()` (`pipeline_health.py:62`) to build the `> ⚠️ **Pipeline-execution warning**` banner. The concrete producer is `parse_step1_output` in boot.py (`boot.py:5753`), which records `phase_a_parse_failed` (`boot.py:5806, 5818`) when neither `### CLEANED PROMPT` heading is found in the cleanup model's output — the exact silent-semantic-degradation case the module was added to close (path S3, per its docstring). The drain is wired at `server.py:6143` importing `collect_and_clear`.

**Oversight heartbeat surface.** `orchestrator/oversight_health.py` reads watcher heartbeat state under `data/oversight/` and reports actual staleness, daemon liveness failure, and incomplete tool-event telemetry. Missing legacy heartbeat files remain ambiguous and do not manufacture a warning. The former `simulated_oversight` warning was removed with the automatic Process Coherence route; there is no live-mode flag to enable.

**Diagnostics.** `scripts/ora-test` POSTs a prompt to the running `/chat` server under a *named saved configuration* (`--config NAME`, resolved from `config/configurations/<name>.json`) without touching the active config, streams SSE stages, prints the pipeline-trace path, and joins `cost-summary.json` for an inline cost table. `--no-wait` detaches via `os.fork()` → `os.setsid()` → `os.dup2` of `/dev/null` onto fds 1/2 (`ora-test:109-127`) so the run outlives the CLI — POSIX-only by construction. `scripts/ora-cost-summary` computes per-model and grand-total token cost by joining `usage.jsonl` against `model-registry.json` pricing via `boot.compute_cost_summary` (`boot.py:13254`); `--latest` picks the newest turn dir, `--write` persists `cost-summary.json`. Both scripts hardcode `#!/usr/bin/env /opt/homebrew/bin/python3`. **There is no instrumented reliability budget here** — cost is measured (token counts joined against a pricing table), but there is no success-rate, per-step-failure, or uptime metric anywhere in this subsystem; where a budget would be useful, nothing measures it.

**Trace Walk operator and API surface (2026-07-16, post-pin).** The primary interface is already in the Findings pane: make the problematic turn current, hover its lower-right edge, and click **Trace** / **How this was made**. The modal shows trace-kind, terminal-status, effective-Gear and retention badges; the truthful manifest categories; a per-step structural projection; lineage links; and **Pin trace**, **Investigate**, and **Export HTML** controls. There is no `/trace` slash command.

Trace expiry has two routes: the explicit/manual sweep and the exact one-shot deadline registered at finalization. Both treat a missing, unreadable, non-object, malformed, symlinked, changed-on-open, or identity-inconsistent manifest as uncertain; an unfinalized, malformed, or timezone-unqualified `finalized_at` is equally non-authorizing. They retain the trace and report the reason. The manual sweep removes only an ordinary trace directory past the configured age cutoff whose manifest is readable, finalized, identity-matching, and unpinned. The deadline queue waits until `finalized_at + ORA_RETENTION_TRACES_DAYS`; its handler additionally requires the manifest's current `finalized_at` to equal the exact value bound into that deadline. Only a readable, finalized, expired, unpinned exact match is deleted. An open trace cannot be pinned until finalization.

The terminal `/trace-probe prepare|approve|execute` flow is separate from Trace Walk's browser/API reads. On `execute`, the terminal handler first asks the real `get_active_endpoint` resolver whether a current endpoint is available, before it calls the one-shot approval consumer. If none resolves, it returns `Trace probe error: no AI endpoints configured` before any model call and leaves the approval unused. Endpoint resolution itself is not approval: `execute_probe` must still authenticate the approval id/digest, conversation, granted/unconsumed/unexpired state, and unchanged source trace/step identities before consuming the match once. When readiness, that approved identity, and the recorded endpoint match all hold, the model callback makes exactly one `call_model` invocation; a replay is refused as already consumed.

The browser calls `GET /api/trace/list/<conversation_id>`, `GET /api/trace/manifest/<conversation_id>/<turn_ts>`, `GET /api/trace/step/<conversation_id>/<turn_ts>/<step_name>`, and `GET /api/trace/export/<conversation_id>/<turn_ts>`; pin/unpin uses `POST /api/trace/retention` with `{"trace_ref":"<conversation>/<turn>","pinned":true|false}`. These endpoints return safe projections or a redacted HTML artifact, never the raw turn files. The module CLI is intentionally narrow: from `~/ora`, `python3 -m orchestrator.pipeline_trace status|pin|unpin '<conversation>/<turn>'` inspects or changes retention state only.

For a controlled reproduction, first copy the exact server URL reported by the launcher (the active port may be 5000–5010), then run `./scripts/ora-test --server "$ORA_URL" --id <diagnostic-id> [--config NAME] [--mode MODE] '<prompt>'`. `--list-configs` and `--list-modes` enumerate valid names. The final two components of the printed trace directory form the trace reference. This direct CLI route is itself traced, including explicit direct-entry behavior and the exact terminal value after routing.

`scripts/diagnose-extraction-pipeline.py` verifies the knowledge-extraction path end to end (transcript build → input-type detect → `source_type` override → Pass A prompt → optional full model run producing `notes_extracted > 0`); tests 1–4 run modelless, tests 5–6 need `--full` and a live model. It inserts `orchestrator/`, `orchestrator/tools/`, and the repo root onto `sys.path` (`diagnose-extraction-pipeline.py:19-21`) and imports `runtime_pipeline`, `input_detect`, and `extraction_engine`. It is a manual script, not part of `unittest discover`.

`scripts/check-portability.py` scans the tree with a CRITICAL/HIGH/MEDIUM/LOW/INFO ruleset, downgrades platform-guarded, docstring, and allowlisted hits, and runs `check_missing_files` (`check-portability.py:275`), whose `missing-windows-launcher` rule (`check-portability.py:278-286`) would flag an absent `start.bat`/`stop.bat` next to `start.sh`/`stop.sh` as CRITICAL — but at 7a5e8f40 both `.bat` files ship, so it does not fire.

**MSI WSJ session guard (scoped current behavior, 2026-09-08).** The Ora-owned `operations/g1-10-current/cloud/scripts/wsj-reauth-guard.sh` remains the source for `/home/oracle/wsj-reauth-guard.sh`, used by the existing daily timer and unified intake preflight. It reads the diagnosis result before deciding what to do: healthy returns success; positive paywall/authentication evidence alone enters the existing reauthentication routine. Profile busy, feed/network/browser failure, timeout, and HTTP errors or short bodies without that evidence are retryable operational outcomes. The existing article-body floor still applies; absence of a paywall does not establish health. Authentication failures retain the existing alert behavior.

MSI’s shared WSJ fetcher owns the subscriber profile for each browser operation, with a bounded wait of at most 30 seconds. Setup, cookie import, news/opinion fetching, diagnosis, initialization, and stale-file cleanup share that boundary; health cleanup defers when busy and never kills a live owner by age. The unified guard allowance is 450 seconds around existing children totaling 420 seconds; timed-out children are terminated and reaped. The profile and login state remain in place.

**Startup maintenance.** At server startup `server.py` shells out to two bash scripts when their outputs are stale: `generate-indexes.sh` (regenerates the modes and framework indexes from their directories) via `subprocess.run(["bash", _gen_idx], capture_output=True, timeout=30)` (`server.py:14884`) only after comparing directory contents against the index text, and `compile-rag-manifest.sh` (recompiles `rag-manifest-compiled.md`) when the canonical is newer than the compiled (`server.py:14893-14897`). `rag_engine.py:451` invokes the manifest compile on its own path as well. **Failure behavior is silent by design and dangerous off-Mac:** `capture_output=True` swallows stdout/stderr and no exception is surfaced, so on any host without bash in PATH — Windows-native without Git-Bash or WSL — both scripts no-op and the server runs with stale indexes and a stale RAG manifest with no diagnostic. This is the one place I would change the design: fail loudly or provide a Python fallback.

### Evidence & Verification

**Automated tests.** `orchestrator/tests/test_endpoint_health.py` covers the breaker (trip, cooldown, half-open probe, reset). `test_router_chain_walk.py` exercises `is_in_cooldown` skipping inside the router chain walk. `test_heartbeat.py` covers the watcher heartbeat read/write path `oversight_health` depends on. `test_portability.py` backs the linter's intent — Windows path tiering, shell-profile command availability, `fcntl`→`msvcrt` fallback, and no hardcoded user paths in checked-in config.

**Live checks I ran at 7a5e8f40.** `git rev-parse HEAD` confirms `7a5e8f40` ("Execution Review Phase 1-4: macOS+Windows portability retrofit (#181)"). `python3 scripts/check-portability.py` returns `CRITICAL: 11, HIGH: 32, MEDIUM: 69, LOW: 59, INFO: 8` — the 11 CRITICAL are the Windows/Linux porting backlog, led by `homebrew-python-hardcode` in `orchestrator/output_runtime.py:474,626,911` not a missing Windows launcher — `start.bat` and `stop.bat` both ship at this commit, so the `missing-windows-launcher` rule does not fire. I confirmed `install.py` exposes `--dry-run/--reset/--resume`, `STATE_PATH`, `COMPLETION_MARKER`, and the `INSTALL_HALTED …--resume` message; that `reset_install` unlinks state and log only (no artifact rollback); that `compute_cost_summary` is defined at `boot.py:13254`; that the breaker is consulted at `router.py:387,1088`; and that both index scripts are invoked via `subprocess.run(["bash", …], capture_output=True)` at `server.py:14884,14897`.

**The clean-room protocol is the installer's real end-to-end verification.** `docs/install-testing.md` defines a three-environment matrix — Mac Mini A, Mac Mini B, and a Parallels Windows 11 ARM VM — reset via Parallels snapshot or `uninstall/uninstall.py`, run in both no-key and keyed modes, against its pass checklist, with "shipped = all three pass with zero glitches under the Solo profile." This is a human protocol, not automated CI.

**Explicitly untested surfaces.** The `ora-test --no-wait` `os.fork` path has never been run on Windows-native — it would raise, because CPython on Windows has no `fork`. `diagnose-extraction-pipeline.py` tests 5–6 need a live model and are not in the automated suite. The server-install path has not been exercised on WSL or Windows. And the biggest untested claim of all: **a fresh `install-server.sh` run pulls `nomic-embed-text` (768-dim) while the live embedding stack migrated to `bge-m3` (1024-dim) on 2026-05-29.** I verified this drift directly — `config/chromadb.json` documents the nomic→bge-m3 / 768→1024 migration and the `_v2` collection suffix (retaining the v1 nomic collections for rollback), while `install-server.sh:174-175`, `install-manual.md:185`, and `install-recovery.md:240` still `ollama pull nomic-embed-text`. A fresh server install would build a nomic-dimensioned ChromaDB inconsistent with the documented runtime. This needs a decision: update the installer to bge-m3, or document that server installs intentionally stay on nomic and the dimension mismatch is handled.

### Expert Reading Notes

Read `install.py`'s nine-step pipeline (`:1877-1894`) and `reset_install` (`:512-519`) first — the single most misunderstood fact in this subsystem is that `--reset` clears progress but rolls back no files, so recovery correctness rests on each step being idempotent, not on a clean filesystem. Then read `router.py:383-388` and `router.py:1084-1088` to see exactly when the breaker skips an endpoint in the chain walk. Then read `oversight_health.py:36-48` (`HEARTBEAT_INTERVALS`) and hold it against the oversight-daemon defaults — the module comment asserts they must match, and if a daemon interval env var changes without updating this table, staleness thresholds silently diverge.

Drift is most likely at five places. First, the **nomic-vs-bge-m3 installer lag** — the highest-value fix; the installer and three docs pull the pre-migration embedder while the runtime is on bge-m3. Second, the **stale historical pair** `docs/upgrade-report-2026-04-06.md` + `docs/pre-upgrade-packages.txt`: both reference `~/local-ai` (the pre-rename repo), commit `49d2f8a`, and a 3-local-model / `index.html`-era system. They are frozen history — keep them, but never cite their "15 dispatcher tools" count or `cd ~/local-ai && git checkout .` rollback as current. Third, the **two bash index scripts** silently no-op on any bash-less host. Fourth, the **portability allowlist** (`ALLOWED_MAC_ONLY_FILES`) will rot as new Mac-only code lands — the CRITICAL count is only meaningful if the allowlist stays honest. Fifth, the **`mlx_worker` heartbeat**: it is an unguarded entry in `HEARTBEAT_INTERVALS`, correct-by-accident on non-Apple hosts (no file → skipped, not stale) but not conditioned on local-MLX presence, so a future change to the "missing file" semantics could start reporting a phantom stale watcher off-Mac.

The subsystem's biggest honest gap remains the **absence of any packaged updater**. Do not read `git pull` as a documented update procedure — it is the de facto one, and writing it down (or replacing it) is open work.

For cross-references: the router's endpoint selection and machine-mutex logic are covered under the routing chapter; the oversight daemon and watcher lifecycle under the meta-layer chapter; the extraction pipeline that `diagnose-extraction-pipeline.py` inspects under the knowledge-extraction chapter. Note that Execution-Review Phase 5 — the specification-driven, judge-gated review the portability retrofit belongs to — is **implemented in an unmerged worktree, at the code-review gate; it is not merged to ~/ora main and not installed at 7a5e8f40.** Phases 1–4 are landed and are exactly what commit `7a5e8f40` ("Execution Review Phase 1-4: macOS+Windows portability retrofit") delivers; the installed system's Execution-Review coverage ends at Phase 4.

### Platform compatibility

Per-subsystem platform checks, labeled by evidence. "Works" appears only where the construct is portable or was actually exercised; everything else is named as needs-porting, untested, or n/a.

- **`ora-test` / `ora-cost-summary` shebang `#!/usr/bin/env /opt/homebrew/bin/python3`** — macOS: works. Windows-native: needs-porting (Windows ignores shebangs; there is no Homebrew path). WSL: needs-porting (Homebrew path absent; Linux Python lives elsewhere). Linux: needs-porting. The portability linter flags `homebrew-python-hardcode` as CRITICAL for exactly this reason.
- **`ora-test --no-wait` (`os.fork` + `os.setsid` + `os.dup2`, `ora-test:109-127`)** — macOS: works. Windows-native: needs-porting (CPython has no `fork`; the `--no-wait` path raises). WSL: untested (POSIX `fork` is present, but not exercised). Linux: untested (same).
- **Startup index regen via `subprocess.run(["bash", …])` (`generate-indexes.sh`, `compile-rag-manifest.sh`; `server.py:14884,14893`, `rag_engine.py:451`)** — macOS: works. Windows-native: needs-porting (no bash without Git-Bash/WSL; `capture_output=True` swallows the failure, so indexes and RAG manifest silently go stale with no diagnostic). WSL: untested (bash present; not exercised). Linux: untested (same).
- **`compile-rag-manifest.sh` stat portability (`stat -f %m || stat -c %Y`, BSD-then-GNU fallback)** — macOS: works. Windows-native: needs-porting (still bash-dependent). WSL: untested (GNU `stat` branch present; not exercised). Linux: untested (same).
- **`oversight_health` `mlx_worker` heartbeat (30s, `mlx_mutex.start_heartbeat`, `server.py:14822`)** — macOS: works (Apple-Silicon MLX/Metal). Windows-native: n/a. WSL: n/a. Linux: n/a. On non-Apple hosts no MLX worker exists, so no heartbeat file is written; `check_health` skips absent files (`oversight_health.py:114-115`), so this correctly does *not* report stale — but the entry is unguarded in `HEARTBEAT_INTERVALS`, correct by behavior rather than by explicit platform check.
- **Desktop recovery/manual assumptions (`~/ora`, `chown -R $USER:staff`, `brew install python@3.12`, `/opt/homebrew/bin/python3.12`)** — macOS: works. Windows-native: needs-porting (`install-manual.md` provides `py -3` / `$env:USERPROFILE` PowerShell variants — the only Windows-native affordance in the whole subsystem). WSL: untested (`install-recovery.md` gives a Linux `chown` variant, but Step-1 fixes are Homebrew-centric). Linux: untested (same).
- **Server-install recovery/manual path (`apt-get`, `ollama`, `.venv`, `source ~/.config/ora-server.env`)** — macOS: n/a. Windows-native: needs-porting (Debian/apt + POSIX venv sourcing). WSL: untested (Ubuntu-shaped; not exercised). Linux: untested (Debian-target; not exercised at 7a5e8f40).
- **Embedding model pulled by installer (`ollama pull nomic-embed-text`, `install-server.sh:174-175`)** — macOS: works-but-drifted. Windows-native: untested. WSL: untested. Linux: works-but-drifted. This is a drift, not a platform gap: the runtime migrated to `bge-m3` (1024-dim) per `config/chromadb.json`, so any fresh install on any platform builds a nomic-dimensioned (768) ChromaDB inconsistent with the documented runtime.
- **`check-portability.py`** — macOS: works. Windows-native: untested (pure stdlib `pathlib`/`os`/`re`, no shell-out; designed to run everywhere but not exercised off-Mac). WSL: untested. Linux: untested. It is the porting gate. Current run on Mac: 12 CRITICAL.
- **`diagnose-extraction-pipeline.py`** — macOS: works. Windows-native: untested (uses portable `expanduser`; only the docstring's suggested run command hardcodes Homebrew python). WSL: untested. Linux: untested. Tests 5–6 need a live model config.
- **`install.log` committed sample** — macOS: works (reference capture from a Mac run; 16 lines carry `/Users/oracle`, `/opt/homebrew`, `launchctl`, `osascript`). Windows-native: n/a. WSL: n/a. Linux: n/a. It is a sample artifact, not executed off-Mac.
- **Windows-native launcher is present but untested.** `start.bat` and `stop.bat` ship. The start path now honors relocation, resolves the checkout interpreter, validates an explicit port, enables oversight by default with `--no-oversight` as the diagnostic opt-out, forwards other arguments safely, and records an identity-bound PID for stop/health handling. It still sets only the Execution Review flag itself, not the POSIX launcher's additional feature defaults, so reduced or differently configured Windows behavior remains possible. It has not completed a clean-room native-Windows run, and several surrounding recovery/diagnostic paths remain POSIX- or Mac-oriented.

## 17. The Project Plugin Convention

- **Current Project preflight boundary.** Project registration and removal do not request approval until their exact pointer target is usable; protected tool and slash invocation checks readable regular program/script files and execute permission for the launched program before approval and again before spawn, without requiring an execute bit on an interpreter-read script. The resolved, authenticated absolute program path is the subprocess argv's first element.
- HTTP and slash callers share `prepare_pointer_mutation` with the registry sink, which repeats the strict preflight under the pointer lock. Symlinked, malformed, non-regular, or changing pointers fail before review; missing and container-only pointers fail before unregister review, while registration may create an absent pointer or extend a valid container-only record.
- Registration carries the canonical project root, manifest path, and byte digest from one stable manifest snapshot through approval. The sink re-reads that canonical root and refuses manifest drift before atomically replacing the shared pointer.

### Problem

Ora is a generic prototype. It is meant to be forked. But the moment it did real work — running Main Street Independent, an AI news publication — it grew code that has nothing to do with a generic orchestrator: a FRED economic-data fetch, an article schema, a `msi_news_articles` ChromaDB collection, a 19th-century-press publication theme, editorial framework tuning. That code has to live somewhere, and the tempting place is `~/ora/orchestrator/`, right next to the pipeline it calls.

That is the failure mode this convention prevents: core-fork contamination. If MSI's FRED fetch lands in `orchestrator/`, then every fork of Ora inherits one publication's specifics, the question "what is core Ora versus what is a downstream project?" stops having an answer, and you can no longer cleanly separate the generic engine from the thing built on top of it. A second, related failure mode is blast radius: if a project's code runs in Ora's own Python process, a project bug — an unhandled exception, a runaway loop, a bad import — takes down the orchestrator with it. A generic prototype that crashes because one downstream publication shipped a bad tool is not a prototype anyone can fork with confidence.

The convention answers both by drawing a hard line: project-specific code lives in the project's own directory, and Ora reaches it only across a process boundary. The mechanism is a directory boundary, a declarative manifest, and subprocess isolation.

This convention is not the first-party **feature-plugin** boundary in Chapter 11.
A Project Plugin belongs to an external named project, is registered through an
`ora-project.json` manifest and pointer file, and executes tools across a JSON
subprocess boundary for failure and dependency isolation. The current video
feature is trusted code shipped under Ora's own `plugins/video` directory and
runs in process so it can share conversation lifecycle safety. Neither mechanism
discovers or installs the other.

### Design Decision & Tradeoffs

The decision is a three-part contract:

1. A hard directory boundary. A project is any directory on disk containing an `ora-project.json` manifest. Project code lives there — `<project>/ora-project/tools/`, `scripts/`, `config/`, `themes/` — never in `~/ora/orchestrator/`. The module header of `project_registry.py` states the rule in the first person of the codebase: "This module is project-agnostic — nothing here references any specific project."

2. A declarative manifest. The manifest declares what the project exposes: `tools`, `slash_commands`, `frameworks`, `peds`, `workflow_specs`, `chromadb_collections`, plus three advanced surfaces — `capability_slots` (see The Capability Registry), `themes` (see The V3 Theme System), and `framework_configurations` (see The Framework Configuration Engine). Frameworks reference tools by abstract name (`news-index`, `news-related-find`), never by file path; the dispatcher resolves the name against the registered manifest at runtime.

3. Subprocess isolation over a JSON-on-stdout contract. Ora invokes a project tool by running it as a subprocess and parsing its stdout as JSON — it does not import the project's Python modules. This is the load-bearing choice, and it is a Unix pipe, not an in-process plugin.

The main alternative rejected was in-process plugin imports (Python `entry_points`, or `importlib` loading the project's modules into Ora's interpreter). That is the conventional Python plugin pattern, and it is faster — a function call instead of a process spawn. It was rejected for two reasons. First, it couples core Ora to every project's dependency tree: if MSI imports a library at version X and Ora needs version Y, they fight inside one interpreter. Second, it destroys failure isolation — an exception in project code propagates straight into the orchestrator's stack. The subprocess boundary makes a project tool's failure a `ToolInvocationError` the caller can catch, never a core crash.

A second rejected alternative was auto-scanning the disk for projects. Ora could walk the filesystem for `ora-project.json` files and register whatever it finds. That was rejected for explicit pointer files: a project becomes visible to Ora only when someone writes `~/ora/data/projects/<nexus>.json` pointing at its root — by hand, programmatically, or via `/project-register`. Registration is a deliberate act, not a side effect of where a directory happens to sit. The cost is one extra step to install a project; the benefit is that Ora's set of active projects is an explicit, inspectable list rather than an emergent property of a disk walk.

The accepted tradeoff: subprocess-per-call has real cost. Every invocation pays process-spawn latency and JSON serialization at the boundary, and the tool cannot share live Python state with the orchestrator — each call starts cold. For a news pipeline that fires tools in bulk, that overhead is not free. It is accepted because the isolation it buys — language-agnosticism (a project tool could be a shell script or a Rust binary as long as it speaks JSON on stdout), dependency independence, and crash containment — is exactly what a forkable prototype needs. This is the microservices tradeoff in miniature: you pay serialization and process cost at every boundary crossing to buy independent failure domains.

Open problems, stated plainly:

- Framework configurations are not wired. The `framework_configurations` surface has a complete engine (`framework_config.py`, 35 tests) and its manifest block parses and validates, but the engine is imported nowhere in the framework-execution path. `compose_framework_spec` never runs at framework-invocation time. This is the largest gap in the convention and is detailed below.
- A hand-mirrored drift trap. To validate `capability_slots` at registration without importing `capability_registry` (which would create a circular import at load time), `project_registry.py` hand-copies the registry's accepted input-type and execution-pattern sets into local frozensets, with a comment: "Update when the registry's accepted sets change." Nothing enforces that update. If someone adds an input type to `capability_registry` without editing `project_registry`, valid project slots get silently rejected at registration.
- Active-project binding is provisional. The active-project pointer does no existence check when set, and new conversations still rely on a global pointer rather than an explicit per-request project. Both are documented deferrals.

### Implementation

The load-bearing module is `orchestrator/project_registry.py` (`/Users/oracle/ora/orchestrator/project_registry.py`). It is project-agnostic by design and holds the entire contract: dataclasses, manifest parsing, pointer-file discovery, and the subprocess invocation API.

Manifest parsing and validation. `load_project_at(root)` (line 750) reads `<root>/ora-project.json` and hands it to `_parse_project_from_manifest` (line 700), which validates every block and returns a `Project` dataclass (line 230). The `nexus` must be lowercase kebab-case (`_NEXUS_RE`, line 39); `name` is required. Each block has its own parser:

- `_parse_tools` / `_parse_slash_commands` validate that each entry has a name and a non-empty `command` argv, and that its `interface` is one of the two known contracts (`argv-stdout-json` or `stdin-stdout-json`). Duplicate names raise. A slash-command name carrying a leading slash raises — the manifest stores it without.
- `_parse_str_list` handles the simple string-list blocks: `frameworks`, `peds`, `workflow_specs`, `chromadb_collections`.
- The three advanced parsers — `_parse_capability_slots`, `_parse_themes`, `_parse_one_framework_configuration` — follow a deliberate graceful-degradation rule: a wrong top-level type raises `ManifestError`, but an individual malformed entry is skipped with a printed diagnostic rather than failing the whole manifest. One bad capability slot costs you that slot, not the project.

Two validations happen at registration time specifically to fail loudly and early rather than silently at invocation: `_parse_one_theme` checks that the theme directory exists and contains both `theme.css` and `manifest.json` (line 518), and `_parse_one_framework_configuration` checks that every overlay file exists on disk (line 634).

Pointer-file discovery. `list_projects` (line 772) globs `~/ora/data/projects/*.json`, sorted for determinism, and for each pointer loads the manifest at its `root`. It skips malformed pointers, skips manifests that fail to load, and enforces one integrity check: if the pointer names a `nexus` that disagrees with the manifest's `nexus`, it skips with a diagnostic (line 803). `get_project(nexus)` (line 813) is the single-project lookup; `register_project(root)` (line 833) validates the manifest before writing the pointer `{nexus, root}`, so registration never leaves a pointer to an invalid project; `unregister_project` (line 852) deletes the pointer and leaves the project's files on disk untouched.

The pointer file has two readers — this is the key subtlety. The same `~/ora/data/projects/<nexus>.json` file carries two meanings:

- The plugin view (`project_registry`) requires a `root` key. A pointer with no `root` is a container-only record, and `list_projects` skips it silently (line 792) — not an error, simply not a plugin.
- The container view (`project_meta.py`, `/Users/oracle/ora/orchestrator/project_meta.py`) reads every pointer for conversation-container metadata: `name`, `status`, `last_accessed_at`, and a set of inert default slots. Here `is_plugin` is just `bool(root)` (line 89). A project can be both a plugin and a conversation container, or a container only.

This dual meaning is deliberate ("unify the two meanings," per the module header) but it is a place a careless reader trips: the two views disagree on whether a `root`-less pointer is meaningful, and they are correct to disagree.

The subprocess invocation contract. `invoke_project_tool(nexus, tool_name, args=/stdin_json=, expected_binding=)` is the load-bearing runtime API. It resolves the project, looks up the tool, and resolves the command argv via `_resolve_command`: the second argv element is treated as a script path and resolved against the project root when relative. It resolves and authenticates the first element (the interpreter or program), then replaces that element with the exact authenticated absolute executable path used by `subprocess.run`; a form that cannot produce that exact executable is refused. The runner uses `shell=False`, an argv list, `capture_output=True`, `timeout=120`, and the bound canonical project root as `cwd`; that same root is exported as `ORA_PROJECT_ROOT`. The two interfaces differ in wiring: `argv-stdout-json` appends `args` to the argv; `stdin-stdout-json` serializes `stdin_json` and pipes it to stdin. Both must produce JSON on stdout (empty stdout returns `None`). The full execution binding covers Project nexus, canonical registered root, registered name, manifest, command, interface, executable/script paths and byte identities, and the exact arguments or stdin digest. Direct chat adapters bind it through the existing Paused one-shot route. An active Trigger binds it at activation and passes the claimed snapshot as `expected_binding`; manual draft/paused Trigger runs use Paused one-shot approval. In every path, the pointer, root, manifest, command, executable/script, interface, and input are re-resolved against that expected binding immediately before the subprocess boundary, so drift starts no child.

Normal-operation and failure behavior. On success, stdout is parsed as JSON and returned. Every failure mode maps to a `ToolInvocationError` carrying exit code, stderr, and stdout: non-zero exit, `TimeoutExpired`, `FileNotFoundError` (executable not on PATH), and a `JSONDecodeError` on malformed stdout. This is the crash-containment guarantee made concrete — an opaque subprocess can fail any way it likes and the caller sees one typed exception. `invoke_project_slash_command` (line 1063) is the same contract with one difference: its output is free-form markdown for the chat UI. Once its protected one-shot approval is consumed, operational failures raise the same typed error so the adapter can write a truthful failed receipt rather than presenting failure as successful output.

The standard environment. `_build_subprocess_env` sets four variables every project tool can rely on: `ORA_HOME` (the Ora install root, derived from `Path(__file__).resolve().parent.parent` — platform-neutral), `ORA_VAULT` (`~/Documents/vault`), `ORA_PROJECT_NEXUS`, and `ORA_PROJECT_ROOT`. It optionally honors `ORA_CONFIG_NAME`. It also plumbs Execution-Review tool-event context to children that import Ora — `ORA_TOOL_EVENTS_PATH`, `ORA_STEALTH_CONTEXT`, `ORA_CONVERSATION_ID`, and `ORA_RISK_TIER` — all wrapped in a bare `except Exception: pass` so tool-event plumbing can never break a tool invocation. The env-plumbing code comments reference Execution-Review Phases 1 and 2 specifically; at this commit the installed Execution-Review work runs through Phase 4 (HEAD is "Execution Review Phase 1-4"), so the surrounding subsystem is further along than the local comments.

The Execution-Review boundary event. Because the tool body is an opaque subprocess, `invoke_project_tool` records the invocation and exit mechanically at the parent, regardless of what the child does — the boundary recorder (line 992 onward) emits a `project_tool` event with `enforcement_model: "boundary_only"` (line 1011), wrapped so a recorder failure can't break the tool. This is the Execution-Review posture for opaque children: the parent watches the boundary because it cannot watch inside.

The model-dispatch seam. A project tool that needs a model does not hardcode one. It calls `model_dispatch.invoke_chat(system_prompt, user_prompt, slot=...)` (`/Users/oracle/ora/orchestrator/model_dispatch.py`, line 42), which resolves the requested slot against the publisher's routing config and calls back into `boot.call_model`. The seven slots are `breadth` (default, high-context generative work like article authoring), `depth`, `sidebar`, `consolidator`, `evaluator`, `step1_cleanup`, and `classification`. The project declares a slot preference; the publisher's `routing-config.json` decides which concrete model fills it. This keeps model choice a publisher decision, out of project code.

The chat surface. `slash_commands.py` exposes four project-agnostic commands: `/project-tool` (invoke a tool; it selects stdin versus argv from the registered `interface` and parses stdin JSON when required), `/project-list`, `/project-register`, and `/project-unregister`. `/project-tool` and `/projects tool` use the exact `project_tool_execute` adapter; `find_project_for_slash_command` routes a project-declared slash command through the exact `project_slash_execute` adapter. Those direct calls remain one-shot. The same module exposes `/maintenance rollback <event-id>`, which calls the existing EventLedger rollback helper: it starts only while every current after-identity's existence, bytes, and permission mode match, restores independently authenticated before-bytes with the bound before-mode, and records `rolled_back` only after durable terminal evidence. The manifest binds each existing backup's `st_mode & 0o7777`; backup chmod drift refuses before restoration, and target chmod drift is a third state that refuses rollback. That authenticated path applies only when the manifest digest and mode-bearing snapshot were independently bound during preparation. Older mode-less material fails closed without inferred compatibility. Startup keeps the refusal as a distinct event diagnostic instead of reporting that event as restored, and continues recovery for later independent events. A partial restore remains `rollback_applying` and resumes only while each path is still one of the bound before/after byte-and-mode identities; it is distinct from `/trigger rollback`, which remains email-only before provider contact. On a cross-project command-name collision, the alphabetically-first nexus wins with a diagnostic, and the convention advises project authors to namespace their command names.

The three merge lifecycle points. The advanced surfaces integrate at three different moments in the runtime lifecycle, and the difference is the point:

- `capability_slots` — at capability-registry build. `capability_registry.merge_project_capability_slots` (defined line 674) is called during registry construction (line 858) when any project declares slots, merging project-defined visual-capability slots into the core `capabilities.json` in place. MSI declares two — `image_generates_cartoon` and `image_generates_barb_cartoon`. This surface is fully wired and exercised in production. Note the guardrail: a project's slot `routing` block may declare only structural directives (`inherits`, `prepend_fallback`, and the like); the keys `preferred` and `fallback` are publisher decisions that belong in `routing-config.json`, and declaring them in a manifest raises a `ManifestError` (line 422). The project may say "I inherit from X"; only the publisher may say "route to model Y."

- `themes` — merged on each theme-list request, served at request time. The V3 server layers project themes onto the core theme index through `_v3_aggregate_themes()` and serves assets from the project's own directory via `/themes/project/<nexus>/<path:filename>`, annotating entries `origin = "project:<nexus>"`. Registration and requests resolve the project root, declared theme directory, and asset and require containment. Before consuming bytes, the opener validates the kernel-reported path of the already-open file against the captured resolved project/theme paths, refusing outward-symlink redirection. Direct Flask serving, list previews, duplication, and export consume that file through the same opener without reopening its pathname. Legitimate nested assets and inward symlinks remain supported. The project directory remains the source of truth; there is no startup copy.

- `framework_configurations` — designed for framework-invocation time, but not wired. This is the honest gap. The design is that a project's configuration profile binds dynamically at framework-invocation time, because which profile applies depends on which project is invoking — the engine (`framework_config.py`) resolves `${config.<key>}` references and splices `<!-- ora-project-extension: <id> -->` overlays into the framework spec. The engine exists and is tested in isolation. But `compose_framework_spec` is imported nowhere in `milestone_executor.py`, where frameworks actually execute — a grep over the framework-execution path confirms no reference. The only Python runtime consumer is `server.py`, and it does not compose a spec: at line 4329 it reads `project.framework_configurations` in a list comprehension purely to list the declared profiles in the project-inspection API. So this surface's status is partial: manifest parse works, the substitution/overlay engine works and is tested, but there is no end-to-end path from a manifest `framework_configuration` to a composed framework prompt at invocation time. MSI declares one profile (framework `document-processing`, profile `msi-editorial-research`), which parses and lists but never composes.

Container binding and the rename cascade. Two more modules ride on the same pointer files. `active_project.py` (`/Users/oracle/ora/orchestrator/active_project.py`) tracks which project new conversations bind to via `~/ora/data/active-project.json`; `resolve_project_ids` maps a nexus to the `project_ids` stamped on a new conversation, with the synthetic Commons project (internal id `general`) mapping to the empty list `[]`. Its `set_active_project` does no existence validation — the module docstring states this is "intentionally NOT done here yet." `nexus_rename.py` cascades a slug change across four surfaces — vault `.md` frontmatter, conversation `project_ids`, the project pointer file, and the active-project pointer — and defaults to a dry run, with a path-traversal guard (`_NEXUS_RE`) on both the old and new slugs.

The reference implementation. `~/sites/mainstreetindependent/ora-project/` is the worked example of "what belongs in a project, not core Ora." Its manifest (`ora-project.json`, nexus `main-street-independent`, v0.1.0) declares 16 tools, 8 slash commands, one ChromaDB collection (`msi_news_articles`), two capability slots, one theme, and one framework configuration (list-only) — these counts are read from the MSI project at `~/sites/mainstreetindependent/`, which lives outside the pinned Ora commit and so cannot be verified from `~/ora` at 7a5e8f40. Its directory carries `tools/`, `scripts/`, `config/`, `themes/`, `framework-overlays/`, and — tellingly — both a `launchd/` (macOS) and a `systemd/` (Linux) scheduling directory, because MSI runs on a Mac in development and on a Linux cloud host in production.

### Evidence & Verification

How to confirm plugin discovery works. Run:

```
python3 -c 'from orchestrator import project_registry as p; print([x.nexus for x in p.list_projects()])'
```

At this commit the three registered pointers are `main-street-independent`, `ora-ai-app`, and `ora-ai-org`, all plugin pointers carrying absolute macOS roots under `/Users/oracle/sites/`.

Test coverage. The suites live in `orchestrator/tests/`:

- `test_project_registry.py` (1277 lines) — the largest suite, covering both invocation interfaces, the error paths (non-zero exit, timeout, missing executable, malformed JSON), manifest validation for every block, pointer/manifest nexus-mismatch, and collision handling.
- `test_project_meta.py` (158 lines) — the container view over the same pointer files.
- `test_active_project.py` (59 lines) — active-pointer read/write and `resolve_project_ids`.
- `test_nexus_rename.py` (234 lines) — the four-surface rename cascade, dry-run default.
- `test_capability_registry.py::TestMergeProjectCapabilitySlots` — the capability-slot merge, including project-shadows-core and project-shadows-project collision handling.
- `test_framework_config.py` (557 lines, 35 tests) — the framework-configuration engine, tested in isolation only.
- `test_project_execution_binding.py` (115 lines) and `test_conversation_projects.py` (132 lines) — conversation-to-project binding and `project_ids` membership stamping.

The live parity check. MSI is the production proof. It exercises the capability-slot surface (two cartoon capability slots) and the theme surface (one theme) in a running publication on both macOS and Linux. That is the strongest verification the convention has — not a test asserting the contract, but a downstream project depending on it in production.

Explicitly untested surfaces:

- Framework-configuration end-to-end is unverified. The engine is tested in isolation, but there is no test — and no code path — exercising a manifest `framework_configuration` through to a composed prompt at framework-invocation time, because the wiring does not exist. Do not read the presence of `test_framework_config.py` as evidence that the surface works end-to-end; it verifies the engine's substitution logic, nothing downstream of it.
- Windows-native is entirely untested. See Platform compatibility.
- The `ora-ai-app` and `ora-ai-org` manifests were not inspected at this pass. Their pointers are present and carry `root` keys (so the plugin view treats them as plugins), but the contents of their manifests were not read; only the pointers were confirmed well-formed and Mac-rooted.

### Expert Reading Notes

Read `project_registry.py` first, end to end — the whole contract is in one file: the dataclasses (lines 119–271), the parsers, pointer discovery (lines 772–864), and `invoke_project_tool` (line 944 onward). Then read the MSI manifest as the worked example, and note that the manifest's tool commands all begin `["python3", "tools/<script>.py", ...]` — the `python3` interpreter is a POSIX assumption baked into every command.

Inspect these three drift sites, in priority order:

1. The framework-configuration wiring gap (highest priority). `CLAUDE.md` describes `framework_configurations` with "`${config.*}` substitution + extension-point overlay splicing at framework invocation time" as if it were live, and the `ProjectFrameworkConfiguration` docstring (project_registry.py:200–227) says the runtime consumer "lives wherever framework prompts are assembled (typically `milestone_executor.py`)." Both are aspirational. `framework_config.py` is imported only by `project_registry.py` and `server.py` (list-only), never by `milestone_executor.py`. A reader who trusts the docstring or `CLAUDE.md` will over-trust this surface. Trust the code: the surface is partial. This is the change most likely to close silently-wrong — if someone wires the splice into `milestone_executor`, the docstring becomes true and this note becomes stale.

2. The hand-mirrored frozensets (project_registry.py:60–64). `_KNOWN_INPUT_TYPES` and `_KNOWN_EXECUTION_PATTERNS` are hand-copied from `capability_registry` to dodge a circular import. This is a live drift trap: change the registry's accepted input types without updating these frozensets, and valid project capability slots get silently rejected at registration. Decide whether to replace this with a lazy import or a shared constants module the next time either side changes.

3. The dual-reader pointer files. Before touching pointer-file logic, hold both readers in mind: `project_registry` (plugin, requires `root`, skips `root`-less pointers at line 792) and `project_meta` (container, `root` optional, `is_plugin = bool(root)`). A change that assumes every pointer has a `root` will break the container view; a change that assumes every pointer is a container will confuse the plugin view.

Capability-slot merging and project-theme serving are wired. Theme aggregation runs on each request, not at startup; the request-time behavior and bound-file reading contract above supersede the older startup wording in `CLAUDE.md`.

### Platform compatibility

The convention assumes POSIX. MSI exercises macOS (development) and Linux (the cloud host) in production; Windows-native is entirely untested; WSL is portable-in-principle but not run on a dedicated matrix. Project-tool and project-slash execution now resolve the declared program to an authenticated absolute path and use that exact path as argv[0]; native Windows behavior remains untested. Per-subsystem:

- Pointer directory path (`~/ora/data/projects/`, via `os.path.expanduser`) — macOS: works. Linux: works (cloud-ora runs MSI here; the `systemd/` directory in the reference impl confirms POSIX paths are exercised in production). WSL: works (POSIX `~` expansion). Windows-native: untested — and the roots inside the live pointer files are absolute macOS paths (`/Users/oracle/sites/...`), so a Windows host would need its own pointer copies regardless.

- Project-tool subprocess (`subprocess.run`, argv list, `cwd=project.root`, `shell=False`) — macOS: works. Linux: works. WSL: works. Windows-native: untested. The invocation itself is portable in principle (`shell=False`, argv list), but see the interpreter row.

- Manifest tool interpreter (`python3`, the first argv element) — macOS: works. Linux: works. WSL: works. Windows-native: needs validation and likely manifest changes. Binding resolves `python3` through the current host's executable search, authenticates the resolved regular executable and its execute permission, and supplies that exact absolute path to the child. Every MSI tool command still declares `python3`, while a Windows host may expose only `py` or `python`.

- Atomic pointer writes (`os.replace` + `.tmp` sidecar, in `project_meta`, `active_project`, `nexus_rename`) — macOS: works. Linux: works. WSL: works. Windows-native: untested. `os.replace` is a cross-platform atomic rename, but Windows filesystem rename-over-existing semantics were not exercised.

- `ORA_HOME` derivation (`Path(__file__).resolve().parent.parent`) — macOS: works. Linux: works. WSL: works. Windows-native: works (pathlib is platform-neutral). This is the one subsystem with no platform caveat.

- Vault Projects folder index (`project_meta.list_project_files`, reads `~/Documents/vault`) — macOS: works (requires Full-Disk-Access to `~/Documents`). Linux: works, degrades cleanly — returns `exists: False` when the vault is absent, and cloud-ora has no vault. WSL: works. Windows-native: untested. Never raises regardless of platform.

- Reference-impl scheduling (MSI ships both `launchd/` and `systemd/`) — macOS: `launchd` works (Apple-only). Linux: `systemd` works. WSL: untested. Windows-native: not applicable — no Windows scheduler equivalent is shipped. This is project-specific, outside the core convention.

- MSI `osascript`-based notify tool — macOS: works. Linux: mac-only (no-ops or degrades off-Mac). WSL, Windows-native: not applicable. Apple-only; project-specific, outside the core convention, and named here only because it is the clearest example of a platform assumption living correctly inside a project rather than in core Ora.


---

# Part VI — Standalone Programming

## 18. Explicit Repository Programming

### Problem

A user must be able to ask Ora to change a real repository without depending on Codex or Claude Code, while retaining a clear scope decision, actual execution, independent verification, safe recovery, and an intentional Git finish line.

The former governed-Process stack did not supply this simply. It intercepted Inquiry, persisted plans and Runs, projected lifecycle state, and accumulated process registries, process-bound automation and triggers, inspectors, receipts, and event records around a repository executor. Standalone Programming replaces that stack with the smallest complete loop. The separate Scheduled Trigger facility described in Chapter 12 is not part of Programming and was not removed by this replacement.

### Entry and planning

Programming is entered only through the Programming toolbar action. Ordinary `/chat` and `/chat/multipart` requests continue through Inquiry without programming classification or interception. Programming is not a framework-picker row.

`POST /api/programming/plan` accepts an objective, Git worktree name or path, question round, and prior answers. Exact paths and `~` paths remain valid. A simple name resolves only when exactly one Git root with that name exists directly under the user's home, `~/Documents`, or `~/sites`. `orchestrator/programming.py::inspect_repository()` then reads:

- current `HEAD`, branch, and porcelain status;
- applicable `AGENTS.md` and `CLAUDE.md` instructions;
- tracked implementation shape;
- likely test files; and
- visible CI or live automation configuration.

The configured planner must then successfully read or search actual repository content; the coordinator snapshot and repository status alone do not unlock either questions or a plan. Only after that direct inspection does it decide whether a material choice remains. It may return one to three questions in each of at most two question rounds. If both rounds ask questions, a third planning pass must produce one concise plan using reasonable defaults.

The returned plan visibly contains outcome, component scope, non-goals, protected work, milestones, completion criteria, checks, authorized effects, and one Git finish line; the coordinator rejects a proposal missing any category or whose visible finish wording disagrees with the structured authority. It also carries `documentation_impact`: when repository instructions place Documentation-Code Parity in scope, this object declares `required: true` and a unique ordered list of stable affected surface identifiers. Runtime independently reads tracked top-level `AGENTS.md` and `CLAUDE.md`; their active DCP mandate determines whether the task uses DCP, and the planner declaration must agree. A DCP plan uses `local_commits` when approval stops with reviewed local branches, or `coordinated_dcp` when approval includes the five-repository coordinator landing the exact reviewed heads; ordinary single-repository push, pull-request, and merge values are invalid for DCP. This planning declaration routes later evidence; it is not a semantic verdict and cannot replace one.

A push, pull request, or merge also names an exact configured remote and its single push URL, while pull request and merge name an explicit base branch. The response carries those authorities plus the inspected root, commit, branch, dirty state, and documentation-impact declaration as runtime input, not a persistent plan ledger.

### Approval and repository authority

`POST /api/programming/run` requires `approved: true`, the objective, repository path, and exact proposed plan. It accepts an optional `documentation_review` object, but a documentation-impacting execution does not treat evidence supplied before mutation as final evidence. After milestone execution and commits, it returns a resumable evidence-required result carrying the current branch and head. The browser then accepts freshly generated packet JSON, passes the raw text through the existing privacy screen, and calls the same endpoint with that object plus `resume_branch`; no new endpoint or persistent evidence record exists. Before mutation, the coordinator verifies the repository root, `HEAD`, branch, and planned dirty state. A dirty path explicitly named in Component scope is task-owned; other pre-existing staged, unstaged, untracked, or symlink paths are protected from executor writes and excluded from review and accepted commits. Programming continues while those paths remain in place. A newly appearing unattributed path or ambiguous recovery path returns `ASK USER` with all work preserved.

The coordinator creates `ora/<objective-slug>-<baseline>` and owns all Git effects. The executor receives repository-scoped tools for read, search, command, write, edit, and delete. Paths are resolved under the worktree and `.git` is inaccessible. Planning commands are read-only. Executor commands cannot perform coordinator Git operations, external network commands, destructive utilities, publication, deployment, messaging, or credential work.

The executor receives the approved plan and current milestone, edits the real worktree, and runs only the exact checks the approved plan schedules for that slice. Work is divided into coherent reviewable milestones rather than mechanical steps.

### Fresh independent review

Every review is a new model call. Its context contains only:

- the approved plan;
- current milestone or `FINAL`;
- runtime Git baseline;
- raw task diff; and
- repository and task-required evidence tools.

It does not contain the executor transcript, hidden reasoning, a summary, or executor claims. The reviewer independently reads repository files and runs only the exact checks the approved plan schedules for review. Neither role adds a test, build, benchmark, audit, lint, or reassurance check; a material risk that cannot be judged within the ceiling requires changed authority before another check runs.

When the approved plan declares Documentation-Code Parity impact, milestone reviews deliberately receive no final documentation packet: later milestone commits would make it stale. Only cumulative `FINAL` review receives the complete cross-repository evidence object. It must contain all five cumulative repository diffs; a `repository_states` mapping keyed by vault, Ora, app, org, and MSI whose records each contain exactly `root`, `base`, `branch`, and `head`; unversioned global/Programming-skill instruction changes; the exact affected-surface list; a nonempty canonical-section change or one exact no-impact trailer plus rationale for every surface, with no overlap; registered propagation results; the complete verbose five-root gate result; and the authorized focused-test output. A passing gate must print exactly one `affected surfaces:` line. Its stable unique set is authoritative, and the plan, packet, canonical/no-impact coverage, and reviewer verdict sets must equal it; list order is retained only for stable presentation.

Programming parses the verbose gate's printed root, base, and head for vault, Ora, app, org, and MSI. Before final review it requires five distinct exact Git roots on explicit `codex/` or `ora/` task branches, verifies every submitted state against both the gate and live worktree, verifies that every base resolves and is an ancestor, and compares each nonempty submitted cumulative diff byte-for-byte with the raw live clean branch's base-to-head diff, including all trailing spaces and newlines. The exact `[no changes]` value is the only sentinel and is valid only when that raw diff is genuinely empty. The current Programming root, baseline, branch, and head must be the gated ones. It repeats that live comparison after review. Missing, malformed, stale, dirty, detached, or drifting evidence returns to the evidence-entry pause and cannot support acceptance.

For every affected surface, an accepting review includes exactly one machine-readable semantic verdict after its first-line outcome: `Documentation-Verdict: <surface-id>: ACCEPT` when the owning canonical section changed, or `Documentation-No-Impact-Verdict: <surface-id>: ACCEPT` when the exact no-impact declaration is honest. A false disposition uses `REJECT` with `FIX`. Repeated, missing, wrong-type, or rejected verdicts make acceptance invalid. The deterministic gate establishes mechanical correspondence; this fresh reviewer owns the prose-versus-behavior judgment.

Reviewer-only tools provide read-only outside and non-text evidence:

- `web_search` and `web_fetch` inspect the smallest sufficient authoritative outside source;
- `inspect_image` attaches image bytes directly to the review model; and
- `inspect_pdf` renders all pages by default, or an explicitly requested page range, in a temporary directory and attaches those page images.

Temporary PDF renders are deleted when inspection returns. A citation or executor description without source inspection is not evidence, and a description of an image or PDF without direct inspection cannot satisfy a non-text criterion.

The review parser accepts exactly four first-line outcomes:

| Outcome | Coordinator action |
|---|---|
| `CONTINUE` | Accept and commit this sound slice, then proceed to remaining milestones. |
| `FIX` | Return one consolidated substantive defect explanation for correction; at DCP `FINAL`, hand it to the five-repository coordinator without invoking the single-repository executor. |
| `DONE` | During ordinary final cumulative review, perform the approved finish line; for DCP, return the five reviewed local branches to the coordinator without single-repository landing. |
| `ASK USER` | Stop for changed scope or authority, an unapproved external effect, human-only access or decision, inseparable user work, three repeated no-progress cycles, or the soft spend boundary. |

The reviewer judges only the approved plan. It may reject wrong user-visible behavior, unmet criteria, content or data loss, runtime failure, unauthorized scope, broken atomicity, or lost user work. It may not add tracking, preferred abstractions, speculative safeguards, style work, or unrequested requirements.

### Commits, correction, and finish line

After `CONTINUE`, the coordinator applies the exact reviewed patch to a temporary index, creates the accepted-slice commit with `commit-tree`, advances the attached task branch atomically, and refreshes only the committed paths in the real index. Concurrent work remains unstaged and uncommitted. Rejected work remains uncommitted until corrected. At `FINAL`, only `DONE` establishes semantic acceptance.

For DCP, a final `FIX` or non-completing `CONTINUE` returns the full consolidated defect in a coordinated-correction handoff. Programming invokes neither the single-repository executor nor the commit path, so it cannot create a synthetic correction or no-op rollback point. The external five-repository coordinator corrects the participating task branches; the task then resumes from Git with a fresh current gate and packet. On DCP `DONE`, Programming rechecks all five live states and returns their roots, bases, branches, heads, and approved finish line. `coordinated_finish_required` is true only for an approved `coordinated_dcp` finish; `local_commits` stops locally and authorizes no push, pull request, or merge. Programming never calls the ordinary single-repository finish function from either result.

Three consecutive review cycles that reproduce the same substantive failure against an unchanged task diff return `ASK USER`. A changed defect list or task diff resets the count. There is no fixed attempt ceiling. The default soft boundary is 90 minutes.

Supported ordinary finish lines are `local_commits`, `push`, `pull_request`, and `merge`; DCP supports only `local_commits` and `coordinated_dcp`. Before a consequential non-DCP finish, runtime rechecks the approved remote's single push URL and refuses drift. It pushes only that remote; `gh` is explicitly bound to the repository derived from the approved URL and to the approved PR base. Before PR creation it queries that repository for the task branch, rejects cross-repository results, and reuses one matching open PR. An already merged PR completes a retry only when its head OID equals local `HEAD`; an open or new PR is merged with `--match-head-commit` bound to that same commit. Target mismatches, closed PRs, and ambiguous matches are refused. This makes create/merge retries idempotent without custom state. DCP never enters this one-repository path. Newly discovered deployment, publication, messaging, credential, or other external-write consequences return `ASK USER` rather than expanding authority.

### State, rollback, and user surface

Git is the execution state:

- the baseline commit proves the starting point;
- the task branch isolates the work;
- accepted-slice commits are rollback points;
- the current diff shows unaccepted correction work; and
- checks prove current behavior.

No Programming-owned Run, artifact, event, receipt, lifecycle, checkpoint, Trigger, automation, process library, or recovery database exists. Resume uses the approved plan, branch, commits, diff, and checks. This says what Programming does not create; it does not deny the separate user-authored Scheduled Trigger objects in Chapter 12.

`server/static/js/programming.js` supplies the minimal browser surface: repository name or path, planning status, material questions, proposed plan, approval/cancel controls, milestone progress, accepted commit identifiers, reviewer outcomes, genuine user decisions, and completion. At the DCP final boundary it renders a JSON evidence field and **Resume final review** control, rejects invalid or non-object JSON locally, sends the raw packet through the existing privacy check exactly as other Programming text is screened, and posts the parsed object only after acceptance. A final DCP defect instead renders **Coordinated documentation correction required**, shows the complete defect, clears the submitted packet, and directs recovery through the five-repository coordinator with fresh evidence. A successful DCP result says that final review accepted five local branches and reports whether the approved finish stops locally or permits coordinated landing; it does not claim that one repository was pushed or merged. The run endpoint streams newline-delimited JSON so the surface updates while the coordinator works. Closing the panel returns to ordinary Inquiry.

The old management interview, Principal/Technical plan projections, Process Library, process-bound Trigger Manager, Run Inspector, process-automation controls, process-attention groups, and automatic entry router are not compatibility surfaces. Scheduled Triggers now live separately under Oversight and do not wrap Programming work.

### Model and capability boundaries

Planning, execution, and review use Ora's configured model endpoints. Endpoints whose transport or identity is Codex, Claude Code, or `claude_code` are rejected. A vision-capable configured slot is preferred for review so direct image/PDF inspection remains available; separate model families are preferred when configuration permits but not required.

Programming preserves Ora's ordinary external and multimodal capabilities. Removing the former process stack does not remove `execution_packet.py`, `execution_families.py`, browser/runtime/render/vision support, audio/video processing, PDF handling, or ordinary Inquiry.

### Verification

Focused proofs cover:

- repository inspection before questions, including instructions, tests, Git, and automation;
- the two-round and three-question limits;
- one approvable plan;
- real edits and tests in a disposable Git repository;
- fresh review without executor claims;
- task-branch creation and accepted-slice commits;
- baseline drift returning `ASK USER` before mutation;
- an unsupported executor completion claim never producing `DONE`;
- independent outside-source retrieval;
- direct image bytes reaching the reviewer; and
- rejection of Codex and Claude Code transports.

Documentation-impact proofs also cover complete packet validation, exact agreement between plan and packet surfaces, rejection of absent or contradictory dispositions, and one correctly typed accepting semantic verdict per affected surface.

Browser tests prove Programming starts inactive, activates only from its toolbar event, plans only after explicit submission, waits for approval before `/api/programming/run`, displays milestone/review progress, and returns a concise terminal message.


# Appendices

## Appendix A — Platform Compatibility Matrix

*Primary tested target: macOS Apple Silicon. Every non-macOS cell is `untested` unless evidence is cited; `needs-porting` marks a Mac-specific dependency; `n/a` means the surface does not apply. This matrix is the reviewable, per-subsystem expansion the platform-honesty discipline requires.*

### A.1 — Installation & Runtime Profiles
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| Desktop installer invocation (scripts/install.py) | works | untested | untested | untested | install.py is pure-Python 3.11+, no shell deps; command is `python3 scripts/install.py` (Mac/WSL/Linux) vs `py -3 scripts\install.py` (Win). Win-native + WSL marked 'pending Parallels test' in install-guide — untested, do NOT claim works. |
| start.sh launcher | works | n/a (use start.bat) | untested | untested | POSIX launcher with browser-open support; WSL/Linux remain untested. Native Windows uses the separate tracked start.bat rather than this script. |
| stop.sh | works | needs-porting | works | works | pkill -f is POSIX; works on WSL/Linux. A Windows-native stop.bat exists but is untested. |
| run-ora-server.sh (launchd wrapper) | works | n/a | mac-only | mac-only | #!/bin/zsh -l + hardcoded /opt/homebrew/bin/python3 + launchd job. Mac-only by construction; UNTRACKED at base commit. |
| Ora.app / Ora Models.app bundles | works | n/a | n/a | n/a | macOS .app bundle format with /opt/homebrew/bin/python3 shebang + subprocess 'open'. No cross-platform equivalent shipped. |
| install-server.sh (Linux server) | needs-porting | n/a | untested | works | Hard uname guard refuses non-Linux (line 82-85); apt-get assumes Debian/Ubuntu. On macOS it deliberately tells you to use install.py. WSL-Ubuntu likely works but untested; non-apt Linux (Fedora/Arch) needs-porting. |
| Python interpreter resolution | works | untested | works | works | start.sh prefers /opt/homebrew/bin/python3 then PATH python3. App bundles + run-ora-server.sh hardcode the homebrew path (Mac-only). On Windows, start.bat implements the `py -3` → `python` fallback (start.bat:15-24); untested. |
| RAM detection (platform_check.get_system_ram_gb + local_models.get_total_ram_gb) | works | untested | works | works | sysctl (Darwin) / /proc/meminfo (Linux) / wmic (Windows). wmic is deprecated/removed in recent Win11 builds — Windows path untested and at risk. |
| Inference engine resolution (platform_check.detect_platform) | works | untested | untested | untested | Apple-Silicon to MLX (Metal, Apple-only); all else to Ollama at localhost:11434. Non-Mac engine path never exercised on desktop; MLX is Apple-only hardware-bound. |
| Advisory file lock (runtime_paths.locked_file) | works | untested | works | works | fcntl.flock (POSIX) / msvcrt.locking (Windows) with guarded imports; Windows branch present but untested; falls to best-effort single-process if neither available. |
| Path root resolution (runtime_paths ORA_HOME/VAULT/etc.) | works | untested | works | works | Built from os.path.expanduser('~'); resolves %USERPROFILE% on Windows in principle (untested). Env-overridable. norm_key handles Windows case/separator normalization but untested. |
| Keyring credential storage | works | untested | untested | untested | install.py _try_keyring_get uses `keyring` lib (macOS Keychain on Mac). Windows Credential Locker / Linux Secret Service backends exist in the lib but untested here. Server path bypasses keyring (uses ~/.config/ora-server.env). |
| Server key store ~/.config/ora-server.env (chmod 600) | n/a | n/a | works | works | install-server.sh chmod 600 + read -rs. POSIX-only; server path is Linux-only anyway. |
| Browser auto-open post-launch | works | untested | untested | works | start.sh cascade open (mac) / xdg-open (linux) / start (win-ish). Ora.app uses `open` only (Mac). WSL browser handoff is notoriously flaky (install-guide warns). |
| Local model download (huggingface snapshot_download, MLX weights) | works | n/a | n/a | n/a | local_models.py recommends mlx-community/* repos (Apple MLX format). MLX runs on Apple Silicon only; non-Mac tiers route workhorses to cloud. Server path explicitly has no local models. |
| Windows launcher (start.bat / stop.bat) | n/a | implemented, untested | n/a | n/a | Honors ORA_HOME, prefers .venv then py -3/python, validates PORT, enables oversight by default, consumes --no-oversight, safely forwards other args, and binds PID/health/stop handling to this checkout. It does not supply the POSIX launcher's additional feature defaults, so reduced or differently configured behavior remains possible; no clean-room Windows result exists. |

### A.2 — Server & Browser-UI Runtime
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| App bind host='localhost' port 5000-5010 first-free | works | untested | untested | untested | socket bind loop server.py:14789-14792; localhost resolvable cross-platform but full boot untested off macOS |
| Hardcoded paths ~/ora and ~/Documents/conversations | works | needs-porting | untested | untested | WORKSPACE = os.environ.get('ORA_HOME') or os.path.expanduser('~/ora') at server.py:22 (honors ORA_HOME first, so relocatable); POSIX ~ fallback; %USERPROFILE% on Windows-native won't match ~/Documents layout |
| Launcher run-ora-server.sh (#!/bin/zsh -l, launchd job ora.server.codex) | mac-only | needs-porting | untested | needs-porting | zsh login shell + launchd are macOS; no .command/.app but this is the effective service wrapper; untracked |
| Python interpreter /opt/homebrew/bin/python3 | mac-only | needs-porting | untested | needs-porting | Homebrew Apple-Silicon path baked into launcher + review.py header; CLAUDE.md:192 requires 3.14.3 with Flask/mlx/keyring; py -3 on Windows |
| MLX heartbeat + mlx_mutex (Apple MLX/Metal) | mac-only | n/a | n/a | n/a | MLX heartbeat mlx_mutex.start_heartbeat server.py:14822; MLX is Apple-Silicon only; guarded by try/except so non-Mac skips but local-model path is dead |
| subprocess 'open'/'open -R' (Finder reveal) + bash script invocation | mac-only | needs-porting | needs-porting | needs-porting | open -R server.py:4951; startup runs bash generate-indexes.sh / compile-rag-manifest.sh :14856/14893 — bash + open are POSIX/mac |
| SIGINT/SIGTERM shutdown handler + threaded Flask | works | untested | works | works | signal.signal server.py:14975-14976; SIGTERM handling differs on Windows-native |
| index-v3.html asset paths (/static absolute URL refs, defer scripts) | works | works | works | works | browser-served absolute URLs are OS-agnostic; served through Flask so no filesystem separator concern client-side |
| Embedding readiness check (Ollama daemon reachability) | works | untested | untested | untested | main block server.py:14833 assert_embedding_ready; comment claims cross-platform (Win/Linux/Mac) but only mac-tested |

### A.3 — Settings, API Keys & Local-Model Handling
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| API-key storage backend (python `keyring`) | works | untested | untested | needs-porting | Mac uses macOS Keychain. keyring maps to Windows Credential Locker / libsecret(Secret Service) on Linux in principle, but Ora never exercises those — the server path deliberately uses a plaintext env-file instead. Headless Linux libsecret needs a running keyring daemon; unconfigured/untested here. |
| Server-side key file ~/.config/ora-server.env (chmod 600, source into env) | works | needs-porting | works | works | install-server.sh is bash + chmod 600 + `source`; the Linux deployment target. Windows-native has no chmod/POSIX perms and no `source`; would need a different secret mechanism. |
| Config path Path.home()/'ora'/'config' and models dir '~/ora/models/' | works | untested | works | works | Path.home() is cross-platform but the hardcoded ~/ora layout + absolute /Users/oracle paths baked into models.json are Mac-shaped; models.json is machine-specific + gitignored so regenerated per host. |
| Local MLX model inference (models/ contents) | mac-only | n/a | n/a | n/a | MLX/Metal is Apple-Silicon-only. Discovery code just reads config.json+safetensors (platform-neutral) but the models only RUN on Apple Silicon; the Linux server path routes everything to OpenRouter/direct APIs, no local models. |
| RAM detection in scripts/local_models.py get_total_ram_gb | works | untested | untested | works | Branches on sys.platform: sysctl(darwin) / /proc/meminfo(linux) / wmic(win32). wmic is deprecated on recent Windows; win32 + WSL branches untested. |
| HF snapshot_download local_dir_use_symlinks=False | works | untested | works | works | Symlinks explicitly disabled (copies), avoiding the Windows symlink-permission pitfall — good portability choice, but not exercised on Windows. |
| re-embed-local.py Ollama dependency (localhost:11434) | works | untested | untested | works | Requires a running Ollama; peripheral to keys/settings. OpenRouter alternative avoids the local dep. |
| user-settings.json atomic write (tmp .with_suffix + replace) | works | works | works | works | Path.replace is atomic same-filesystem on all platforms; no fcntl used here. |

### A.4 — Model / Provider Configuration & Routing
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| MLX local inference engine (call_local_endpoint engine=mlx / auto) | works | n/a | n/a | n/a | In-process mlx_lm (Apple Metal). engine='auto' -> mlx only on Darwin+arm64, else ollama. MLX is Apple-Silicon-only. |
| Ollama local fallback engine (localhost:11434) | works | untested | untested | untested | HTTP to localhost:11434; cross-platform in principle but untested off macOS. Only local path for non-Apple hardware. |
| mlx_mutex per-machine SIGSEGV serialization | works | n/a | n/a | n/a | Guards the MLX Metal segfault; irrelevant where MLX isn't the engine. API-only installs never take the blocking acquire. |
| API-only / cloud routing (OpenRouter + direct APIs) | works | untested | untested | untested | Pure HTTP + keyring; the zero-local-hardware path. Portable in principle, never tested off macOS. |
| API key storage via keyring (service 'ora') | works | untested | needs-porting | untested | python keyring: macOS Keychain on Mac; Secret Service / Credential Locker elsewhere in principle; WSL headless typically lacks a Secret Service backend. |
| Config/model paths (~/ora, ~/ora/models scan) | works | needs-porting | untested | untested | POSIX ~ + forward-slash paths throughout (Path.home()/'ora'/'models', ORA_HOME defaults ~/ora); no %USERPROFILE% handling. |
| Local model discovery scan of ~/ora/models/ safetensors | works | untested | untested | untested | Filesystem glob only, no Apple calls; portable but only MLX models are useful and MLX is Mac-only. |
| Server startup (Flask localhost) + refresh subprocess scripts | works | untested | untested | untested | Refresh scripts documented with /opt/homebrew/bin/python3; interpreter path Homebrew-specific. |
| MLX worker heartbeat + machine id 'studio-128' | works | n/a | n/a | n/a | DEFAULT_MACHINE_ID hardcoded to Mac Studio; single-machine config. Multi-machine Bonjour not built. |

### A.5 — Gear Pipeline & Mode Dispatch
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| Ora root paths (~/ora, ~/Documents/vault) via runtime_paths.py | works | untested | untested | untested | expanduser('~') + env-overridable (ORA_HOME/ORA_VAULT); norm_key uses os.path.normcase+realpath for Windows-safe prefix checks. POSIX-shaped defaults but constructed cross-platform. Never run on Windows/Linux per project target. |
| MLX/Metal inference engine (depth/breadth analyst model calls in gears 3/4) | works | n/a | n/a | n/a | MLX is Apple-Silicon-only; platform_check.py routes non-Apple to Ollama. The parallel-safe/mutex logic in run_gear4 (mlx_mutex.py) is Mac-specific. |
| launchers (start.sh / start.bat) | works | implemented, untested | untested | untested | Both default oversight on and accept --no-oversight as a diagnostic opt-out. start.bat additionally honors ORA_HOME, validates PORT, forwards other arguments safely, and uses checkout-bound process identity; the surrounding native-Windows pipeline remains unverified. |
| python3 interpreter (/opt/homebrew/bin/python3 3.14) | works | needs-porting | untested | untested | CLAUDE.md hardcodes /opt/homebrew/bin/python3 (Homebrew Mac path); Windows uses py -3. boot.py uses from __future__ import annotations for 3.9 compat. |
| localhost:5000 Flask server startup (server.py imports pipeline from boot.py) | works | untested | untested | untested | Flask; the /chat pipeline is synchronous JSON + browser polling (SSE only on 3 residual endpoints, not /chat). localhost binding portable in principle but untested off-Mac. |
| ThreadPoolExecutor parallel analyst calls (run_gear4) | works | untested | untested | untested | stdlib concurrent.futures — portable; MLX mutex serialization is the Mac-specific part. |
| Trace/data directory writes (data/pipeline-traces via pipeline_trace) | works | untested | untested | untested | path-based writes through runtime_paths; case-sensitivity differs on Windows (normcase handled). |

### A.6 — Framework Loading & Execution
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| FRAMEWORKS_DIR (os.path.expanduser('~/ora/frameworks/book')) | works | needs-porting | works | works | expanduser resolves ~ on POSIX; hardcoded ~/ora layout untested off macOS. |
| framework_config ORA_HOME override | works | untested | works | works | framework_config.py:60 honors ORA_HOME; framework_parser.py:20 does NOT (hardcodes path) -- inconsistent. |
| framework_invocability CONFIG_PATH (Path.resolve().parent.parent/config) | works | works | works | works | pathlib relative resolution cross-platform. |
| Scratch writes (~/ora/scratch/<execution_id>/) | works | untested | works | works | format_execution_result hardcodes ~/ora/scratch/ in failure message (line 969). |
| Model dispatch (local MLX/Metal slots for drift + mode-select) | works | mac-only | mac-only | mac-only | Local slots need MLX/Metal (Apple-only); off-platform only cloud slots work; drift/mode-select degrade to SKIPPED/None when endpoint None. |
| compose_framework_spec overlay reads (utf-8, OSError-guarded) | works | untested | works | works | Whole path unwired in production so effectively n/a today. |

### A.7 — Output Styles & Personal Values
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| mind.md / custom-styles.json / self-spec.md paths under ~/ora (WORKSPACE derived from __file__) | works | untested | untested | untested | WORKSPACE = dirname(dirname(abspath(__file__))) in style_assembly/style_store/boot; path-separator-agnostic via os.path.join, so likely portable, but only exercised on macOS (M4 Max). MIND_MD_PATH/STORE_PATH/SELF_SPEC_PATH all os.path.join-built. |
| Atomic file writes via tmp + os.replace (style_store._save_all, mind endpoints, mind.md writes) | works | untested | untested | untested | os.replace is atomic within a filesystem on POSIX and Windows; behavior should hold cross-platform but is only tested on macOS. No fcntl/symlink use in this subsystem. |
| PyYAML dependency for style_assembly.load_registry (parses style-*.md yaml blocks) | works | untested | untested | untested | yaml import guarded (raises RuntimeError if PyYAML missing at :57); _is_known_style_id degrades to accept, and _compose_output_style swallows exceptions to '' — so a missing-PyYAML environment silently disables Output Styles rather than crashing. Pure-Python, no native deps. |
| mind_projection model call via model_dispatch.invoke_chat (breadth slot) | works | untested | untested | untested | Depends on the model routing/MLX layer, which is Apple-Silicon-oriented for local slots; the projection call itself is provider-agnostic (any breadth-slot model). Only the local-model backing is Mac-specific. |
| Server endpoints bound to localhost:5000 (Flask) | works | untested | untested | untested | Standard Flask; localhost binding is portable. Not specifically exercised off macOS for this subsystem. |

### A.8 — Vault, RAG, Conversations & Persistent State
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| Server-authoritative history/fork reconstruction | works | untested | untested | untested | Provider-neutral Python; complete off-Mac application path not verified. |
| Endpoint-aware token packing | works | untested | untested | untested | Depends on truthful endpoint limits; unknown endpoints deliberately fail small. |
| Ollama/local embedding and reranking | works | untested | untested | untested | Requires local daemons/models; platform packages differ. |
| Cloud embedding/reranking | works | untested | untested | untested | Network/provider-neutral; credentials and keyring backend are platform-specific. |
| ChromaDB persistent collections | works | untested | untested | untested | Embedder, dimension, and physical collection map must match on every machine. |
| Isolated five-file Aside help corpus with lexical fallback | works | untested | untested | untested | Indexed acceleration depends on configured embeddings/Chroma; local lexical ranking over the same tracked `help/` files remains available. |
| Relocatable session/conversation/vault paths | works | untested | untested | untested | Runtime paths are configurable; full non-Mac lifecycle is not verified. |
| Atomic envelope writes | works | untested | works | works | Same-filesystem replace is atomic; the per-Dialogue lock is process-local. |
| Stealth Ora-managed purge | works | untested | untested | untested | Cannot erase exports or provider/Git/backup/external copies. |

### A.9 — Tool Dispatch, the Capability Gate & Execution Telemetry
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| Ora root / path handling (WORKSPACE, DATA_DIR) via runtime_paths + _matchable/_cmp_key normalization | works | untested | untested | untested | tool_events._matchable/_cmp_key explicitly forward-slash + normcase for cross-platform regex/startswith matching, and use runtime_paths abstraction; code is written for portability but only macOS is the tested target. |
| MCP stdio reader threads and bounded binary JSON-RPC | works | untested | untested | untested | mcp_client uses reader threads to avoid select-on-Windows-pipe limitations, enforces byte bounds before decoding, and correlates each reply to the exact request id; no off-Mac execution evidence. |
| Pinned local MCP packages and Chromium (vault-fs/playwright/github) | works | untested | untested | untested | Exact Node entrypoints and the package lock replace runtime npx resolution. Installation provisions the locked Chromium. Compatible Node/install behavior and child launch remain untested off-Mac; an unavailable child fails soft. |
| Approvals file locking (execution-approvals.json via runtime_paths.locked_file / fcntl-style) | works | untested | untested | untested | tool_events._with_approvals_lock uses _rp.locked_file (cross-platform lock abstraction in runtime_paths) + atomic os.replace; portability depends on runtime_paths.locked_file which is out-of-scope here. |
| Tool-event sink write via os.open(O_WRONLY\|O_APPEND\|O_CREAT, 0o644) | works | untested | works | works | 0o644 mode bits are POSIX-permission semantics; on Windows os.open ignores most mode bits but the append write still functions. Untested on Windows-native. |
| yt-dlp URL import binary resolution | mac-only | needs-porting | untested | untested | url_import.YTDLP_BINARY = shutil.which('yt-dlp') or '/opt/homebrew/bin/yt-dlp' — Homebrew-specific fallback; error message says 'brew install yt-dlp'. which() would find a Linux-installed binary but the fallback + guidance are Mac-only. |
| local_diffusers capability provider (MLX/Metal diffusion, auto-registered) | works | n/a | n/a | n/a | Auto-registered in capability_registry.load_registry best-effort; MLX/Metal is Apple-Silicon-only. On other platforms the import fails and is silently skipped (registry falls through to API providers). |
| web_fetch browser tier (channel=local Playwright) used by web_extraction/consultation | works | untested | untested | untested | web_consultation passes channel to web_fetch; the internal Playwright/Chrome tier depends on a browser install. Not exercised here; only macOS tested. |
| pipeline_trace / tool-events sink directory data/pipeline-traces under ORA_HOME | works | untested | works | works | _resolve_ora_home derives from __file__ to avoid literal '~' when HOME unset (POSIX-observed bug); refuses '~' paths. Filesystem-agnostic otherwise. |
| Secret/sensitive path patterns (.ssh, .aws, .config/ora, id_rsa, .pem) | works | untested | works | works | Patterns are POSIX-dotfile-oriented and forward-slash-anchored after _matchable normalization; Windows secret conventions (e.g. %APPDATA% credential stores) are not in the pattern set — untested + likely under-covers Windows. |

### A.10 — Visual Intelligence
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| Server-side envelope→SVG render (primary): Playwright + Chromium headless | works | untested | untested | untested | boot.py:_render_visual_svg_browser uses p.chromium.launch(); Playwright supports all OSes but Ora-tested only on macOS. Requires playwright browser install. |
| Server-side envelope→SVG render (fallback): node + jsdom CLI | works | untested | untested | untested | boot.py:880-884 subprocess ['node', render-envelope.js]; jsdom vendored under tests/node_modules. Node cross-platform but path assumes ~/ora/server/static via WORKSPACE. |
| SVG rasterization (render-critique loop): Playwright Chromium set_content + screenshot | works | untested | untested | untested | _rasterize_svg_light boot.py:909; same Playwright dependency; only runs in render-critique-refine path. |
| Video render fonts (_resolve_font_file) | works | needs-porting | needs-porting | needs-porting | render.py:247 candidates are all /System/Library/Fonts and /Library/Fonts Mac paths; final fallback is a hardcoded Mac path that won't exist elsewhere — mac-only. |
| FFmpeg binary lookup (render.py, media_capture.py, waveform.py, transcription.py) | works | needs-porting | untested | untested | shutil.which('ffmpeg') or '/opt/homebrew/bin/ffmpeg' — PATH lookup is portable but the fallback is a Homebrew-on-Apple-Silicon path; drawtext requires homebrew-ffmpeg tap on Mac (per MEMORY). |
| Screen/mic capture (media_capture.py) | works | needs-porting | n/a | needs-porting | docstring 'screen + microphone capture on macOS'; ffmpeg avfoundation input is macOS-specific; would need gdigrab/dshow (Windows) or x11grab (Linux). |
| Transcription (transcription.py): whisper-cli (whisper.cpp) | works | needs-porting | untested | untested | WHISPER_BINARY fallback /opt/homebrew/bin/whisper-cli; whisper.cpp builds cross-platform but binary path + models dir assume Mac layout. |
| WORKSPACE / path form (~/ora, /Users/oracle) | works | needs-porting | untested | untested | SCHEMAS_ROOT/WORKSPACE use os.path.expanduser('~/ora'); tilde expands cross-platform but %USERPROFILE%\ora layout untested; vault path ~/Documents/vault Mac-specific. |
| 'node' on PATH for CLI render fallback | works | untested | untested | untested | subprocess ['node', ...] relies on node in PATH; no absolute fallback like ffmpeg has. |
| Client compiler + Konva canvas (browser JS) | works | works | n/a | works | Pure browser JS, vendored libs, no CDN (CSP-safe); runs in any modern browser regardless of server OS. |
| JS test harness (tests/run.js, jsdom) | works | untested | untested | untested | cd tests && node run.js; jsdom + node cross-platform but only run on macOS. |

### A.11 — Audio/Video Media & Timeline Editing
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| Explicit feature loader and browser seams | works | untested | untested | untested | Standard Python/Flask/browser code; an absent or invalid package is skipped and core startup continues. |
| FFmpeg / ffprobe and software codecs | works | untested | untested | untested | Plugin backends prefer PATH and retain Homebrew fallbacks; installed codecs, filters, and fonts vary by host. |
| drawtext overlays and watermark | works conditionally | needs-porting | needs-porting | needs-porting | The working Mac path needs an FFmpeg build with drawtext; plugin font resolution still contains Mac-specific candidates. |
| Screen, microphone, and webcam capture | mac-only | needs-porting | n/a | needs-porting | AVFoundation is the working path; no other desktop-capture path is claimed. |
| Core local Whisper transcription | works | untested | untested | untested | `orchestrator/transcription.py` remains outside the plugin; cloud alternatives are network-dependent rather than OS-specific. |
| Plugin timeline/library atomic state and process cancellation | works | untested | untested | untested | Uses Python atomic replacement and subprocess APIs; non-Mac concurrency and termination behavior is unverified. |
| Plugin removal fallback | works | untested | untested | untested | No valid descriptor means no video routes/assets/UI; core transcription, Inquiry A/V drop, and the shared job queue remain. |

### A.12 — Meta-Layer Oversight
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| Data/vault paths (~/ora, ~/Documents/vault) via runtime_paths | works | untested | untested | untested | runtime_paths.py builds from expanduser('~') and normcase, env-overridable (ORA_HOME/ORA_VAULT); designed relocatable but only exercised on macOS |
| Advisory file lock on Decision Log / human-queue | works | untested | works | works | runtime_paths.locked_file (runtime_paths.py:83-94) uses fcntl.flock (POSIX) with msvcrt.locking fallback (Windows), guarded imports (runtime_paths.py:66-91). CLAUDE.md still says 'fcntl advisory lock' — stale (cross-platform since). Windows msvcrt path untested. |
| Oversight daemon lanes (OS event / exact deadline / watchdog) | works | untested | untested | untested | Event and deadline lanes are pure Python around OS notifications and persisted queues; the watchdog restarts either lane and exposes restart counts. The complete off-Mac lifecycle has not been exercised. |
| Launch oversight default / `--no-oversight` opt-out | works | implemented, untested | untested | untested | `start.sh` and `start.bat` both default oversight on and consume the explicit diagnostic opt-out. The Windows launcher forwards all other arguments with delayed expansion disabled. |
| Trigger action process and whole-firing deadline | works | implemented, untested | untested | untested | Real project-tool/framework/email work runs in a spawned action process. POSIX verifies the group is gone; Windows assigns the held wrapper to a non-breakaway, kill-on-close Job before action start and requires the Job's active-process count to reach zero, but still has no native-Windows result. An unacknowledged containment boundary keeps its overlap claim. |
| Trigger completion and watcher-publication durability | works | untested | untested | untested | Watcher event bytes and successful-handler delivery markers are separately durable; the router claims the same stable identity in `EventLedger`, so a rescan cannot repeat its effects. Dependent deliveries bind exact admissions before source completion and replay at startup. Pure file/state machinery, but verified only on the current Mac path. |
| CLI standalone runners (python3 orchestrator/*.py) | works | untested | untested | untested | CLAUDE.md hardcodes /opt/homebrew/bin/python3 (Mac Homebrew); on Windows would be py -3 / python |
| daily_note vault git activity (core.quotepath=off subprocess) | works | untested | untested | untested | shells out to git; stat-scan fallback for non-git vaults; git invocation cross-platform but path handling untested off-Mac |
| server.log O_APPEND truncation (retention sweeper) | works | needs-porting | untested | works | in-place truncate rather than rename because Ora.app launcher holds long-lived O_APPEND fd (Mac .app); Windows file-locking semantics differ |
| mlx-worker heartbeat (oversight_health input) | mac-only | n/a | n/a | n/a | MLX/Metal is Apple-Silicon only; heartbeat emitted from server.py:14805 for local inference worker |

### A.13 — Execution-Review Architecture
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| code_execute sandbox backend (check-execution enforcement) | works | needs-porting | needs-porting | needs-porting | code_execute.py:43-51 — Phase 1 implements ONLY macOS sandbox-exec (returns 'orchestrated'); _sandbox_backend returns None off-darwin so code_execute fail-closes/gates. A Phase 5 ORA_EVIDENCE_SANDBOX wrapper + Linux unshare -rn is implemented in the unmerged phase5-impl worktree (code-review gate) — not merged to ~/ora main, not installed. |
| MLX/Metal local inference (upstream of, not inside, Execution Review) | works | n/a | n/a | n/a | Apple-only; Execution Review itself is pure-Python and does not touch MLX, but the enclosing orchestrator is Apple-Silicon-tested. |
| Direct bash_execute prepared-command path | works | untested | untested | untested | One immutable argv, clean environment, executable identity, semantic effect profile, and authority scopes flow through approval to shell=False execution. Windows-native parsing is simulation-tested only; WSL/Linux have not been exercised for this accepted boundary. |
| Advisory file locking on approvals + sticky JSON (runtime_paths.locked_file) | works | untested | untested | works | tool_events uses _rp.locked_file around approvals RMW; POSIX fcntl vs Windows msvcrt path lives in runtime_paths — POSIX branch tested, Windows branch untested (test_portability simulates but no live Windows run). |
| Protected-path + secret/sensitive regex matching (case/separator normalization) | works | untested | works | works | tool_events _matchable/_cmp_key forward-slash + normcase-fold so /-anchored patterns fire on both; Windows case-fold path is coded but only simulation-tested, not run on native Windows. |
| Trace-local sinks + packet paths (~/ora, data/pipeline-traces, os.O_APPEND writes) | works | untested | works | works | record() uses os.open(O_WRONLY\|O_APPEND\|O_CREAT, 0o644) — the 0o644 mode is a no-op on Windows; O_APPEND cross-platform. All roots resolved via runtime_paths (no hardcoded /Users/... in the subsystem). |
| SCRATCH_DIR location for sandboxed check execution | works | needs-porting | untested | untested | HANDOFF OQ11/OQ13: SCRATCH_DIR is INSIDE ~/ora/$HOME; the sandbox read-side re-allow is macOS-specific; off-mac containment lives in the unmerged phase5-impl worktree (code-review gate), not merged/installed. |

### A.14 — Operation-Matrix (Mission / Objectives / Milestones)
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| vault_root() path default ~/Documents/vault via os.path.expanduser | works | untested | untested | untested | operation_matrix.py:44-52 — os.path.expanduser resolves %USERPROFILE% on Windows and $HOME on Linux, so it is portable in principle, but the '~/Documents/vault' default assumes a macOS-style Documents dir and has only been exercised on macOS. Overridable via ORA_VAULT_PATH/ORA_VAULT env vars. |
| Atomic write via tmp file + os.replace (operation_matrix.py:347-350) | works | untested | untested | untested | os.replace is cross-platform atomic-within-same-filesystem on all three OSes; no fcntl/flock used here (unlike oversight_actions). Untested off macOS but no OS-specific primitive. |
| _safe_name filename sanitization strips only \ and / (operation_matrix.py:59-62) | works | needs-porting | works | works | Does NOT strip Windows-illegal filename chars : * ? " < > \|. A project display name containing e.g. a colon would produce an invalid 'Project Matrix <Name>.md' filename on native Windows/NTFS. Fine on macOS/Linux/WSL where only / is illegal. |
| obsidian:// URI construction for file links (server.py:4790-4802) | works | untested | untested | untested | URI scheme itself is cross-platform (Obsidian registers it on all desktop OSes); uses relative_to(vault_root) + urllib quote. No OS-specific code, but only verified on macOS. |
| reveal-in-Finder (server.py:4914-4951, sibling /api/fs/reveal used by files modal) | mac-only | n/a | n/a | n/a | Explicitly gated: returns 501 'reveal-in-Finder is macOS-only' on non-Darwin; runs subprocess ['open','-R',path]. Not part of core MOM but is the 'reveal' action wired to the files-index entries this subsystem produces. Windows would need explorer /select, Linux xdg-open. |
| PyYAML frontmatter parse (operation_matrix.py:36-39, 82-88) | works | works | works | works | Pure-Python with graceful ImportError fallback; no platform concern. |

### A.15 — Documentation-Code Parity & Vault↔Repo Mirror Governance
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| Five-root documentation-integrity input | works | untested | untested | untested | The focused check requires explicit vault, Ora, app, org, and MSI roots plus base commits. It does not infer worktrees or fall back to hardcoded live checkouts. Cross-platform path handling is coded but not claimed as live-tested off macOS. |
| Homebrew python3 hardcode in run instructions | works | needs-porting | untested | untested | verify-configuration-equivalence.py docstring says '/opt/homebrew/bin/python3 scripts/...'; check-capability-sync usage same. Homebrew path is Apple-Silicon-only; check-portability.py flags this as CRITICAL homebrew-python-hardcode elsewhere. Windows uses py -3. |
| check-portability.py subprocess/os.walk portability linter itself | works | untested | works | works | Pure-Python stdlib (os.walk, re, pathlib), no shell-out; should run cross-platform. Its RULES encode macOS/POSIX detection — the linter is the porting-safety net, so it is designed to be run on the porting host. |
| check-model-refresh-drift.py shells out to git subprocess | works | untested | works | works | subprocess.run(['git','status','--porcelain',...]) — portable if git on PATH; no shell=True; forward-slash WATCHED paths are git-normalized so work on Windows git too. |
| Coordinated task gate and hooks | works | needs-porting | untested | untested | The read-only coordinator-driven final check is active. Seven local hooks are installed: five blocking pre-push hooks and the Ora/vault fail-open post-commit audits. They remain bypassable and provide neither remote enforcement nor an atomic five-repository transaction. The POSIX hook scripts are not verified on native Windows. |
| DCP output paths (`<resolved-vault>/Administration/DCP/`) | works | untested | untested | untested | The vault root is resolved rather than pinned to one user's home. Case-sensitivity still matters: registered names with a spaced em dash and mixed-case prefixes must match exactly. |

### A.16 — Update, Recovery, Troubleshooting & Diagnostics
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| ora-test / ora-cost-summary shebang '#!/usr/bin/env /opt/homebrew/bin/python3' | works | needs-porting | untested | needs-porting | Homebrew path is Apple-Silicon-specific; on Linux/WSL Python is elsewhere; Windows has no shebang. Portability linter flags homebrew-python-hardcode as CRITICAL. |
| ora-test --no-wait uses os.fork() + os.setsid() + os.dup2 | works | needs-porting | works | works | os.fork/setsid are POSIX-only; Windows CPython has no fork. --no-wait path would crash on Windows-native. |
| generate-indexes.sh + compile-rag-manifest.sh invoked via subprocess.run(['bash', ...]) at server startup | works | needs-porting | works | works | Requires bash + awk/sed/grep in PATH. Windows-native lacks bash unless Git-Bash/WSL present; startup index regen and RAG recompile would silently fail (capture_output, no error surfaced). |
| compile-rag-manifest.sh stat portability | works | untested | works | works | Uses 'stat -f %m \|\| stat -c %Y' fallback (BSD then GNU) — already portable across Mac/Linux; still bash-dependent so untested on Windows-native. |
| oversight_health mlx_worker heartbeat (30s), emitted by mlx_mutex.start_heartbeat | works | n/a | n/a | n/a | MLX/Metal is Apple-Silicon-only; on non-Mac hosts there is no MLX worker so this heartbeat is inapplicable (should be conditioned on local-MLX presence). |
| Recovery/manual docs assume ~/ora, chown -R $USER:staff, brew install python@3.12, /opt/homebrew/bin/python3.12 | works | needs-porting | works | works | install-recovery.md gives a Linux chown variant but Step-1 fixes are Homebrew-centric; install-manual.md does provide py -3 / $env:USERPROFILE PowerShell variants. |
| Server-install recovery/manual path (apt-get, ollama, .venv, source ~/.config/ora-server.env) | n/a | needs-porting | works | works | Debian/apt + POSIX venv sourcing; targets Linux servers. WSL Ubuntu works; native Windows would need a rewrite. |
| Manual/server install pulls ollama embedding model 'nomic-embed-text' | works | untested | untested | works | Live embedding stack migrated to bge-m3 1024-dim (CLAUDE.md), but install-server.sh + install docs still pull nomic-embed-text 768-dim — a fresh server install would build a nomic-dimensioned ChromaDB. Drift, not a platform gap, but affects any non-Mac server install. |
| check-portability.py itself | works | works | works | works | Pure stdlib (pathlib/os/re), no shell-out; designed to run everywhere as the porting gate. |
| diagnose-extraction-pipeline.py (os.path.expanduser('~/ora') + /opt/homebrew python in docstring) | works | untested | works | works | Uses expanduser (portable); only the suggested run command in the docstring hardcodes Homebrew python. Needs a live model config to exercise tests 5-6. |
| install.log (runtime output, not committed) contains Mac-only absolute paths when generated (/Users/oracle, /opt/homebrew) | works | n/a | n/a | n/a | Not a tracked artifact at 7a5e8f40 — install.py writes it at REPO_ROOT/install.log at runtime; executed on other platforms. 16 lines carry Mac-only tokens. |

### A.17 — The Project Plugin Convention
| Item | macOS | Windows-native | WSL | Linux | Note |
|---|---|---|---|---|---|
| Pointer dir path ~/ora/data/projects/ (os.path.expanduser) | works | needs-porting | works | works | expanduser resolves ~; MSI actually runs on cloud-ora Linux (systemd/ dir present), so POSIX paths are exercised. Windows-native untested and the /Users/... roots in pointer files are absolute Mac paths. |
| Project tool subprocess (subprocess.run, argv list, cwd=project.root, no shell) | works | untested | works | works | shell=False, argv-list — portable in principle; but tool commands hardcode 'python3' interpreter which does not exist by that name on Windows (would need 'py -3'/'python'). |
| Manifest tool interpreter 'python3' (first argv element) | works | needs-porting | works | works | project_registry passes head argv through as-is expecting python3 on PATH; MSI manifests all use python3. Windows-native lacks python3 alias. |
| Atomic writes via os.replace + .tmp sidecar (project_meta/active_project/nexus_rename) | works | untested | works | works | os.replace is cross-platform atomic-rename; not tested on Windows filesystem semantics. |
| MSI reference-impl scheduling: launchd/ AND systemd/ dirs | works | n/a | untested | works | launchd = mac-only; systemd = linux. Project ships both because MSI runs Mac (dev) + cloud-ora (Linux prod). No Windows scheduler equivalent shipped. |
| MSI notify tool (osascript macOS Notification Center) | works | n/a | n/a | mac-only | tools/notify.py + backfill_watchdog use osascript — Apple-only; degrades/no-ops off-Mac. Project-specific, outside core convention. |
| Vault Projects folder index (~/Documents/vault, Full-Disk-Access dependency) | works | untested | works | works | ensure_project_folder/list_project_files best-effort; return exists:False when absent (cloud-ora has no vault) — degrades cleanly cross-platform. |
| ORA_HOME derivation (Path(__file__).parent.parent) | works | works | works | works | pathlib-based, platform-neutral. |

## Appendix B — Implemented-vs-Planned Ledger

*Everything in the installed system at `7a5e8f40` that is not fully implemented, so the prose never overstates. Status vocabulary: partial (present but flag-gated-off or incomplete), planned (designed, not built), deferred (explicitly punted), stale (drift/superseded), unmerged (built but not on `~/ora` main).*

### B.1 — Installation & Runtime Profiles
- **[planned]** Hybrid deployment profile (multi-machine / local-plus-API) — _supported_now=False; step_select_profile hard-refuses with 'reserved for future' message (install.py lines 1017-1021); gated on G1.27 network discovery._
- **[planned]** Organization deployment profile (shared / API-pool) — _supported_now=False; hard-refuse; gated on concurrency validation. cloud-ora-install.md notes Organization 'blocked on the concurrency architecture spec'._
- **[partial]** Windows-native install (`py -3 scripts\install.py`) — _the command remains pending a Parallels clean-install result. `start.bat`/`stop.bat` ship with checkout-bound process identity, oversight on by default, `--no-oversight`, safe argument forwarding, interpreter/port selection, and health verification; those behaviors are implemented but untested on native Windows. Mac app bundles and launchd paths remain Mac-only._
- **[partial]** Windows WSL install — _Documented as 'practical fallback' in install-guide; behaves as Linux-desktop but untested in the formal matrix (Parallels covers it, not yet passed)._
- **[partial]** Linux desktop install — _install-guide: 'Best effort/community... not the primary tested desktop path'; out of the desktop test matrix (install-testing.md line 23)._
- **[deferred]** Packaged double-click installer (.app/.dmg/.pkg) — _install.py docstring line 78: 'What this script does NOT do yet: Package a double-click installer'; install-guide 'There is no packaged app installer yet.'_
- **[partial]** Empirical vision-capability probe during install — _install.py step 6 only runs probe when ORA_INSTALL_PROBE=1 set (default appends --no-probe, lines 1185-1188) — default-OFF => partial._
- **[stale]** Server embedding model = nomic-embed-text — _install-server.sh line 174-175 pulls nomic-embed-text; CLAUDE.md says Mac migrated to bge-m3 (1024-dim) 2026-05-29. chromadb.json is machine-specific so server may legitimately stay nomic, but the server default now diverges from the desktop default — flag._
- **[retired]** Legacy natural-language installer — _the 21 Markdown files under `installer/` (manifest, appendix, phase1/, phase2/) were deleted rather than reconciled; they were never the live path. `installer/macos/` remains and is live._
- **[partial]** launchd auto-start wrapper (run-ora-server.sh) — _Present but UNTRACKED at base commit (git status ??); Mac/zsh/homebrew-only; not part of the committed install._
- **[deferred]** systemd/cron config for server cadence — _install-server.sh 'What it does NOT do': 'systemd / cron configuration — the MSI cadence is configured separately'; cloud-ora-install.md same._
- **[not in commit 7a5e8f40 — untracked local artifact]** `/Users/oracle/ora/run-ora-server.sh` — launchd wrapper present on the author's Mac but absent from a fresh clone (the tracked launcher at this commit is start.sh, which sets ORA_HOME + 7 ORA_* flags): cd + export ORA_HOME + flags + exec homebrew python3 server
- **[retired]** `installer/install-manifest.md` and its 20 layer files — Legacy natural-language layered installer spec (phase1 6 layers + phase2 12 layers + gate + appendix), since deleted. Explicitl
- **[partial]** `/Users/oracle/ora/help/install-guide.md` — Happy-path install guide with per-platform command matrix (macOS/Windows-native/WSL/Linux-desktop/Linux-server).
- **[partial]** `/Users/oracle/ora/docs/install-testing.md` — Clean-room test protocol: 3-env matrix (Mac Mini A/B + Parallels Win11 ARM), Solo-only ship criterion, uninstall reset, 
- **[stale]** `/Users/oracle/ora/docs/cloud-ora-install.md` — Linux server operator guide for the MSI publication cadence.
- **[stale]** `/Users/oracle/ora/docs/hardware-report.md` — Sample hardware-evaluation report (M4 Max 128GB, gpt-oss-120b MXFP4).
- **[stale]** `/Users/oracle/ora/docs/README.md` — End-user 'Your Local AI System' orientation page.

### B.2 — Server & Browser-UI Runtime
- **[partial]** vision-retry queue (/chat/queue-retry) — persistence only, NO automatic flush — _chat_queue_retry server.py:9034 (docstring at 9060) 'NO automatic retry here — a future daemon or user-triggered action will flush'_
- **[planned]** CSP / Content-Security-Policy header on served pages — _grep content-security/http-equiv in index-v3.html = 0 matches; no CSP meta or Flask after_request header found_
- **[planned]** HTTP auth / bind beyond localhost (multi-machine remote access) — _app.run(host='localhost') server.py:15040 — no auth middleware; MEMORY notes multi-machine 'planned'; server is single-user localhost only_
- **[unknown]** `~/ora/server/.port` — 4-byte file recording chosen port (side-channel for other tools)

### B.3 — Settings, API Keys & Local-Model Handling
- **[planned]** Server-side (Linux) keys stored in an OS credential store — _No libsecret/keyring usage on the server path; keys are plaintext env-file only. Keyring backend on headless Linux is untested/unconfigured_
- **[stale]** scripts/local_models.py recommendation catalog reflecting the deployed 2026-05-26 vision lineup — _Catalog still lists Qwen2.5-7B/27B/72B + Llama-3.1-70B; deployed models.json runs Qwen3.5/GLM-4.6V/Mistral-Medium-3.5 vision models_
- **[stale]** user-settings.json cleaned of removed keys (default_system_audio, whisper.provider) — _On-disk file retains keys absent from DEFAULTS; preserved by forward-compatible merge but drift_
- **[stale]** `/Users/oracle/ora/config/user-settings.json` — Persisted non-secret user settings (capture/whisper/interface/external_apis(empty)/styles). Contains stale keys not in c
- **[unknown]** `/Users/oracle/ora/config/local-models.json` — State file for scripts/local_models.py install-side download tracking ({installed:[], selected:{}}). Distinct from model

### B.4 — Model / Provider Configuration & Routing
- **[partial]** OpenRouter + direct-API + vendor-authoritative registry refresh pipeline — _sync_model_registry/refresh-* wired to /api/model-registry/refresh; vendor_catalog_registry + direct_catalog behind enabled() gates_
- **[stale]** Legacy bucket-walk resolution path (pipelines[context] -> buckets -> endpoints) — _code remains router.py l.338-392/1095-1147 but routing-config has no pipelines block; dead, Chunk-12 removal pending_
- **[deferred]** Six-bucket tier UI + machine panel + resource reservation + Bonjour multi-machine discovery (design doc) — _design-model-configuration.md Historical/superseded; buckets tab retired; single machine studio-128; provider DB removed 2026-07-01_
- **[partial]** Empirical reach-probe populating registry last_probe_at — _reach endpoints exist but registry last_probe_at=null_
- **[stale]** verify-configuration-equivalence.py / migrate-to-configurations.py — _one-time migration helpers; migration complete, pipelines gone_
- **[partial]** `orchestrator/direct_catalog.py` — Resolves OpenRouter-style id to direct-provider id by fetching+caching each provider's /models listing (TTL + disk cache
- **[partial]** `orchestrator/vendor_catalog_registry.py` — Builds vendor-authoritative merged registry (native + AA + OpenRouter): size classification, vision-family, AA pricing, 
- **[partial]** `scripts/build_vendor_authoritative_registry.py` — Builds vendor-authoritative registry via vendor_catalog_registry.py.
- **[stale]** `scripts/verify-configuration-equivalence.py` — One-off verifier that migrated configs resolve equivalently to old pipelines chains.
- **[stale]** `scripts/migrate-to-configurations.py` — One-time migration: routing-config pipelines -> config/configurations/*.json.
- **[stale]** `docs/design-model-configuration.md` — Design history for six-bucket tiers + pipeline-config UI + resource reservation + multi-machine Bonjour.

### B.5 — Gear Pipeline & Mode Dispatch
- **[implemented — default-ON]** Step 8.5 deterministic deliverable scrub (pipeline-leak line removal) — _boot.py step 8.5; ORA_DELIVERABLE_SCRUB code-default-off but start.sh force-exports =1 (start.sh:47), so on by default on the installed system — flag state unverified in this pass_
- **[partial]** Model-driven tool escape hatch (## REQUESTABLE TOOLS) — __model_tool_selection_enabled reads ORA_MODEL_TOOL_SELECTION default '0' (OFF) boot.py:8376 — flag-gated-off => partial_
- **[partial]** Gear 2 `fast` model slot — _boot.py:9349 comment 'new in 2026-05-24 redesign, being wired by parallel model-selector thread'; falls back to step1_cleanup/active — slot not yet fully wired into config files_
- **[deferred]** Gear 5 — _CLAUDE.md 'No Gear 5: model switcher UI makes it redundant'; else-branch boot.py:9389 is a bare fallback_
- **[stale]** `/Users/oracle/ora/architecture/trusted-web-sources.md` — 9th file in ~/ora/architecture/ — a web-source allowlist read by f-consult / web consultation. NOT in CLAUDE.md's Decisi
- **[stale]** `/Users/oracle/ora/frameworks/book/f-evaluate.md` — Step-4 cross-adversarial evaluator spec (universal 7-section contract). References mode's `## EVALUATION CRITERIA` — LEG
- **[stale]** `/Users/oracle/ora/frameworks/book/f-revise.md` — Step-5 reviser spec (mirror contract). Also references legacy `## EVALUATION CRITERIA` name.
- **[stale]** `/Users/oracle/ora/boot/boot.md` — System prompt loaded into pipeline-step prompts (trimmed behavioral preamble via _extract_boot_behavioral_preamble). Con

### B.6 — Framework Loading & Execution
- **[partial]** Canonical suite executability: DCA — _No MILESTONES DELIVERED, FrameworkParseError, absent from invocability. Suite is 5 executable + DCA analytical-only._
- **[partial]** framework_config ${config.*} substitution + overlay splicing (compose_framework_spec) — _Coded + unit-tested, NO production call site; not wired into executor._
- **[partial]** Per-framework default configuration via framework-routing.json — _Loader/lookup implemented but JSON has only _example_pef stub; always no-ops._
- **[planned]** Multi-step orchestration chains with per-step configuration — _execute_framework docstring 'when those land'; framework-routing _format note per_mode_configuration 'for future'._
- **[partial]** `/Users/oracle/ora/orchestrator/framework_config.py` — Project-config binding runtime: parse_config_interface, substitute_config_refs (${config.<key>}), splice_extension_overl
- **[partial]** `/Users/oracle/ora/frameworks/book/decision-clarity-analysis.md` — DCA canonical framework spec (renamed from Wicked Problems 2026-05-01).
- **[partial]** `/Users/oracle/ora/config/framework-routing.json` — Per-framework default named-configuration map; consulted when /framework invoked without --config.
- **[planned/absent]** `/Users/oracle/ora/frameworks/personal/` — a personal-frameworks slot referenced in design, but the directory does not exist at 7a5e8f40 (no `frameworks/personal/`, no README.md); only `frameworks/user/` exists (a single `.gitkeep`).
- **[planned]** `/Users/oracle/ora/frameworks/user/` — User frameworks slot (empty).

### B.7 — Output Styles & Personal Values
- **[partial]** Situational deltas (one-rung demeanor swaps) + written/conversational register switching driven at runtime — _resolve_demeanor()/deltas math + conversational-register overlay EXIST and are unit-tested in style_assembly, but boot.py hardcodes style_register='written' :7767 and style_deltas=None :7768 with no code path setting them. The register-derivation is explicitly noted as future ('once the resolution chain is wired', compose() docstring :207-208)._
- **[deferred]** 'Fiction register' pane section — _Vault 'Reference — Ora Output Styles Pane.md' §1 lists it as 'placeholder; deferred, not built'._
- **[stale]** style_assembly docstring claim 'Nothing in the pipeline imports it yet' — _style_assembly.py:8-9 says the pipeline doesn't import it; boot.py::_compose_output_style :8442 imports and calls compose(). Doc drift, code is wired._
- **[planned]** `mindspec/self-spec.md` — Archived full self-spec written by every completed MSI-Self interview (source for mind_projection). Absent until a MindS
- **[stale]** `config/interface.json` — Active layout + theme only (solo preset, default-dark). Does NOT contain the Output Styles tab.

### B.8 — Vault, RAG, Conversations & Persistent State
- **[implemented]** Local-only fork storage and recursive effective-history cutoffs; `fork_point_chunk_id` remains legacy-only.
- **[implemented]** Ordered, deduplicated, uncapped contributor references with cutoff-safe conversation and indexed atomic-note resolution.
- **[implemented]** Endpoint-safe complete-unit packing with a 200,000-token maximum, smaller provider-safe effective budgets, and soft shared 50/40/10 planning.
- **[implemented]** Cumulative privacy, archive exclusion, complete-ancestry eligibility, fair source scheduling, and source-wide global exclusions.
- **[implemented contract]** Full Context + User + Assistant stored document with separately bounded Context + User vector orientation for live and replayed rows.
- **[migration required]** Existing conversation rows need validated replay before the storage split is fully materialized operationally; no production cutover is claimed here.
- **[implemented]** Promotion-only supplemental handling, numeric-safe public `context_coverage`, and sensitive private supplemental forensics.
- **[implemented]** Standard/Private Close+restore, navigation-only Exit Stealth, and protected Stealth Delete Forever with descendant detachment.
- **[boundary]** Deliberate exports and provider, Git, backup, or other external copies are not erased by Ora-managed purge.
- **[documentation schema]** `docs/conversation-file-format.py` describes current envelopes/chunks/index rows but is not a runtime Pydantic validation claim.
- **[implemented boundary]** Aside help retrieves only the five tracked `help/` files through `orchestrator/help_retrieval.py`; indexed and lexical paths share the allowlist, and prompt/answer/help context remain ephemeral.

### B.9 — Tool Dispatch, the Capability Gate & Execution Telemetry
- **[partial]** Model-driven tool escape hatch (analyst requests read-category tools mid-analysis) — _ORA_MODEL_TOOL_SELECTION default '0'/off (boot.py:8376); build_requestable_tools_catalog returns '' unless enabled. Flag-gated-off => partial._
- **[boundary]** MCP HTTP transport is unsupported — _the accepted registry permits only the three pinned stdio declarations; there is no HTTP launch branch or floating-package fallback._
- **[implemented — launcher-enabled]** Web extraction escalation (deep-fetch thin snippets) — _code default off (boot.py:7042) but start.sh:26 force-exports ORA_WEB_EXTRACTION=1, so active on a normally-launched installed system._
- **[planned]** Evidence-runner check vocabulary (.ora/evidence.yaml validation) — _tool_events.validate_check_declaration + EVIDENCE_RUNNER_DEFAULTS present, but comment lines 980-981 say 'runner is Phase 5; an undeclared check is un-runnable by construction' — vocabulary declared, runner not built._
- **[implemented — launcher-enabled]** `orchestrator/web_extraction.py` — Extraction-failure escalation: selects thin high-trust snippets, deep-fetches full pages via web_fetch, chunks, fit-gate

### B.10 — Visual Intelligence
- **[stale]** Per-mode structural SUCCESS-CRITERIA findings in review_envelope (mode_success_criterion_<id>) — _CLAUDE.md §240 explicitly notes mode_success_criteria.py deleted 2026-05-10; only a placeholder hook comment remains in visual_adversarial.review_envelope_
- **[stale]** CLAUDE.md §101 claim that V3 server-side render 'mirrors render-envelope.js' jsdom path as primary — _Code shows Playwright/Chromium is PRIMARY (_render_visual_svg_browser) and node/jsdom CLI is only FALLBACK (boot.py:897-908); §101 describes the client boot sequence, not the server render path — drift risk in reader interpretation_
- **[planned]** mode-to-visual.json auto-generation from vault Reference doc — __comment: 'Keep in sync manually until generation is automated'_

### B.11 — Audio/Video Media & Timeline Editing
- **[implemented]** One explicit first-party video loader, validated descriptor,
  31 plugin routes, ordered browser assets, four host registries, and three
  Dialogue lifecycle callbacks.
- **[implemented]** Video-owned capture, media library, URL import, timeline,
  preview, render, waveform, suggestions, suggestion framework/schema, settings,
  and browser UI below `plugins/video/`.
- **[implemented boundary]** Core retains transcription, vault transcript writing,
  Inquiry A/V drop, shared job queue, and shared async-capability bootstrap.
- **[removal behavior]** An absent or broken video package is logged and skipped;
  Ora boots without video routes/assets/UI and retains the core services above.
- **[needs-porting]** Non-macOS capture and Mac-specific FFmpeg/font assumptions
  remain unverified or unimplemented.

### B.12 — Meta-Layer Oversight
- **[implemented]** Observational oversight event logging and parent fan-out; Process Coherence remains explicit project/framework judgment rather than a router-invoked runtime path.
- **[deferred]** CFF model-driven modes C-Design / C-Modify — _corpus_runtime.py:5 explicit 'not implemented here'; run via framework flow_
- **[partial]** OFF model-driven modes O-Design/O-Modify/O-Audit — _run through gear pipeline via milestone_executor, not in output_runtime; mechanical redirect only covers O-Render_
- **[implemented]** `~/ora/orchestrator/oversight_router.py` — observational event logger with Stealth suppression, fan-out recursion guard, and child-to-parent visibility.
- **[partial]** `~/ora/orchestrator/corpus_runtime.py` — Mechanical CFF: c_instance + c_validate; emits CorpusInstanceCreated/CorpusValidated
- **[partial]** `~/ora/orchestrator/output_runtime.py` — Mechanical OFF O-Render: corpus+bespoke-OFF -> md/html/docx/pptx/xlsx artifact; emits OFFRendered
- **[deleted 2026-08-16]** `~/ora/orchestrator/scheduler.py` and `config/scheduled-tasks.json` — the retired interval scheduler and its permanently empty registry, together with the four chat tools that managed it. Previously kept as a fail-closed migration-evidence surface; removed because absence is a stronger guarantee than a stub whose every path raises, and the tool surface was presenting a control panel for a machine that no longer existed. Recoverable from git history
- **[stale]** `~/ora/orchestrator/job_queue.py` — Async capability-dispatch queue (WP-7.6, video/training) — NOT part of oversight subsystem

### B.13 — Execution-Review Architecture
- **[partial]** Phase 4 — §6 consistency note (declared vs observed output_type) recorded-only — _consistency_note computed but dormant: output_type is 'unknown' on every live turn (no mode declares it); test-covered only_
- **[partial]** Phase 4 — render_for_review / _format_artifact_for_review evidence-first packet-to-review renderer — _docstring 'TESTED-BUT-UNWIRED in P4 — NOT installed at the six in-gear review sites'; no live verifier consumes a packet (Phase 6 loop)_
- **[planned]** Phase 4 — EvidenceLane.generated_by/result/sufficient population (real evidence verdicts) — _lanes declared-empty; sufficient tri-state None=unfilled=NOT sufficient; Phase 5 runner fills_
- **[planned]** Phase 4 — execution.mode/state_before/state_after (§11 dirty-state fields) — _reserved as None in build_execution_packet execution block; Phase 5 fills_
- **[unmerged / code-review]** Phase 5 — evidence_runner.py (catalog parse+validate + sandboxed check runner + Contract producer + dirty-state modes) — _IMPLEMENTED in the unmerged worktree ~/ora-worktrees/phase5-impl (branch execution-review-phase5-impl; test_evidence_runner.py = 58 tests) at the CODE-REVIEW gate; not merged to ~/ora main (absent from committed history) and not installed_
- **[partial / unmerged]** Phase 5 — .ora/evidence.yaml catalog (what checks EXIST) — write-gated so an executor can't silently weaken a check — _the write-gating vocabulary IS landed on ~/ora main (evidence.yaml in _PROTECTED_BASENAMES; EVIDENCE_RUNNER_DEFAULTS / validate_check_declaration) but has zero non-test consumers there; the catalog reader + runner are implemented only in the unmerged phase5-impl worktree (code-review gate), not installed_
- **[unmerged / code-review]** Phase 5 — planning-stage task-specific Evidence Contract (what checks THIS task must pass) — _implemented in the phase5-impl worktree (flag-gated ORA_EVIDENCE_CONTRACT in one revision); at the code-review gate, not merged to ~/ora main, not installed_
- **[unmerged / code-review]** Phase 5 — enforce-or-refuse sandbox backend (macOS sandbox-exec / ORA_EVIDENCE_SANDBOX wrapper / Linux unshare -rn; refuse where unenforceable) — _implemented in the phase5-impl worktree; on installed ~/ora main only code_execute's macOS sandbox-exec exists (no ORA_EVIDENCE_SANDBOX consumer, no unshare path) — Phase 5 not merged/installed_
- **[planned]** Phase 6/7/8 — live packet review loop, durable tiered packet persistence, provenance/claim-to-source lane — _packet persistence hardcoded tier trace_local; collect_provenance lane declared but never filled; HANDOFF 'Phase 6/7/8 = future'_
- **[partial]** `~/ora/orchestrator/execution_packet.py` — Phase 4: EvidenceLane/JudgmentLane (§5 structural separation), route_lanes router, polymorphic ExecutionPacket (§9 reser
- **[partial]** `~/ora/orchestrator/tools/code_execute.py` — Sandboxed code executor; macOS sandbox-exec backend = enforcement_model 'orchestrated', else fail-closed/gated. The reus
- **[stale]** `~/ora/server/review.py` — NOT part of Execution-Review. This is the unrelated Phase-10 document-extraction human-review web app (localhost:5005, d
- **[unmerged / code-review]** `~/ora-worktrees/phase5-design.md` — Phase 5 design packet (Rev 2 enforce-or-refuse), design APPROVED-with-conditions; the implementation lives in the phase5-impl worktree at the code-review gate — not merged to ~/ora main, not installed
- **[unmerged / code-review]** `~/ora-worktrees/phase5-impl.diff + phase5-impl/` — the Phase 5 implementation (diff + worktree) at the code-review gate; exists on disk but NOT merged to ~/ora main — must NOT be read as installed/landed

### B.14 — Operation-Matrix (Mission / Objectives / Milestones)
- No partial / planned / deferred items — the subsystem is fully implemented at 7a5e8f40. Latent porting issue (see Appendix A): `_safe_name` (operation_matrix.py:59-62) strips only `\` and `/`, so Windows-illegal filename characters (`: * ? " < > |`) in a project display name would yield an invalid matrix filename on native Windows. Performance note: there is no read cache — every MOM read re-parses the file, with an O(n) frontmatter scan on name-mismatch (fine at modal rates, unverified at large project counts).

### B.15 — Documentation-Code Parity & Vault↔Repo Mirror Governance
- **[implemented]** Focused documentation-integrity gate — _Explicit roots and base commits for vault, Ora, app, org, and MSI; ownership/discovery, task dispositions, typed references, lifecycle state, accepted findings, and registered propagation are checked without falling back to live roots._
- **[implemented and installed]** Hook enforcement — _Five blocking pre-push hooks are installed across vault, Ora, app, org, and MSI; Ora and vault also retain the fail-open post-commit framework-pair queue audit. These seven local hooks remain bypassable and do not establish remote enforcement._
- **[implemented]** Three installed-documentation mirrors — _Technical, Accessible, and User Guide canonicals regenerate body-only Ora mirrors under an exact frontmatter-stripping byte rule._
- **[specified, not built]** DCP-Sweep / Audit / Reconcile / Specify / Deprecate runtime framework — _The framework specification exists, but there is no installed or callable runtime framework body. Semantic truth remains an independent-review or explicit campaign responsibility._
- **[implemented]** `/Users/oracle/ora/scripts/verify-implementation.py` — focused five-root documentation-integrity gate plus the current 15-pair direct drift check and manifest-backed framework lifecycle check.
- **[implemented]** `/Users/oracle/Documents/vault/Projects/Ora/Reference — Ora Technical Documentation.md` — active vault-canonical technical documentation (this document).
- **[implemented]** `/Users/oracle/ora/docs/technical-documentation.md` — body-only mirror of the technical documentation under the exact registered propagation rule.
- **[implemented]** `/Users/oracle/ora/help/accessible-overview.md` — body-only mirror of `Reference — Ora Accessible Overview.md` in the isolated help corpus.
- **[implemented]** `/Users/oracle/ora/help/user-guide.md` — body-only mirror of `Guide — Using Ora.md` in the isolated help corpus.

### B.16 — Update, Recovery, Troubleshooting & Diagnostics
- **[partial]** MLX-worker liveness heartbeat surfaced through oversight_health — _oversight_health HEARTBEAT_INTERVALS['mlx_worker']=30 + server.py:14805 heartbeat emit; Apple-Silicon-only (MLX/Metal), no non-Mac equivalent_
- **[planned]** Packaged app auto-updater / installer for updates — _No update command anywhere; update path is git pull + re-run installer (docs/install-testing.md 'Rolling-main install'). No .pkg/.msi/Sparkle/self-update code found._
- **[deferred]** Hybrid and Organization deployment profiles in the test/recovery matrix — _help/install-recovery.md:104-108 + install-testing.md:25; the current reservation is not a feature-plugin dependency_
- **[deferred]** Browser permission approval modal UI (from 2026-04-06 upgrade) — _docs/upgrade-report-2026-04-06.md 'Unresolved Issues' #1: auto-approve only, full modal deferred_
- **[stale]** `~/ora/docs/upgrade-report-2026-04-06.md` — Historical point-in-time upgrade report (7→15 dispatcher tools) with test/eval scores and rollback via 'cd ~/local-ai &&
- **[stale]** `~/ora/docs/pre-upgrade-packages.txt` — pip-freeze snapshot captured before the 2026-04-06 upgrade (keyring + build deps), companion to the upgrade report.

### B.17 — The Project Plugin Convention
- **[partial]** §14 framework_configurations manifest parse + ${config.*} substitution + extension-overlay splice engine — _framework_config.py engine + 35 tests exist and manifest parses (project_registry.py:647), but the engine is NOT imported by milestone_executor.py where frameworks execute; server.py only lists configs (line 4318). No runtime splice at framework-invocation time._
- **[deferred]** Active-project existence validation at set time — _active_project.py set_active_project docstring: 'Validation that a given project actually exists is intentionally NOT done here yet'_
- **[planned]** Project-explicit new-conversation binding (removing reliance on global active pointer at save time) — _active_project.py module docstring: 'A later sub-step will let the frontend send the project explicitly'_
- **[partial]** Inert project slots (interaction_style/output_style/persona/model_locks) — _project_meta.py _DEFAULT_SLOTS comment: 'stay inert until G1.36/G1.37'; only private + default_model_profile honored earliest_
- **[partial]** `/Users/oracle/ora/orchestrator/framework_config.py` — §14 runtime engine: parse_config_interface, substitute_config_refs (${config.*}), splice_extension_overlays (<!-- ora-pr

## Appendix C — Vault↔Repo Mirror Parity Rule

This document exists in two places, and two copies of the same document are a drift hazard the moment either side is edited alone. This appendix states the rule that prevents it. The rule is not decoration; it is the reason the Documentation-Code Parity chapter's machinery applies to this document as much as to any framework spec.

**The pairing.** The canonical file is the vault document `[[Reference — Ora Technical Documentation]]`. Its repository mirror is `~/ora/docs/technical-documentation.md`. The vault file carries YAML frontmatter (nexus / type / tags / dates); the repo mirror carries the body only. This is the same vault-canonical, ora-operational convention every paired Ora document follows.

**The sibling mirrors (added at Gate 7).** The identical rule governs the two derived installed-system documents created alongside this one: `[[Reference — Ora Accessible Overview]]` ↔ `~/ora/help/accessible-overview.md`, and `[[Guide — Using Ora]]` ↔ `~/ora/help/user-guide.md`. Both are body-only mirrors under the same vault-canonical direction of truth and are part of Aside's tracked five-file help corpus.

**Direction of truth.** The vault is canonical. Edits happen in the vault file; the repo mirror is regenerated from the vault body. If the two disagree, the vault wins and the mirror is rewritten — never the reverse. This mirrors the standing rule in `~/ora/CLAUDE.md`: files under `~/ora` are operational copies; the vault is where the source of truth lives.

**What must stay in sync.** The entire body: every chapter, every status label, every platform matrix cell. Nothing that changes the meaning may live in one copy and not the other. The only permitted difference is the vault frontmatter, which the mirror does not carry.

**Registration.** The machine-readable ownership block in [[Reference — Documentation-Code Parity Configuration]] declares all three pairs with `ora_body_only` propagation. The focused five-root gate strips the vault YAML block and exactly one following blank line, then requires every remaining byte, including the trailing newline, to match the registered Ora path.

**When the code moves.** If a code change alters a load-bearing claim, the change updates this vault canonical in the same coordinated task and regenerates the mirror before the focused final gate. If the change truly has no documentation impact, its final task commit names the owning surface in a `Documentation-No-Impact` trailer and independent review confirms the judgment.

**Enforcement limit.** Seven local hooks are installed: five blocking pre-push hooks and the fail-open Ora/vault post-commit audits. The authoritative protection remains the coordinator's explicit five-root finalization check and merge hold, with semantic comparison performed by the independent reviewer. Hooks remain bypassable, no remote cross-repository CI guarantee is claimed, and the repositories do not commit atomically.

## Appendix D — Glossary

Precise, one-sentence definitions of the load-bearing terms, defined here once so the chapters can use them without re-explanation. Terms are ordered for reading, not alphabetized.

- **Assisted human intelligence (AHI).** The framing this document uses for the technology: intelligence stays in the human's loop and the system is a tool that becomes useful when a skilled human directs it — the reframe is structural, not cosmetic, and the prose does not relativize it.
- **Foreground vs. background execution.** The operative runtime distinction: a task that finishes while you watch runs in the foreground; a task that outlives your patience runs in the background and reports through the observability log — same architecture, two runtime modes. This replaces the "manual vs. agentic" framing.
- **Orchestrator.** The Python process (`orchestrator/boot.py` and the modules around it) that sits between the user and the model, assembles context, runs the pipeline, executes tools, and persists state — the thing a raw chat interface is missing.
- **Gear.** A named pipeline configuration selecting how much adversarial machinery a prompt receives: Gear 1–2 are single-model direct responses; Gear 3 is sequential adversarial review; Gear 4 is parallel adversarial review with independent retrieval. The gear is chosen deterministically from the mode file, not by the model.
- **Mode.** A specification file (`modes/<name>.md`) for one analytical approach — its trigger conditions, epistemic posture, default gear, retrieval profile, model instructions, content contract, and success criteria — loaded to govern a single pipeline run.
- **Territory.** One of the twenty-one analytical regions the pre-routing pipeline disambiguates before it selects a resident mode; territory routing is the selection layer above the flat mode list.
- **The adversarial pipeline.** The eight-step sequence (analyst → cross-evaluator → reviser → verifier → consolidator → formatter, with a quality gate) in which an independent model reviews another model's output, on the premise that a model cannot catch its own self-generated errors.
- **Slot.** A named role a model fills — `breadth`, `depth`, `sidebar`, `consolidator`, `evaluator`, `step1_cleanup`, `classification` — decoupled from any specific model so a configuration can bind any model to any role.
- **Configuration (preset).** A named set of slot-to-model bindings (Free / Budget / Speed / Premium and user-defined customs) plus a fallback chain per slot; the active configuration is a pointer the router consults.
- **Provenance weighting.** Retrieval priority assigned by the epistemic status of a source — a confirmed synthesis (engram) outranks raw conversation (chat), which outranks unreviewed working notes — applied when the RAG layer ranks candidate context.
- **Engram.** A refined, confirmed knowledge asset in the vault (`type: engram`), the highest-provenance retrieval source; distinct from a raw conversation chunk or a working note.
- **The capability gate.** The always-on enforcement-and-audit boundary at the dispatcher chokepoint: every tool call is classified on risk axes, recorded, and allowed or refused fail-closed, before execution and independent of the user's permission mode.
- **Framework.** A background-executable procedure specification, invoked by name and run as a sequence of milestones through the gear pipeline — the precise construct the industry markets as an "agent." The framework registry holds many (about two dozen registered user-facing frameworks); the **canonical framework suite** is the closed six that cover the space of specifiable cognitive work — PEF (Problem Evolution), PIF (Process Inference), PFF (Process Formalization), CFF (Corpus Formalization), OFF (Output Formalization), and DCA (Decision Clarity Analysis, formerly the Wicked Problems Framework).
- **PED (Project Evolution Document).** The vault file that records a project's Mission, Excluded Outcomes, Constraints, Milestones, and Decision Log, and that the oversight apparatus watches for drift.
- **Process Coherence.** An explicitly invoked project/framework judgment that returns exactly PROCEED, REVISE, or ESCALATE; it does not fire automatically or participate in Programming.
- **Execution Packet.** The evidence-first record built at each gear terminal that separates an evidence lane (what was observed) from a judgment lane (what was claimed), so a reviewer grades observed reality rather than a fluent claim.
- **Evidence Contract.** The task-specific set of checks a given task must pass, produced at planning time and distinct from the repository's evidence catalog (which merely declares what checks exist) — the load-bearing distinction is that a green evidence run means nothing broke that you knew to check, not that the right problem was solved.
- **MLX.** Apple's array framework for on-device model inference on Apple Silicon (Metal); the local-inference engine on Mac, guarded so non-Apple hardware resolves to Ollama instead.
- **`runtime_paths`.** The single module that resolves every Ora filesystem root from the home directory with per-root environment overrides and supplies the cross-platform advisory file lock — the project's portability strategy in one place.
- **Documentation-Code Parity (DCP).** The framework and linters that detect drift between the vault's canonical markdown and the operational copies under `~/ora`, and the governance that keeps this very document's mirror in sync (see Appendix C).

## Appendix E — Source Map (subsystem → source-of-truth files)

*The files an expert opens to verify each chapter. Status in brackets.*

### E.1 — Installation & Runtime Profiles
- `/Users/oracle/ora/scripts/install.py` — [implemented] nine-step macOS desktop source installer (Solo profile): preflight to profile-select to catalog-refresh to regist
- `/Users/oracle/ora/scripts/install-server.sh` — [implemented] Headless Linux/API-only server install: apt deps to venv to pip Linux subset (no mlx-lm) to ollama+nomic-embed
- `/Users/oracle/ora/scripts/install_server_config.py` — [implemented] Rewrites config/routing-config.json for server use: all slots to API-only free OpenRouter chains, synthesizes 
- `/Users/oracle/ora/scripts/install_server_smoke.py` — [implemented] Server-install verifier: routing-config loads via boot.load_routing_config, every slot resolves to an endpoint
- `/Users/oracle/ora/scripts/local_models.py` — [implemented] Local-model tier selection + HuggingFace download. RAM-tier classifier (small_only/workhorse_choice/full_local
- `/Users/oracle/ora/scripts/check-portability.py` — [implemented] Cross-platform portability linter: regex rules for pkill/homebrew-path/osascript/launchctl/tmp/Library paths e
- `/Users/oracle/ora/scripts/auto-populate-configuration.py` — [implemented] Populates named pipeline configurations from a preset spec (invoked by install.py step 7 as 'budget user-pipel
- `/Users/oracle/ora/orchestrator/platform_check.py` — [implemented] Platform detection + engine resolution: Apple Silicon to MLX, everything else to Ollama; resolves engine:auto 
- `/Users/oracle/ora/orchestrator/runtime_paths.py` — [implemented] Single cross-platform source for all Ora roots (ORA_HOME/VAULT/CONVERSATIONS/runtime overlay dirs), env-overri
- `/Users/oracle/ora/start.sh` — [implemented] Primary macOS launcher: exports ORA_HOME + 7 runtime feature-flag env vars, pkills stale server, finds Python 
- `/Users/oracle/ora/stop.sh` — [implemented] One-line server stopper via pkill.
- `/Users/oracle/ora/run-ora-server.sh` — [not in commit 7a5e8f40 — untracked local artifact] launchd wrapper (job ora.server.codex), present on the author's Mac but absent from a fresh clone: cd + export ORA_HOME + 5 flags + exec homebrew pyth
- `/Users/oracle/ora/Ora.app/Contents/MacOS/ai` — [not in commit 7a5e8f40 — untracked local .app bundle, absent from a fresh clone] macOS .app launcher: pkill stale, Popen server detached (start_new_session), poll 5000-5010, open browser. Har
- `/Users/oracle/ora/Ora Models.app/Contents/MacOS/models` — [implemented] macOS .app launcher for the local-model selection flow (companion to Ora.app).
- `installer/install-manifest.md` and its 20 layer files — [retired] Legacy natural-language layered installer spec (phase1 6 layers + phase2 12 layers + gate + appendix), since deleted; `installer/macos/` remains and is live.
- `/Users/oracle/ora/help/install-guide.md` — [partial] Happy-path install guide with per-platform command matrix (macOS/Windows-native/WSL/Linux-desktop/Linux-server
- `/Users/oracle/ora/help/install-manual.md` — [implemented] Manual command fallback when the installer script itself is broken.
- `/Users/oracle/ora/help/install-recovery.md` — [implemented] Recovery guide for partial install failures: read install.log, check install-state.json, --dry-run, --resume.
- `/Users/oracle/ora/docs/install-testing.md` — [partial] Clean-room test protocol: 3-env matrix (Mac Mini A/B + Parallels Win11 ARM), Solo-only ship criterion, uninsta
- `/Users/oracle/ora/docs/cloud-ora-install.md` — [stale] Linux server operator guide for the MSI publication cadence.
- `/Users/oracle/ora/docs/hardware-report.md` — [stale] Sample hardware-evaluation report (M4 Max 128GB, gpt-oss-120b MXFP4).
- `/Users/oracle/ora/docs/README.md` — [stale] End-user 'Your Local AI System' orientation page.

### E.2 — Server & Browser-UI Runtime
- `~/ora/server/app.py` — [implemented] Main Flask app: ~180 @app.route endpoints, SSE helpers, /chat sync-pipeline invoke, conversation persistence,
- `~/ora/server/index-v3.html` — [implemented] V3 single-page browser UI served at / and /v3; 124KB, 132 <script> tags loading static JS (theme, Konva visual
- `~/ora/server/review.py` — [implemented] SEPARATE standalone Flask app (Note Review interface) on localhost:5005; NOT imported/wired into server.py
- `~/ora/orchestrator/overview_widgets.py` — [implemented] Renderer-neutral, read-only projection shared by the D1 active-project ring and Project priority card. Four independent sources retain honest state/freshness/count qualification; exact project Overview/Files/Dialogues/Knowledge actions use existing owners. Saved layout, stores, caches, connectors, schedulers and source mutation remain outside this projection.
- `~/ora/orchestrator/tests/test_overview_widgets.py` — [implemented] Focused source-adapter coverage for fixed ordering and row shape, canonical project order, partial/unavailable isolation, prior-day missing/error distinction, and supported actions without Daily Note generation.
- `~/ora/run-ora-server.sh` — [not in commit 7a5e8f40 — untracked local artifact] launchd wrapper (job ora.server.codex) on the author's Mac; the tracked launcher at this commit is start.sh, which sets ORA_* env flags then launches python3 server/app.py --ov
- `~/ora/server/static/js/v3-conversation.js` — [implemented] Renders assistant turns; fetches /api/conversation/<id>, mark-read, fork; calls v3-visual-dispatch on each tur
- `~/ora/server/static/js/sidebar.js` — [implemented] Conversation sidebar list; 12s visibility-gated polling of list endpoint (the async reconciliation loop)
- `~/ora/server/static/js/sidebar-oversight.js` — [implemented] Paused/Operating supergroups accordion; polls /api/oversight/paused + /operating every 12s; Approve/Deny/Discu
- `~/ora/server/static/js/v3-visual-dispatch.js` — [implemented] Extracts ```ora-visual fenced JSON from assistant turns, hands to OraPanels.visual.onBridgeUpdate (canvas_acti
- `~/ora/server/static/` — [implemented] ~130 JS + css/themes/vendor(Konva)/visual-schemas/config assets; served via /static/<path> and specialized /st
- `~/ora/server/.port` — [unknown] 4-byte file recording chosen port (side-channel for other tools)

### E.3 — Settings, API Keys & Local-Model Handling
- `/Users/oracle/ora/orchestrator/user_settings.py` — [implemented] Settings persistence + keyring API-key CRUD. load/save/reset/get_setting over user-settings.json (atomic tmp+r
- `/Users/oracle/ora/orchestrator/provider_registry.py` — [implemented] Single source of truth for 24 external providers: id, label, category, keyring_username, env_var, signup/conso
- `/Users/oracle/ora/orchestrator/local_model_discovery.py` — [implemented] Auto-discovery of MLX models in ~/ora/models/. probe_model_dir reads each subdir's config.json + *.safetensors
- `/Users/oracle/ora/scripts/local_models.py` — [implemented] Install-side local-model selection/download (Chunk 8). RAM-tier classifier (small_only/workhorse_choice/full_l
- `/Users/oracle/ora/scripts/re-embed-local.py` — [implemented] Parallel-collection ChromaDB re-embed helper (peripheral to settings/keys). Reads v1 collections, re-embeds vi
- `/Users/oracle/ora/scripts/promote-runtime-model-state.py` — [implemented] Maintainer-only: copies gitignored runtime model state (data/runtime/config/*) into checked-in config/ seed fi
- `/Users/oracle/ora/orchestrator/boot.py` — [implemented] Keyring→env bridge: _export_provider_keys_to_env() at import (line 173-200) mirrors every provider_registry en
- `/Users/oracle/ora/server/app.py` — [implemented] Settings External-APIs HTTP surface. GET /api/settings (settings + list_api_key_status rows, no secrets); POST
- `/Users/oracle/ora/config/user-settings.json` — [stale] Persisted non-secret user settings (capture/whisper/interface/external_apis(empty)/styles). Contains stale key
- `/Users/oracle/ora/config/models.json` — [implemented] Active local-model registry (machine-specific, GITIGNORED). local_models auto-managed by discovery; commercial
- `/Users/oracle/ora/scripts/install-server.sh` — [implemented] LINUX server deployment key handling — the contrast case. Writes OPENROUTER_API_KEY (+ optional ANTHROPIC_API_
- `/Users/oracle/ora/config/local-models.json` — [unknown] State file for scripts/local_models.py install-side download tracking ({installed:[], selected:{}}). Distinct 
- `/Users/oracle/Documents/vault/Projects/Ora/Framework — API Key Setup.md` — [implemented] 'Why' source — conversational guided key-setup framework (v2.1, registry-driven). Documents storage convention

### E.4 — Model / Provider Configuration & Routing
- `orchestrator/router.py` — [implemented] Core routing engine. Resolves slots to endpoints via named-configuration cell chains (_resolve_from_configurat
- `orchestrator/model_dispatch.py` — [implemented] Project-facing public API invoke_chat(system,user,slot=...) -> str. Loads routing-config, calls boot.get_slot_
- `orchestrator/model_registry.py` — [implemented] Runtime reader for curated model-registry.json. overlay_routing_config merges vision_capable/intelligence/AA/l
- `orchestrator/active_configuration.py` — [implemented] Named-configuration + preset lifecycle backing the V3 Models pane. Active-pointer, preset toggles, bake_missin
- `orchestrator/provider_registry.py` — [implemented] Static provider catalog: keyring username map, labels, env-bridge pairs, direct-LLM vs catalog-authoritative s
- `orchestrator/direct_catalog.py` — [partial] Resolves OpenRouter-style id to direct-provider id by fetching+caching each provider's /models listing (TTL + 
- `orchestrator/endpoint_health.py` — [implemented] Circuit breaker: 3 failures in 60s trips cooldown; is_in_cooldown() consulted by router chain walk; half-open 
- `orchestrator/vendor_catalog_registry.py` — [partial] Builds vendor-authoritative merged registry (native + AA + OpenRouter): size classification, vision-family, AA
- `orchestrator/local_model_discovery.py` — [implemented] Apple-Silicon MLX discovery. Scans ~/ora/models/ for config.json + safetensors, derives id/ram/type/vision/rol
- `orchestrator/mlx_mutex.py` — [implemented] Per-machine MLX mutex (MLX segfaults on concurrent same-machine load/invoke). Blocking acquire around local ca
- `orchestrator/user_settings.py` — [implemented] API key storage via system keyring (service 'ora'), provider whitelist from provider_registry, presence checks
- `orchestrator/boot.py` — [implemented] Routing integration seam: load_routing_config (+overlay), _get_router singleton, reload_router, get_slot_endpo
- `config/routing-config.json` — [implemented] Endpoints (547), machines (studio-128), buckets[legacy], capability slots (image/video/vision_input), diversit
- `config/configurations/*.json` — [implemented] 8 named configurations (free/budget/premium/user-pipeline/background-default/background-speed/qwen-9b-only/experiment-optimum-premium-consolidator; no campaign-* files
- `config/model-registry.json` — [implemented] Curated capability registry (337 models). Written by sync_model_registry.py; overlaid onto endpoints at load. 
- `config/model-catalog.json` — [implemented] Model catalog (source for preset auto-population). Written by refresh-catalog.py.
- `config/models.json` — [implemented] Machine-specific active local + commercial registry (gitignored). local_models auto-managed by discovery (6), 
- `config/configuration-presets.json` — [implemented] Preset algorithm params (premium/budget/free/speed): floor_pct, cost ceilings, adaptive ceiling, sort_by, mode
- `config/family-classification.json` — [implemented] Training-family classification for same-family adversarial-diversity warning.
- `config/openrouter-catalog.json` — [implemented] OpenRouter catalog snapshot (370KB). Refreshed by refresh-openrouter.py; served at /config/openrouter/catalog.
- `config/openrouter-supplement.json` — [implemented] Manual supplement for models missing from OpenRouter listing.
- `scripts/sync_model_registry.py` — [implemented] Primary offline registry builder (124KB): OpenRouter+LiteLLM+Arena+AA, optional reach probe, writes model-regi

### E.5 — Gear Pipeline & Mode Dispatch
- `/Users/oracle/ora/orchestrator/boot.py` — [implemented] The orchestrator (14,614 lines). Holds run_pre_routing_pipeline (Stages 1-3, line 5513), stage1/stage2/stage3 
- `/Users/oracle/ora/orchestrator/dispatcher.py` — [implemented] NOT the gear/mode dispatcher — it is the TOOL dispatcher (permission gating, path validation, command classifi
- `/Users/oracle/ora/orchestrator/style_assembly.py` — [implemented] Composes the Output Style block (demeanor axes + arrangement schemas + craft floor) injected into pipeline pro
- `/Users/oracle/ora/orchestrator/style_store.py` — [implemented] Persists user-authored custom Output Style profiles to ~/ora/data/custom-styles.json; merged into style compos
- `/Users/oracle/ora/orchestrator/slash_command_registry.py` — [implemented] Shared catalogue of user-facing slash commands (names, categories, aliases, arg shapes, discoverability metada
- `/Users/oracle/ora/orchestrator/operation_matrix.py` — [implemented] Reads/writes a project's Mission/Objectives/Milestones (MOM) in vault Matrix/ markdown. Meta-layer/project-man
- `/Users/oracle/ora/architecture/pre-routing-pipeline.md` — [implemented] The canonical spec for the 4-stage pipeline + Baseline Criteria Injection + Analytical Perspectives injection.
- `/Users/oracle/ora/architecture/territories.md` — [implemented] 21-territory inventory (T1-T21) consulted across stages for disambiguation.
- `/Users/oracle/ora/architecture/trusted-web-sources.md` — [stale] 9th file in ~/ora/architecture/ — a web-source allowlist read by f-consult / web consultation. NOT in CLAUDE.m
- `/Users/oracle/ora/modes/cui-bono.md` — [implemented] Representative Gear-4 mode. Carries the locked flat-H2 template: DEPTH/BREADTH ANALYSIS GUIDANCE, ANALYTICAL B
- `/Users/oracle/ora/modes/INDEX.md` — [implemented] Mode inventory file; the only non-mode .md in modes/ (has no ## ANALYTICAL PERSPECTIVES).
- `/Users/oracle/ora/frameworks/book/f-consult.md` — [implemented] Step-2 consultation-package spec. MECHANICAL: 'Loaded into Python orchestration. No model produces the consult
- `/Users/oracle/ora/frameworks/book/f-analysis-depth.md` — [implemented] Step-3 depth analyst spec (Black+White Hat: single best-supported answer + risks). Injected as universal scaff
- `/Users/oracle/ora/frameworks/book/f-analysis-breadth.md` — [implemented] Step-3 breadth analyst spec (Green+Yellow Hat: map plausible-answer range). Paired adversarially with depth.
- `/Users/oracle/ora/frameworks/book/f-evaluate.md` — [stale] Step-4 cross-adversarial evaluator spec (universal 7-section contract). References mode's `## EVALUATION CRITE
- `/Users/oracle/ora/frameworks/book/f-revise.md` — [stale] Step-5 reviser spec (mirror contract). Also references legacy `## EVALUATION CRITERIA` name.
- `/Users/oracle/ora/frameworks/book/f-verify.md` — [implemented] Step-6 PER-STREAM verifier spec (nine universal checks; gate not editor). Explicitly NOT post-consolidation si
- `/Users/oracle/ora/frameworks/book/f-consolidate.md` — [implemented] Step-7 consolidator spec — produces the irreducible corpus (extract → dedup → bloat-strip → synthesize per mod
- `/Users/oracle/ora/frameworks/book/f-format.md` — [implemented] Step-8 formatter spec — places the step-7 corpus into the deliverable per mode OUTPUT FORMAT GUIDANCE. Gear-4 
- `/Users/oracle/ora/frameworks/book/f-quality-gate.md` — [implemented] Final-output quality gate (Gear-3 step 6.5, Gear-4 step 8.6). LLM judge on the verification slot grades the de
- `/Users/oracle/ora/orchestrator/runtime_paths.py` — [implemented] Single cross-platform source for Ora roots (ORA_HOME/VAULT/CONVERSATIONS). Env-overridable, built from expandu
- `/Users/oracle/ora/orchestrator/platform_check.py` — [implemented] Platform detection: Apple Silicon → MLX engine, everything else → Ollama. Resolves engine:'auto' in routing-co

### E.6 — Framework Loading & Execution
- `/Users/oracle/ora/orchestrator/framework_parser.py` — [implemented] Strict parser: framework markdown to Framework dataclass (milestones, layers, M0Routing, multi-mode modes) per
- `/Users/oracle/ora/orchestrator/framework_invocability.py` — [implemented] Curated user-invocable/pickable boundary; resolve_user_invocable_framework maps token to filename, applies ali
- `/Users/oracle/ora/orchestrator/framework_config.py` — [partial] Project-config binding runtime: parse_config_interface, substitute_config_refs (${config.<key>}), splice_exten
- `/Users/oracle/ora/orchestrator/framework_input_gap.py` — [implemented] Framework input-gap analyzer for V3 popup: structured path (## Setup Questions) vs LLM-fallback (## INPUT CONT
- `/Users/oracle/ora/orchestrator/milestone_executor.py` — [implemented] Framework execution engine: execute_framework runs milestones, builds handoff packets, runs via run_gear3/4, p
- `/Users/oracle/ora/frameworks/book/` — [implemented] Operational framework spec files (39 .md): executable canonical five + MOM + terrain-mapping + F-* stage specs
- `/Users/oracle/ora/frameworks/book/decision-clarity-analysis.md` — [partial] DCA canonical framework spec (renamed from Wicked Problems 2026-05-01).
- `/Users/oracle/ora/config/framework-invocability.json` — [implemented] Curated allow-list: invocable (11), pickable (11), aliases (cff/deep-research/knowledge-artifact-coaching/off/pff), internal_only (10 incl F-* + phase-a
- `/Users/oracle/ora/config/framework-routing.json` — [partial] Per-framework default named-configuration map; consulted when /framework invoked without --config.
- `/Users/oracle/ora/frameworks/framework-registry.md` — [implemented] Human-facing registry (Registry Part 2): purpose/inputs/outputs/limitations/file-location per framework.
- `/Users/oracle/ora/frameworks/pff-cff-off-integration-architecture.md` — [implemented] Reference doc for PFF/CFF/OFF triad integration (corpus template lifecycle between PFF inputs and OFF renders)
- `/Users/oracle/ora/frameworks/territories/` — [implemented] 21 self-contained territory frameworks (Phase 10); share frameworks/ tree, not the primary milestone-execution
- `/Users/oracle/ora/frameworks/personal/` — [absent at 7a5e8f40] no such directory in the commit (only `frameworks/user/` with a `.gitkeep`).
- `/Users/oracle/ora/frameworks/user/` — [planned] User frameworks slot (empty).
- `/Users/oracle/ora/orchestrator/scratch.py` — [implemented] ScratchSession persistence: per-milestone deliverable writes + prior reads; cleanup on success, preserve on fa

### E.7 — Output Styles & Personal Values
- `orchestrator/style_assembly.py` — [implemented] Composes an Output Style profile into injected text: compact DEMEANOR block (gears 1-2) vs full STYLE block (g
- `orchestrator/style_store.py` — [implemented] User custom style profiles: create/read/update/delete forked genre profiles persisted to data/custom-styles.js
- `orchestrator/mind_guided.py` — [implemented] Guided values setup wizard: deterministic (no model calls) question registry (12 questions across 5 sections) 
- `orchestrator/mind_projection.py` — [implemented] Self-spec => assistant-directives transform: one model call (breadth slot) compiling a MindSpec self-portrait 
- `orchestrator/boot.py` — [implemented] Injection wiring. _compose_output_style :8432 + gear-conditional injection (:8534 gears1-2 demeanor high; :870
- `server/app.py` — [implemented] HTTP surface. Mind endpoints: GET/POST /api/mind (:1229/:1237), GET/POST /api/mind/guided (:1293/:1319), POST
- `mind.md` — [implemented] Live user values file at repo root (save-dest == load-source). Currently the stock default (carries the '*Defa
- `mindspec/mind-template.md` — [implemented] Stock template seeded by POST /api/mind action=create_from_template. Includes the guided/interview/edit custom
- `mindspec/default-mindspec.md` — [implemented] 50KB canonical MindSpec library reference (commitment weights, near-enemy patterns, families) driving the /fra
- `mindspec/self-spec.md` — [planned] Archived full self-spec written by every completed MSI-Self interview (source for mind_projection). Absent unt
- `frameworks/book/style-registry.md` — [implemented] Read-only source: 12 built-in genre style profiles (explainer, technical, how-to, journalism, business, white-
- `frameworks/book/style-demeanor-axes.md` — [implemented] Read-only source: 7 demeanor axes x 3 rungs (warmth/force/energy/outlook/playfulness/directness/agreeableness)
- `frameworks/book/style-arrangement-schemas.md` — [implemented] Read-only source: 12 ordered slot-list arrangement schemas (answer-first, motivation-first, inverted-pyramid, 
- `frameworks/book/style-craft-floor.md` — [implemented] Read-only source: craft-floor bullet lines that outrank style in the full STYLE block.
- `orchestrator/user_settings.py` — [implemented] Settings schema/validation for styles.default_id (string, '' = none) and styles.use_custom_values (bool) — the
- `server/static/styles-pane.js` — [implemented] V3 Output Styles Settings-tab pane: OraStylesPane.init/destroy; mind.md switch + panel states, genre preset ca
- `server/static/js/style-audience-toggle.js` — [implemented] Honne/tatemae input-toolbar toggle (G1.36): marks the next turn internal (project interaction_style) vs extern
- `frameworks/book/mindspec-interview.md` — [implemented] The /framework mindspec-interview definition (MSI-Self mode) producing the deep self-specification that feeds 
- `config/interface.json` — [stale] Active layout + theme only (solo preset, default-dark). Does NOT contain the Output Styles tab.

### E.8 — Vault, RAG, Conversations & Persistent State
- `orchestrator/conversation_memory.py` — authoritative envelope, local turns, recursive cutoff-safe history, contributors, close state, detach behavior.
- `orchestrator/conversation_chunk.py` — shared live/replay conversation-row construction and stored-document/vector separation.
- `orchestrator/conversation_closeout.py` — retained Close and protected Stealth purge across Ora-managed surfaces.
- `server/app.py` and V3 Dialogue UI modules — creation review, sidebar/Manage projection, restore, Exit Stealth navigation, Delete Forever protection.
- `orchestrator/boot.py` — shared server context assembly for Phase A, Direct, G1–G4, and special consumers.
- `orchestrator/rag_engine.py` and `orchestrator/tools/knowledge_search.py` — semantic/lexical retrieval, eligibility, ranking, deduplication, exclusions, and packing inputs.
- `orchestrator/embedding.py` and `orchestrator/retrieval_config.py` — embedding adapter plus embedder/dimension/physical-collection binding.
- `config/chromadb.json.template` — tracked install-default retrieval profile; live machine config remains local.
- `docs/conversation-file-format.py` — documentation schema for envelope, turn, contributor, chunk, and index metadata.
- `frameworks/book/supplemental-rag-protocol.md` — promotion-only supplemental request and coverage-gap contract.
- `orchestrator/pipeline_trace.py` — numeric `context_coverage`, sensitive supplemental forensics, and private trace-mechanics boundary.
- `orchestrator/help_retrieval.py` and the five tracked `help/*.md` files — isolated Aside help loading, deterministic chunks, owned-index refresh, and lexical fallback.
- `Reference — Conversational RAG for Persistent AI Memory` — detailed current storage, eligibility, source, packing, and migration contract.

### E.9 — Tool Dispatch, the Capability Gate & Execution Telemetry
- `orchestrator/tool_events.py` — [implemented] Execution-Review Phase 1 substrate: capability manifest (mutability/sensitivity/egress axes + fail-closed defa
- `orchestrator/capability_registry.py` — [implemented] Runtime capability-slot registry: loads capabilities.json, contract-validates inputs, resolves preferred/fallb
- `orchestrator/dispatcher.py` — [implemented] Unified tool dispatcher and pre-effect authority seam: resolves call-specific MCP policy, prepares one immutable command, gates it, invokes it, and records its actual outcome.
- `orchestrator/tool_selector.py` — [implemented] Deterministic tool resolver (Option C): mode declares tools in ## TOOLS section; derives params mechanically f
- `orchestrator/mcp_client.py` — [implemented] Pinned stdio MCP manager: validates exact local packages, builds fresh minimal environments, intersects discovered tools with policy, bounds and correlates JSON-RPC, and cleans up children.
- `orchestrator/web_consultation.py` — [implemented] Step-2 F-Consult CAG stream: parallel web search from prompt intents, source tiering (trusted/corroborated/sin
- `orchestrator/web_extraction.py` — [partial] Extraction-failure escalation: selects thin high-trust snippets, deep-fetches full pages via web_fetch, chunks
- `orchestrator/claim_verification.py` — [implemented] Claim-verification pre-flight for Steps 5/6: parses evaluator ## FLAGGED CLAIMS, runs challenge queries in par
- `orchestrator/pipeline_trace.py` — [implemented] Per-turn forensic trace: writes step JSON+MD, RAG-failure log, supplemental-RAG log, step-health summary, corp
- `orchestrator/pipeline_health.py` — [implemented] Per-thread pipeline-execution warning surface: record(category,message) from any pipeline step, collect_and_cl
- `orchestrator/hooks.py` — [implemented] Lifecycle hooks: fire config/hooks/*.json shell scripts at pre_tool/post_tool/session_*/pre_compact events; re
- `orchestrator/url_import.py` — [implemented] URL-import subsystem: wraps yt-dlp subprocess to download public video/audio URLs into per-conversation media 
- `config/capabilities.json` — [implemented] Canonical capability-slot contracts (required/optional inputs, output, execution_pattern, common_errors). 14 s
- `config/mcp-servers.json` — [implemented] Exact registry for the three preserved servers (vault-fs, Playwright, GitHub), with pinned entrypoints, minimal-environment declarations, and per-tool policy/adapters; no server-wide authorization fallback.
- `orchestrator/tests/test_dispatcher_gate.py` — [implemented] Gate-enforcement tests at the dispatcher boundary (44 tests).
- `orchestrator/tests/test_risk_gate.py + test_risk_gate_pipeline.py` — [implemented] Execution-Review Phase 2 risk-tier gate tests (per-turn risk tier + task-approval one-shot token by fingerprin

### E.10 — Visual Intelligence
- `orchestrator/boot.py` — [implemented] Server-side integration point: _run_visual_hook (l.286) orchestrates validate→recover→synthesize; _render_visu
- `orchestrator/visual_validator.py` — [implemented] Schema + structural-invariant validation of ora-visual envelopes; also serialize_spatial_representation_to_tex
- `orchestrator/visual_adversarial.py` — [implemented] Tufte T-rule review (15 _t* rules) + LLM-prior-inversion + WCAG/overlap checks; process_response/review_envelo
- `orchestrator/visual_recovery.py` — [implemented] Phase-1a: deterministically recover the model's OWN diagram (Mermaid/DAGitty/Structurizr DSL, or malformed ora
- `orchestrator/visual_synthesis.py` — [implemented] Phase-1b: synthesize a schema-valid envelope from analyst prose via narrow model call + validate→repair loop (
- `orchestrator/visual_extraction.py` — [implemented] WP-4.3 vision-model extraction: invoke vision-capable model on an image, parse into spatial_representation JSO
- `orchestrator/canvas_file_format.py` — [implemented] Server-side reader/writer for .ora-canvas / .ora-canvas.json (gzip-wrapped canonical JSON); mirror of client c
- `orchestrator/transcription.py` — [implemented] whisper.cpp (whisper-cli) subprocess wrapper: ffmpeg 16kHz mono wav extract → whisper-cli JSON, progress parsi
- `orchestrator/icon_set_builder.py` — [implemented] Deterministic staleness-gated rebuild of icon set from source-file scan (no model call); rebuild_if_stale l.13
- `server/static/ora-visual-compiler/index.js` — [implemented] Compiler entry: OraVisualCompiler.compileWithNav / bootstrapAjv; boots renderers→accessibility→adversarial. 10
- `server/static/ora-visual-compiler/renderers/` — [implemented] 16 renderer files (one is stub.js) → ~22 kinds: mermaid.js (flowchart/sequence/state), vega-lite.js (comparison/time_series/dis
- `server/static/ora-visual-compiler/alt-text-generator.js / aria-annotator.js / keyboard-nav.js` — [implemented] Accessibility layer: Lundgard-Satyanarayan 4-level descriptions, element-level ARIA, Olli-style keyboard navig
- `server/static/ora-visual-compiler/artifact-adversarial.js` — [implemented] Client artifact-level adversarial review (overlap / text-truncation / WCAG 2.1 contrast); Critical forces fall
- `server/static/ora-visual-compiler/tools/render-envelope.js` — [implemented] Node CLI: stdin envelope → stdout SVG via jsdom-booted compiler + all vendor libs. FALLBACK render path (not V
- `server/static/ora-visual-compiler/vendor/` — [implemented] Vendored libs (local-first, no CDN): vega 6.2.0 (BSD-3), vega-lite 6.4.2 (BSD-3), mermaid 11.14.0 (MIT), d3 7.
- `server/static/vendor/konva/konva.min.js` — [implemented] Konva (MIT, Anton Lavrenov) — dual-pane canvas engine for visual-panel.js.

### E.11 — Audio/Video Media & Timeline Editing
- `server/feature_plugins.py` — explicit one-video loader, descriptor/context
  contract, validation, route/static registration, and browser asset injection.
- `plugins/video/__init__.py`, `routes.py`, and `runtime.py` — descriptor,
  31-route list, lifecycle callbacks, and context access.
- `plugins/video/backend/` — plugin-owned capture, media library, preview, render,
  timeline, URL import, video suggestions, and waveform implementations.
- `plugins/video/frameworks/video-editing-suggestions.md` and
  `plugins/video/frameworks/video-editing-suggestions.schema.json` — plugin-local
  suggestion instructions and output contract.
- `plugins/video/static/` — editor, capture, library, timeline, preview,
  suggestions, settings, styles, and four host-seam registrations.
- `orchestrator/transcription.py`, `orchestrator/vault_transcript.py`, and
  `orchestrator/job_queue.py` — core-retained transcription, note, and shared job
  infrastructure.
- `server/static/transcribe-input.js` and
  `server/static/js/v3-capability-async.js` — core-retained Inquiry A/V drop and
  shared async-capability bootstrap.
- `server/app.py` — host loader call, core transcription/job routes, and lifecycle
  dispatch to installed feature callbacks.

### E.12 — Meta-Layer Oversight
- `~/ora/orchestrator/oversight_events.py` — [implemented] In-process event emitter + durable JSONL log at data/oversight/events.jsonl
- `~/ora/orchestrator/oversight_context.py` — [implemented] Context loader: bundles project-level (PED) + workflow-level (corpus template + workflow spec) locks; classify
- `~/ora/orchestrator/oversight_router.py` — [implemented] Observational event logger with Stealth suppression, fan-out recursion guard, and child-to-parent visibility.
- `~/ora/orchestrator/oversight_actions.py` — [implemented] Remaining durable utilities: managed Decision Log derivatives, privacy/delete support, human-queue and action-log helpers; no Process Coherence verdict parser or dispatcher.
- `~/ora/orchestrator/oversight_daemon.py` — [implemented] OS-event + exact persisted-deadline runtime with lane-liveness watchdog; former interval methods are explicit compatibility only
- `~/ora/orchestrator/oversight_health.py` — [implemented] Event/deadline thread-liveness and telemetry-degradation checks plus independent MLX heartbeat.
- `~/ora/orchestrator/triggers.py` — [implemented] Separately authorized Trigger authoring/lifecycle, exact-event and persisted-deadline firing, digest-bound activation, process-bound action deadlines, durable dependent-completion delivery, startup replay, truthful local-only routine/debug descriptors, and macOS idle-sleep protection scoped to real action execution; remote execution and wake remain unsupported.
- `~/ora/server/static/js/sidebar-oversight.js` — [implemented] Oversight's Scheduled group: author/review/activate, health and internal-deadline visibility, inspect/run, lifecycle, rollback, and firing history.
- `~/ora/orchestrator/oversight_queue.py` — [implemented] Managed Paused queue: stable deterministic ids, engagement (unseen/seen/discussing), discussion linkage, AI-ge
- `~/ora/orchestrator/oversight_relationships.py` — [implemented] Cross-project oversight: child->parent Decision Log fan-out via parent_nexus; synthesized Child<Event> audit r
- `~/ora/orchestrator/ped_parser.py` — [implemented] PED structured extraction: Mission/Excluded Outcomes/Constraints/Milestones/Decision Log
- `~/ora/orchestrator/ped_watcher.py` — [implemented] PED checkbox-state diff sweep -> MilestoneClaimed events; heartbeat
- `~/ora/orchestrator/corpus_parser.py` — [implemented] Corpus template/instance parser: section schemas, cadences, cross-section rules
- `~/ora/orchestrator/corpus_watcher.py` — [implemented] Corpus instance state diff -> CorpusSectionPopulated / ChainPropagationRequired
- `~/ora/orchestrator/corpus_runtime.py` — [partial] Mechanical CFF: c_instance + c_validate; emits CorpusInstanceCreated/CorpusValidated
- `~/ora/orchestrator/output_runtime.py` — [partial] Mechanical OFF O-Render: corpus+bespoke-OFF -> md/html/docx/pptx/xlsx artifact; emits OFFRendered
- `~/ora/orchestrator/workflow_spec_parser.py` — [implemented] Workflow spec parser: PFFs/OFFs/chain rules/oversight extensions + reference-integrity check
- `~/ora/orchestrator/workflow_spec_sweeper.py` — [implemented] Workflow spec drift trap: issue-signature dedup, dual-gate auto-deregister (>=3 misses AND >=10min), tombstone
- `~/ora/orchestrator/revisit_sweeper.py` — [implemented] Working Assumption revisit triggers + age-based reviews
- `~/ora/orchestrator/maintenance_scheduler.py` — [retired compatibility] parses historical controls for reports/explicit diagnostics; production clock dispatch is absent and News/Engram remain visibly `off`
- `~/ora/orchestrator/retention_sweeper.py` — [explicit campaign compatibility] historical logs/archives; finalized traces now use exact persisted expiry deadlines rather than a sweep
- `~/ora/orchestrator/runtime_event_dispatcher.py` — [implemented] filtered operating-system notification bridge for exact changed identities; no interval fallback
- `~/ora/orchestrator/runtime_hygiene.py` — [implemented] idempotent event ledger, mutation rollback, restart recovery, drift-safe rollback, and exact-deadline queue
- `~/ora/orchestrator/milestone_executor.py` — [implemented] Framework event hooks (FrameworkStarted/MilestoneComplete/FrameworkComplete/MilestoneBlocked) + select_mode mu
- `~/ora/orchestrator/resolution_chain.py` — [implemented] Conversation-mediated Paused-entry resolution: hidden ora-resolution marker + numbered options block; 1/2/3 co
- `~/ora/orchestrator/redefinition_handler.py` — [implemented] Approved ESCALATE(redefinition) follow-through per Meta-Layer Arch §9; list_pending_redefinitions; approve/den

### E.13 — Execution-Review Architecture
- `~/ora/orchestrator/tool_events.py` — [implemented] Phase 1: capability manifest (mutability/sensitivity/egress axes + fail-closed default), tool-event recorder (
- `~/ora/orchestrator/risk_gate.py` — [implemented] Phase 2/3: risk-tier before-clock (deterministic Stage-A pattern floors + Stage-B mode ## RISK TIER heading, n
- `~/ora/orchestrator/execution_packet.py` — [partial] Phase 4: EvidenceLane/JudgmentLane (§5 structural separation), route_lanes router, polymorphic ExecutionPacket
- `~/ora/orchestrator/dispatcher.py` — [implemented] The single model-facing tool call site that resolves policy, binds prepared commands and MCP calls to approval, invokes the Phase-1 gate before each effect, and records the real result.
- `~/ora/orchestrator/tools/bash_execute.py` — [implemented] One-command direct executor: rejects shell grammar, prepares and classifies an immutable argv with semantic Git/download/path scopes, and executes the same object with shell=False.
- `~/ora/orchestrator/tools/code_execute.py` — [partial] Sandboxed code executor; macOS sandbox-exec backend = enforcement_model 'orchestrated', else fail-closed/gated
- `~/ora/orchestrator/pipeline_trace.py` — [implemented] Per-turn forensic trace directory (data/pipeline-traces/<conv>/<turn>/) that HOSTS the tool-events.jsonl sink 
- `~/ora/orchestrator/runtime_paths.py` — [implemented] Cross-platform roots + path normalization + advisory file locking (locked_file) that tool_events/risk_gate dep
- `~/ora/server/review.py` — [stale] NOT part of Execution-Review. This is the unrelated Phase-10 document-extraction human-review web app (localho
- `~/ora-worktrees/phase5-design.md` — [unmerged / code-review] Phase 5 design packet (Rev 2 enforce-or-refuse), design approved-with-conditions; the implementation is in the phase5-impl worktree at the code-review gate — not merged to ~/ora main, not installed
- `~/ora-worktrees/HANDOFF-execution-review.md` — [implemented] Durable cross-phase state. TOP block = Phase 5 DESIGN-APPROVED + IMPLEMENTED (unmerged phase5-impl worktree, code-review gate); below = Phase 4 LANDED confirmation and prior-phase history
- `~/ora-worktrees/ora-execution-review-spec.md` — [implemented] The build spec (§5 lanes, §6 two signals/two clocks, §7 fail-closed manifest, §9 packet, §12 renderer, §15 Pha
- `~/ora-worktrees/phase5-impl.diff + phase5-impl/` — [unmerged / code-review] the Phase 5 implementation (diff + worktree) at the code-review gate; exists on disk but NOT merged to ~/ora main — must NOT be read as installed/landed

### E.14 — Operation-Matrix (Mission / Objectives / Milestones)
- `/Users/oracle/ora/orchestrator/operation_matrix.py` — [implemented] Canonical MOM read/write engine, shared Matrix resolution and Tasks entry points; `list_active_project_meta` supplies the same canonical active-project order to Overview Priority and Tasks without local re-sorting.
- `/Users/oracle/ora/orchestrator/matrix_tasks.py` — [implemented] Lossless Tasks scanner and named operations, source-bound references, authenticated group states, digest conflicts, mode-preserving atomic replacement and post-write outcome confirmation.
- `/Users/oracle/ora/server/app.py` — [implemented] Existing MOM/assist/file-index routes plus GET/POST `/api/projects/<nexus>/tasks`, using persisted project identity and strict one-operation request bodies.
- `/Users/oracle/ora/orchestrator/project_meta.py` — [implemented] Canonical project-metadata reader and priority authority: nonnegative ranks sort ascending, then immutable nexus breaks ties and orders the unranked tail; activity/display-name changes do not reorder projects. The synthetic Commons row remains first in its complete list.
- `/Users/oracle/ora/orchestrator/tests/test_operation_matrix.py` — [implemented] Synthetic Matrix checks for MOM/Tasks preservation and writer coordination, task operations, identity/conflict/date/insertion behavior and canonical active-project order.
- `/Users/oracle/ora/orchestrator/tests/test_mom_assist_endpoint.py` — [implemented] Tests the model-draft suggestion endpoint: unparseable→502, model-unavailable→503, the Commons→400, parse of MISSI
- `/Users/oracle/ora/orchestrator/tests/test_projects_mom_endpoints.py` — [implemented] Real temporary-file MOM and Tasks endpoint checks, including strict request authority, read-only/unavailable states and write outcomes.

### E.15 — Documentation-Code Parity & Vault↔Repo Mirror Governance
- `/Users/oracle/Documents/vault/Projects/Ora/Framework — Documentation-Code Parity.md` — [specified, not built as a runtime framework] Five semantic modes plus the current deterministic companion boundary.
- `/Users/oracle/Documents/vault/Projects/Ora/Reference — Documentation-Code Parity Configuration.md` — [implemented] Five-root ownership/discovery registry, accepted-finding baseline, same-task rule, propagation contracts, and testing ceiling.
- `/Users/oracle/ora/scripts/install-dcp-hooks.sh` — [implemented and installed] Reproducible source for five local blocking pre-push hooks plus the Ora/vault fail-open post-commit audits; seven managed hooks in total.
- `/Users/oracle/ora/scripts/check-capability-sync` — [implemented] Mechanical drift detector: verifies capabilities.json + routing-config.json slots block + vault Reference — Ca
- `/Users/oracle/ora/scripts/verify-implementation.py` — [implemented] Focused read-only `documentation-integrity` gate with explicit roots and bases; also owns framework-pair lifecycle verification.
- `/Users/oracle/ora/orchestrator/programming.py` — [implemented] Documentation-impact declaration, complete five-repository evidence-packet validation, and one explicit semantic verdict per affected surface in fresh review.
- `/Users/oracle/ora/scripts/verify-configuration-equivalence.py` — [implemented] Behavioral-equivalence check: for each (pipeline,slot,gear) tuple, compares live Router.resolve_endpoint outpu
- `/Users/oracle/ora/scripts/check-model-refresh-drift.py` — [implemented] Guards that live model-refresh runtime activity did not dirty tracked seed config/data files; instructs live r
- `/Users/oracle/ora/scripts/check-portability.py` — [implemented] Cross-platform portability linter: 16 regex rules for macOS/POSIX-only constructs (pkill, osascript, homebrew 
- `/Users/oracle/ora/CLAUDE.md` — [implemented] Vault Canonical Rule section: authoritative statement of the pairing convention (which ora path pairs with whi
- `/Users/oracle/Documents/vault/Projects/Ora/Reference — Ora Technical Documentation.md` — [implemented] active vault-canonical technical documentation (this document).
- `/Users/oracle/ora/docs/technical-documentation.md` — [implemented] body-only mirror of the technical documentation under the exact registered parity rule.
- `/Users/oracle/Documents/vault/Projects/Ora/Reference — Ora Accessible Overview.md` — [implemented] active vault-canonical accessible overview derived from this document.
- `/Users/oracle/ora/help/accessible-overview.md` — [implemented] repo-side mirror of the accessible overview and member of the isolated five-file help corpus
- `/Users/oracle/Documents/vault/Projects/Ora/Guide — Using Ora.md` — [implemented] active vault-canonical how-to guide derived from this document.
- `/Users/oracle/ora/help/user-guide.md` — [implemented] repo-side mirror of the how-to guide and member of the isolated five-file help corpus
- `/Users/oracle/Documents/vault/Projects/Ora/Reference — Thinking Tools Library.md` — [implemented] canonical singleton analytical-procedure library.
- `/Users/oracle/ora/thinking-tools.md` — [implemented] body-only mirror of the Thinking Tools singleton under the registered `ora_body_only` propagation rule.
- `/Users/oracle/Documents/vault/Administration/DCP/` — [implemented] DCP output directory: dated Drift Reports, Decision Log, Escalation Queue, Run History, Deprecation Candidates

### E.16 — Update, Recovery, Troubleshooting & Diagnostics
- `~/ora/help/install-recovery.md` — [implemented] Operator guide for recovering from a partial install: --dry-run/--reset/--resume, install-state.json inspection
- `~/ora/docs/install-testing.md` — [implemented] Clean-room test protocol: 3-environment matrix (Mac Mini A/B + Parallels Windows 11 ARM), reset via Parallels 
- `~/ora/help/install-manual.md` — [implemented] Manual command fallback reproducing the Solo desktop install by hand (clone → dependencies → vault skeleton → converters → refresh-catalog → sync-model registry).
- `~/ora/docs/upgrade-report-2026-04-06.md` — [stale] Historical point-in-time upgrade report (7→15 dispatcher tools) with test/eval scores and rollback via 'cd ~/l
- `~/ora/docs/pre-upgrade-packages.txt` — [stale] pip-freeze snapshot captured before the 2026-04-06 upgrade (keyring + build deps), companion to the upgrade re
- `~/ora/orchestrator/pipeline_health.py` — [implemented] Per-thread in-pipeline warning surface: pipeline steps call record(category,message); the chat handler calls c
- `~/ora/orchestrator/endpoint_health.py` — [implemented] Per-endpoint circuit breaker for graceful degradation: 3 failures/60s trips a 300s cooldown, then a half-open 
- `~/ora/orchestrator/oversight_health.py` — [implemented] Meta-layer heartbeat-staleness surface: reads each watcher's <name>-heartbeat.json, flags 'stale' (>2× interva
- `~/ora/scripts/ora-test` — [implemented] CLI to submit a test prompt to the running chat server under a named saved configuration without changing the 
- `~/ora/scripts/ora-cost-summary` — [implemented] CLI to compute/display per-model + grand-total token cost for a pipeline trace by joining usage.jsonl against 
- `~/ora/orchestrator/pipeline_trace.py` — [implemented, post-pin PR #269] Per-turn Gear 1–4/direct/framework trace writer, truthful manifest taxonomy, effective-Gear correction, exact post-routing terminal capture, retention state, safe browser/export projections, and status/pin/unpin CLI; all writes fail open.
- `~/ora/orchestrator/trace_debug.py` — [implemented] Bounded same-Dialogue P-Debug context construction, execution-time contract preservation, step/lineage walk, evidence budgeting, four-way diagnostic verdict contract, and privacy-tag propagation; diagnosis is separate from and does not mutate the original trace.
- `~/ora/server/static/js/export-toolbar.js + server/static/js/trace-walk.js` — [implemented] Findings-pane hover entrypoint and Trace Walk modal with manifest/step inspection, lineage navigation, pin/unpin, separate Investigate turn, and redacted HTML export.
- `~/ora/server/app.py` (`/api/trace/*`) — [implemented, post-pin PR #269] Safe list/manifest/step/export reads plus retention metadata update; real server pipeline, direct-entry, output-routing, persistence-error, and lifecycle trace integration.
- `~/ora/orchestrator/tests/test_trace_manifest.py + test_trace_walk_ui.py` — [implemented, post-pin PR #269] Manifest truth, production-path behavioral coverage, import-order isolation, privacy/export boundaries, fail-open writer behavior, and UI route/control assertions.
- `~/ora/scripts/diagnose-extraction-pipeline.py` — [implemented] Standalone diagnostic that verifies the runtime knowledge-extraction pipeline end to end (transcript build → i
- `~/ora/scripts/check-portability.py` — [implemented] Cross-platform portability linter: walks the repo, regex-matches Mac/POSIX-only constructs (pkill, homebrew-py
- `~/ora/scripts/generate-indexes.sh` — [implemented] Regenerates config/framework-library-index.md and config/modes-index.md from the frameworks/ and modes/ direct
- `~/ora/scripts/compile-rag-manifest.sh` — [implemented] Compiles config/rag-manifest.md into a token-optimized rag-manifest-compiled.md (resource table + approved/exc
- `~/ora/install.log` — [implemented] Captured stdout of the last install run; ends with the INSTALL_COMPLETE marker the recovery doc tells operator
- `~/ora/scripts/install.py` — [implemented] The desktop installer itself (context, not a target file): source of --dry-run/--reset/--resume, install-state

### E.17 — The Project Plugin Convention
- `/Users/oracle/ora/orchestrator/project_registry.py` — [implemented] The load-bearing module: manifest parsing/validation (tools, slash_commands, capability_slots §12, themes §13,
- `/Users/oracle/ora/orchestrator/project_meta.py` — [implemented] Container view over the SAME pointer files (G1.33): conversation-container metadata (name/status/last_accessed
- `/Users/oracle/ora/orchestrator/active_project.py` — [implemented] Active-project pointer at ~/ora/data/active-project.json: which project new conversations bind to; resolve_pro
- `/Users/oracle/ora/orchestrator/nexus_rename.py` — [implemented] Bulk rename cascade: rewrites nexus slug across (1) vault .md frontmatter surgically, (2) conversation project
- `/Users/oracle/ora/orchestrator/capability_registry.py` — [implemented] §12 runtime consumer: merge_project_capability_slots merges project-declared visual-capability slots into core
- `/Users/oracle/ora/orchestrator/framework_config.py` — [partial] §14 runtime engine: parse_config_interface, substitute_config_refs (${config.*}), splice_extension_overlays (<
- `/Users/oracle/ora/orchestrator/model_dispatch.py` — [implemented] The dispatch seam project tools use to call models without hardcoding: invoke_chat(system_prompt, user_prompt,
- `/Users/oracle/ora/orchestrator/slash_commands.py` — [implemented] Project-agnostic chat surface: /project-tool, /project-list, /project-register, /project-unregister (dispatche
- `/Users/oracle/ora/data/projects/` — [implemented] Pointer directory. Three live pointers: main-street-independent.json (root→MSI ora-project), ora-ai-app.json, 
- `/Users/oracle/sites/mainstreetindependent/ora-project/ora-project.json` — [implemented] Reference implementation manifest (nexus=main-street-independent, v0.1.0): 17 tools, slash_commands, 1 chromad
- `/Users/oracle/sites/mainstreetindependent/ora-project/` — [implemented] Reference impl root: tools/ (118 entries), scripts/, config/, themes/, framework-overlays/, clusters/, launchd
- `/Users/oracle/Documents/vault/Projects/Ora/Reference — Ora Project Plugin Convention.md` — [implemented] Vault canonical 'why' + full manifest schema spec (v1.3, 68KB). §1 architectural divide, §2 structure, §3 mani
- `/Users/oracle/ora/server/app.py` — [implemented] HTTP surface: project inspection endpoints (list_projects at ~4375/4380), §13 theme routes (/api/v3-themes/list
- `/Users/oracle/ora/orchestrator/tests/test_project_execution_binding.py` — [implemented] Tests that a conversation's active project binds into execution context.
- `/Users/oracle/ora/orchestrator/tests/test_conversation_projects.py` — [implemented] Tests conversation project_ids membership stamping.

---

## Changelog

- **2026-08-29** — Reconciled scoped current behavior: Windows launch now defaults oversight on with `--no-oversight`, checkout-bound process identity, safe argument forwarding, and native slash-path preservation; Chapter 12 now documents the separate Scheduled Trigger routes/UI plus durable publication, completion-delivery, timeout, and health behavior; Chapter 15 now documents section-specific DCP dispositions, complete semantic-review evidence, exact-head push boundaries, the Thinking Tools mirror, and vault backup transport; Chapter 18 now documents Programming's evidence packet and per-surface verdict contract.
- **2026-08-05** — Replaced the removed governed-process Chapters 18–21 with the current standalone Programming architecture: explicit entry, inspect-first planning, one approval, real repository tools, fresh reviewer calls, direct outside/non-text evidence inspection, accepted-slice commits, and Git-native recovery. The former process-bound entry, persistence, automation, trigger, inspector, attention, and lifecycle modules are not compatibility surfaces; the later separate Scheduled Trigger facility is described in Chapter 12.
- **2026-07-24** — Corrected G1.19 after gate review: the firing claim now persists and exclusively supplies the complete prepared invocation and deterministic Run identity; activation is draft-only, lifecycle retries are original-result idempotent, and every new claim atomically reauthenticates active state; the 30-second scan was replaced by startup reconciliation plus lifecycle-recalculated one-shot calendar wakes.
- **2026-07-24** — Added Chapter 21 for G1.19: immutable separately approved Trigger specs, content/source-bound exactly-once firing, ordinary governed Run invocation/recovery, OS file and accepted-framework events, Project milestone snapshots, named-zone justified app-only calendar operation, Trigger Manager UI/API, and the held G1.17/G1.21/G1.22 boundaries.
- **2026-07-24** — Closed the remaining G1.20 integrity findings: Pause authenticates before mutation and atomically commits its checkpoint/state pair, so rejected, stale, and racing losers leave no checkpoint; quality starts independently derive the current source, subject, and evaluator binding at the runtime boundary, and direct fabricated start/outcome records are unavailable.
- **2026-07-24** — Corrected the G1.20 gate submission: user Pause is now a persistent authority state releasable only by an authenticated Resume control; execute/retry, direct runtime calls, concurrency, interruption, and restart fail closed around it. Removed inferred zero token/cost claims from no-tools workers. Layer 2 now receives digest-verified bounded Artifact content, exact inputs, criteria, instructions, and failure trace, or deterministically returns `INDETERMINATE` without a model call.
- **2026-07-24** — Added Chapter 20 for G1.20: deterministic Run telemetry, authenticated worker liveness, restart-safe pause/resume/stop for G1.18 automated Runs, authority-inert opt-in quality evaluation only at handoff/output-failure seams, browser/sidebar surfaces, and explicit scheduling/Persona/channel/security boundaries.
- **2026-07-24** — Corrected the G1.18 gate submission after independent review: replaced presence-only verification with structured criterion-by-criterion assessment plus controller recomputation; bound verification starts, failures, checkpoints, attempts, restart retries, and exhaustion; enforced the complete admitted JSON Schema subset while rejecting unsupported keywords; reserved every action and final-verification baseline separately from the shared correction allowance; and isolated G1.18 attempt creation/completion from the generic runtime APIs. Added adversarial proofs for wrong/partial results, fabricated `PASS`, verifier outage/restart/recovery/exhaustion, input and output constraints, public API refusal, full action-retry consumption before final review, restart at that exact boundary, deterministic admission-ceiling blocking, and mutation-free generic wrong/future-node refusal followed by legitimate `ACCEPT`.
- **2026-07-23** — Added Chapter 19 for the G1.18 gate submission: exact G1.1 reconciliation, strict reusable-definition authoring and approval, authenticated registration/promotion, schema-driven Project-confirmed Library execution, existing Model/Style Run binding, no Persona/Trigger/external effects, isolated worker lineage, human checkpoint and recovery behavior, and the end-to-end unsent-email proof.
- **2026-07-19** — Added the Phase 3.5 closeout evidence binding and rendered-interface provenance against accepted Gate 3.4 commit `6824bb03`. Recorded the seven screenshot surfaces, final packet path, explicit residual limitations, and Gate 3.4 acceptance without changing runtime behavior or issued definition/package identity.
- **2026-07-19** — Expanded Chapter 18 for G1.1 Phase 3.4 against accepted Gate 3.3 commit `5bcba502`: added the maintainer reference for runtime objects/storage, contracts and bounded judgment, graph grammar, transitions, correction, parent-child continuation, recovery, evidence/identity, packages/catalogs/versioning, lifecycle, regression/change protocol, migration/disablement/rollback, troubleshooting, and known limits. Updated the browser plan surface/API map and retained exact vault-canonical/body-mirror parity. No runtime behavior or issued definition/package changed.
- **2026-07-19** — Added Chapter 18 as the G1.1 as-built governed-process reconciliation pinned to Ora commit `6740f2fc`. Replaced the opening closed-suite and Agent-object claims, documented the generic kernel, exact package/registry identity, Gear 3/F-Quality boundary, management interface, code/API map, authority/defaults, evidence, deviations, and limitations, and preserved G1.2's ownership of the full Gear 1/2/3 specification.
- **2026-07-16** — Added a scoped post-pin technical record for the landed PR #269 Trace Walk/trace-completeness subsystem: complete Gear 1–4 and direct/framework entry coverage, post-routing terminal evidence, truthful manifest categories and effective-Gear fallback, fail-open/privacy/Stealth boundaries, UI/API/CLI invocation, retention, behavioral verification, and source-map entries. The remainder of the document remains pinned to `7a5e8f40`.
- **2026-07-12** — Post-pin closure currency note: `commons` is the canonical runtime sentinel, `general` remains a permanent legacy input/wire alias, and Commons is the universal view rather than a stored membership—empty `project_ids` and empty YAML `nexus:` are its representation, while explicitly assigned project material also remains visible there. Commons output now defaults to the vault root; dormant config-driven layout/legacy-theme APIs and files are retired in favor of the fixed V3 workspace and V3 Theme Library; server launch paths are consolidated around a foreground launcher suitable for launchd supervision. Chapter bodies and line references remain pinned to `7a5e8f40`; this is a reader-orientation note, not a re-pin.
- **2026-07-11** — Default project renamed in user-facing prose: General → **Commons** (nomenclature note extended; seven body mentions updated in the Operation-Matrix and project-container chapters). The internal nexus id at the pinned commit is still `general` — the code-side rename is pending — so code identifiers, quoted literals, and line references are unchanged. Terminology only — content remains pinned to ora commit `7a5e8f40`; this is not a re-pin and the parity audit was not re-run.
- **2026-07-11** — Currency note added: the code-level rename referenced above landed the same day on `main` at commit `062b67a7` (well after this document's `7a5e8f40` pin) — internal nexus id `general` → `commons`, with `general` still recognized permanently as a legacy value. Chapter bodies, tests, and line references throughout this document remain pinned to `7a5e8f40` and reflect the pre-rename state; this is a reader-orientation note, not a re-pin, and the parity audit was not re-run.
- **2026-07-11** — User-facing nomenclature note added to "How To Read This Document" (Dialogues, Inquiry / Findings / Exhibits panes, Aside, Library). Terminology only — content remains pinned to ora commit `7a5e8f40`; this is not a re-pin and the parity audit was not re-run. Internal identifiers and quoted UI strings are unchanged by design.
- **2026-07-04** — Initial version (Documentation-Code Parity closeout, pinned to `7a5e8f40`).
